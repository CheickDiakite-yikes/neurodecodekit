# DREYER-C5R-1 H-L1R1 Qualification Decision

Date: 2026-08-29

Status: **packet-bound generated qualification authorization recorded;
effective only after this exact decision is tested, committed, pushed, and both
required CI jobs are remotely green**

Machine decision:

- `registries/dreyer_c5r_1_stage_h_live_recovery_qualification_decision.v0.json`

Request ID: `DREYER-C5R-1-HL1R1-QA0`

## Actual Maintainer Decision

After Codex identified the sole green qualification request, proof commit
`d1c003f303083d23685da858bca069397b1f9c58`, CI `33250971717`, and the
boundary between generated qualification and separately closed real-data work,
the maintainer said:

> continue

This record preserves those words exactly. It does not claim that the
maintainer typed the packet's long scope. The short instruction incorporates
the one immutable, remotely green packet by reference without widening it.

## Why The Short Form Is Valid

1. `DREYER-C5R-1-HL1R1-QA0` was the one named packet awaiting a decision.
2. Request `0213e7050dd845de16b5f1abac4573f30b534452` and proof
   `d1c003f303083d23685da858bca069397b1f9c58` were already on GitHub
   `main` with both required CI jobs green.
3. Codex named the packet, proof, CI, qualification boundary, and exclusions
   immediately before the maintainer's fresh message.
4. The maintainer then unambiguously directed continuation.
5. This separate record quotes the actual words and binds the exact request and
   proof artifacts.
6. No H-L2, real/private path, network, EDF, target, model, score, device,
   release, other-project, or scientific-claim authority is inferred.

This decision is ineffective until its own exact commit passes Base Python and
Optional Neuro Readers on GitHub `main`.

## Exact Authorized Work

After this decision is remotely green, add a narrow generated-only
qualification coordinator, CLI, and tests that call the exact green recovery
implementation without modifying it. Unit qualification may use generated
fixtures, temporary directories, mocked resource readers, and refusal-only
injections.

The exact coordinator must then be committed, pushed, and pass both required
CI jobs before one registered qualification may run. That sole run must execute
two deterministic H1 replays, the inherited two valid Stage H cases and 18
Stage H refusals, and all 43 ordered H-L1R1 refusal identities. It must enforce
the frozen 30-second, 256 MiB RSS, 8 MiB generated I/O, 16 MiB temporary disk,
1 MiB public output, one-thread, one-worker, zero-network envelope.

The attempt is consumed on pass or failure. No retry, rerun, repair, resume,
substitution, threshold change, or post-result amendment is authorized. The
coordinator may expose no real path, URL, network, or H-L2 command. H-L2 and
the real 14,805,604-byte EDF remain separately closed regardless of the result.

## Decision-Only Measurements

```text
fresh GitHub CI verification calls:             1
real/private path operations:                    0
real requests / network bytes / EDF bytes:       0 / 0 / 0
fixed-header / annotation / signal reads:        0 / 0 / 0
target or label reads:                           0
model loads / training / inference / scoring:    0 / 0 / 0 / 0
generated qualification attempts consumed:      0
provider / stream / device / hardware actions:   0 / 0 / 0 / 0
retries / reruns / releases / claim upgrades:    0 / 0 / 0 / 0
end-to-end latency measured:                     false
```

Engineering capability authorized: build, green, and run one bounded
generated-only qualification coordinator over the complete frozen H-L1R1
matrix.

Scientific claim not established: this decision accesses no real EEG and
establishes no neural, decoding, unseen-person, peripheral-adjusted, live,
hardware, or clinical result.
