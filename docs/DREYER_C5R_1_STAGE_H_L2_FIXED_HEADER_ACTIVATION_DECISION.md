# DREYER-C5R-1 H-L2 Fixed-Header Activation Decision

Date: 2026-08-29

Status: **packet-bound ordered H-L2 authorization recorded; effective only
after this exact decision is tested, committed, pushed, and both required CI
jobs are remotely green**

Machine decision:

- `registries/dreyer_c5r_1_stage_h_l2_fixed_header_activation_decision.v0.json`

Request ID: `DREYER-C5R-1-HL2-A0`

## Actual Maintainer Decision

After Codex identified `DREYER-C5R-1-HL2-A0` as the single active packet,
reported proof commit `036a9ec7e78b460d464c3349151ddb0e35914d87`, CI
`33257159816`, both green jobs, and the boundary before implementation or any
real EDF access, the maintainer said:

> continue

This record preserves those words exactly. It does not claim that the
maintainer typed the packet's long scope. The short instruction incorporates
the one immutable, remotely green packet by reference without widening it.

## Why The Short Form Is Valid

1. `DREYER-C5R-1-HL2-A0` was the one named packet awaiting a decision.
2. Request `a97fc191106e5fe42859d871d78e59930bef79ac` and proof
   `036a9ec7e78b460d464c3349151ddb0e35914d87` were already on GitHub
   `main` with both required CI jobs green.
3. Codex named the packet, proof, CI, ordered barriers, and real-data boundary
   immediately before the maintainer's fresh message.
4. The maintainer then unambiguously directed continuation.
5. This record quotes the actual words and binds the exact request and proof
   artifacts.
6. No authority beyond the request's exact object, operations, order, caps, or
   exclusions is inferred.

This decision is ineffective until its own exact commit passes Base Python and
Optional Neuro Readers on GitHub `main`.

## Exact Ordered Authorization

After this decision is remotely green, implement one additive standard-library
H-L2 execution adapter and qualify its wiring and refusal behavior using only
generated fixtures. The adapter must not modify any frozen predecessor or
expose annotations, signal samples, targets, models, predictions, or scoring.

The exact adapter must then be committed, pushed, and pass both required CI
jobs. Only after that may one no-authority activation record be created. The
activation must bind the exact green adapter and preserve the request's object,
transport, sensor, resource, publication, and exclusion contracts. It must
itself be committed, pushed, and pass both required CI jobs before execution.

Only after every barrier may one registered invocation write its durable
consumed marker before constructing an opener or making a request, perform one
verified direct HTTPS GET for exactly this object, and parse at most its EDF
fixed header:

```text
sourcedata/sub-01/eeg/sub-01_task-R1acquisition_eeg.edf
declared bytes: 14,805,604
SHA-256: a678fe6d37e0496eb381dcac6b877b047d02dfffc659ae4cfc38226f4850e185
```

The invocation must require HTTP success, exact final URL, exact identity
content length, EOF, exact SHA-256, no redirects, no retries, no ranges, no
resume, no proxy, no cookies or credentials, and cleanup of only
invocation-created unaccepted staging. It may retain an accepted private,
Git-ignored payload only on exact identity and H1 sensor-contract success.

The fixed header may establish only the registered label roster, channel-role
counts, physical sampling rate, and aggregate H1/H0 route. It may not open or
interpret annotations, samples, targets, labels, trials, events, participant
outcomes, or the remaining 119 EDFs. The sole attempt is consumed on success or
failure and may not be retried, rerun, repaired, resumed, substituted, or
reinterpreted.

## Resource Envelope

```text
CPU threads / workers / numerical jobs:          1 / 1 / 0
runtime maximum:                                 300 seconds
peak process-tree RSS maximum:                   268,435,456 bytes
payload network maximum:                         16,777,216 bytes
incremental disk maximum:                        33,554,432 bytes
public output maximum:                           1,048,576 bytes
stream chunk maximum:                            1,048,576 bytes
free disk minimum before invocation:             10,737,418,240 bytes
real HTTP GETs / redirects / retries / reruns:   1 / 0 / 0 / 0
```

## Decision-Only Measurements

```text
fresh GitHub CI verification calls:             1
implementation or generated qualification:      0
activation or consumed-marker operations:        0
real/private path operations:                    0
real requests / network bytes / EDF bytes:       0 / 0 / 0
header / annotation / signal / target reads:     0 / 0 / 0 / 0
model / prediction / target delivery / scoring:  0 / 0 / 0 / 0
provider / stream / device / hardware actions:   0 / 0 / 0 / 0
retries / reruns / releases / claim upgrades:    0 / 0 / 0 / 0
end-to-end latency measured:                     false
```

Engineering capability authorized: build and green one bounded H-L2 adapter,
green one no-authority activation record, and then perform one exact public EDF
fixed-header checkpoint under the frozen transaction and sensor contracts.

Scientific claim not established: this decision accesses no real EEG and
establishes no neural, decoding, unseen-person, peripheral-adjusted, live,
hardware, or clinical result.
