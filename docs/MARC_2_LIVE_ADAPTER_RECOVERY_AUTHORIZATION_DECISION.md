# MARC2-LA2 Live Adapter Recovery Authorization Decision

Date: 2026-08-16

Lane: `MARC2-LA2`

Status: **Packet-bound short-form authorization recorded; ineffective until
this decision is committed, pushed, and both required CI jobs are green; no
executor, private path, output root, archive, neural, target, model, or score
operation occurred**

Machine decision:
`registries/marc2_live_adapter_recovery_authorization_decision.v0.json`

## Maintainer Decision

After Codex identified `MARC2-LA2` as the sole active Tier C packet and named
its exact commit, CI run, two required jobs, packet/request hashes, two-stage
scope, and fresh-decision boundary, the maintainer's next message was exactly:

```text
continue
```

The machine record preserves those actual eight UTF-8 bytes and their SHA-256.
It does not fabricate the packet's long scope as a maintainer utterance. Under
the approved Research Autonomy Charter, this fresh unambiguous short form binds
only the immutable remotely green `MARC2-LA2` packet by reference.

## Green Request Bound By This Decision

```text
request commit:       f9f24a37d840e3408c19dc00830096f6c24b8e03
CI run:               31937038394
Base Python job:      95140483613
Optional Neuro job:  95140483638
packet SHA-256:       8df6efb504e54c3d7ca96f0fda982c0c1e3443eb140df1d0babbc3b91fd7cda6
request SHA-256:      770b3525c549f7d28cfb278ac7af9d166f6ddcf17a54c4fc1625b9e60c2a2fe0
```

The local branch and request commit matched before this decision was written.
GitHub reported both required jobs successful. The pre-existing untracked
tracker inspection sidecar was not opened, modified, staged, or deleted.

## Delayed Effect

This decision is not effective merely because these files exist. Before any
executor implementation or retained-path operation, this exact decision must:

1. pass focused and complete local verification;
2. be committed as one coherent decision-only milestone;
3. be pushed on the current branch; and
4. pass both required remote CI jobs.

Until all four conditions hold, implementation and private access remain
closed.

## Authorized Sequence After Green Decision

### Stage 1: generated/mock executor

After this exact decision is remotely green, one additive standard-library
module may be implemented and qualified:

```text
neurodecodekit.datasets.marc2_live_schema_adapter_recovery
```

Development may use only generated manifests, mocked no-follow filesystem
facts, injected readers, and invocation-created temporary outputs. The module
may import the exact shared proof validator, LA1 live-schema adapter, and
frozen selector. It must not import, call, edit, copy, or expose either
consumed private executor or either consumed output root.

The implementation must preserve two proof records: a native registry with
`lane_id: MARC2-LA2` and a distinct FW1B-format proof certificate supplied to
the exact `validate_implementation_record` symbol. Both proof envelopes must
bind the future executor HEAD, packet-bound decision, module, registry, tests,
green LA1 artifacts, shared validator, and selector. A copied, forked, aliased,
or weaker validator or adapter must fail closed before any path operation.

Generated qualification must exercise 32 proof-certificate mutations and 24
executor mutations, for 56 direct mutations. The complete suite must also run
the inherited LA1 and selector tests. Canonical and reversed generated inputs,
exact adapter/selector call counts, deterministic replay, bounded output, and
zero private operations are required.

The exact Stage 1 implementation, tests, native registry, proof certificate,
and qualification must then be committed, pushed, and pass both remote jobs.
Stage 1 may not inspect the retained manifest.

### Stage 2: one target-free structural selection

Only after the exact Stage 1 executor is remotely green may one no-retry
invocation read this literal retained file:

```text
.codex_work/marc1_central_directory/live_audit_v0/member_inventory.private.v0.json
```

Bound identity:

```text
bytes:              418,755
mode:               0600
SHA-256:            2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031
schema:             neurodecodekit.marc1_central_directory_private_manifest
schema version:     0.1.0
entries:            1,227
regular entries:    1,025
directory entries:  202
run bundles:        195
```

The future executor must reject resolution, globbing, directory listing,
sibling inspection, symlink components, a symlink final path, non-regular
files, wrong ownership or mode, and alternate paths. It may perform one
no-follow content open, one open/fstat reconciliation, one bounded sequential
read, one SHA-256 pass, and one strict JSON parse.

The new output root is exactly:

```text
.codex_work/marc2_freewill_prefix/live_alias_recovery_v2
```

It must be absent, non-symlinked, and distinct from both consumed roots. After
proof, machine, output, and path preflight but before the private content open,
the executor must atomically write one mode-`0600` consumed marker. Every later
route is final.

If source identity and strict JSON pass, the executor may call LA1's exact
`adapt_live_shaped_source` once, prove source immutability and mutable-object
independence, require TA1 to map only `directory` to `central_directory`, and
call the unchanged target-free selector once. The frozen rule remains a
maximal ranked 12-to-19-participant prefix, `ses-01` fit, `ses-02` held out,
six bundles and 24 members per participant, first non-fitting stop, and an
8 GiB reservation ceiling. No target, label, event, quality, signal, channel,
trial, outcome, archive member, or local-header value may influence selection.

At most one mode-`0600` consumed marker, one mode-`0600` private structural
selection manifest, and one aggregate report may be written. The aggregate
report may not expose member names, offsets, CRCs, retained paths, private
rows, or source bodies.

## Frozen Routes And Resources

Routes remain `MARC2LAR-F00` through `MARC2LAR-F06`, then success
`MARC2LAR-R1`. Every route consumes the invocation. Success does not authorize
an archive member, payload, `MARC2-FW2`, or a scientific claim.

```text
CPU threads / workers / numerical jobs: 1 / 1 / 1
runtime:                                 <= 30 seconds per stage
peak RSS:                                <= 256 MiB
private input opens / bytes:             1 / 418,755 exact
network requests / bytes:                0 / 0
archive local-header/member bytes:       0
combined output:                         <= 2 MiB
incremental disk:                        <= 4 MiB
minimum free disk:                       >= 15 GiB
selected future reservation:             <= 8 GiB accounting only
retry / rerun / resume:                  0 / 0 / 0
```

## Explicitly Not Authorized

This decision does not authorize implementation before this decision is
remotely green; private access before the exact executor is remotely green;
operation on consumed executors or roots; another path, sibling, listing,
network request, or download; archive headers, members, payloads, extraction,
or range requests; EEG samples, signals, events, channels, geometry, targets,
labels, quality, onsets, responses, trials, or outcomes; derivatives, caches,
features, split payloads, or NeuroTokens; training, checkpoints, inference,
predictions, freezes, target delivery, or scoring; `MARC2-FW2`, `MARC2-CIL1`,
`MARC2-ORTH1`, or `NDK-LANG1`; providers, language models, RW3, streams,
devices, or hardware; retries, reruns, resumes, substitutions, fallbacks, or
amendments; pre-existing cleanup, overwrite, move, rename, or deletion;
operation on another project; release, publication, participant-level
protected output, or scientific claim upgrade.

## Current Counters And Next Gate

At decision recording, one GitHub CI verification was performed. Every
executor, private-path, output-root, archive, payload, neural, target,
derivative, model, score, provider, hardware, retry, cleanup, release, and
claim counter is zero. End-to-end latency was not measured.

Verify this decision, commit it, push it, and require both CI jobs green. Only
then may Stage 1 generated/mock implementation begin. Stage 2 remains closed
until that exact executor is separately committed, pushed, and remotely green.

## Claim Boundary

Engineering capability authorized after green decision: one proof-gated
additive executor may be generated-qualified and, only after its own green
proof, apply the exact LA1 adapter and frozen selector to one exact private
structural manifest without opening an archive member.

Scientific claim not established: this decision is not neural data or a
result and establishes no neural effect, decoding accuracy, brain-specific
origin, language decoding, or thought-to-text capability.
