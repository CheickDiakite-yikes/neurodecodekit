# COMM-P0-G Registration Proof Closeout

Date: 2026-08-27

Status: **proof-only closeout pending its own remote CI; no qualification
execution authorized**

Proof: `COMM-P0-G-PROOF-v0`

Machine record:
`registries/communication_eeg_prospective_generated_qualification_proof.v0.json`

## Exact Green Registration

The generated-only registration commit
`002128bd9cbacddd8ceea1820b3b91622c40867f` passed GitHub CI run
`33136788477`:

- Base Python job `98738427136`: success;
- Optional Neuro Readers job `98738427224`: success; and
- both the working branch and GitHub `main` resolved to the exact commit before
  this closeout was written.

The proof binds the unchanged preregistration and machine contract plus the
closeout-era matching test, totaling 47,562 bytes under canonical artifact-set
SHA-256
`6d73c138a0da1839951522234c2fadf334c31acfce077bed887f643ed68e5f76`.
Their byte, SHA-256, and Git-blob identities are recorded in the machine proof.
All three are present in the proof commit, so a depth-1 CI checkout can verify
the evidence without fetching historical Git objects.

## What Is Frozen

This closeout preserves the complete `COMM-P0-G-v0` contract:

- two isolated child-process replays over 42 complete fictional participants;
- 10,752 structural rows, 1,428 prediction sets, and 91,392 prediction rows per
  replay;
- all 73 synchronized biosignal roles plus separately typed microphone and
  trigger surfaces;
- fixed-size target precommits, four-second post-washout context, and strict
  scorer isolation;
- participant-first scoring and the authoritative 70% stable-commit coverage
  threshold;
- all 70 named refusal families in both replays; and
- one-thread, 180-second, 512 MiB RSS, zero-network, bounded-output limits.

No condition name, trial count, target rule, model family, statistic, live
endpoint, refusal family, resource cap, or claim boundary changes here.

## Next Ordered Barrier

After this exact closeout is committed, pushed, and Base Python plus Optional
Neuro Readers are green, bounded Tier B implementation may begin against
generated fixtures only. The implementation must be committed, pushed, and
remotely green before a separate activation can permit the one official
generated qualification.

This closeout does not authorize the official qualification invocation. It
also does not authorize recruitment, consent collection, voice capture,
physical devices, real or private data, model access outside the frozen
generated family, release, or any scientific claim. `DREYER-C5R-1-HL` remains
the sole active Tier C packet with all authority flags false.

## Measured Operations

This proof read only the three tracked registration artifacts, their Git
identities, one GitHub CI run, and the two remote refs. It created no generated
study payload and retained no generated fixture. Real/private path operations,
signal reads, target reads, model runs, training, inference, prediction sets,
target deliveries, scores, provider calls, network payload bytes, device
operations, and cleanup operations were all zero.

Engineering capability added: the generated qualification registration is
immutably bound to its exact green code, protocol, tests, CI jobs, and GitHub
`main` identity, enabling a drift-resistant implementation milestone.

Scientific claim not established: this proof accessed no real EEG, person,
device, target, prediction, or score and therefore establishes no communication
decoding, EEG-beyond-peripheral information, unseen-person generalization,
independent replication, live decoding, hardware performance, or clinical
value.
