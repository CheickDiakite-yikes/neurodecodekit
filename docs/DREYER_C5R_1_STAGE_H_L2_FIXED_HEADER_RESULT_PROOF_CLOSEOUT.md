# DREYER-C5R-1 H-L2 Fixed-Header Result Proof Closeout

Date: 2026-08-29

Status: **proof-only closeout recorded; effective after this exact closeout is
committed, pushed, and both required CI jobs are green**

Machine proof:

- `registries/dreyer_c5r_1_stage_h_l2_fixed_header_result_proof.v0.json`

## Remotely Green Result Closeout

Exact result closeout commit
`eec3db2a225d6ea2377e6a2426297c4d327059b1` passed on GitHub `main`:

- Base Python job `99128522536` in 5m14s;
- Optional Neuro Readers job `99128522652` in 11m58s; and
- CI `33263185054`.

The same commit also passed branch CI `33263182131`, Base Python job
`99128514962`, and Optional Neuro Readers job `99128514958`.

The proof binds four unchanged public result artifacts totaling 14,734 bytes
under canonical artifact-set SHA-256:

```text
642f681b8836f6c044bda27df5a2b3e84ade95b325b9d02cee6e8631cc0b0e07
```

## What Is Closed

The sole `DREYER-C5R-1-HL2-R0` invocation is permanently consumed at aggregate
route `DREYER-H0` with sanitized refusal `HL2-TRANSPORT`. It opened one public
response and refused during strict transport validation before reading any
response body.

The result records zero network body bytes, payload hashes, EDF fixed-header
reads or parses, annotation reads, signal reads, target reads, model runs,
training runs, predictions, or scores. Runtime was
0.6479716249741614 seconds, peak process-tree RSS was 36,454,400 bytes, and no
payload was retained.

This is a fail-closed transport result, not a biological null. It neither
accepts nor rejects the frozen Dreyer neural hypothesis.

## Proof-Only Operations

This transition verifies two existing CI runs and four committed public
artifacts. It performs no retry, rerun, repair, resume, substitution,
reinterpretation, ignored or private path access, research-source request,
payload or marker read, EDF read, target delivery, model operation, scoring,
device operation, release, or scientific-claim upgrade.

## Next Boundary

Commit, push, and green this exact proof-only closeout. No Tier C packet is
active. The next reversible task is source reselection: identify and
preregister a transport-verified scientific source without reopening this
consumed attempt. Any later real-data transaction requires a separately named,
exactly scoped, remotely green Tier C decision.

Engineering capability added: the consumed H-L2 transport refusal now has an immutable, hash-bound, remotely verified public evidence chain.

Scientific claim not established: no EDF body or header was read, so no sensor roster, neural information, decoding performance, unseen-person generalization, peripheral-adjusted effect, live operation, hardware result, or clinical value was established.
