# MARC2-VR13P R4 Private Discriminator Authorization Packet

Date: 2026-08-20

Lane: `MARC2-VR13P`

Status: **All-false Tier C request prepared; no decision or private access**

Request:
`registries/marc2_r4_private_discriminator_authorization_request.v0.json`

## Decision Requested

After this exact request is committed, pushed, and both required CI jobs are
green, and after Codex identifies it as the sole active Tier C packet, request
one future two-stage sequence:

1. implement and generated-qualify one additive fixed-path VR13P wrapper; and
2. only after that exact implementation and a proof-only closeout are each
   committed, pushed, and remotely green, perform one target-free structural
   read that either freezes one cohort or emits one residual class route.

This packet records no decision and grants no authority now. Earlier
`continue`, `approve`, or `proceed` messages are not retroactive authority for
this new packet. A fresh unambiguous packet-bound maintainer message is required
after the request and its proof closeout are remotely green.

## Why This Read Can Move The Program

Consumed VR12P called the repaired VR12A adapter once and returned broad route
`MARC2VR12P-R4`, retaining no private value or cohort. Remotely green VR13A now
proves that all seven residual identity/task/companion/taxonomy classes are
independently reachable and deterministic through the unchanged adapter.

The proposed private command asks the smallest remaining structural question:

- if VR12A succeeds, freeze its exact target-free source selection as one
  private cohort manifest and emit aggregate success route `MARC2VR13P-R1`;
- if VR12A refuses, map only its exact allowlisted route/reason pair through
  the remotely green VR13A table and emit one of `MARC2VR13P-R2` through R8.

R1 would make a separate FW2 preregistration eligible. It would not authorize
an archive member, EEG sample, target, model, training run, score, or FW2
execution. R2-R8 would identify one structural repair subject without
revealing the private value.

## Green Predecessor Proof

VR13A registration `1177174c1d466cf357ef3a81a4d96b39321af063`
passed CI `32424688012`. Exact implementation
`63a0b8ea4cc72b942a6b7dbdcd96680859f5f059` passed Base Python job
`96610793887`, Optional Neuro Readers job `96610793714`, and CI
`32426975815`. Proof-only closeout
`122c69e7fa60e7c193f12759e3b16d98fdb4f363` passed Base Python job
`96612707218`, Optional Neuro Readers job `96612707432`, and CI
`32427617438`.

Seventeen committed predecessor artifacts totaling 321,145 bytes are bound by
exact path, size, and SHA-256. Packet preparation does not inspect a private or
ignored path.

## Proposed Generated Stage

Only after a separate decision is remotely green may Stage 1 implement:

- exact request, decision, predecessor, implementation, and proof validation;
- a fixed-path no-follow state machine qualified only in temporary generated
  directories;
- one clean success control and all seven VR13A witnesses in canonical and
  reversed order across two exact replays;
- exactly one unchanged VR12A call per path and one VR13A residual-map call
  only on refusal;
- an R1 mock cohort manifest and R2-R8 aggregate-only failure reports;
- at least 80 direct refusals across proof, path, race, mode, hash, JSON,
  route, leakage, replay, resource, and forbidden-operation boundaries;
- deterministic replay, strict output firewalls, resource receipts, and zero
  retained generated output; and
- fixed `plan`, `qualify`, `inspect`, and `execute` CLI surfaces with no path,
  URL, output, retry, resume, fallback, substitution, or route override.

Generated qualification must not stat, resolve, hash, open, alter, delete, or
reuse any real `.codex_work` path or consumed VR11P/VR12P state. It must not
import, call, patch, copy, or modify a consumed private executor.

## Proposed One Private Diagnostic

Only after the exact future implementation and its proof-only closeout are
remotely green may one fixed command:

1. validate every exact green proof and all fixed path constants;
2. collect three consecutive passing machine-readiness samples;
3. create one fresh mode-`0600` certificate only at
   `.codex_work/marc2_machine_readiness/vr13p/readiness.v0.json`;
4. require `.codex_work/marc2_r4_private_discriminator/v0` to be absent;
5. no-follow preflight the registered structural source;
6. create one mode-`0600` consumed marker immediately before content open;
7. open, read, hash, and strict-parse exactly 418,755 bytes once;
8. call the unchanged remotely green VR12A adapter exactly once;
9. on success, write one source-exact private cohort manifest and one aggregate
   R1 report; otherwise call the exact VR13A residual map once and write one
   aggregate R2-R8 report; and
10. close and consume the lane with no retry or reinspection.

The immutable source identity is copied only from committed records:

```text
path:        .codex_work/marc1_central_directory/live_audit_v0/member_inventory.private.v0.json
mode:        0600
bytes:       418,755
SHA-256:     2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031
rows:        1,227
files:       1,025
directories: 202
bundles:     238 total / 195 eligible / 43 valid ineligible
```

Packet preparation performs zero path checks, stats, resolves, hashes, opens,
reads, or parses against that source, readiness path, output root, or consumed
state.

## Frozen Route Contract

| Route | Maximum engineering meaning |
|---|---|
| `MARC2VR13P-R1` | VR12A passed and one target-free structural cohort was frozen |
| `MARC2VR13P-R2` | residual suffix-bearing BIDS identity class |
| `MARC2VR13P-R3` | exact Freewill task-token class |
| `MARC2VR13P-R4` | companion run-token inconsistency class |
| `MARC2VR13P-R5` | normalized companion-collision class |
| `MARC2VR13P-R6` | incomplete companion-set class |
| `MARC2VR13P-R7` | repaired bundle-total mismatch class |
| `MARC2VR13P-R8` | taxonomy or eligibility mismatch class |

R1 may create one mode-`0600` private manifest containing 12-19 selected
subjects, 72-114 selected run bundles, 288-456 core members, disjoint session
roles, source-exact names, reservation accounting at or below 8 GiB, and the
already frozen target-free selection metadata. Its public report may expose
only aggregate counts and nonreversible cohort/provenance hashes.

R2-R8 create no cohort manifest. Every route retains no exception text,
predicate, failed value, row, path, identity, task spelling, companion state,
or candidate selection. Any unknown route, source mutation, inconsistent
selection, leakage, or resource breach consumes and parks the lane.

## Resource Limits

```text
CPU threads / workers / numerical jobs:  1 / 1 / 1
generated qualification:                 <= 60 seconds
future private command:                  <= 650 seconds
peak RSS:                                < 256 MiB
minimum free disk before marker:         15 GiB
fresh readiness wait:                    <= 600 seconds
private source read:                     exactly 418,755 bytes once
VR12A calls:                             exactly 1
VR13A residual-map calls:                0 on R1 / exactly 1 on R2-R8
network / new payload bytes:             0 / 0
archive-member / signal / target bytes:  0 / 0 / 0
combined incremental output:             <= 2 MiB
retries / reruns / resumes:              0 / 0 / 0
```

## Explicitly Not Requested

This packet does not request any operation before separate green request and
decision barriers; any consumed-state reuse; a second source open; retry,
rerun, resume, repair, fallback, substitution, or post-result reinspection;
an archive local-header or member-payload read; EEG/MEG signal, event, onset,
channel, geometry, sentence, key, label, target, quality, or participant-level
output access; derivative, cache, feature, NeuroToken, training, model,
checkpoint, inference, prediction, target delivery, scoring, selection, or
post-result tuning; download, provider, language model, RW3, stream, device,
or hardware work; FW2 or CIL1 execution; release or publication; or a
scientific, decoding, neural, real-time, portable, home-use, assistive, or
clinical claim upgrade.

## Failure Semantics

A failure before the marker performs zero private content opens and still
consumes the one registered invocation. A failure after the marker consumes the
diagnostic. Neither case permits a retry, rerun, resume, repair, fallback,
substitution, or private reinspection.

Engineering capability requested: one proof-gated target-free structural open
can either freeze the cohort needed to preregister FW2 or localize the one
remaining structural class that blocks it.

Scientific claim not established: packet preparation performs no private read,
and even the proposed diagnostic would access no neural payload, target,
prediction, or score, so it establishes no neural effect, decoding accuracy,
language decoding, live decoding, or thought-to-text capability.
