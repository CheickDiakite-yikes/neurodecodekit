# BNCI-C3C5-1 Stage G1 Recovery Authorization Decision

Date: 2026-08-24

Status: **Authorized only after this decision is tested, committed, pushed,
and remotely green; no recovery qualification or real operation has occurred**

Machine decision:

- `registries/bnci_2014_001_cross_participant_eeg_gain_stage_g1_recovery_authorization_decision.v0.json`

## Actual Maintainer Decision

After Codex identified `BNCI-C3C5-1-G1-recovery` as the sole active Tier C
packet and named proof commit `e1ad1eae373e0175eb05b2bb42f2f4a567a0fd49`,
CI `32761632135`, both required green jobs, the generated-only scope, the hard
stop before Stage A, and the need for a fresh decision, the maintainer said:

> continue

This record preserves those eight UTF-8 bytes exactly. It does not claim that
the maintainer typed the packet's long scope. The short instruction
incorporates the immutable, remotely green recovery packet by reference and
does not widen it.

## Why The Short Form Is Valid

1. `BNCI-C3C5-1-G1-recovery` was the sole active Tier C packet.
2. Proof commit `e1ad1ea` passed both required jobs in CI `32761632135`.
3. Codex named the packet, proof, scope, stop boundary, and fresh-decision gate.
4. The maintainer's next exact message was `continue`.
5. This additive record quotes the actual message and binds the immutable
   packet and proof artifacts.
6. No real-data, Stage A, retry, release, hardware, or claim authority is
   inferred.

The decision is ineffective until its own commit passes Base Python and
Optional Neuro Readers CI.

It binds six packet/proof artifacts totaling 25,369 bytes under canonical
artifact-set SHA-256:

```text
970d1ca5a10f51c4d73367ba5e9c7ebe9af8bf0811c43ae8c6cb500de5a99923
```

## Ordered Authorization

### Gate 1: remotely green this decision

Test, commit, push, and pass both required CI jobs for this exact record.
Before that proof, the replacement qualification remains closed.

### Gate 2: one replacement generated qualification

After Gate 1, run exactly one replacement generated/mock G1 qualification with
the already-frozen implementation. It may:

- create generated MAT fixtures only in one invocation-owned temporary root;
- use mocked transport and generated signal, feature, and target rows only;
- execute nine spawned folds sequentially under one active worker;
- perform exactly 468 synthetic parameter-update fits and 495 synthetic
  prediction sets;
- freeze generated predictions, deliver generated targets once, and run the
  synthetic aggregate scorer once; and
- emit one aggregate result under 4 MiB before removing only its own generated
  payload and derivative files.

No second replacement pass is authorized. A refusal parks G1 at R1. A pass
must be recorded, committed, pushed, and remotely green, then stop before
Stage A.

## Exact Limits

```text
replacement qualifications:    1
outer fold processes:           9, sequential
active workers / threads / jobs: 1 / 1 / 1
synthetic fits:                 exactly 468
synthetic prediction sets:      exactly 495
synthetic target deliveries:    1
synthetic scoring events:       1
runtime:                        3,600 seconds
peak process-tree RSS:          1,073,741,824 bytes
private generated bytes:        536,870,912
public output bytes:             4,194,304
minimum free disk:               5,368,709,120 bytes
network bytes:                   0
real-data operations:            0
retries / reruns:                0 / 0
```

## Still Forbidden

The decision does not authorize Stage A, network access, payload acquisition,
any existing or ignored data path, real MAT opening or parsing, real signal,
event, artifact, target, or label access, real feature or derivative creation,
real model fitting or inference, scientific scoring, post-result tuning, a
provider or language model, RW3, hardware, release, publication, or claim
upgrade.

## Decision-Only Measurements

```text
GitHub CI verification calls:               1
replacement generated qualifications:       0
generated MAT writes / parses:               0 / 0
mock transport calls:                        0
synthetic fits / prediction sets:            0 / 0
synthetic target deliveries / scores:        0 / 0
network requests / bytes:                    0 / 0
real or existing path operations:            0
MAT / signal / event / target / label reads: 0
Stage A or later operations:                 0
release / scientific claim upgrades:         0 / 0
end-to-end latency measured:                 false
```

## Local Verification Before Commit

- decision tests: 8 passed in 0.108 seconds;
- focused BNCI tests: 64 passed in 5.406 seconds;
- full dependency-free suite: 5,984 passed with 216 expected skips in
  209.188 seconds;
- pinned Ruff, Python compilation, all JSON registry parsing, and
  `git diff --check`: passed.

This verification executed no replacement qualification and did not touch a
real, ignored, consumed, or other-project payload.

Engineering capability authorized for testing: one clean, bounded replacement
run may test the generated target-firewalled BNCI pipeline end to end.

Scientific claim not established: this decision is not neural data or a
result and establishes no decoding, unseen-person, or EEG-beyond-EOG finding.
