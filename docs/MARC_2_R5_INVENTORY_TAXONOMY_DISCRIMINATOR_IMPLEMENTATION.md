# MARC2-VR27A R5 Inventory/Taxonomy Discriminator Implementation

Date: 2026-08-22

Lane: `MARC2-VR27A`

Status: **Generated qualification passed; exact implementation remote proof
pending**

Registration commit `47ceba3ed89df9610540fe3ed2ee8071ac1b84df`
passed Base Python job `97124216923`, Optional Neuro Readers job
`97124216871`, and CI `32611101033` before implementation.

## What Was Added

`src/neurodecodekit/datasets/marc2_r5_inventory_taxonomy_discriminator.py`
is a standard-library-only generated adapter over unchanged VR25A. It exposes
only `plan` and `qualify` commands. Each matrix path calls VR25A exactly once,
verifies that the generated source remains byte-identical, and maps only:

| VR25A route | VR27A route | Aggregate meaning |
|---|---|---|
| `MARC2VR25A-G1` | `MARC2VR27A-G1` | generated exact control passes |
| `MARC2VR25A-R1` | `MARC2VR27A-R1` | eligible inventory or distribution differs |
| `MARC2VR25A-R2` | `MARC2VR27A-R2` | recognized participant taxonomy differs |

Every other upstream route refuses. There is no `execute` command, private
path, URL, output root, threshold, retry, or resource override.

## Measured Qualification

The one measured generated qualification returned `MARC2VR27A-G1`:

- 5 cases x 2 orders x 2 replays = 20 paths;
- exactly 20 unchanged VR25A calls;
- VR27A route counts G1=4, R1=12, and R2=4;
- matching VR25A route counts G1=4, R1=12, and R2=4;
- 57 direct refusals;
- exact replay digest
  `128e03b565ec290e17c5e20aa516ef8f0c4020763c6d07d650db4702548dd68d`;
- zero source mutations and zero retained generated bytes;
- 8,836,136 generated input bytes;
- 1.1202826249646023 seconds runtime;
- 33,439,744-byte peak RSS;
- 2,561 aggregate output bytes;
- one CPU thread, worker, and numerical job;
- zero raw-data reads, real-cache reads, model runs, training runs, network
  bytes, and new payload bytes.

All private, ignored-path, readiness, consumed-state, archive, neural, target,
model, score, provider, FW2/CIL1, hardware, other-project, and claim counters
were zero.

## Interpretation

VR27A proves that the generated diagnostic interface can distinguish VR25A R1
eligible-inventory drift from R2 unknown-participant taxonomy while preserving
the original adapter and source bytes. It does not identify which branch
caused the consumed VR26P R5 result. No real cohort exists.

The implementation and result records deliberately leave remote proof null.
Commit, push, and green these exact bytes before adding a proof-only closeout.
Only after that separate closeout is remotely green may Tier A prepare a new
all-false private discriminator packet. Any private read still requires fresh
packet-bound Tier C permission.

Engineering capability added: deterministic generated route discrimination
between eligible-inventory drift and unknown-participant taxonomy.

Scientific claim not established: no neural payload, target, model,
prediction, or score was accessed, so this establishes no neural effect,
decoding accuracy, language decoding, unseen-person generalization, live
decoding, or thought-to-text capability.
