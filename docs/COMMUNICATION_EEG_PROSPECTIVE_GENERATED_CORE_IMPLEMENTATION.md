# COMM-P0-G Generated Core Implementation

Date: 2026-08-27

Status: **core implementation milestone pending remote CI; official generated
qualification not authorized or executed**

Machine record:
`registries/communication_eeg_prospective_generated_core_implementation.v0.json`

## Ordered Barrier

Registration proof closeout commit
`eafe1dfbcc300643c7638d0f01131f3bad2a1885` passed Base Python job
`98742273166`, Optional Neuro Readers job `98742273226`, and CI
`33137998642`, then reached GitHub `main`. That green barrier permits bounded
generated-only implementation against the unchanged `COMM-P0-G-v0` contract.

## What This Milestone Adds

The new dependency-free core in
`src/neurodecodekit/experiments/comm_p0_generated.py` adds:

- strict SHA-256-bound loading of the frozen registration;
- deterministic identities for 44 fictional enrollees, including exactly 42
  complete participants and two target-free hardware exclusions;
- the full 10,752-row structural plan with the exact six trial-role counts,
  96 combined free-choice washouts, 84-second padding budget, and active
  intention windows constrained to one segment;
- fixed-size HMAC-SHA256 target precommitments and one-shot cohort delivery
  gates, with recursively target-free public and decoder-facing records;
- a deterministic three-shard `SourceBindings` adapter for 32 + 32 + 9
  biosignal roles, separately typed microphone and trigger surfaces, shared
  sample/clock/gap/geometry ledgers, and strict bundle validation;
- streamed prediction-inventory hashing with four-class probability checks;
- participant-first 21-person summaries and exact `2^21` one-sided sign-flip
  enumeration using bounded Gray-code updates;
- live coverage, per-command coverage, inactive false-commit, dropped-chunk,
  deadline, stable-commit latency, and capture-to-presentation accounting;
- exact wrapper and unchanged-transaction checks for all 70 frozen refusal
  families; and
- generated-only `plan`, activation-locked `qualify`, and aggregate `inspect`
  CLI commands.

The implementation deliberately does not yet contain the full 630-classifier,
630-temperature-fit, 1,428-prediction-set model coordinator or the two isolated
official replay processes. Those are the next implementation milestone. The
official `qualify` command therefore fails closed before any fixture, model,
prediction, target delivery, score, or output is created.

## Development Evidence

The focused core suite generated and validated the complete fictional
10,752-row structural plan twice, checked all 73 synchronized roles, exercised
all 70 refusal wrappers, and completed one exact `2^21` sign-flip enumeration.
It ran 12 tests in approximately 1.12 seconds in the local development
environment. The registration, proof, and core-focused combined suite ran 28
tests in approximately 1.18 seconds. These are disposable engineering tests,
not the one official two-replay qualification.

No dense signal recording was materialized and no generated payload artifact
was retained. Network bytes, real/private path operations, real signal reads,
human recordings, device operations, model fits, official prediction sets,
target deliveries, scores, releases, and scientific claim upgrades were all
zero.

## Next Barrier

After this exact milestone is committed, pushed, and both required GitHub CI
jobs are green, implementation may continue with the compact model schedule,
isolated replay coordinator, scorer process, resource monitor, and
non-replacing publication. A later exact implementation record and separate
activation must both become remotely green before the single official
generated qualification invocation.

Engineering capability added: NeuroDecodeKit can now deterministically
represent and validate the full target-firewalled prospective communication
study structure, synchronized sensor bundle, participant-first statistic, live
metrics, and refusal inventory without real data or devices.

Scientific claim not established: this generated core accessed no person,
device, real EEG, target score, or official model result and therefore
establishes no communication decoding, EEG-beyond-peripheral information,
unseen-person generalization, independent replication, causal live decoding,
hardware performance, or clinical value.
