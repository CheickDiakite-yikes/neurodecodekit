# COMM-G2 Generated Proof Qualification Closeout

**Date:** 2026-08-27  
**Execution status:** consumed; no rerun  
**Closeout route:** `COMM-G2-R0`  
**Scientific value:** none

## What happened

Implementation commit `83658976fb334a6b125a441af5dbc8cccf416e1f`
passed Base Python job `98465586781`, Optional Neuro Readers job
`98465586951`, and CI `33056826167`. Only after that proof barrier, the one
registered generated-only qualification was invoked once.

The first replay coordinator did not return before its frozen 85-second child
deadline. The parent refused with `G2-CHILD-TIMEOUT`, published no official
qualification result, and securely removed its invocation-owned temporary
tree. A post-failure check found no `.comm-g2-*` directory and no intended
result file.

## Binding route

The preregistered router assigns every structural, isolation, filesystem,
resource, completeness, or refusal-family failure to `COMM-G2-R0`. A child
timeout prevents two complete replay records, byte-equivalence proof, and a
complete adversarial ledger. The binding route is therefore `COMM-G2-R0`.

No numerical route is accepted. The invocation is consumed and may not be
rerun, repaired in place, overwritten, or reinterpreted as a passed proof.

## Measurements and unavailable fields

| Measure | Observed |
|---|---:|
| Official invocations | 1 |
| Completed replay records returned to parent | 0 |
| Child deadline | 85 s |
| Overall registered wall cap | 180 s |
| Published public result bytes | 0 |
| Residual temporary directories | 0 |
| Real/private dataset bytes | 0 |
| Provider calls | 0 |

The failing path did not persist a failure receipt before cleanup. Exact
process-tree RSS, generated bytes, temporary peak bytes, completed parameter
updates, prediction sets, prediction rows, synthetic target deliveries, and
scores are therefore unavailable. They are not inferred from development
replays and are not reported as zero.

## Engineering lesson

The wrapper correctly failed closed and cleaned its own files, but the official
path was not observably complete under the registered per-child deadline. A
future lane would need prospective timeout diagnosis and a failure receipt that
survives cleanup. It must use a new identifier and registration; COMM-G2 itself
is closed.

## Boundaries

No real EEG or MEG was accessed. No real target, label, training run, model
inference, provider, stream, device, release, or claim operation occurred.
No communication decoding, EEG-beyond-peripheral information, unseen-person
generalization, independent replication, live decoding, hardware performance,
or clinical result is established. `DREYER-C5R-1-HL` remains the sole active
Tier C packet with every authority flag false.
