# MARC2-VR7P Dynamic Private Selection Recovery Authorization Decision

Date: 2026-08-16

Lane: `MARC2-VR7P`

Status: **Packet-bound short-form authorization recorded; ineffective until
this decision is committed, pushed, and both required CI jobs are green; no
readiness, private path, output root, marker, structural source, cohort,
archive, neural, target, model, prediction, or score operation occurred while
recording this decision**

Machine decision:
`registries/marc2_dynamic_private_selection_recovery_authorization_decision.v0.json`

## Maintainer Decision

After Codex identified `MARC2-VR7P` as the sole active Tier C packet and named
its remotely green request and proof commits, CI runs, two required jobs,
two-stage order, structural-only scope, and fresh-decision boundary, the
maintainer's next message was exactly:

```text
continue
```

The machine record preserves those actual eight UTF-8 bytes and their SHA-256.
It does not fabricate the packet's long scope as a maintainer utterance. Under
the approved Research Autonomy Charter, the fresh `continue` binds only the
immutable, remotely green `MARC2-VR7P` packet by reference, without expansion.

## Green Packet Bound By This Decision

```text
request commit:       9d42bac29b695a97639c4a197812865f0ac4f7d5
request CI:           31976595268
request Base job:     95236917861
request Optional job: 95236917836
proof commit:         ecaa2aba82726b6c428facf917868b78e0340969
proof CI:             31976872160
proof Base job:       95237574426
proof Optional job:   95237574453
```

Both required jobs passed for both exact commits. The immutable request
document, registry, and test hashes are preserved separately from the additive
green proof snapshot. The pre-existing untracked tracker inspection sidecar was
not opened, modified, staged, or deleted.

## Delayed Effect

This decision is not effective merely because these files exist. Before any
wrapper implementation, readiness invocation, output-root operation, or
private-source path check, this exact decision must:

1. pass focused and complete local verification;
2. be committed as one coherent decision-only milestone;
3. be pushed on the current branch; and
4. pass both required remote CI jobs.

Until all four conditions hold, implementation and private access remain
closed.

Local verification completed with 25 focused request/decision tests passing.
The canonical dependency-light inventory ran 3,932 tests: 3,896 passed, 35
skipped, and the sole error was the established sandbox refusal to create a
forkserver Unix socket. That exact test passed outside the sandbox. The
decision adds 14 tests and zero new failures.

## Authorized Sequence After Green Decision

### Stage 1: additive generated/mock wrapper

After this exact decision is remotely green, one additive standard-library
wrapper may be implemented and qualified using only invocation-created
generated fixtures, injected machine samples, mocked no-follow filesystem
facts, and temporary generated output roots.

The wrapper must expose no generic source, output-root, path, retry, resume,
fallback, cleanup, or substitution argument. It must preserve the fixed VR7P
paths, fresh readiness state machine, marker-before-open transition, one strict
JSON read, one VR6 adapter call, dynamic 12-19-subject selection, aggregate
privacy, deterministic replay, resource monitors, and zero-retry semantics.

Qualification must cover 12, 14, 16, 18, and 19 subjects in canonical and
reversed row orders, at least ten success paths, and at least 72 direct refusal
mutations. It may not stat, resolve, hash, open, alter, delete, or reuse a real
`.codex_work` path.

The exact Stage 1 implementation and its proof record must then be committed,
pushed, and pass both remote CI jobs before any real operation begins.

### Stage 2: one machine-stable structural pass

Only after the exact Stage 1 implementation is remotely green may one command:

1. verify its exact green proof record;
2. obtain three consecutive passing machine-readiness samples and write one
   fresh mode-`0600` certificate at the fixed VR7P path;
3. require the new output root to be absent and recheck disk, thread, load, and
   RSS safety immediately before the irreversible boundary;
4. no-follow validate the exact registered structural source;
5. atomically write one mode-`0600` consumed marker immediately before the
   source content open;
6. read, hash, and strict-parse exactly 418,755 structural bytes once;
7. call the green VR6 adapter exactly once; and
8. write one private cohort manifest and one aggregate-safe report.

The selected cohort count, reservation bytes, and selection hash are measured
outputs. Success requires the frozen target-free rank's maximal contiguous
prefix, 12-19 subjects, six bundles and 24 core members per subject, exact
session split, no row-random split, at most 8 GiB declared reservation, and
either all 19 subjects or proof that the exact next ranked subject exceeds the
remaining cap.

No generated subject count, reservation total, or selection hash may be used
as a live expected value.

## Resources And Failure Semantics

```text
CPU threads / workers / numerical jobs: 1 / 1 / 1
generated qualification:                <= 30 seconds
future real command:                    <= 650 seconds
peak RSS:                               < 256 MiB
minimum free disk before marker:        >= 15 GiB
fresh readiness wait:                   <= 600 seconds
private structural input:               exactly 418,755 bytes once
network / archive payload bytes:        0 / 0
combined incremental output:            <= 4 MiB
retry / rerun / resume / fallback:      0 / 0 / 0 / 0
```

A refusal before the marker performs zero private structural content opens and
permits no retry. Every route after the marker consumes the one structural
pass, also without retry, rerun, resume, repair, substitution, or fallback.
Cleanup authority is limited to temporary files created by the future
invocation; no other project, consumed root, certificate, marker, or artifact
is in scope.

## Explicitly Not Authorized

This decision does not authorize implementation before this decision is
remotely green; real operation before the exact wrapper is remotely green; any
operation on a named consumed path; another path, source, output root, project,
dataset, participant, session, or file; network or download; archive-member,
ZIP-range, EEG, MEG, signal, event, onset, channel, geometry, sentence, key,
label, target, response, trial, quality, or payload access; derivative or split
creation; training, parameter updates, checkpoint access, inference,
prediction, freeze, target delivery, scoring, or tuning; `MARC2-FW2` or
`MARC2-CIL1`; a language model, provider, RW3, stream, device, or hardware
operation; release or publication; or any scientific, decoding, neural,
real-time, portable, home-use, assistive, or clinical claim upgrade.

## Next Gate

After this decision is remotely green, implement and generated-qualify the
exact fixed-path wrapper. After that exact implementation is remotely green,
perform the one registered structural pass. Only a successful, remotely green
cohort freeze makes Tier A `MARC2-FW2` preregistration eligible.

Any future archive payload, neural derivative, model fit, prediction freeze,
target delivery, score, or live decoding run requires its own immutable
all-false Tier C packet and fresh decision.

## Claim Boundary

Engineering capability authorized after green decision: one newly proven,
machine-stable wrapper may freeze a dynamically measured real target-free
cohort identity from one exact structural manifest without opening an archive
member.

Scientific claim not established: this decision is not neural data or a result
and establishes no neural effect, decoding performance, brain-specific origin,
language decoding, live decoding, or thought-to-text capability.
