# DREYER-C5R-1 Generated Qualification Result

Date: 2026-08-26

Status: **passed once; consumed; generated engineering evidence only**

The sole registered Stage G qualification ran only after exact implementation
commit `7fb185837ec33a9491212264f7e41a12b6a8d9c6` passed Base Python job
`98062624957`, Optional Neuro Readers job `98062625144`, and CI run
`32930838438`. The executor recollected that proof from fresh remote metadata
before starting numerical work.

## What Passed

The generated fixture exercised the full proposed target-firewall path:

- one valid synthetic EDF fixed header and eight malformed-header refusals;
- causal one-second spectral features with stable three-band localization;
- six synthetic participants, two runs each, and 120 identity-bound rows;
- 330 parameter-update fits and 258 model-inference calls;
- 102 held-out prediction sets containing 2,040 prediction rows;
- one aggregate hash-only prediction freeze;
- refusal of premature and repeated target delivery;
- refusal of a prediction mutation after freeze;
- exactly one synthetic target delivery and one aggregate synthetic score; and
- zero post-target updates.

The known generated central effect routed to `DREYERC5R-R1`. That route proves
that the scorer can detect its deliberately planted synthetic positive control.
It is not a scientific result.

## Measurements

- runtime: 2.448583166929893 seconds;
- peak process-tree RSS: 218,497,024 bytes;
- generated input: 197,632 bytes;
- private temporary predictions: 297,663 bytes;
- public result: 3,695 bytes;
- public prediction freeze: 2,228 bytes;
- producer: causal with 1.0 second required context; and
- end-to-end latency: not measured.

The exact result is
`registries/dreyer_c5r_1_generated_qualification_result.v0.json`, 3,695 bytes,
SHA-256 `58fa7207a935edefc1337813319aa17ee8a3e9faee126970733fa52436a03a74`.

## Access Boundary

Real or private path opens, EDF downloads, real EDF header reads, annotation
reads, signal reads, target reads, real training runs, real prediction sets,
real target deliveries, real scores, analysis-network calls, model downloads,
language-model calls, and claim upgrades were all zero. The only networked
operations were one Git remote-metadata check and two GitHub Actions metadata
checks required to prove the implementation barrier.

Stage G is consumed and must not be rerun. Its committed result makes the
registered output path no-clobber, and the executor refuses before remote or
numerical work when that path exists.

## Next Gate

The next step is a separate, bounded Stage H preflight for exactly one pinned
14,805,604-byte source EDF. Stage H may inspect only the fixed EDF header to
confirm the prospective 27 EEG, three EOG, two EMG, 512 Hz sensor contract. It
must park the lane before the remaining 119 files if that roster is absent or
ambiguous. Real access still requires its own exact Tier C decision after the
implementation and authorization packet are committed, pushed, and remotely
green.

Engineering capability added: the complete compact-model, source-only
calibration, target-firewall, prediction-freeze, and one-delivery scorer path
passed a bounded generated end-to-end qualification.

Scientific claim not established: no real EEG, peripheral signal, annotation,
target, unseen participant, neural effect, movement intention, language,
live-decoding, hardware, or clinical result was tested.
