# MARC2-VR22P R5 Private Discriminator Result

Date: 2026-08-22

Lane: `MARC2-VR22P`

Status: consumed at `MARC2VR22P-R4`; no retry, rerun, resume, repair,
fallback, substitution, cleanup, amendment, or private reinspection

Machine result:
`registries/marc2_r5_private_discriminator_result.v0.json`

## Proof Before Execution

Packet-bound decision `197f253e9e0411085f6ecdccf466f9f7059bd479`
passed Base Python job `97080176920`, Optional Neuro Readers job
`97080176717`, and CI `32593234295` before Stage 1.

Exact Stage 1 implementation `bc8ecc38374c57f6be9269874cce8d4497f83551`
passed Base Python job `97082352527`, Optional Neuro Readers job
`97082352441`, and CI `32594134914`. Proof-only closeout
`ea39319f4c2660154cf64bcb7e38aa5fce7829c5` then passed Base Python job
`97083802440`, Optional Neuro Readers job `97083802313`, and CI
`32594732196`. No qualification or private operation was repeated during
closeout.

## One-Shot Result

The registered command collected three fresh readiness samples, opened the
fixed 418,755-byte target-free structural source once, strict-parsed it once,
called VR20A once, and applied the frozen VR21A route map once. It returned
`MARC2VR22P-R4` and created no private cohort manifest. Runtime was
0.056165415968280286 seconds, peak RSS was 30,900,224 bytes, combined output
was 2,872 bytes, and network and new payload bytes were zero.

Under the frozen route table, R4 means the source reached VR20A F06. That
class contains exactly two public refusal sites: source-kind or run-bundle
total arithmetic, and participant taxonomy or eligibility arithmetic. The
ordered result excludes the F07 selection, split, rank, and reservation class
for this exact invocation. It does not identify which F06 predicate failed
and does not retain a value, member name, path, row, identity, participant,
taxonomy detail, selection, or cohort.

## Operation Boundary

The invocation wrote only its authorized readiness certificate, consumed
marker, and aggregate report. The aggregate report was inspected once through
the fixed public `inspect` command. No private cohort manifest was created or
inspected, and the private source was not reopened or rehashed.

Archive members, neural payloads, signals, events, channels, geometry,
targets, labels, caches, features, splits, models, training, inference,
predictions, scoring, FW2/CIL1, network, providers, streams, devices,
hardware, releases, and other projects were untouched.

## What Comes Next

The next safe task is an artifact-only and generated-only decomposition of the
two exact F06 call sites. It can separately exercise source-domain arithmetic
and taxonomy/eligibility arithmetic through the unchanged public adapter
without touching the consumed private source. Any later private confirmation,
cohort freeze, archive or neural payload access, target/model/score work, FW2,
or CIL1 remains a new Tier C packet and decision.

Engineering capability added: one proof-separated target-free structural
invocation localized the remaining blocker from F06-or-F07 to the exact F06
taxonomy/eligibility class without exposing private detail.

Scientific claim not established: no neural signal, target, model,
prediction, or score was accessed, so this establishes no neural effect,
decoding performance, language decoding, unseen-person generalization, live
decoding, or thought-to-text capability.
