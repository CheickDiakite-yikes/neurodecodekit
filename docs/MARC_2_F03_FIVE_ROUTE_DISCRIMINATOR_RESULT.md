# MARC2-VR10B F03 Five-Route Discriminator Result

Date: 2026-08-17

Route: `MARC2VR10B-G1`

Status: **Artifact-only and generated-only qualification remotely green**

Machine result:
`registries/marc2_f03_five_route_discriminator_result.v0.json`

## Result

The ordered classifier cleanly separated all five frozen generated F03
mechanisms. Across canonical and reversed source order and two exact replays,
each class route appeared four times:

| Generated structural class | Aggregate route | Paths |
|---|---|---:|
| member-name UTF-8 length | `MARC2VR10B-R1` | 4 |
| suffix-bearing BIDS identity | `MARC2VR10B-R2` | 4 |
| exact task token | `MARC2VR10B-R3` | 4 |
| logical companion uniqueness | `MARC2VR10B-R4` | 4 |
| four-companion completeness | `MARC2VR10B-R5` | 4 |

The four generated clean controls returned `MARC2VR10B-G1`. All 20 witnesses
still produced outer `MARC2VR6-F02` and nested `MARC2VR2-F03`, so the new
routes refine the generated engineering diagnosis without changing the frozen
upstream validator.

## Measurements

The qualification completed 24 exact parser/producer paths, 24 VR6 calls, 24
discriminator calls, and 29,448 parser-entry visits. It processed 6,979,708
generated input bytes in 2.725759166991338 seconds at 44,564,480-byte peak RSS.
The aggregate report was 7,515 bytes, retained output was zero, and 60 direct
refusals passed.

The complete internal replay digest was
`e2c184bbe53c6a1d298cfcd4fef86f0910450b2557e906404816c1175b5a21df`.
Every classifier call preserved its source object. Every private, archive,
neural, target, model, score, network, provider, hardware, FW2/CIL1,
other-project, retry, release, and claim counter remained zero.

## What This Proves

The five still-plausible F03 mechanisms are mechanically distinguishable at
the exact generated parser/producer boundary using one coarse route code. The
router is order-invariant, replay-deterministic, resource-bounded, and does not
need to publish a failed value, row, path, identity, or per-item outcome.

## What Remains Unknown

This result does not identify the cause of the consumed private F03 outcome.
No private discriminator was invoked, and G1 is not private validity evidence.
No cohort, neural sample, event, channel, geometry, target, label, derivative,
model, prediction, or score was accessed.

FW2 and CIL1 remain closed because no real cohort identity has been recovered.
Exact implementation/result commit
`61bb801689eb2885b1e96aa4b56c86658dc3b333` passed Base Python job
`95320325187`, Optional Neuro Readers job `95320325136`, and CI
`32007641751`. No generated qualification or private operation was repeated
for proof closeout. A future private invocation still requires a new immutable
Tier C packet and fresh decision.

## Verification

Thirty-two focused tests and all 4,074 base tests pass, with 204 expected
skips and zero failures. Full static checks pass. Exact implementation/result
commit `61bb801` is remotely green in both required jobs.

Engineering capability added: the broad generated F03 class now has five
deterministic aggregate-safe routes with exact parser and replay proof.

Scientific claim not established: this result uses no private or neural data,
target, model, prediction, or score and demonstrates no neural effect, decoding
accuracy, language decoding, live decoding, or thought-to-text capability.
