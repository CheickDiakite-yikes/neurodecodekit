# WO9R Low-Frequency Cohort Confirmation Prediction Freeze

Date: 2026-08-10

Status: **One acquisition and one target-blind execution consumed successfully;
combined aggregate freeze created; final targets remain sealed pending this
exact freeze commit's remote-green CI.**

Public aggregate ledger:
`registries/physionet_low_frequency_cohort_confirmation_prediction_freeze.v0.json`

Ledger file SHA-256:
`6a546ca32a92b35c9c3448cecb5831f926d02f519a563d2ad803944c8d1f487a`

Internal canonical record SHA-256:
`a2c957e9cd4fb112f38cd2bc1eea00323c1d726f0e263435042938766b6e50fc`

## Green Implementation Parent

Exact implementation commit
`8242674e5821b2c923c0c79baa3a6ea20a27d838` passed Base Python job
`93365527795` and Optional Neuro Readers job `93365527849` in CI
`31359548779`. Both required jobs were green before the registered acquisition
or any S004-S015 EDF operation.

Before acquisition, the tracked tree was clean, HEAD matched the implementation
evidence, and 49,899,960,000 bytes of disk were free. The unrelated untracked
tracker-inspection NDJSON remained untouched.

## Consumed Acquisition

The one registered no-retry acquisition completed successfully. It:

1. Reverified the exact 15 public metadata documents.
2. Requested only the 72 registered S004-S015 EDF paths.
3. Streamed exactly 184,252,032 EDF payload bytes sequentially.
4. Made one local no-follow size and SHA-256 pass per EDF.
5. Matched all 72 official hashes and promoted one complete isolated bundle.
6. Retained 184,274,970 bytes including the bundle and bounded receipts.

Acquisition runtime was 518.051205 seconds and peak RSS was 73,089,024 bytes.
Metadata bodies totaled 522,962 bytes. No EDF header, annotation, signal,
target, model, checkpoint, or derivative was read during acquisition. There
was no retry or rerun.

## Consumed Target-Blind Execution

The one registered no-network analysis then completed successfully. It:

1. Verified the acquisition manifest and exact 72-file bundle membership.
2. Made one new sequential size and SHA-256 pass and one semantic MNE parse per
   EDF.
3. Read the exact 64-channel geometry, 160 Hz signal, and only T0/T1/T2
   annotations, accepting 15 task events per file and 1,080 total events.
4. Applied all-channel common-average reference and the frozen causal 0.5-4 Hz
   continuous-run filter.
5. Created 720 labeled fit rows, 360 target-free final rows, and one separately
   sealed combined 360-target scorer input.
6. Completed exactly 144 participant-specific fits, 216 target-blind inference
   runs, 216 prediction sets, and 3,240 private predictions across all 18
   registered conditions.
7. Wrote one combined aggregate hash-only freeze and stopped before scoring.

The model stage received zero final targets. No post-fit parameter update,
selection, threshold change, retry, rerun, network call, new payload, language
model, checkpoint, device, stream, or hardware operation occurred.

## Frozen Inventory

```text
input files / bytes:                         72 / 184,252,032
acquisition manifest reads:                  1
EDF size+SHA-256 passes:                     72
EDF semantic/header/annotation/signal reads: 72 / 72 / 72 / 72
task events:                                 1,080
fit target rows:                             720
target-free final signal rows:               360
final target rows delivered to model stage: 0
parameter-update fits:                       144
target-blind model-inference runs:           216
prediction sets frozen:                      216
individual private predictions:              3,240
final target deliveries / scores:            0 / 0
```

The public ledger binds all 216 participant-condition prediction sets and the
private payload, fit derivative, target-free derivative, sealed scorer input,
source manifest, configuration, split protocol, implementation, dependencies,
operation counts, and resources. It contains no individual prediction,
probability, target, or participant outcome.

## Resources

```text
analysis runtime through freeze:             19.864386 seconds
target-blind model-stage runtime:             1.160913 seconds
peak RSS:                                    303,153,152 bytes
private generated bytes before final report: 4,206,464
CPU threads / workers / numerical jobs:       1 / 1 / 1
network bytes / new payload bytes:             0 / 0
retries / reruns:                              0 / 0
public freeze bytes:                           23,174
```

All values are below the frozen acquisition and analysis caps. The producer is
cue-causal with zero right context and a three-second cue-relative decision
point. End-to-end latency, pre-movement timing, and asynchronous operation were
not measured.

## Verification

Five focused freeze invariants pass. They recompute the canonical freeze hash,
require all 216 participant-condition hashes, reject individual public output,
bind the exact green implementation, verify the target firewall, and enforce
the registered resource ceilings.

The complete dependency-free suite passes 1,533 tests with 177 expected skips
in 3.044 seconds. The exact classical suite passes 1,589 tests with 34 expected
skips in 18.015 seconds. The broad optional suite passes 1,604 tests with eight
expected skips in 79.495 seconds outside the macOS sandbox so its established
forkserver test can create a local Unix socket. Each profile adds exactly five
tests over the implementation milestone without losing a prior test.

These verification runs used committed/public artifacts and generated test
fixtures. They did not reopen the private predictions, target-free derivative,
sealed targets, or real EDF bundle and performed no real fit, inference, or
score.

## Warnings

- Left/right action is coupled to a left/right visual cue.
- Separate EOG, EMG, and measured movement onset are unavailable.
- Participant-specific models do not test unseen-person generalization.
- Per-participant hashes are commitments, not participant outcomes.
- No predictive, physiology, localization, or control gate is known until the
  isolated scorer opens the same sealed 360 targets once.
- Even the maximum registered route cannot prove brain-specific origin or
  independent replication.

## Next Gate

Commit and push this exact aggregate ledger, document, invariant test, and
status updates. Base Python and Optional Neuro Readers must both pass at the
exact freeze commit. Only then may the isolated scorer open the same sealed
360-target artifact once, verify every private hash and identity, compute
aggregate metrics, and apply the frozen `WO9R-R0` through `WO9R-R4` router. Any
freeze or scoring failure consumes and parks the work order. No retry, rerun,
or post-target update is open.

Engineering capability added: the real S004-S015 inputs, target-blind
predictions, controls, split, configuration, dependencies, and resources are
now bound in one public aggregate freeze without publishing individual output.

Scientific claim not established: no final target has been delivered or
scored, so this freeze establishes no task accuracy, neural effect,
motor-compatible physiology, brain-specific origin, generalization, decoding,
latency, hardware, or human-benefit result.
