# MARC1-CD1A Live Freewill Archive Inventory Authorization Packet

Date: 2026-08-11

Status: **exact packet-bound Tier C decision requested; not granted**

Lane: **MARC1-CD1A Live Freewill Archive Metadata Range Audit**

Green generated result:
`431ee8dc14118e4de5f5a3a9ae6e34a202cc238e`

Green result CI: `31512598915` (Base Python `93849853477`; Optional Neuro
Readers `93849853538`)

Machine request:
`registries/marc1_freewill_central_directory_authorization_request.v0.json`

## Decision In Plain Language

This packet asks permission for one storage-bounded inventory of the public
Freewill-23 ZIP without downloading or opening a member.

After a separate decision is committed, pushed, and both CI jobs are green,
Codex may build and qualify one additive standard-library live wrapper around
the green central-directory parser using generated bodies and mocked responses
only. Only after that exact wrapper is committed, pushed, and both jobs are
green may one fresh invocation:

1. read one bounded Figshare version-files metadata response;
2. read exactly the final 131,072 bytes of the named 13,591,548,048-byte ZIP;
3. parse the complete EOCD and ZIP64 trailer in memory; and
4. only if every frozen gate passes, read exactly one derived central-directory
   range no larger than 16 MiB.

The invocation may write one Git-ignored private inventory and one aggregate
public report under the frozen caps. It may not download the archive, open a
local header, read a member payload, select or acquire participants, parse a
signal, inspect an event or target, run a model, score, or upgrade a claim.

This packet authorizes nothing by itself.

## Why A Fresh Decision Is Required

`MARC1CDG-R1` is remotely green generated engineering evidence. It made zero
public requests. Dataset-specific Figshare metadata and archive ranges are
Tier C public real-data access even though they contain only archive metadata.

The maintainer's current `continue` preceded this immutable request and cannot
authorize it retroactively. After this request is committed, pushed, and both
CI jobs are green, Codex may identify its exact commit, CI run, three-body
scope, and decision boundary. If it is the sole active Tier C packet, a fresh
unambiguous `continue`, `approve`, or `proceed` may bind it by reference. The
separate decision artifact must quote the maintainer's actual words and bind
the request by SHA-256. Codex must not fabricate a long authorization sentence
as a user utterance.

## Immutable Proof

Research `93faf368ed01dda418b836e794ba354d8f180794` passed Base Python job
`93834276391` and Optional Neuro Readers job `93834276150` in CI
`31507965329`.

Generated/mock contract `cf6304385f61fc7713ae7fd4526d86e45e4c03e5`
passed Base Python job `93837415016` and Optional Neuro Readers job
`93837415174` in CI `31508903399`.

Exact generated implementation
`211fd78fba82a660c4730a586541819b2eb264fd` passed Base Python job
`93846584402` and Optional Neuro Readers job `93846584527` in CI
`31511626051` before the one generated closeout.

Consumed generated result `431ee8dc14118e4de5f5a3a9ae6e34a202cc238e`
passed Base Python job `93849853477` and Optional Neuro Readers job
`93849853538` in CI `31512598915`. `MARC1CDG-R1` represented the 13.59 GB
identity with 280,249 generated bytes, parsed 18 entries, passed both transport
paths, all 32 refusals, and all 14 gates in 0.006544457981362939 seconds at
27,131,904-byte peak RSS. All 82 focused tests and the complete 2,355-test
suite passed. Every public, real-data, target, model, score, and claim counter
was zero.

## Requested Ordered Sequence

Only after a separate decision commit is pushed and both CI jobs pass may the
following sequence begin:

1. Implement one additive standard-library live wrapper around the exact green
   parser. Before its green commit, all transport tests must use generated
   bodies, mocked responses, and injected name resolution only.
2. Qualify metadata identity, exact request order, bodyless redirect safety,
   `200` refusal for ranges, exact `206` and `Content-Range`, bounded reads,
   EOCD/ZIP64, directory parsing, privacy, collision, symlink, consumed-marker,
   resource, and failure-cleanup behavior with zero network access.
3. Commit and push that exact wrapper and require Base Python and Optional
   Neuro Readers CI to pass. A failed, changed, or ungreen wrapper is
   ineligible.
4. Before consumption, require the exact green hashes, all one-thread
   environment values, at least 12 GiB free disk, and normalized one-minute
   load no greater than `1.0` per logical CPU. Refuse before a marker if any
   machine value is unavailable or outside the gate.
5. Create one new isolated Git-ignored invocation root and one consumed marker.
   Any failure after that marker consumes MARC1-CD1A.
6. Send one unauthenticated `GET` to the exact version-files endpoint and read
   at most 131,073 bytes, accepting at most 131,072 bytes.
7. After exact source identity passes, send one `GET` with the fixed final-tail
   range. Permit at most two bodyless HTTPS redirects to globally routable
   destinations and accept exactly one 131,072-byte terminal `206` body.
8. Parse the trailer in memory. If the directory range is not fully determined,
   plain, single-disk, in bounds, and no larger than 16 MiB, emit one aggregate
   failure result and stop without another request.
9. If eligible, send one range `GET` to the same terminal archive URL with no
   additional redirect and accept exactly the derived directory body.
10. Parse the directory once, emit one private exact manifest and one aggregate
    public report, remove raw response bytes from memory, and stop.

There is no HEAD request, retry, rerun, resume, restart, second metadata body,
second tail, second directory, exploratory ZIP64 range, whole-file fallback,
alternate record, file, version, or provider, or follow-on member acquisition
in this packet.

## Exact Public Identity And Requests

```text
provider:             Figshare
record ID/version:    28632599 / 1
DOI:                  10.6084/m9.figshare.28632599.v1
file ID:              57518986
file name:            Freewill_EEG_Reaching_Grasping.zip
file bytes:           13,591,548,048
registered file MD5:  3b7c3039c5c9fb6abf1429a830301711
license:              CC BY 4.0
metadata endpoint:    https://api.figshare.com/v2/articles/28632599/versions/1/files
download URL:         https://ndownloader.figshare.com/files/57518986
tail range:           bytes=13591416976-13591548047
tail bytes:           131,072
directory range:      derived exactly from the accepted ZIP64 trailer
directory cap:        16,777,216 bytes
```

There are exactly three accepted response bodies: metadata, tail, and the
conditional directory. The direct path uses three HTTP request attempts. The
redirected path may use at most five attempts because only the tail request
may follow up to two bodyless redirects; the directory request must use the
same terminal URL and may not redirect.

Every archive request must use `GET`, no body, no credential, no cookie,
`Accept-Encoding: identity`, and one exact `Range`. The terminal response must
be a non-multipart `206` with exact canonical `Content-Range`, exact
`Content-Length`, identity encoding, and exact cap-plus-one byte-count proof.
`ETag`, `Last-Modified`, `Accept-Ranges`, redirect URLs, and raw headers are
provenance only and cannot replace the frozen file identity.

## Parser, Privacy, And Output Boundary

The wrapper must import and bind the exact green structural parser. It may not
amend the EOCD, ZIP64, central-entry, path, flag, method, file-kind, ZIP64-extra,
duplicate, privacy, or route policy after seeing a public byte.

Raw metadata, tail, and directory response bodies are in-memory only and are
not persisted. Exact member names, CRC-32 values, sizes, methods, flags,
local-header offsets, source attributes, kinds, and ZIP64 use may appear only
in the Git-ignored private manifest. The public result may contain aggregate
counts, byte totals, method/suffix/role summaries, domain-separated hashes,
transport measurements, counters, warnings, unavailable fields, and the claim
boundary.

The public result must not contain a member name, local-header offset,
download/redirect URL, query string, response header value, per-member
checksum, raw response body, participant row, signal value, target, prediction,
secret, or local path. Success route `MARC1CD-R1` is an archive inventory and
acquisition-planning result only.

## Resource Caps

| Stage | Wall time | Peak RSS | Accepted response bodies | Generated output |
| --- | ---: | ---: | ---: | ---: |
| Generated/mock live-wrapper qualification | 30 s | 268,435,456 bytes | 0 | 1,048,576 bytes |
| One live metadata/range audit | 120 s | 268,435,456 bytes | 17,039,360 bytes | 8,388,608 bytes |

Both stages use one CPU thread, one worker, and one numerical job. The live
stage permits at most five HTTP request attempts, exactly three accepted body
responses, at most two bodyless redirects, 32 MiB incremental disk, and one
Git-ignored invocation root. At least 12 GiB free disk is required before the
consumed marker. An overflow-detection byte may be read but never accepted or
retained. Retries and reruns are zero.

## Explicitly Not Authorized

- any action before a separate packet-bound decision is remotely green;
- any public request before the exact additive live wrapper is remotely green;
- any HEAD request, retry, rerun, resume, restart, exploratory range,
  additional redirect, fallback, substitution, or whole-file download;
- any alternate endpoint, record, version, file, provider, query, credential,
  API key, cookie, authenticated request, or extra response body;
- any member local-header request, member payload request, extraction,
  decompression, CRC verification, whole-file MD5 pass, acquisition, cache,
  split, or derivative;
- any preexisting local archive, data bundle, consumed root, marker, cache,
  result, or another project path operation;
- any EEG, EOG, acceleration, audio, channel, geometry, event, onset, signal,
  target, label, response, sentence, key, or trial read;
- any participant selection, cohort selection, model, checkpoint, training,
  inference, prediction, freeze, target delivery, score, threshold, seed,
  architecture, or hyperparameter selection;
- any dependency installation, language or foundation model, provider call,
  RW3, stream, device, hardware, upload, publication, release, or destructive
  action;
- any publication of raw bodies, raw headers, URLs, or private-manifest rows;
  and
- any scientific, decoding, brain-specific, real-time, portable, home-use,
  assistive, or clinical claim upgrade.

## Current State

Every implementation, public-data, local-data, member, neural, target, model,
scoring, release, cleanup, and claim authorization flag in the machine request
is false. Preparing this packet made zero public requests, DNS queries, body
reads, archive-range reads, local-path operations, signal/event/target reads,
model runs, predictions, scores, retries, reruns, releases, deletions, or claim
changes.

Engineering capability requested: one machine-gated standard-library wrapper
can inventory the current 13.59 GB public ZIP using no more than 17,039,360
accepted response-body bytes and without reading a member payload.

Scientific claim not established by this request: this all-false packet is not
EEG data or a result and establishes no neural effect or decoding capability.
