# COMM-P0-G Two-Child Rehearsal Implementation Proof

Recorded: 2026-08-28

Status: exact implementation proof recorded with delayed effect pending this
proof commit, push, both remote CI jobs, and GitHub `main` verification.

Gate: `COMM-P0-G-FS2-v0`

## Exact implementation evidence

Implementation commit
`e98330fbb72a4f89b81c154420a01b2ee42918c2` passed CI run
`33168320870`:

- Base Python job `98838987492`: green;
- Optional Neuro Readers job `98838987264`: green; and
- GitHub `main` and branch `codex/loop-54a-vhdr-preregistration` both resolved
  to the exact implementation commit after fast-forward promotion.

The machine proof binds the frozen registration, exact contract hash, all five
implementation artifacts, their aggregate hash, the exact green commit, and
the two required job identifiers. It keeps official qualification, official
marker, real/private data, network, device, release, and scientific-claim
authority false.

## Delayed effect

The proof record performs no rehearsal, model fit, inference, target delivery,
score, network request, private read, device operation, or official marker
operation. `COMM-P0-G-FS2-R0` remains unattempted until this proof-only commit
is itself pushed, both jobs are green, and the exact commit is present on
GitHub `main`.

After those barriers, exactly one full-scale generated rehearsal may run. Its
receipt is durable before temporary payload work; any later failure, timeout,
or refusal consumes the one attempt. No retry, rerun, resume, or substitution
is permitted.

Engineering capability added: the bounded two-child rehearsal implementation
now has a fixed, machine-checkable identity ready for remote proof.

Scientific claim not established: this proof-only record runs no generated or
real EEG experiment and establishes no communication decoding,
EEG-beyond-peripheral information, unseen-person generalization, independent
replication, causal live decoding, hardware performance, or clinical value.
