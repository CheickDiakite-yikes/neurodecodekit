# Inner-speech launch stopped before participant access

September 23, 2026. **No new decoding result.**

The single `INNER-SPEECH-TEST-1` invocation used unchanged commit
`f72d953b0890bc3241dee4a581be4f5e47731233`, verified on
remote main with both CI jobs successful before the sole prediction invocation.
It exited with `RuntimeError: fixed_local_windows_output` at line 357, before
the scientific attempt initialized. No retry or score followed.

The frozen local-output-path check refused before scientific execution.
Private path diagnostics and approval text are deliberately excluded from this
public summary. No source corruption or biological conclusion is inferred.

The guard runs before directory creation, code/source hashing, the resource
watchdog, participant access, event parsing, features, fitting or scoring.
The attempt directory, prediction freeze and scientific result remain absent.
Because the guard also precedes the runner's exception handler, there is no
automatic private failure marker. The
[aggregate refusal record](../registries/inner_speech_execution_refusal.v0.json)
preserves the invocation without manufacturing private run state.

## Belief update and next action

This failure changes no belief about EEG or imagined-word decoding. It is not
a failed primary endpoint or a biological null, and the fresh labels remain
unopened. Do not change models, controls, participants, trial counts or thresholds.

The smallest proposed correction is to validate the actual canonical local
destination for both source and output containment, retaining the Windows-only,
non-OneDrive and exact-destination safeguards. Another launch requires fresh
approval because the frozen protocol disallows automatic recovery. The proposed
run retains all ten people, all three conditions, eight arms, a 20-minute total
cap, 1 GiB RSS, 32 MiB generated output and one frozen score. Neither the code
correction nor a second launch has been performed.
