# COMM-P0-SYNC Registration Proof Closeout

Date: 2026-08-27

Status: **registration remotely green; proof-only closeout pending its own
remote CI**

Machine proof:
`registries/communication_eeg_prospective_synchronized_cohort_proof.v0.json`

## Exact Proof

Registration commit
`df3266ed09132017cc8a9dcc10e8a7d61ea92f61` passed GitHub Actions CI
`33134791405`:

- Base Python job `98732241444`: success;
- Optional Neuro Readers job `98732241603`: success.

The exact commit is present on both the working branch and GitHub `main`.

This proof binds the preregistration, machine contract, and contract test:

- three tracked artifacts;
- 38,364 total bytes; and
- canonical artifact-set SHA-256
  `793311e450bedeeac0d5a2d6b7a118d4fc21268ddba77b6c9f186715d5d84966`.

The proof test recomputes every byte count, file SHA-256, Git blob, and
canonical set digest from the committed artifacts.

## Effect

The registration proof also exposed one human/machine mismatch: the immutable
human document says 60% stable-commit coverage, while the machine contract and
test say 70%. Amendment 1 makes the stricter 70% value authoritative without
changing the parent artifacts.

After this closeout and Amendment 1 are committed, pushed, and both required CI
jobs are green on the same exact commit, bounded generated-only qualification
design may begin. That next stage
must freeze its own deterministic fixtures, target firewall, causal stream,
control matrix, participant split, prediction freezer, scorer isolation,
adversarial refusals, and resource caps before adding an implementation.

Before those barriers are green, generated qualification design remains
paused. This proof does not authorize a generated execution. Any generated
implementation and one-shot qualification require their own ordered,
remotely-green proof barriers. Human recruitment, recording, hardware use,
voice capture, real/private data, training, scoring, release, and scientific
claim operations remain closed.

## Non-Operation

This closeout read only the three tracked registration artifacts, their Git
blob identities, the exact GitHub CI result, and the two remote branch refs. It
did not contact an EEG repository, open a real or private path, recruit or
record a person, operate a device, capture voice, read a signal or target, run
a model, train, infer, freeze predictions, score, call a provider, publish a
release, or upgrade a scientific claim.

The 20 GiB total research-storage ceiling, 10 GiB raw ceiling, and 20 GiB
post-reservation free-space floor are unchanged. No cleanup occurred, no other
project was touched, and the unrelated tracker inspection file remains
untracked and untouched.

`DREYER-C5R-1-HL` remains the sole active Tier C packet with every authority
flag false.

Engineering capability added: the prospective communication study is now
bound to exact protocol files, hashes, Git objects, remote CI, and GitHub
`main`, so generated qualification can be built against an immutable target.

Scientific claim not established: no person, device, neural payload, model,
prediction, or score was used, so this proves no communication decoding, EEG
information beyond peripheral controls, unseen-person generalization,
independent replication, causal live decoding, hardware performance, or
clinical value.
