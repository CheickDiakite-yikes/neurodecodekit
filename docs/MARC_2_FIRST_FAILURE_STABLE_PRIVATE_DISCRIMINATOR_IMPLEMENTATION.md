# MARC2-VR18P First-Failure-Stable Private Discriminator Implementation

Date: 2026-08-21

Lane: `MARC2-VR18P`

Status: **Stage 1 generated implementation qualified; private Stage 2 remains
closed pending exact remote implementation proof and a separately green
proof-only closeout**

Machine record:
`registries/marc2_first_failure_stable_private_discriminator_implementation.v0.json`

## Proven Decision Boundary

Authorization decision `5113be7fee63d769276a781c0ed3af5ac2bbf567`
passed Base Python job `96751646673`, Optional Neuro Readers job
`96751646346`, and CI `32475765286` before implementation began.

The implementation reads and hash-verifies only committed decision, request,
VR16A, and VR17C artifacts during Stage 1. It imports neither consumed VR15P
nor consumed VR16P executors. Generated qualification uses invocation-created
temporary directories only; it does not inspect or mutate repository
`.codex_work` state.

## Interface

The additive standard-library module
`neurodecodekit.datasets.marc2_first_failure_stable_private_discriminator`
exposes exactly four positional commands:

- `plan`: inspect the frozen proof and resource boundary;
- `qualify`: run the bounded generated Stage 1 matrix;
- `inspect`: inspect only the future fixed aggregate report; and
- `execute`: run the future proof-gated fixed-path Stage 2 sequence.

There is no generic source, output, URL, route, threshold, cap, retry, resume,
fallback, or substitution override. Before the exact implementation proof is
installed, `execute` refuses at `MARC2VR18P-F01` before readiness collection or
private-source preflight.

## One-Call Composition

Each source is copied only by the generated fixture builder and then passed to
the unchanged `VR16A` adapter exactly once. Success returns generated route
`MARC2VR18P-G1`. A VR16A refusal is looked up exactly once in the frozen VR17C
evidence map:

| VR16A evidence | VR18P route |
|---|---|
| F04 / core identity differs | `MARC2VR18P-R4` |
| F05 / companion run spelling differs | `MARC2VR18P-R5` |
| F05 / normalized run companion is duplicated | `MARC2VR18P-R6` |
| F05 / run companion set is incomplete | `MARC2VR18P-R7` |

Unknown evidence fails closed rather than being assigned to a nearby class.
Every path hashes the source before and after composition and refuses mutation.

## Generated Qualification

The single registered fresh-process qualification used five cases, two source
orders, and two exact replays:

- 20 matrix paths;
- 20 matrix VR16A calls plus one fixed-state VR16A call;
- 16 VR17C map lookups, all on failure paths;
- zero map lookups on success paths;
- G1 and R4-R7 exactly four times each;
- replay SHA-256
  `5c214a1e9e5b3aa53b30931ff2d4573b675cb9bf18b41753b0da2eaae9c8bd35`;
- one temporary fixed-path success rehearsal;
- 82 direct fail-closed refusal checks; and
- zero retained generated bytes.

The fixed-state rehearsal wrote mode-`0600` certificate, marker, and private
manifest plus a mode-`0644` aggregate report under a temporary root. Peak
temporary output was 217,041 bytes, then the temporary root was removed.

## Resource Measurements

```text
generated input bytes:       9,037,650
aggregate output bytes:      2,020
retained output bytes:       0
runtime seconds:             1.4131330830277875
peak RSS bytes:              37,879,808
CPU threads/workers/jobs:    1 / 1 / 1
network/new payload bytes:   0 / 0
private or ignored ops:      0
archive/signal/target ops:   0 / 0 / 0
model/training/score ops:    0 / 0 / 0
```

## Stage 2 Remains Closed

The implementation registry deliberately contains
`remote_implementation_proof: null`. The exact implementation/result commit
must be pushed and both jobs must pass. A separate proof-only closeout must
then bind those exact bytes, be pushed, and pass both jobs before one Stage 2
invocation may begin.

No generated result, local test, or future proof closeout may itself inspect
readiness, `.codex_work`, the private source, an output root, or consumed or
parked lane state.

Engineering capability added: a fixed one-call wrapper can deterministically
separate the four generated-qualified first-failure classes or preserve a
successful VR16A selection without source mutation.

Scientific claim not established: this generated interface qualification did
not access neural data, freeze a real cohort, train or score a model, measure
decoding performance, or demonstrate language, live, or thought decoding.
