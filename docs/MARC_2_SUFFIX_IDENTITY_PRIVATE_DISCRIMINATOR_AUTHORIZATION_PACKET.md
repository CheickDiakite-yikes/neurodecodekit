# MARC2-VR15P Suffix-Identity Private Discriminator Authorization Packet

Date: 2026-08-21

Lane: `MARC2-VR15P`

Status: **All-false Tier C request prepared; no decision or private access**

Request:
`registries/marc2_suffix_identity_private_discriminator_authorization_request.v0.json`

## Decision Requested

After this exact request and a separate non-scope-changing proof closeout are
each committed, pushed, and green in both required CI jobs, and after Codex
identifies VR15P as the sole active Tier C packet, request one future two-stage
sequence:

1. implement and generated-qualify one additive fixed-path VR15P wrapper; and
2. only after that exact implementation and its proof-only closeout are each
   committed, pushed, and remotely green, perform one target-free structural
   read and emit exactly one aggregate suffix-identity route.

This packet records no decision and grants no authority now. Earlier or
standing `continue`, `approve`, or `proceed` messages are not retroactive
authority for this new packet. A fresh unambiguous packet-bound maintainer
message is required after the request proof closeout is remotely green.

## Why This Is The Smallest Useful Read

The consumed VR14P aggregate recovered only `MARC2VR13P-R2`, the broad P15
suffix-bearing identity class. Remotely green VR15A proves that fifteen
single-failure grammar classes and one multiple-class state are independently
reachable and deterministic through the unchanged VR12A adapter.

The proposed private command asks one bounded structural question: which one
of those sixteen aggregate classes describes the same immutable target-free
source? It retains no member name, path, row, identity, task, run, failed value,
or exception text. A result would select the next generated repair design; it
would not itself fix the source, freeze a cohort, or open FW2.

## Green Predecessor Proof

VR15A registration `185fbc54366fd0eaf0ed4e994511e4485514b53e`
passed CI `32447836662`. Exact implementation
`bfb0dcb7752433b4af841d57bbfcbf613a341124` passed Base Python job
`96674484190`, Optional Neuro Readers job `96674484279`, and CI
`32449260503`. Proof-only closeout
`0bbd8a39f38c27931b09a41b9576585863496dbb` passed Base Python job
`96675574923`, Optional Neuro Readers job `96675574757`, and CI
`32449648386`.

Seventeen committed predecessor artifacts totaling 308,187 bytes are bound by
exact path, size, and SHA-256. Packet preparation does not inspect a private or
Git-ignored path.

## Proposed Generated Stage

Only after a separate decision is remotely green may Stage 1 implement:

- exact request, decision, predecessor, implementation, and proof validation;
- a fixed-path no-follow state machine qualified only in temporary generated
  directories;
- the 17 frozen VR15A cases in canonical and reversed order across two exact
  replays;
- exactly one VR15A classifier call, containing exactly one unchanged VR12A
  call, per generated path;
- R1-R16 aggregate reports with no source name or per-source outcome;
- at least 100 direct refusals across proof, path, race, mode, hash, JSON,
  route, leakage, replay, resource, and forbidden-operation boundaries;
- deterministic replay, strict output firewalls, resource receipts, and zero
  retained generated output; and
- fixed `plan`, `qualify`, `inspect`, and `execute` CLI surfaces with no path,
  URL, output, retry, resume, fallback, substitution, or route override.

Generated qualification must not stat, resolve, hash, open, alter, delete, or
reuse any real `.codex_work` path or consumed VR13P/VR14P state. It must not
import, call, patch, copy, or modify a consumed private executor.

## Proposed One Private Diagnostic

Only after the exact future implementation and its proof-only closeout are
remotely green may one fixed command:

1. validate every exact green proof and fixed path constant;
2. collect three consecutive passing machine-readiness samples;
3. create one fresh mode-`0600` certificate only at
   `.codex_work/marc2_machine_readiness/vr15p/readiness.v0.json`;
4. require `.codex_work/marc2_suffix_identity_private_discriminator/v0` to be
   absent;
5. no-follow preflight the registered structural source;
6. create one mode-`0600` consumed marker immediately before content open;
7. open, read, hash, and strict-parse exactly 418,755 bytes once;
8. call the exact remotely green VR15A classifier once, including exactly one
   unchanged nested VR12A call;
9. write one aggregate R1-R16 report containing no private identity; and
10. close and consume the lane with no retry or reinspection.

The immutable source identity is copied only from committed records:

```text
path:        .codex_work/marc1_central_directory/live_audit_v0/member_inventory.private.v0.json
mode:        0600
bytes:       418,755
SHA-256:     2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031
rows:        1,227
files:       1,025
directories: 202
```

Packet preparation performs zero path checks, stats, resolves, hashes, opens,
reads, or parses against that source, readiness path, output root, or consumed
state.

## Frozen Route Contract

| Route | Maximum engineering meaning |
|---|---|
| `MARC2VR15P-R1` | tail component count class |
| `MARC2VR15P-R2` | prefix segment grammar class |
| `MARC2VR15P-R3` | subject-directory shape class |
| `MARC2VR15P-R4` | session-directory shape class |
| `MARC2VR15P-R5` | modality-directory class |
| `MARC2VR15P-R6` | filename subject-shape class |
| `MARC2VR15P-R7` | path/filename subject disagreement class |
| `MARC2VR15P-R8` | filename session-shape class |
| `MARC2VR15P-R9` | path/filename session disagreement class |
| `MARC2VR15P-R10` | task-entity shape class |
| `MARC2VR15P-R11` | optional-entity shape class |
| `MARC2VR15P-R12` | run entity absent class |
| `MARC2VR15P-R13` | run entity not terminal class |
| `MARC2VR15P-R14` | run token nonnumeric class |
| `MARC2VR15P-R15` | run token width class |
| `MARC2VR15P-R16` | multiple suffix-identity classes present |

Every route creates no cohort manifest and retains no exception text,
predicate, failed value, row, path, identity, task spelling, run token,
candidate, or selection. VR15A G1 is not an allowed private result because the
committed aggregate evidence binds this source to P15; G1, an unknown route,
source mutation, leakage, or a resource breach consumes and parks the lane.

## Resource Limits

```text
CPU threads / workers / numerical jobs:  1 / 1 / 1
generated qualification:                 <= 90 seconds
future private command:                  <= 650 seconds
peak RSS:                                < 256 MiB
minimum free disk before marker:         2 GiB
fresh readiness wait:                    <= 600 seconds
private source read:                     exactly 418,755 bytes once
VR15A classifier calls:                  exactly 1
nested VR12A calls:                      exactly 1
network / new payload bytes:             0 / 0
archive-member / signal / target bytes:  0 / 0 / 0
combined incremental output:             <= 1 MiB
retries / reruns / resumes:              0 / 0 / 0
```

## Explicitly Not Requested

This packet does not request any operation before separate green request and
decision barriers; any consumed-state reuse; a second source open; retry,
rerun, resume, repair, fallback, substitution, or post-result reinspection;
an archive local-header or member-payload read; EEG/MEG signal, event, onset,
channel, geometry, sentence, key, label, target, quality, or participant-level
output access; cohort creation; derivative, cache, feature, NeuroToken,
training, model, checkpoint, inference, prediction, target delivery, scoring,
selection, or post-result tuning; download, provider, language model, RW3,
stream, device, or hardware work; FW2 or CIL1 execution; release or
publication; or a scientific, decoding, neural, real-time, portable, home-use,
assistive, or clinical claim upgrade.

## Failure Semantics

A failure before the marker performs zero private content opens and still
consumes the one registered invocation. A failure after the marker consumes the
diagnostic. Neither case permits a retry, rerun, resume, repair, fallback,
substitution, or private reinspection.

Engineering capability requested: one proof-gated target-free structural open
can reduce broad P15 to one of sixteen aggregate grammar classes and make the
next repair design specific.

Scientific claim not established: packet preparation performs no private read,
and even the proposed diagnostic would access no neural payload, target,
prediction, or score, so it establishes no neural effect, decoding accuracy,
language decoding, live decoding, or thought-to-text capability.
