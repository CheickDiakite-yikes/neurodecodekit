# FMSR1-v1 Registration Proof Closeout

Date: 2026-08-29

Status: **registration remotely green; proof-only closeout effective after this
exact closeout commit is pushed and both required CI jobs are green**

Machine proof:

- `registries/fresh_motor_source_research_registration_proof.v0.json`

## Remotely Green Registration

Exact successor commit
`e09f6cc014744485940713c148dacad9dbbe59e3` passed on GitHub `main`:

- Base Python job `99197577034` in 6m52s;
- Optional Neuro Readers job `99197577007` in 12m29s; and
- CI `33289147031`.

The pushed v0 draft at `4cac22507fab376da3d76965339a40aabfd9c6a0`
was not accepted and performed zero protected operations. Independent review
required the additive v1 successor to add joint nuisance controls,
noncircular routing, a total candidate order, complete discovery-packet bounds,
exact storage arithmetic, conditional acceptance language, and mutation tests.

## Bound Evidence

This proof binds five exact registration, frontier, and test artifacts totaling
47,128 bytes. Their canonical artifact-set SHA-256 is:

```text
f7afef74e7cf4b44e0fe39d8819d949903b05589c504bb6b3293ead7392d5aa4
```

The accepted v1 protocol prospectively binds five official indexes, four exact
queries, complete-or-park pagination, deterministic candidate
canonicalization and ordering, every-hop redirect allowlisting before contact,
joint nuisance and deranged-joint controls, EMG for every relevant effector,
and exact 12+2+2+1+3 GiB storage arithmetic.

## Proof Boundary

This closeout reads only five committed artifacts and their Git identities. It
does not contact an index, query a source, follow a redirect, read real or
private data, select a candidate, retain a payload URL, open a payload or
header, parse a signal, event, annotation, target, or label, run a model,
train, predict, score, stream, operate hardware, publish a release, or upgrade
a scientific claim.

No Tier C packet is active. Consumed Dreyer and Ofner work remains closed, and
the unrelated tracker inspection file remains untouched.

## Next Gate

After this closeout is remotely green, the only eligible transition is the
separately recorded all-false `FMSR1-DISCOVERY-M0` authorization request. It
may become active only through fresh packet-bound maintainer words and a
separate decision commit that becomes remotely green. General continuation
does not authorize network access.

Engineering capability pending remote proof: this closeout hash-binds the corrected fresh-source admission protocol to its exact-green commit, complete candidate-ordering rules, bounded discovery shape, nuisance controls, storage arithmetic, and regression tests; it becomes effective only after this exact closeout commit is remotely green.

Scientific claim not established: no source was selected and no neural measurement was accessed or analyzed, so no neural advantage, unseen-person generalization, movement-intention decoding, language decoding, live operation, hardware result, or clinical value was established.
