# MARC2-VR10A F03 Predicate Decomposition Implementation

Date: 2026-08-17

Lane: `MARC2-VR10A`

Status: **Exact artifact-only generated implementation remotely green**

Machine implementation record:
`registries/marc2_f03_predicate_decomposition_implementation.v0.json`

## Proof Order

Registration `80175a7e6483a6d156b23a24f9503a9ae32e7201` passed Base Python
job `95290665076`, Optional Neuro Readers job `95290665173`, and CI
`31997129703` before implementation began. The implementation loads the exact
registration by SHA-256 and verifies all 14 fixed inputs plus the registration
document and test with no-follow, regular-file reads.

No private path, ignored MARC2 path, consumed VR9P state, archive member,
signal, event, target, model, prediction, or score is part of the module's
surface.

## AST-Backed Inventory

The implementation parses the exact tracked validator modules with the Python
AST and binds each frozen predicate to its defining function and source
signature:

- six path predicates in `_normalize_member_name`;
- ten row and BIDS predicates in `_validate_entry`;
- three uniqueness/completeness predicates in `_group_source_rows`; and
- one entry-kind count predicate in `validate_live_domain_source`.

The resulting 20-leaf inventory must match the registration byte for byte in
order and status. It validates the 15 excluded and five unresolved partition,
then separately verifies the committed live aggregate's exact 1,227 entries,
1,025 regular files, 202 directories, and methods 0/8 counts. These are code
and aggregate implications, not private row observations.

## Generated Witness Path

Each case starts from the green full-scale generated source. A witness changes
one member-name mechanism before ZIP construction, then traverses:

```text
generated blueprint -> exact ZIP fixture -> exact central-directory parser
-> exact live manifest producer -> VR2 validator -> VR6 route relay
```

The six cases are one unmodified success control plus witnesses for overlong
member name, suffix-bearing BIDS identity, task-token case, logical companion
aliasing, and incomplete companion grouping. Canonical and reversed order are
run twice, for exactly 24 VR6 calls and 29,448 parser-entry visits.

The existing generated interface normalizes only
`transport_body_sha256` after producer construction. That normalization is
identical in every control and witness path; it is not a witness mutation and
is reported explicitly. No witness mutation occurs after the exact parser.

## Refusal And Output Surface

Forty-seven direct mutations cover contract identity, the full 20-predicate
inventory, replay, route shape, privacy fields, counters, thread binding, and
every resource/output cap. Public output contains only predicate IDs and
statuses, case names, aggregate route codes, digests, counts, resources,
warnings, unavailable fields, and zero counters.

The CLI exposes only:

```text
python -m neurodecodekit.datasets.marc2_f03_predicate_decomposition plan
python -m neurodecodekit.datasets.marc2_f03_predicate_decomposition qualify
```

There is no path, URL, output, execute, retry, resume, cleanup, threshold,
selection, or private-mode argument. Generated output is returned in memory or
to stdout and retained output is zero.

## Measured Qualification

One one-thread qualification returned `MARC2VR10A-G1`:

- 17 fixed artifacts and 480,963 tracked bytes verified;
- 20 AST-backed predicates: 15 excluded and five unresolved;
- 24 exact parser/producer/VR2/VR6 paths;
- four control successes and 20 outer-F02/nested-F03 witness refusals;
- 29,448 parser-entry visits and 24 VR6 calls;
- 6,979,708 generated input bytes;
- 10,751 aggregate output bytes and zero retained bytes;
- 1.8363693330029491 seconds runtime;
- 45,072,384-byte peak RSS; and
- 47 direct refusals passed.

Thirty-one focused contract, behavior, and result tests pass, including
deterministic replay with fixed resource probes, strict JSON output, route
isolation, thread/resource drift, and the no-path CLI surface. The complete
local suite passes 4,042 tests with 204 expected skips and zero failures in
178.505 seconds under the one-thread environment.

Exact implementation `84103a5fab86b7c7c8d3cf3af00c9efe3457470c`
subsequently passed Base Python job `95295212461`, Optional Neuro Readers job
`95295212440`, and CI `31998811585` without another generated qualification.

## Claim Boundary

Engineering capability added: NeuroDecodeKit can now enumerate and generated-
exercise every still-plausible F03 mechanism through the exact full-scale
structural path before proposing a repair or private discriminator.

Scientific claim not established: tracked-code analysis and generated ZIP
metadata establish no private cause, cohort, neural effect, decoding accuracy,
language decoding, live decoding, or thought-to-text capability.

## Next Gate

The exact implementation and result are remotely green. Tier A/B may freeze a
new aggregate-safe five-route discriminator and qualify it on the existing
generated witnesses. Any private read remains a separately named all-false
Tier C packet and fresh decision after that new implementation is itself
remotely green. VR9P remains consumed and must not be reused or inspected.
