# Fresh Motor Source Discovery Authorization Decision

Date: 2026-08-30

Decision: `FMSR1-DISCOVERY-M0-D0`

Status: **packet-bound ordered authorization recorded; effective only after
this exact decision is tested, committed, pushed, and both required CI jobs are
remotely green**

Machine decision:

- `registries/fresh_motor_source_discovery_authorization_decision.v0.json`

Request: `FMSR1-DISCOVERY-M0`

## Actual Maintainer Decision

After Codex identified `FMSR1-DISCOVERY-M0` as the single remotely green gate,
named its metadata-only boundary, and stated that payload, model, score, and
claim authority remained closed, the maintainer said:

> lets do it, attack this with tremendous relentlessness, get real results, do what it takes, think outside the box

This record preserves those words exactly. They incorporate the one named
packet by reference. The words do not authorize doing whatever is technically
possible, changing the packet, skipping its barriers, or widening it to real
EEG, targets, models, scores, releases, or scientific claims.

## Bound Green Proof

The request and proof closeout are remotely green at corrective commit
`5c466efd6086db0acce6a57a9f32172c67e157f6`, Base Python job
`99204462828`, Optional Neuro Readers job `99204462714`, and CI
`33291744779` on GitHub `main`. Codex freshly reverified that run before
creating this decision.

The decision binds the exact human packet, machine request, request test,
registration proof closeout, machine proof, and proof test. Their size,
SHA-256, and Git-blob identities are frozen in the machine decision.

## Exact Ordered Authorization

Only after this decision is remotely green:

1. implement the additive standard-library discovery planner, strict response
   reader, every-hop redirect verifier, canonicalizer, pagination ledger,
   deterministic router, aggregate reporter, resource monitor, and CLI;
2. qualify that exact implementation using generated fixtures and mocked
   network responses only;
3. commit and push the implementation and require both remote CI jobs green;
   and then
4. execute exactly one metadata-only discovery pass over the five frozen
   official indexes and four frozen queries or complete motor/EEG category
   traversal where an index has no text search.

The single execution is consumed on success, park, or refusal. It may emit
exactly one `ELIGIBLE_FOR_METADATA_RESEARCH` candidate,
`NO_QUALIFYING_SOURCE`, `DISCOVERY_CAP_PARK`, or a registered fail-closed
route. It cannot emit `FULL_CONFIRMATION_SOURCE`.

## Frozen Limits

```text
official indexes:                            5
exact text queries:                         4
network requests maximum:                 128
wire-body bytes maximum:            33,554,432
decoded-body bytes maximum:         33,554,432
retained public artifacts maximum:    8,388,608
runtime maximum:                         300 s
peak RSS maximum:                   268,435,456
CPU threads / workers:                     1 / 1
per-request timeout maximum:                30 s
redirects per request maximum:                 3
retries / reruns:                           0 / 0
payload or derivative disk bytes:               0
```

Every initial contact, redirect hop, pagination request, error-response open,
or failed open counts before contact. Partial, truncated, cyclic, duplicate,
off-allowlist, unsupported-encoding, malformed, or over-cap surfaces cannot be
ranked or selected.

## Explicit Exclusions

This decision does not authorize source-specific publication research,
payload URLs, archives, ranges, members, headers, EEG, EOG, EMG, events,
annotations, targets, labels, acquisition, derivatives, models, checkpoints,
training, inference, prediction freezing, scoring, language models, providers,
streams, devices, hardware, another project, cleanup, deletion, release,
publication, rerun, substitution, or scientific-claim upgrade.

## Decision-Only Measurements

```text
fresh GitHub CI verification calls:             1
implementation or generated qualification:      0
network requests / bytes:                        0 / 0
candidate selections:                            0
real/private or payload/header reads:             0
signal/event/target/label reads:                  0
model/training/prediction/score operations:       0
provider/stream/device/hardware operations:       0
release/deletion/claim upgrades:                  0
end-to-end latency measured:                      false
```

Engineering capability authorized: after this decision and the implementation
are separately remotely green, execute one complete-or-park metadata-only
source-discovery pass under the frozen network and resource limits.

Scientific claim not established: this decision accesses no neural payload and
establishes no EEG-specific information, unseen-person generalization,
movement-intention decoding, language decoding, live operation, hardware
result, or clinical value.
