# MARC2-VR4P Machine-Stable Private Recovery Authorization Decision

Date: 2026-08-16

Lane: `MARC2-VR4P`

Status: **Packet-bound short-form authorization recorded; ineffective until
this decision is committed, pushed, and both required CI jobs are green; no
expired certificate, private path, output root, structural manifest, archive,
neural, target, model, prediction, or score operation occurred while recording
this decision**

Machine decision:
`registries/marc2_machine_stable_private_recovery_authorization_decision.v0.json`

## Maintainer Decision

After Codex identified `MARC2-VR4P` as the sole active Tier C packet and named
its remotely green request commit, CI run, two required jobs, exact structural
scope, two-stage order, and fresh-decision boundary, the maintainer's next
message was exactly:

```text
continue
```

The machine record preserves those actual eight UTF-8 bytes and their SHA-256.
It does not fabricate the packet's long scope as a maintainer utterance. Under
the approved Research Autonomy Charter, the fresh `continue` binds only the
immutable, remotely green `MARC2-VR4P` packet by reference, without expansion.

## Green Request Bound By This Decision

```text
request commit:       a5b73d6859c71054a1f20ab6c1c500341539efea
CI run:               31967933217
Base Python job:      95215825208
Optional Neuro job:  95215825263
packet SHA-256:       56458e19991e468f501731d555e144f9ee0d31b19f70b6e666f7c950c3a796e8
request SHA-256:      cf10a7bcd40baa941c81f3966694aa63e80a173c1fc0c4e6e5c2d6c2bcce34a1
```

GitHub reported both required jobs successful for the exact request commit.
The pre-existing untracked tracker inspection sidecar was not opened,
modified, staged, or deleted.

## Delayed Effect

This decision is not effective merely because these files exist. Before any
executor implementation or operation on the expired certificate or retained
structural manifest, this exact decision must:

1. pass focused and complete local verification;
2. be committed as one coherent decision-only milestone;
3. be pushed on the current branch; and
4. pass both required remote CI jobs.

Until all four conditions hold, implementation and private access remain
closed.

## Authorized Sequence After Green Decision

### Stage 1: additive executor, generated/mock qualification only

After this exact decision is remotely green, one standard-library executor may
be implemented. Development and qualification must use only invocation-created
generated fixtures, injected machine samples, mocked no-follow filesystem
facts, and temporary generated output roots. Qualification may not inspect,
read, unlink, rename, copy, or overwrite the real expired certificate; operate
on the retained private manifest; open an archive member; or access a neural,
target, model, prediction, or score surface.

The implementation must bind its future exact remotely green commit through an
explicit proof record, never ambient branch HEAD. It must preserve the packet's
fixed paths, exact expired-certificate identity, fresh readiness gate,
marker-before-open state machine, one strict structural read, one VR2 adapter
call, aggregate privacy boundary, resource monitors, deterministic replay, and
zero-retry rule. No generic source, output, cleanup, project, or consumed-root
override is permitted.

The exact Stage 1 implementation must then be committed, pushed, and pass both
remote CI jobs before any real operation begins.

### Stage 2: one machine-stable structural pass

Only after the exact Stage 1 implementation is remotely green may one command:

1. no-follow validate, read exactly once, and unlink only the registered
   expired mode-`0600`, 4,551-byte readiness certificate;
2. obtain a fresh three-sample readiness certificate bound to the exact green
   executor proof;
3. require the new output root to be absent and recheck thread, disk, and RSS
   safety immediately before the irreversible boundary;
4. atomically write a mode-`0600` consumed marker immediately before one
   content open of the exact 418,755-byte retained structural manifest;
5. read, hash, and strict-parse those bytes exactly once;
6. call the green VR2 adapter exactly once; and
7. write only one private structural cohort manifest and one aggregate-safe
   report, with all three output files under 4 MiB combined.

The expected target-free reconciliation remains 1,227 rows and 238 complete
bundles, dynamically classified as 195 eligible plus 43 valid ineligible,
then frozen to 16 subjects, 96 bundles, and 384 selected members. The
8,105,207,776 selected declared bytes are reservation metadata only. This
decision authorizes no allocation, materialization, download, open, or read of
those archive bytes.

## Resources And Failure Semantics

```text
CPU threads / workers / numerical jobs: 1 / 1 / 1
runtime:                                 <= 650 seconds
peak RSS:                                < 256 MiB
minimum free disk before marker:         >= 15 GiB
fresh readiness wait:                    <= 600 seconds
expired certificate input:               exactly 4,551 bytes once
private structural input:                exactly 418,755 bytes once
network / archive payload bytes:         0 / 0
combined output:                         <= 4 MiB
retry / rerun / resume / fallback:       0 / 0 / 0 / 0
```

A refusal before the marker performs zero private structural content opens and
permits no retry. Every route after the marker consumes the one structural
pass, also without retry, rerun, resume, repair, substitution, or fallback.
Cleanup authority is limited to the exact expired certificate and temporary
files created by the future invocation; no other project or artifact is in
scope.

## Explicitly Not Authorized

This decision does not authorize implementation before this decision is
remotely green; real operation before the exact executor is remotely green;
operation on an old consumed executor or root; another file, path, sibling,
project, dataset, participant, session, or source; network or download;
archive-member, ZIP-range, EEG, MEG, signal, event, onset, channel, geometry,
sentence, key, label, target, response, trial, or payload access; derivative or
split creation; training, parameter updates, checkpoint access, inference,
prediction, freeze, target delivery, scoring, or tuning; `MARC2-FW2` or
`MARC2-CIL1`; a language model, provider, RW3, stream, device, or hardware
operation; release or publication; or any scientific, decoding, neural,
real-time, portable, home-use, assistive, or clinical claim upgrade.

## Next Gate

After this decision is remotely green, implement and qualify the exact
generated/mock executor. After that implementation is remotely green, perform
the one registered structural pass. Only a successful, remotely green cohort
freeze makes Tier A `MARC2-FW2` preregistration eligible. Any future archive
payload, neural derivative, model fit, prediction freeze, target delivery, or
score requires its own immutable all-false Tier C packet and fresh decision.

## Claim Boundary

Engineering capability authorized after green decision: one newly proven,
machine-stable wrapper may freeze a real target-free cohort identity from one
exact structural manifest without opening an archive member.

Scientific claim not established: this decision is not neural data or a result
and establishes no neural effect, decoding performance, brain-specific origin,
language decoding, or thought-to-text capability.
