# MARC2-VR1 Source-Validity / Eligibility Repair Implementation

Date: 2026-08-16

Lane: `MARC2-VR1`

Status: **Generated-only implementation ready; exact remote proof and measured
closeout pending**

Contract:
`registries/marc2_source_validity_eligibility_repair_contract.v0.json`

Module:
`src/neurodecodekit/datasets/marc2_source_validity_eligibility_repair.py`

Implementation record:
`registries/marc2_source_validity_eligibility_repair_implementation.v0.json`

## Green Registration Boundary

Implementation began only after registration commit
`9dedfe6f649b7f8044598c7047ddeadcd9bfab76` passed Base Python job
`95153164447`, Optional Neuro Readers job `95153164463`, and CI
`31942316544`.

That proof opened generated-only Tier B implementation. It did not authorize a
private path, the consumed LA2 root, an archive member, neural payload, target,
model, score, network operation, `MARC2-FW2`, release, or claim upgrade.

## Implementation

The additive standard-library module starts from the frozen selector's
generated 1,227-row fixture. It replaces exactly 172 of its 245 generic
auxiliary regular-file rows with 43 complete four-companion Freewill-shaped
adversary bundles. Seventy-three generic auxiliary rows remain.

The constructed source contains:

| Class | Bundles | Companions |
|---|---:|---:|
| eligible session-1/2 | 195 | 780 |
| excluded single-session participant | 12 | 48 |
| excluded sampling-tier participant | 24 | 96 |
| extra session | 7 | 28 |
| **total** | **238** | **952** |

The adversary identities are deterministic test values. They do not claim to
reproduce any private archive row or published participant assignment.

The validator enforces this order:

1. Verify the frozen contract, green registration proof, and fixed inputs.
2. Validate the complete source envelope and all 1,227 rows.
3. Group and validate all 238 complete companion sets.
4. Classify every bundle into one of four aggregate predicate codes.
5. Filter to the frozen participant and session eligibility domain.
6. Assert the exact 195 eligible bundles and per-subject session counts.
7. Apply the frozen rank, first-three-run split, reservation formula, and
   contiguous-prefix mechanics.
8. Compare canonical and reversed source-order replays.
9. Emit only aggregate counts, hashes, measurements, warnings, and zero access
   counters.

No global 195-bundle assertion is applied to the unfiltered 238-bundle source.
No ineligible bundle can enter rank, split, reservation, or selection.

## Frozen Replay

The behavior tests reproduce the existing generated target-free selection:

```text
eligible subjects:                  19
selected subjects:                  16
selected run bundles:               96
selected core members:             384
fit / heldout bundles:          48 / 48
fit-heldout overlap:                  0
selected reservation bytes: 8,105,207,776
selection identity SHA-256: dee065bfdb5f8439fe711042eaadbea0dca3d83f8be0d6b7b9d1637e84d9f641
ineligible selected bundles:          0
```

These are generated structural identities and storage reservations. They are
not participant measurements, acquired bytes, or decoding results.

## Adversarial Coverage

All 36 registered generated mutations refuse, and all eight refusal classes
are exercised:

| Route | Mutations |
|---|---:|
| `MARC2VR-F01` contract or green-proof binding | 1 |
| `MARC2VR-F02` source envelope or count | 10 |
| `MARC2VR-F03` row or path safety | 16 |
| `MARC2VR-F04` companion uniqueness or completeness | 3 |
| `MARC2VR-F05` eligibility or predicate boundary | 3 |
| `MARC2VR-F06` frozen selection or replay | 1 |
| `MARC2VR-F07` public-output or forbidden-operation boundary | 1 |
| `MARC2VR-F08` resource or cleanup cap | 1 |

The CLI exposes only `plan`, `qualify`, and `inspect`. It has no `execute`
command, URL, generic source path, private root, archive reader, neural reader,
target, trainer, predictor, scorer, provider, or hardware surface.

## Local Verification

```text
focused registration tests:         10 passed
focused behavior tests:             17 passed
focused implementation tests:        9 passed
focused total:                       36 passed
complete dependency-free suite:   3,532 passed / 204 skipped
optional A-M process:             3,090 passed / 28 skipped
optional N-Z process:               513 passed / 7 skipped
optional total:                   3,603 passed / 35 skipped
registry JSON documents:            237 passed
Ruff / compileall:         passed / passed
CLI help/plan/inspect:               passed
git diff hygiene:                    passed
```

The optional suite ran in fresh A-M and N-Z processes to avoid cross-test RSS
accumulation. The A-M shard used the established permitted process context for
one legacy forkserver timing test after its sandboxed run failed only because
the sandbox denied the local Unix socket.

The CLI `qualify` closeout is deliberately deferred until this exact
implementation commit passes both remote CI jobs. Tests exercise its generated
logic with deterministic clocks and RSS readers; they create no retained
artifact and do not substitute for the one measured registered closeout.

## Access And Authority

Implementation and local tests performed zero private or Git-ignored path
operations, consumed-root operations, archive reads, neural or target reads,
derivative creation, model operations, network calls, provider calls, hardware
operations, LA2 retry or repair operations, `MARC2-FW2` operations, and claim
upgrades.

The module may inspect only its aggregate generated report. It does not expose
private rows, member names, participant/session/run identities, sizes, CRCs,
offsets, signals, events, targets, quality, channels, or geometry.

## Next Gate

Commit and push this exact implementation, then require both Base Python and
Optional Neuro Readers jobs to pass. Only that exact remote-green proof opens
the single measured generated qualification and closeout record.

Even a successful generated closeout authorizes no private read. Any new
private structural attempt requires a separately named prospective contract,
an all-false Tier C request, a fresh packet-bound maintainer decision, and a
new one-shot executor. LA2 remains consumed and may not be patched, reused,
retried, resumed, or reinspected.

## Boundary

Engineering capability added: NeuroDecodeKit now has a generated-only adapter
that validates all 238 source-shaped run bundles, filters eligibility before
the exact 195-run assertion, and preserves the frozen target-free prefix
selection without admitting excluded bundles.

Scientific claim not established: no private archive, neural payload, target,
prediction, or score was accessed, so this implementation establishes no
neural effect, decoding accuracy, language decoding, or thought-to-text
capability.
