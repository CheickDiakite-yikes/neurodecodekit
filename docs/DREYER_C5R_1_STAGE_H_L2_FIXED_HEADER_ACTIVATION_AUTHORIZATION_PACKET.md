# DREYER-C5R-1 H-L2 Fixed-Header Activation Authorization Packet

Date: 2026-08-29

Status: **request only; every authority flag remains false**

Machine request:

- `registries/dreyer_c5r_1_stage_h_l2_fixed_header_activation_authorization_request.v0.json`

Request ID: `DREYER-C5R-1-HL2-A0`

## Why This Is The Next Gate

The corrected one-file transaction passed its sole 65-case generated
qualification, the result is consumed, and its exact proof chain is remotely
green. The next belief-changing uncertainty is whether the first frozen Dreyer
source object actually has the sensor roles and sampling rate required by the
60-person nuisance-controlled unseen-participant experiment.

This packet requests the smallest irreversible observation that can answer
that question: one exact 14,805,604-byte public EDF, one fixed-header parse,
and one aggregate H1 or H0 report. It grants no authority now.

## Immutable Green Basis

Exact result-proof commit `1ae340354352d544d6d99fe5af6f354ab668bf9c`
passed:

- Base Python job `99107500593` in 7m3s;
- Optional Neuro Readers job `99107500410` in 11m48s; and
- CI `33255170805`.

This request binds the unchanged proof document, machine proof, and proof
test: three artifacts totaling 10,425 bytes under canonical artifact-set
SHA-256:

```text
192c6ba2427d399912f59bd8352e84668e7970cdd2f801d84a47308a434b8a72
```

## Exact Proposed Member

- dataset: Dreyer et al. Dataset A, NEMAR `nm000250`, revision `v1.0.4`;
- participant and run: `sub-01`, R1;
- path: `sourcedata/sub-01/eeg/sub-01_task-R1acquisition_eeg.edf`;
- direct URL: `https://data.nemar.org/nm000250/v1.0.4/sourcedata/sub-01/eeg/sub-01_task-R1acquisition_eeg.edf`;
- expected bytes: `14,805,604`; and
- expected SHA-256:
  `a678fe6d37e0496eb381dcac6b877b047d02dfffc659ae4cfc38226f4850e185`.

The other 119 R1/R2 EDFs, every R3-R6 object, sidecar, derivative,
participant, dataset, alternate host, redirect target, retry, and substitute
remain closed.

## Requested Ordered Work

Only after this request and its proof-only closeout are remotely green, and
only after fresh packet-bound maintainer words are recorded in a separate
decision that itself becomes remotely green, may the project:

1. add one standard-library H-L2 execution adapter around the exact green
   recovery transaction without modifying any frozen predecessor;
2. qualify only the adapter wiring with generated payloads, a mock opener,
   generated activation evidence, and adversarial refusal tests;
3. commit, push, and remotely green that exact implementation;
4. create one no-authority activation record binding the exact implementation,
   decision, member, caps, and one-shot identity;
5. commit, push, and remotely green that exact activation; and
6. execute exactly one H-L2 invocation.

The live invocation must write and sync a unique no-clobber consumed marker
before constructing an opener or request. It must use one verified-TLS direct
GET with `Accept-Encoding: identity`, no proxy, redirect, retry, range, resume,
cookie, credential, fallback, or substitution. It may stream and SHA-256
verify only the exact body above, parse only the EDF fixed header, and publish
only an aggregate sensor/resource/operation ledger.

The attempt is consumed on H1, H0, or failure. There is no retry, rerun,
resume, repair, substitution, threshold change, or post-result amendment.

## Fixed Sensor Gate

H1 requires exactly the frozen 27 EEG labels, exactly three unambiguous EOG
labels, exactly two unambiguous wrist-EMG labels, at most one recognized EDF
annotation channel, no unknown, blank, or duplicate labels, and exactly 512 Hz
on every physiological channel. H1 retains the private payload and permits
only a later narrowing amendment that binds observed EOG and EMG names. It
does not authorize acquisition of the remaining cohort or Stage A.

H0 retains no unaccepted payload and permanently parks the Dreyer lane.

## Resource Envelope

| Resource | Frozen maximum or requirement |
|---|---:|
| CPU threads / workers / numerical jobs | 1 / 1 / 0 |
| Wall time | 300 seconds |
| Peak process-tree RSS | 256 MiB |
| Real HTTP GET requests | exactly 1 |
| Successful body bytes | exactly 14,805,604 |
| Payload-network cap | 16 MiB |
| Incremental disk peak | 32 MiB |
| Public output | 1 MiB |
| Stream chunk size | 1 MiB |
| Required free disk | 10 GiB |
| Redirects / retries / reruns | 0 / 0 / 0 |

TLS and transport-header bytes unavailable to the standard library must be
reported as unavailable. The enforceable network ledger is one request, exact
body bytes, and zero redirect or retry.

## Explicit Exclusions

This request authorizes nothing now. Its proposed maximum excludes annotation
records, signal samples, events, trials, task labels, targets, individual
outcomes, raw patient or recording fields, raw-header publication, geometry,
reference, impedance, and signal quality. It also excludes splits, epochs,
windows, features, caches, derivatives, checkpoints, models, training,
inference, calibration, selection, predictions, target delivery, scoring,
language models, providers, RW3, streams, devices, hardware, other projects,
release, publication, and every scientific or clinical claim upgrade.

No ignored Q0 evidence, consumed BNCI or EEGMMIDB evidence, S20, S21, S24,
S25, SpanishBCBL, or other private payload may be opened, inspected, moved,
modified, deleted, or published.

## Decision Boundary

Every authority flag in the machine request is false. This request and a
separate proof-only closeout must first be committed, pushed, and remotely
green. Only then may fresh unambiguous maintainer words activate exactly this
packet through a separately committed and remotely green decision.

Engineering capability requested: expose the qualified recovery transaction
through one proof-bound, one-file fixed-header execution path.

Scientific claim not established: this all-false request performs no real EEG
operation and establishes no neural information, decoding, unseen-person,
peripheral-adjusted, live, hardware, or clinical result.
