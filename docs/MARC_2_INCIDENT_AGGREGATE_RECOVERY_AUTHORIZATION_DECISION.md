# MARC2-VR14P Incident Aggregate Recovery Authorization Decision

Date: 2026-08-20

Lane: `MARC2-VR14P`

Status: **Packet-bound short-form authorization recorded; ineffective until
this exact decision is committed, pushed, and both required CI jobs are green**

Machine decision:
`registries/marc2_incident_aggregate_recovery_authorization_decision.v0.json`

## Maintainer Decision

After Codex identified `MARC2-VR14P` as the sole active Tier C packet and
reported its exact green proof, aggregate-only scope, and scientific boundary,
the maintainer's next message was exactly:

```text
youre approve to keep going, continue, you have my continuous approval and pre-approved approval
```

The machine decision preserves those actual 96 UTF-8 bytes and their SHA-256.
The words `continuous approval` do not expand authority to a future packet,
neural payload, target, model, score, release, or scientific claim. Detailed
scope comes only from the unchanged remotely green VR14P packet by reference.

## Green Packet Bound By This Decision

```text
request commit:       d920e8eeaf7a7e9c980232c5de59f0e390c374be
request CI:           32443248466
request Base job:     96657974654
request Optional job: 96657974564
proof head:           3274a728ccf25a2e7bb5a7c208d2e8d53f2db6fb
proof CI:             32443804353
proof Base job:       96659529617
proof Optional job:   96659529824
```

Both required jobs passed for both exact heads. The three request artifacts
remain byte-, SHA-256-, and Git-blob-bound at 13,334 bytes total. The unrelated
tracker inspection NDJSON was not opened, modified, staged, or deleted.

## Delayed Effect

This decision does not become effective because its files exist. Before any
wrapper implementation, generated qualification, ignored-path check, or
aggregate-report access, this exact decision must pass focused local
verification, be committed, be pushed, and pass both required remote CI jobs.

## Authorized Sequence After Green Decision

### Stage 1: generated fixed-path recovery reader

One additive standard-library wrapper may expose fixed `plan`, `qualify`,
`inspect`, and proof-gated `execute` surfaces. Qualification may use only
generated aggregate reports, mocked no-follow facts, and invocation-created
temporary paths. The exact implementation must then be committed, pushed, and
pass both CI jobs. A separate proof-only closeout must also be committed,
pushed, and pass both jobs before Stage 2.

The reader must accept only the eight frozen `MARC2VR13P-R1` through
`MARC2VR13P-R8` routes and a strict aggregate-safe schema. It must reject
symlinks, aliases, oversized content, malformed JSON, unknown routes, private
identity, source paths or hashes, exceptions, reasons, predicates, rows,
members, participants, sessions, runs, tasks, signals, events, targets,
models, predictions, scores, and arbitrary path or output overrides.

### Stage 2: one aggregate-only incident recovery

Only after Stage 1 and its proof closeout are remotely green may one fixed
command:

1. no-follow validate and content-open only
   `.codex_work/marc2_r4_private_discriminator/v0/report.aggregate.v0.json`;
2. read and strict-parse at most 65,536 bytes exactly once;
3. enforce the frozen aggregate schema and route allowlist; and
4. write one aggregate recovery receipt under the fixed new root
   `.codex_work/marc2_incident_aggregate_recovery/v0`.

The command may not stat, resolve, hash, list, open, or parse the readiness
certificate, consumed marker, private cohort manifest, structural source,
sibling, or any other ignored path. Missing, symlinked, oversized, malformed,
leaking, unknown-route, or resource-exceeding input consumes and parks VR14P.
There is no retry, rerun, resume, cleanup, fallback, substitution, or
post-result reinspection.

## Result Ceiling

`MARC2VR13P-R1` may only make a separately frozen private-manifest recovery
packet eligible. It does not make FW2 or CIL1 executable. Routes R2-R8 may
recover only one aggregate structural class. No route authorizes archive or
neural payload access, targets, models, training, inference, scoring, or a
scientific claim.

## Resource Ceiling

```text
CPU threads / workers / jobs:  1 / 1 / 1
runtime:                       <= 30 seconds
peak RSS:                      <= 256 MiB
aggregate content opens:       1
aggregate bytes:               <= 65,536
combined generated output:     <= 1 MiB
network / new payload:         0 / 0 bytes
structural-source operations:  0
private-manifest operations:   0
retry / rerun / resume:        0 / 0 / 0
```

## Explicitly Not Authorized

This decision does not authorize implementation before its own remote green;
aggregate access before exact Stage 1 and proof-closeout green; access to any
other ignored path; archive, EEG, MEG, signal, event, channel, geometry,
target, label, sentence, trial, response, cache, feature, split, NeuroToken,
checkpoint, model, prediction, score, provider, language model, stream,
device, or hardware work; network or download; FW2 or CIL1 execution; another
project; release or publication; or a scientific, decoding, neural, live,
portable, home-use, assistive, or clinical claim upgrade.

Engineering capability authorized after green decision: one proof-separated
reader may recover only the existing aggregate structural route without
reopening the source or private manifest.

Scientific claim not established: this decision is not neural data or a
result and establishes no neural effect, decoding performance, language
decoding, live decoding, or thought-to-text capability.
