# MARC2-VR36P Task-Aware Private Cohort Confirmation Proof Closeout

Date: 2026-08-23

Lane: `MARC2-VR36P`

Status: **Proof-only closeout recorded; ineffective until its own exact commit
is pushed and both required CI jobs are green**

## Remote Implementation Proof

Exact Stage 1 implementation
`8179f6fd4acb721ef25b023e02ac9160789f9d49` passed CI `32650171033`, Base
Python job `97220389999`, and Optional Neuro Readers job `97220389862`.
Both required jobs are green. The wrapper module and its sole measured
qualification are unchanged from that Stage 1 commit.

The closeout binds these immutable preproof snapshots:

| Artifact | Bytes | SHA-256 | Git blob |
| --- | ---: | --- | --- |
| Implementation registry | 5,950 | `3b32c341372a8221731d399be1f93758607facb8a34821b7765df0449aa470ab` | `ad6d8e411593651544af32882ad11e82d740febc` |
| Result registry | 4,647 | `286059c3b82c89875d5200d4a58d16b67bb59364d11a99536e06083b5c8b5b2e` | `4d55f03559b93e4cd5d1d014e65b3ddc97f38f71` |

The canonical six-artifact implementation-set hash is
`cdcfc4bf5d6cbe29116585612b68e593daeaaf425fbf6c19ffdbf77b06d635e8`.
The closeout also binds the exact Stage 1 Git blobs for the module, behavior
test, result registry, result test, implementation document, and authorization
decision.

## No Repeated Operation

This closeout does not repeat the registered generated qualification. It does
not run readiness, inspect or construct a `.codex_work` path, read the private
structural source, inspect a consumed lane, create a real cohort, or access an
archive member, neural signal, event, channel, geometry, target, model,
prediction, or score. Every such operation counter remains zero.

## Delayed Effect

This record is ineffective until its own exact commit is pushed and both CI
jobs are green. Before that remote proof, no readiness or private path may be
touched.

After this exact closeout is remotely green, the existing packet-bound
decision authorizes one registered invocation only. It makes exactly three
fresh readiness-provider calls and two fixed five-second sleeper calls. A
nonpassing readiness result consumes at aggregate R6 with zero source opens.
Only passing readiness may preflight the registered 418,755-byte target-free
structural source, create the consumed marker, open and strict-parse that
source once, and call unchanged VR35A once. VR35A G1/G2 may retain one private
source-bound 16-subject, 96-bundle, 384-row cohort; R1/R2/R3 retain no cohort.
The public result contains only the aggregate VR36P route and measurements.
There is no retry, rerun, resume, repair, fallback, or substitution.

Engineering capability added: the generated-qualified exact-readiness and
task-aware cohort-freeze wrapper is cryptographically bound to its exact
remote CI proof and a separate activation barrier.

Scientific claim not established: this proof-only closeout accessed no
private source or neural payload and established no neural effect, decoding
accuracy, language decoding, unseen-person generalization, or live decoding
result.
