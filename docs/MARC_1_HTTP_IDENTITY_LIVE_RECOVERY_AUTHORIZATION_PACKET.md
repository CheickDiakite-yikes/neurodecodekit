# MARC1-HT1A Standards-Aligned Live Metadata Recovery Authorization Packet

Date: 2026-08-12

Status: **exact packet-bound Tier C decision requested; not granted**

Lane: **MARC1-HT1A Standards-Aligned Live Metadata Recovery**

Green generated-semantics result:
`5344d73bb74431e9bba05e3608c2a1523a84cd00`

Green result CI: `31584662864` (Base Python `94075586323`; Optional Neuro
Readers `94075586171`)

Machine request:
`registries/marc1_http_identity_live_recovery_authorization_request.v0.json`

## Decision In Plain Language

This packet asks permission for one new additive, standard-library live
metadata wrapper and, only after that exact wrapper is committed, pushed, and
both CI jobs are green, one new metadata-only selection attempt.

The future wrapper may combine the already green target-free pilot selector
with the already green HTTP identity rule. The future invocation may verify
and read exactly one already sealed 418,755-byte Freewill central-directory
manifest and accept exactly one bounded public Figshare v3 Wrist metadata
body. It may then choose only the 12 participants per axis fixed before any
live response and write one private selection manifest plus one aggregate
report. It may not open a ZIP local header, member, participant archive,
signal, event, target, model, or score.

This packet authorizes nothing by itself.

## Same Path, Not A Pivot

MARC1-HT1A is a control and attribution checkpoint on the existing path:

```text
confound resolution
  -> controlled neural positive control
  -> language-specific held-out decoding
  -> stronger thought-to-text evidence
```

The purpose of this metadata step is to establish a reproducible, bounded
cohort before later signal work. It does not replace language decoding with
movement decoding, and it does not change the project destination.

## Why A Fresh Decision Is Required

The prior live lane `MARC1-P1A` consumed at `MARC1PS-F03` after one sealed
inventory read and one Wrist response open. It accepted zero public-body bytes
and opened no payload. Its private invocation root and retained private
material are permanently out of scope.

The repair was then designed, frozen, implemented, and run entirely on
generated fixtures. Exact result `5344d73` passed both jobs in CI
`31584662864`; `MARC1HT-G1` accepted all four frozen uncoded-response forms,
refused all 20 malformed or coded forms, passed all 16 gates, and replayed the
exact target-free 12+12 selection. It used zero real or network bytes.

A fresh upstream private-manifest read and a new public metadata request are
Tier C operations. The maintainer's current and earlier messages preceded
this immutable request and cannot authorize it retroactively. After this
exact request is committed, pushed, and both CI jobs are green, Codex may
identify its commit, CI run, exact scope, and boundary. If this remains the
sole active Tier C packet, a fresh unambiguous `continue`, `approve`, or
`proceed` may bind it by reference. The separate decision record must quote
the maintainer's actual words and bind the request commit and SHA-256. Codex
must not fabricate a longer authorization sentence as a user utterance.

## Immutable Proof Chain

The target-free pilot-selection contract
`d1218066e64dea502d263acf0c096ed7eab55a11` passed both jobs in CI
`31569417204`. Exact generated selector `0c0a698` passed CI `31571668853`,
and its consumed generated result `fd246294` passed CI `31572950727`.

The retained upstream Freewill inventory is bound only through green aggregate
result `MARC1CD-R1`: 418,755 bytes, mode `0600`, SHA-256
`2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031`.
That sealed upstream artifact is distinct from the consumed `MARC1-P1A`
invocation root. Preparing this packet did not stat, open, hash, or parse
either path.

The failed `MARC1-P1A` result `8d9cae1` passed both jobs in CI `31579626846`.
Its `MARC1PS-F03` route is final and cannot be retried, amended, or used as a
source of the unretained response-header value.

MARC1-HT1 research `f515b36`, contract `1f99d0a`, implementation `b2cb48c`,
and result `5344d73` became remotely green in that order. The frozen policy
accepts only an absent `Content-Encoding` field or one case-insensitive
`identity` token and refuses all other present values, duplicates, lists,
transfer coding, decoding, and decompression.

## Requested Ordered Sequence

Only after a separate decision commit is pushed and both CI jobs pass may the
following sequence begin:

1. Implement a new additive standard-library wrapper at
   `src/neurodecodekit/datasets/marc1_http_identity_live.py`. It may use the
   green target-free selector and HTTP-semantics module but must not import,
   call, modify, or expose the consumed `marc1_pilot_selection_live.py`
   executor.
2. Use only generated private manifests, mocked HTTP responses, injected name
   resolution, and temporary paths before that wrapper becomes remotely green.
3. Freeze no-follow private reading, HTTP response-envelope handling,
   participant-name parsing, target-free selection, privacy firewalls, bounded
   output, resource checks, consumed-marker behavior, and aggregate routing.
4. Qualify both accepted encoding forms and every registered refusal,
   including malformed JSON, duplicate headers or keys, transfer coding,
   aliases, wrong source hash, symlinks, mode mismatch, cap overflow,
   target-leakage vocabulary, old-root access, overwrite, and consumed failure.
5. Commit and push the exact wrapper and require Base Python and Optional
   Neuro Readers CI to pass. A changed, failed, or ungreen implementation is
   ineligible for live access.
6. Before consumption, verify every immutable proof, one-thread environment
   values, at least 12 GiB free disk, and normalized one-minute load no greater
   than `1.0` per logical CPU. Refuse before a marker if any value is missing
   or outside the gate.
7. Create one new isolated Git-ignored root at
   `.codex_work/marc1_http_identity/live_recovery_v0` and one new consumed
   marker. The old `.codex_work/marc1_pilot_selection/live_selection_v0` root
   is forbidden. Any later failure consumes MARC1-HT1A.
8. Validate the sealed upstream manifest with no-follow checks, require one
   regular mode-`0600` file of exactly 418,755 bytes, then make exactly one
   bounded content open, SHA-256 pass, and strict JSON parse.
9. Send one unauthenticated `GET` to the exact Figshare v3 files endpoint.
   Permit no more than two bodyless HTTPS redirects to globally routable hosts
   and accept one terminal `200` JSON body through a cap-plus-one read of at
   most 2 MiB.
10. Accept an uncoded body only when `Content-Encoding` is absent or is one
    case-insensitive `identity` token. Refuse all actual codings, duplicates,
    lists, transfer coding, decoding, decompression, fallback, or amendment.
11. Validate exactly 55 file rows, 45 unique participant archives, ten
    supplementary rows, the 3,683,416,050-byte record total, and the frozen
    participant-name rule. Any mismatch parks without substitution.
12. Run only the frozen target-free selector. Require the exact participant
    ranks, sessions, runs, member suffixes, split identities, and source and
    joint byte caps.
13. Write one mode-`0600` Git-ignored private selection manifest and one
    aggregate public report, discard the response body from memory, and stop.

There is no retry, rerun, resume, restart, second manifest open, second
metadata body, alternate source, fallback parser, payload acquisition,
local-header request, archive/member open, model, or score in this packet.

## Exact Input Identities

### Freewill upstream private inventory

```text
relative path: .codex_work/marc1_central_directory/live_audit_v0/member_inventory.private.v0.json
bytes:         418,755 exact
mode:          0600 exact
SHA-256:       2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031
source DOI:    10.6084/m9.figshare.28632599.v1
rows:          1,227
regular files: 1,025
directories:   202
content opens: 1 future maximum
payload opens: 0
```

### Wrist public metadata

```text
provider:          Figshare
record/version:    29666735 / 3
DOI:               10.6084/m9.figshare.29666735.v3
license:           CC BY 4.0
metadata endpoint: https://api.figshare.com/v2/articles/29666735/versions/3/files
accepted bodies:   1 future maximum
body cap:          2,097,152 bytes
expected rows:     55
participant rows:  45
supplementary:     10
record bytes:      3,683,416,050
payload requests:  0
```

Raw metadata-body SHA-256 and transport headers are provenance, not source
identity. Content is accepted only after bounded transport and exact semantic
validation. If the live body differs from the frozen schema or selection
rules, the sole attempt parks without interpretation or amendment.

## Frozen Selection And Privacy Boundary

The participant IDs were preregistered before any live response:

```text
Freewill: sub-08 sub-10 sub-07 sub-22 sub-19 sub-16
          sub-14 sub-04 sub-05 sub-03 sub-09 sub-11

Wrist:    sub-08 sub-11 sub-09 sub-23 sub-20 sub-16
          sub-42 sub-38 sub-36 sub-30 sub-45 sub-21
```

Freewill must select the first three complete numeric run bundles from
`ses-01` as fit and `ses-02` as held out: 72 bundles and 288 core members.
Wrist must select exactly one archive per frozen participant and bind runs 1-6
as future fit and 7-8 as future held out. Size and checksums may enforce caps
after selection but may not influence participant, session, run, or split.

Exact member names, archive names, file IDs, offsets, CRCs, sizes, checksums,
and download URLs remain private. The public report may contain only the
preregistered participant IDs, aggregate counts and byte totals, split totals,
domain-separated hashes, resource measurements, counters, warnings,
unavailable fields, route, and claim boundary.

The public report must contain no non-preregistered participant identity,
member or archive name, file ID, offset, checksum, URL, response header, local
path, raw manifest row, raw response body, event, target, signal value,
prediction, or secret.

## Resource Caps

| Stage | Wall time | Peak RSS | Input/body bytes | Generated output |
| --- | ---: | ---: | ---: | ---: |
| Generated/mock wrapper qualification | 30 s | 268,435,456 | generated only | 2,097,152 |
| One live metadata selection | 30 s | 268,435,456 | 418,755 local + 2,097,152 network | 2,097,152 |

Both stages use one CPU thread, one worker, and one numerical job. The live
stage permits one private content open, one accepted network body, at most
three HTTP attempts including two bodyless redirects, 4 MiB incremental disk,
and one new Git-ignored invocation root. At least 12 GiB free disk is required
before the consumed marker. An overflow-detection byte may be read but never
accepted or retained. Retries and reruns are zero.

The selected future payload reservation remains capped at 6 GiB for Freewill,
2 GiB for Wrist, and 8 GiB jointly. Passing this metadata gate does not
authorize moving any payload byte.

## Explicitly Not Authorized

- any action before a separate packet-bound decision is remotely green;
- any real path or public request before the exact additive wrapper is
  remotely green;
- importing, calling, modifying, or reusing the consumed `MARC1-P1A`
  executor;
- opening or operating on the old `MARC1-P1A` invocation root or retained
  private material;
- opening, statting, hashing, parsing, moving, renaming, deleting, or changing
  any other private file, consumed root, cache, dataset, project, or sidecar;
- a second manifest open, metadata body, redirect beyond the cap, retry,
  rerun, resume, restart, fallback parser, substitution, or amendment;
- a ZIP local header, member payload, participant archive, extraction,
  decompression, CRC verification, whole-file MD5 pass, or payload download;
- any signal, channel, geometry, EOG, EMG, acceleration, encoder, audio, event,
  onset, target, label, trial, response, sentence, key, or quality read;
- any cache, epoch, window, feature, derivative array, training, inference,
  prediction, freeze, target delivery, score, threshold, model, seed,
  architecture, or hyperparameter operation;
- any dependency installation, language or foundation model, provider call,
  RW3, stream, device, hardware, upload, publication, release, or destructive
  action; and
- any scientific, decoding, brain-specific, language, thought-to-text,
  real-time, portable, home-use, assistive, or clinical claim upgrade.

## Current State

Every implementation, private-read, public-request, payload, neural, target,
model, score, release, cleanup, and claim authorization flag in the machine
request is false. Preparing this packet made zero private-path operations,
public requests, DNS queries, body reads, payload reads, signal/event/target
reads, model runs, predictions, scores, retries, reruns, deletions, or claim
changes.

Twelve focused request tests and all 412 MARC tests pass. The dependency-light
suite passes 2,551 tests with 204 expected skips in 21.459 seconds at
280,821,760-byte external maximum RSS. The optional-neuro suite passes 2,622
tests with 35 expected skips in 56.737 seconds at 778,092,544-byte external
maximum RSS. Each complete suite adds exactly 12 tests and zero skips over the
green generated-result baseline. Ruff, compilation, all 181 registry JSON
files, and `git diff --check` pass.

This packet must now be committed, pushed, and pass both remote CI jobs before
it can be presented as the sole active Tier C gate.

Engineering capability requested: one proof-gated standard-library wrapper
can combine the green HTTP identity semantics and frozen target-free selector
to bind the exact real two-axis pilot without opening a neural payload.

Scientific claim not established by this request: this all-false packet is
not a neural experiment and establishes no neural effect, movement decoding,
source attribution, language decoding, or thought-to-text capability.
