# MARC1-P1A Real Metadata Selection Authorization Decision

Date: 2026-08-12

Status: **authorized only after this decision is tested, committed, pushed,
and remotely green; no real-selector implementation or metadata access has
occurred**

Machine decision:
`registries/marc1_privacy_preserving_pilot_selection_authorization_decision.v0.json`

Frozen request commit:
`7f1ba0936e4e0266c0210648aa641feab63cd0eb`

Frozen packet:
`docs/MARC_1_PRIVACY_PRESERVING_PILOT_SELECTION_AUTHORIZATION_PACKET.md`

## Actual Maintainer Decision

After Codex identified MARC1-P1A as the sole active Tier C packet, named
request commit `7f1ba0936e4e0266c0210648aa641feab63cd0eb`, green CI
`31573969646`, both required green jobs, the generated-wrapper-first order,
the one-private-plus-one-public metadata scope, the zero-payload boundary, and
the need for a fresh decision, the maintainer said:

> approved, continue, achieve a scientific claim, achieve thought to text 😎

This record preserves those exact 76 UTF-8 bytes. It does not claim that the
maintainer typed the packet's long scope. The short approval incorporates the
immutable, remotely green packet by reference without widening it.

The request to achieve a scientific claim and thought-to-text is the enduring
research objective, not permission to announce an outcome before evidence or
to skip the registered ladder. Scientific claims remain contingent on a
future preregistered experiment, proper controls, held-out scoring, and the
observed result.

## Why The Short Form Is Valid

All fail-closed conditions were satisfied before the instruction:

1. MARC1-P1A was the sole active Tier C packet.
2. The packet and all-false request were committed and pushed at `7f1ba09`.
3. Base Python job `94041819046` and Optional Neuro Readers job `94041819022`
   were green in CI `31573969646`.
4. Codex named the packet, commit, CI proof, exact two-input metadata scope,
   zero-payload boundary, and decision gate.
5. The maintainer then unambiguously said `approved, continue`.
6. This separate record quotes the complete actual message and binds every
   immutable scope artifact.
7. No payload, signal, event, target, model, score, release, or scientific
   upgrade is inferred from the aspiration to reach thought-to-text.

The decision is ineffective until its own commit passes both remote CI jobs.

## Bound Evidence

```text
authorization parent:  7f1ba0936e4e0266c0210648aa641feab63cd0eb
request push CI:        31573969646
Base Python job:        94041819046
Optional Neuro job:    94041819022
contract SHA-256:      2099849ad13c6c1a97488e81cef8b21dcd61e59914d00fd43b9e76e8ccd5c39c
implementation SHA:    09f3c559ba83b2eec47a36b8772a8904c4b2783e1e443f8999bf2c2371e6a4d1
generated result SHA:  e795bed7effbf7f69f80804a1ac770c1d18bafe597d55f1627984c9dae878add
request SHA-256:       8eebf5f34294bc266e81552d31ff376cb81240d2ee18b2fc6857600fbd3aba85
packet SHA-256:        ba0943a28e155a6eaf505f2e214c8a89fe02c3917d7f4f99057b74a5622f31eb
user-message SHA-256:  aedb564a57be18493fc20376676ef404794ca169d02985228aa8424cd7f7e6e8
```

The registration, generated selector, generated result, packet, request, and
tests remain immutable snapshots. Their pending fields are not rewritten
after approval. This additive record supplies only the packet-bound
permission.

## Ordered Authorization

### Gate 1: this decision must become remotely green

This exact decision must be tested, committed, pushed, and pass Base Python
and Optional Neuro Readers CI. Until then, implementation and access remain
closed.

### Gate 2: generated and mocked real-selector qualification

After Gate 1, Codex may implement the one additive standard-library wrapper
named by the packet. It may use only generated private manifests, generated
Wrist metadata rows, fresh temporary paths, injected DNS answers, and mocked
HTTP responses. It must bind the green selector, freeze the source-specific
participant-name parser, validate decision and request proofs before enabling
its fixed real route, implement the no-follow reader, machine gate, target
firewall, private/public writer, consumed marker, and all registered refusals.

The existing environment may be reused. Dependency installation, the retained
Freewill path, public Figshare access, member/archive payload access, signal,
target, model, score, and claim work remain closed during implementation. The
exact wrapper commit must pass both remote CI jobs before Gate 3.

### Gate 3: one real metadata selection

After Gate 2 is remotely green, first require all numerical thread values to
equal one, at least 12,884,901,888 bytes free, and normalized one-minute load
no greater than `1.0` per logical CPU. Any unavailable or failed measure
refuses before a new private consumed marker.

One no-retry invocation may then:

1. create the new isolated Git-ignored root and consumed marker;
2. no-follow validate the registered Freewill manifest as a regular
   418,755-byte mode-`0600` file;
3. make exactly one content open, one bounded read, one SHA-256 pass, and one
   strict JSON parse, requiring SHA-256
   `2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031`;
4. make one unauthenticated `GET` to the exact Wrist Figshare v3 files
   endpoint, following at most two bodyless globally routable HTTPS redirects;
5. accept exactly one terminal `200` JSON body no larger than 2,097,152 bytes;
6. require exactly 55 rows, 45 unique participant archives, ten supplementary
   rows, 3,683,416,050 total declared bytes, and the participant-name rule
   frozen in the green implementation;
7. run only the preregistered target-free 12-person selection and split logic;
8. emit one mode-`0600` private selection manifest and one aggregate public
   report, discard the public response body, and stop.

Any failure after the marker consumes MARC1-P1A. There is no second manifest
open, second metadata body, retry, rerun, resume, restart, fallback parser,
alternate endpoint, participant substitution, local-header or payload open,
acquisition, signal read, target read, model, prediction, or score.

## Exact Limits

```text
Freewill private input:                exactly 418,755 bytes / one content open
Wrist metadata bodies / bytes:         1 / <= 2,097,152
HTTP attempts / bodyless redirects:    <= 3 / <= 2
selected participants per axis:        12
Freewill bundles / members:            72 / 288
Wrist archives:                        12
payload requests / bytes:              0 / 0
real execution wall / RSS:             30 sec / 268,435,456 bytes
aggregate / combined output:           <= 1,048,576 / <= 2,097,152 bytes
incremental disk / minimum free:       <= 4,194,304 / >= 12,884,901,888 bytes
threads / workers / jobs:              1 / 1 / 1
retries / reruns:                      0 / 0
signals / targets / models / scores:   0 / 0 / 0 / 0
```

The 6-GiB Freewill, 2-GiB Wrist, and 8-GiB joint payload values are future
reservation ceilings only. This decision authorizes moving zero payload bytes.

## Decision-Only Measurements

```text
GitHub CI verification calls:                    1
private Freewill path/open/read/hash/parse:       0 / 0 / 0 / 0 / 0
Wrist metadata requests/bodies/bytes/parses:      0 / 0 / 0 / 0
DNS queries / redirects:                          0 / 0
real participant/member/archive selections:       0 / 0 / 0
local headers / payload requests / payload bytes: 0 / 0 / 0
private markers/manifests / public reports:       0 / 0 / 0
signal / event / target reads:                    0 / 0 / 0
models / predictions / freezes / scores:          0 / 0 / 0 / 0
dependency installs / cleanup operations:         0 / 0
provider-model / hardware / release operations:   0 / 0 / 0
scientific claim upgrades:                        0
end-to-end latency measured:                      false
```

## Claim Boundary

Engineering capability authorized for testing: one exact standard-library,
machine-gated wrapper may be qualified and, after its own green proof, bind the
real frozen two-axis pilot from no more than 2,515,907 metadata bytes without
opening a neural payload.

Scientific claim not established: this decision is not neural data or a
result. Even a future metadata-selection pass would establish no neural
effect, movement decoding, source attribution, language decoding, or
thought-to-text capability.

Research objective preserved: this is a control and attribution rung on the
same path toward a properly evidenced thought-to-text result, not a pivot or a
substitute for the eventual language-specific experiment.
