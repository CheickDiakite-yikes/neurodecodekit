# MARC2-VR36P Task-Aware Private Cohort Confirmation Authorization Decision

Date: 2026-08-23

Lane: `MARC2-VR36P`

Status: **Packet-bound short-form authorization recorded; ineffective until
this exact decision is committed, pushed, and both required CI jobs are green**

Machine decision:

- `registries/marc2_task_aware_private_cohort_confirmation_authorization_decision.v0.json`

## Maintainer Decision

After Codex identified `MARC2-VR36P` as the sole active Tier C packet and
reported its exact request commit, proof-closeout commit, CI evidence,
two-stage scope, and fresh-decision boundary, the maintainer's next message
was exactly:

```text
coninue
```

The machine decision preserves those actual seven UTF-8 bytes and their
SHA-256. Under the approved Research Autonomy Charter short-form rule, this
unambiguous continuation binds only the unchanged, remotely green VR36P packet
by reference. It does not silently correct the message, fabricate a long-form
user utterance, or expand the registered scope.

## Green Packet Bound By This Decision

```text
request commit:       8ec87ced3c0072fec62328a9635eb9774e13e605
request CI:           32646648532
request Base job:     97211815865
request Optional job: 97211815879
proof commit:         2813d60707f9fd97acbfa35cd57504d6b3db86c5
proof CI:             32647453505
proof Base job:       97213774526
proof Optional job:   97213774452
```

Both required jobs passed for both commits. The request and proof artifact set
contains six unchanged files totaling 45,152 bytes. The unrelated untracked
tracker inspection NDJSON remains untouched.

## Delayed Effect

This decision does not become effective merely because these files exist.
Before generated implementation or qualification, this exact decision must
pass local checks, be committed and pushed, and pass both required remote CI
jobs.

Recording the decision performs zero `.codex_work`, readiness, private-source,
output-root, consumed-state, archive, neural, target, model, score, network,
device, release, or scientific-claim operation.

## Authorized Sequence After Green Decision

### Stage 1: generated/mock fixed-path wrapper

After this decision is remotely green, one additive standard-library wrapper
may be implemented and qualified using only generated structural fixtures,
injected readiness providers, mocked no-follow facts, and temporary output
roots.

The wrapper must expose fixed `plan`, `qualify`, `inspect`, and proof-gated
`execute` surfaces. It may expose no generic path, output, URL, task, count,
threshold, rank, cap, route, readiness-count, interval, timeout, retry, resume,
fallback, substitution, or resource override. It must not import, call, patch,
copy, modify, or inspect a consumed private wrapper or path.

Qualification must run the five exact VR35A cases in ready and not-ready
states, two row orders, and two exact replays. Across 40 paths it must call
unchanged VR33A exactly 40 times, make exactly 120 readiness-provider calls
and 80 sleeper calls, and call unchanged VR35A exactly 20 times only for
`PPP`. Non-ready paths must construct no source and call VR35A zero times.
Every G1, G2, R1, R2, R3, and R6 outcome, at least 100 direct refusals, source
immutability, fixed-path state behavior, exact replay, zero retention, and all
forbidden zero counters must pass.

The exact implementation and result must then be committed, pushed, and pass
both remote jobs. A separate proof-only closeout must also be committed,
pushed, and pass both jobs before Stage 2.

### Stage 2: one target-free private cohort confirmation

Only after the exact Stage 1 implementation and proof closeout are remotely
green may one fixed-path command:

1. validate every exact proof and the one-thread environment;
2. call unchanged VR33A once for exactly three fresh readiness samples with
   exactly two fixed five-second sleeps;
3. write one fresh readiness certificate at the registered VR36P path;
4. consume a non-`PPP` result at R6 with zero structural source opens;
5. for `PPP`, require the fixed output root to be absent and reject symlinks or
   aliases;
6. write one consumed marker immediately before content open;
7. read, hash, and strict-parse exactly 418,755 target-free structural bytes
   once;
8. call exact VR35A once without mutating the source;
9. on exact VR35A G1 or G2 only, write one mode-0600 source-bound cohort
   manifest containing 16 subjects, 96 run bundles, and 384 source-exact
   structural rows; and
10. write only one aggregate R1-R6 public report.

Every route consumes the one invocation. No retry, rerun, resume, repair,
fallback, substitution, overwrite, private reinspection, or amendment is
authorized.

## Fixed Route Contract

| Route | Maximum engineering meaning |
|---|---|
| `MARC2VR36P-R1` | exact target-task inventory passed directly and one target-free cohort was frozen |
| `MARC2VR36P-R2` | exact task projection removed mixed-task surplus and one target-free cohort was frozen |
| `MARC2VR36P-R3` | exact target-task eligible total remained above 195; no cohort was frozen |
| `MARC2VR36P-R4` | exact target-task eligible total was below 195; no cohort was frozen |
| `MARC2VR36P-R5` | structural, task, taxonomy, selection, split, storage, or privacy validation refused |
| `MARC2VR36P-R6` | readiness, fixed-path, output, replay, or resource precondition refused |

No route may expose a count, difference, task distribution, member, path,
participant, subject, session, run, reservation, private hash, row, predicate,
value, exception, target, prediction, or score.

## Fixed Paths And Resources

```text
readiness certificate: .codex_work/marc2_machine_readiness/vr36p/readiness.v0.json
output root:           .codex_work/marc2_task_aware_private_cohort_confirmation/v0
private source:        .codex_work/marc1_central_directory/live_audit_v0/member_inventory.private.v0.json
source bytes:          418,755 at most once and only after PPP
source SHA-256:        2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031
CPU / workers / jobs:  1 / 1 / 1
readiness samples:     3 exactly
readiness sleeps:      2 exactly, 5 seconds each
generated runtime:     <= 90 seconds
private runtime:       <= 120 seconds
peak RSS:              < 256 MiB
minimum free disk:     >= 15 GiB
combined output:       <= 2 MiB
network/new payload:   0 / 0 bytes
archive/signal/target: 0 / 0 / 0 bytes
retry/rerun/resume:    0 / 0 / 0
```

## Explicitly Not Authorized

This decision does not authorize implementation before its own remote green
proof; Stage 2 before exact Stage 1 and closeout proof; consumed-state access;
another source, path, project, participant, session, run, or dataset; network
or downloads; archive members; EEG/MEG samples, events, channels, geometry,
quality, labels, targets, or outcomes; derivative signal arrays, caches,
features, or NeuroTokens; training, inference, prediction, scoring, tuning, or
language models; FW2, CIL1, RW3, streams, devices, hardware, release,
publication, or any scientific or clinical claim upgrade.

## Next Gate

Commit, push, and green this exact decision. Then implement and generated-
qualify Stage 1. Only after that implementation and its proof-only closeout
are remotely green may the registered target-free structural confirmation run
once.

Engineering capability authorized after green decision: one proven task-aware
fixed-path wrapper and one later proof-gated target-free structural read may
freeze a deterministic source-bound cohort without opening neural payload.

Scientific claim not established: this decision is not neural data or a result
and establishes no neural effect, decoding performance, language decoding,
unseen-person generalization, or live decoding.
