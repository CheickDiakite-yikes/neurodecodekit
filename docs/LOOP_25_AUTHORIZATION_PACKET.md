# Loop 25 Authorization Packet

Prepared: 2026-07-12
Status: **Awaiting explicit user authorization; no implementation or execution
authorized**

Machine request:
`registries/loop25_authorization_request.v0.json`

This packet is a decision surface, not permission. It creates no filter
coefficients, fixture, cache, model output, or experiment result.

## Frozen Registration

The complete Loop 25 registration is already committed, pushed, and remotely
green:

```text
registration commit:       a36d97b8556e95637a21c86c44095b7e8d4c4863
registration CI run:       29193935671 (success)
contract SHA-256:          42781526225c556d0df54d1b6924fd5d9ecf95578a84c3e3922b6d5c7035050e
contract Git blob SHA-1:   380cc8d6d8eca013ac2e8ee4cbcf5afd3b74f1f9
preregistration SHA-256:   c681be25a633705f14ae5e2850908c4d911762d380ee2342fb7f2a587a3ebe7c
research SHA-256:          6e2b6c1ff584fc186926e6c631b2e1397117f13564de13fd276d64497b874ed4
contract invariants:       10 passed
complete local suite:      323 passed, 3 expected skips
remote jobs:               base Python passed; optional neuro readers passed
```

The registration snapshot remains immutable. A future authorization would be
recorded in a new decision file rather than rewriting the evidence boundary
above.

## Decision Requested

To authorize exactly the registered target-free synthetic Loop 25 gate, use
this exact sentence:

> Authorize Loop 25 implementation exactly as scoped in
> docs/LOOP_25_CAUSAL_PREPROCESSING_PREREGISTRATION.md. Do not authorize real
> or consumed data, targets, model inference, training, RW3 Stage A, streams,
> devices, or hardware.

To hold it, use:

> Hold Loop 25 implementation. Keep every Loop 25 execution authorization flag
> false.

To change it before deciding, begin with:

> Amend the Loop 25 packet before authorization:

Silence, roadmap approval, general continuation, Loop 24 history, and any RW3
decision are not Loop 25 authorization.

## What Exact Authorization Would Permit

Authorization would first permit only a separate **authorization-only commit**.
That record may authorize these registered operations and nothing broader:

1. Implement the six preregistered source/test files and four CLI commands.
2. Generate a target-free development fixture from seed 2501.
3. Construct the registered filter coefficients once after development opens.
4. Run the exact causal filter, phase-locked decimator, frozen scaler, schedules,
   resume checks, mutation controls, frequency/timing checks, and resource gate.
5. Freeze and hash the development report.
6. Open seed 2502 once only if every development gate passes.
7. Close Loop 25 as proceed, park, or kill without rerun or post-result tuning.

The authorization-only record must be tested, committed, pushed, and remotely
green before implementation starts or either seed opens.

## Exact Technical Scope

The registered path has five float32 channels at 1000 Hz and emits float32 at
100 Hz. Its stages are:

```text
validate finite contiguous source samples
-> float64 compute cast
-> stateful 50 Hz Q30 notch SOS
-> stateful fourth-order 0.5-45 Hz Butterworth SOS
-> keep global source indices 0, 10, 20, ...
-> apply contract-fixed center and scale
-> clamp to [-5, +5]
-> float32 output plus absolute source indices and timestamps
```

Zero-phase or backward filtering, interpolation, padding, whole-item statistics,
silent state resets, and target-dependent endpoint logic are forbidden. The
declared right context is zero samples. Frequency-dependent IIR delay must be
reported separately and is not end-to-end text latency.

## Target-Free Evidence Matrix

The future fixture contains 24 items: 12 physical development items from seed
2501 and 12 physically separate qualification items from seed 2502. It has six
signal families and exact lengths from 1024 through 4096 source samples.

Every item must pass seven frozen chunk schedules:

1. whole item;
2. single sample;
3. fixed chunks of seven;
4. decimation-boundary cycle;
5. frame-boundary cycle;
6. powers-of-two cycle;
7. seed-2511 irregular chunks.

Ten registered resume cuts and three future-mutation cuts test state continuity
and future independence. Forty exact refusal IDs and 21 access counters keep
malformed input and forbidden work inspectable.

No fixture member may contain targets, labels, frame labels, sample labels,
text, predictions, participant/subject/session IDs, recording paths, model
outputs, or checkpoints.

## Resource Envelope

| Resource | Hard cap |
|---|---:|
| Numerical threads / workers | 1 / 1 |
| Fixture bytes, both partitions | 4 MiB |
| Materialized working array | 16 MiB |
| Mutable state | 4 KiB |
| Report bytes | 1 MiB |
| All generated bytes | 8 MiB |
| Internal runtime | 45 seconds |
| Peak RSS | 1 GiB |
| Network, real data/cache, consumed evidence, targets, checkpoints, models, training, RW3, stream/device/hardware operations | 0 each |

The 8 MiB Loop 25 cap is stricter than the roadmap's default 32 MiB cap and
therefore wins.

## Forbidden Even After Exact Authorization

- any real recording or existing real cache read;
- S21 session-1 source-test or S21 session-2 access;
- S7 EEG consumed evaluation access;
- seed 2401 reuse or seed 2402 repurposing;
- target, label, prompt text, prediction, checkpoint, or model output access;
- model inference, decoder execution, training, or parameter updates;
- network, download, BrainFlow, LSL, PyXDF, socket, stream, board, device, or
  hardware operations;
- RW3 Stage A or Loop 26 implementation;
- changing thresholds, tolerances, schedules, cuts, seeds, or caps after a
  partition opens.

## Acceptance And Stop Rules

Proceed only if development and the one-time qualification both pass every
causality, schedule, resume, frequency, timing, normalization, flush, resource,
hash, and access gate.

Park if a registered mechanics or resource gate fails without forbidden access.
Kill if the path leaks future information, silently changes timing, uses target
information, opens protected evidence, or performs another forbidden operation.

A Loop 25 proceed would close only this mechanics gate and make Loop 26 eligible
for its own separate decision. It would not authorize Loop 26.

## Current Access Ledger

At packet creation:

```text
fixture generations:                0
filter-design runs:                 0
numeric preprocessing runs:         0
development partition opens:        0
qualification partition opens:      0
real data/cache reads:              0 / 0
consumed evidence reads:            0
target/model/checkpoint reads:      0 / 0 / 0
model/training/parameter runs:       0 / 0 / 0
network/RW3/device operations:      0 / 0 / 0
generated experiment payload bytes: 0
```

## Claim Boundary

**Engineering capability this authorization could permit:** a future bounded
implementation and one-time qualification of one registered target-free,
stateful causal preprocessing path.

**Scientific or decoding claim not established:** this packet and any future
mechanics pass do not establish Brain2Qwerty v2 equivalence, neural information,
decoding accuracy, CER/WER improvement, end-to-end latency, unseen-person
generalization, EEG/MEG usefulness, portable hardware, assistive efficacy, or
clinical utility.

## Current Decision

`authorized_now` is `false`. The next action is exactly one of: authorize using
the exact sentence, amend the packet, or hold.
