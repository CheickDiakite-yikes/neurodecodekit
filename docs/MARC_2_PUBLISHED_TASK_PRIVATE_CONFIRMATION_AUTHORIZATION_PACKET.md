# MARC2-VR20P Published-Task Private Confirmation Authorization Packet

Date: 2026-08-21

Lane: `MARC2-VR20P`

Status: **All-false Tier C request prepared; no decision and no private access**

Request:
`registries/marc2_published_task_private_confirmation_authorization_request.v0.json`

## Decision Requested

Request one bounded two-stage sequence after, and only after, this exact packet
is committed, pushed, remotely green in both required jobs, receives a
non-scope-changing proof closeout that is also remotely green, is identified as
the sole active Tier C gate, and is incorporated by a fresh maintainer decision:

1. implement and generated-qualify one additive fixed-path wrapper around the
   exact green VR20A published-task adapter; and
2. only after that exact wrapper and its proof-only closeout are committed,
   pushed, and remotely green, perform one target-free private structural read
   and either freeze one source-exact private cohort or consume the lane at one
   aggregate refusal route.

This packet records no decision and grants no authority now. The maintainer's
current `continue` preceded this exact packet and is not retroactive Tier C
authority. Fresh unambiguous packet-bound words are required after the request
and its proof closeout are remotely green.

## Why This Is The Smallest Needle-Moving Read

Consumed VR18P reached `MARC2VR18P-R4`, proving only that the prior
`task-freewill` selector rejected the target-free structural inventory. It
retained no private task value, filename, path, row, identity, participant,
selection, or cohort and cannot be retried or reinspected.

VR19A proved from exact committed code that this route requires a task token
other than lowercase `freewill`. VR20R then identified the public dataset's
published raw BIDS identity as `task-reachingandgrasping`. VR20A implemented
that exact identity in a source-preserving generated-only adapter. Exact
implementation `bf4d2b729ac948d32aa1c7b239d3c65a30f18017` passed Base
Python job `96784482381`, Optional Neuro Readers job `96784482602`, and CI
`32486620566`. Proof-only closeout
`9b5bea4b4aed21234cf3e79ae682ce1606ad2f44` passed Base Python job
`96788350279`, Optional Neuro Readers job `96788350655`, and CI
`32487854026` without repeating qualification or touching a private path.

The next useful question is therefore narrow: does the exact corrected adapter
accept the immutable target-free structural inventory and produce a bounded
cohort? The packet does not open an archive member, signal sample, event,
channel, target, model, prediction, or score.

## Proposed Generated Stage

Only after a separate decision is remotely green may Stage 1 add a
dependency-free wrapper that:

- validates this request, its future decision, and the exact green VR20A proof
  chain;
- exposes fixed `plan`, `qualify`, `inspect`, and proof-gated `execute`
  surfaces with no generic path, URL, output, task, run, threshold, retry,
  fallback, substitution, route, reason, or resource-cap override;
- replays one VR20A success source and five aggregate refusal groups in two
  source orders and two exact replays;
- calls unchanged VR20A exactly once per generated path;
- qualifies fresh-path readiness, no-follow source handling, marker order,
  strict JSON, private/public output separation, deterministic replay, and
  fail-closed resources using only generated temporary fixtures;
- passes at least 90 direct refusal mutations; and
- retains zero generated output and performs zero `.codex_work` operations.

The wrapper must not import, call, patch, copy, modify, or inspect any consumed
VR18P executor, source, readiness certificate, marker, or output root.

## Proposed One Private Confirmation

Only after the exact future wrapper and its proof-only closeout are remotely
green may one command:

1. validate every exact green proof and the one-thread environment;
2. obtain three consecutive passing machine-readiness samples;
3. create one fresh mode-`0600` readiness certificate only at the registered
   VR20P readiness path;
4. require the registered VR20P output root to be absent and reject every
   symlink or alias;
5. no-follow preflight only the registered target-free structural source;
6. create one mode-`0600` consumed marker immediately before content open;
7. open, read, SHA-256 verify, and strict-parse exactly 418,755 bytes once;
8. call the exact green VR20A adapter once without mutating the source;
9. on success, write one mode-`0600` source-exact private cohort manifest; and
10. write one aggregate-safe mode-`0644` report containing no private identity
    or failure reason.

The immutable source identity is copied only from committed aggregate records:

```text
path:        .codex_work/marc1_central_directory/live_audit_v0/member_inventory.private.v0.json
mode:        0600
bytes:       418,755
SHA-256:     2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031
rows:        1,227
files:       1,025
directories: 202
bundles:     238 total / 195 eligible / 43 valid ineligible
```

Packet preparation performs zero stats, resolves, hashes, opens, reads,
parses, existence checks, or writes against that path or any `.codex_work`
path.

## Frozen Adapter Mapping

The future wrapper may retain only a coarse route. It may not publish the
VR20A exception reason, source value, member name, path, identity, participant,
row, or candidate.

| VR20A route | VR20P route | Maximum engineering meaning |
|---|---|---|
| success | `MARC2VR20P-R1` | exact published-task adapter accepted and froze one bounded structural cohort |
| `F01` | `MARC2VR20P-R2` | proof, fixed-path, or adapter precondition refused |
| `F02` | `MARC2VR20P-R3` | source identity or strict structural envelope refused |
| `F03`-`F05` | `MARC2VR20P-R4` | published task, identity, path, or companion validation refused |
| `F06`-`F07` | `MARC2VR20P-R5` | taxonomy, selection, split, rank, or reservation refused |
| `F08`-`F09` or unknown | `MARC2VR20P-R6` | privacy, determinism, resource, or unknown refusal |

Only R1 may contain a private cohort manifest. R6 parks rather than guessing.
Every failure consumes the one invocation. No route permits retry, rerun,
resume, repair, fallback, substitution, private reinspection, cleanup, or
post-result amendment.

## Conditional R1 Cohort

R1 must preserve the exact VR20A output without rewriting source rows:

- a maximal contiguous prefix of 12-19 ranked subjects;
- exactly semantic runs 1, 2, and 3 from `ses-01` and `ses-02` per subject;
- 72-114 selected run bundles and 288-456 source-exact core members;
- equal fit and held-out bundle counts with zero overlap;
- reservation at or below 8 GiB using exact UTF-8 source names;
- task label `reachingandgrasping`, exact lowercase and unaliased;
- source, contract, semantic cohort, and selected-name hashes; and
- no quality-, target-, label-, outcome-, or participant-performance field.

Public output may contain only aggregate counts, hashes, bytes, route,
resources, warnings, unavailable fields, and claim boundaries. It may not
contain member names, private paths, subject or participant IDs, sessions,
runs, companions, offsets, CRCs, per-item rows, reasons, exception text,
labels, targets, predictions, or scores.

## Resource Limits

```text
CPU threads / workers / numerical jobs:  1 / 1 / 1
generated qualification:                 <= 60 seconds
future private command:                  <= 650 seconds
peak RSS:                                < 256 MiB
minimum free disk before marker:         15 GiB
fresh readiness wait:                    <= 600 seconds
private source read:                     exactly 418,755 bytes once
VR20A real adapter calls:                exactly 1
network / new payload bytes:             0 / 0
archive member / signal / target bytes:  0 / 0 / 0
combined incremental output:             <= 2 MiB
retries / reruns / resumes:              0 / 0 / 0
```

## Explicitly Not Requested

This packet does not request access to consumed VR18P state, archive local
headers, compressed members, EEG or MEG samples, events, channels, geometry,
quality, labels, targets, sentences, keys, models, predictions, or scores. It
does not request downloads, network access, feature extraction, caches,
NeuroTokens, training, inference, scoring, threshold selection, FW2, CIL1,
RW3, providers, language models, streams, devices, hardware, release,
publication, or scientific-claim authority.

## What Success Changes

A valid R1 result would freeze the target-free structural cohort needed to
prepare a prospective FW2 neural-payload contract. It would not authorize FW2,
open neural data, or establish a neural effect. R2-R6 would retain only a
coarse engineering blocker and would not justify private follow-up by default.

Engineering capability requested: one proof-gated target-free structural read
can confirm the primary-source-corrected selector and freeze a source-exact
cohort without opening a neural payload.

Scientific claim not established: this request performs no private read, and
even future R1 would establish only structural cohort eligibility, not neural
information, decoding accuracy, language decoding, live decoding, or thought-
to-text capability.
