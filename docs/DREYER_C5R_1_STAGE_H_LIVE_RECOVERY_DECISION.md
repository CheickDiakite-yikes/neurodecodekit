# DREYER-C5R-1 H-L1R1 Recovery Decision

Date: 2026-08-29

Status: **packet-bound implementation authorization recorded; effective only
after this exact decision is tested, committed, pushed, and both required CI
jobs are remotely green**

Machine decision:

- `registries/dreyer_c5r_1_stage_h_live_recovery_decision.v0.json`

Packet ID: `DREYER-C5R-1-HL1R1`

## Actual Maintainer Decision

After Codex identified the sole active recovery packet, its exact proof commit
`8868a0866fd8f31bf7ba435e94b1b619314910ec`, green CI `33234406143`,
and the boundary between generated implementation, separately activated
qualification, and separately closed real-data work, the maintainer said:

> continue

This record preserves those words exactly. It does not claim that the
maintainer typed the packet's long scope. The short instruction incorporates
the one immutable, remotely green packet by reference without widening it.

## Why The Short Form Is Valid

1. `DREYER-C5R-1-HL1R1` was the one named packet awaiting a decision.
2. Request `0152fa5417f85c54d8c022634e73bee69bc8ef70` and proof
   `8868a0866fd8f31bf7ba435e94b1b619314910ec` were already on GitHub
   `main` with both required CI jobs green.
3. Codex named the packet, proof, CI, authorized implementation boundary, and
   exclusions immediately before the maintainer's fresh message.
4. The maintainer then unambiguously directed continuation.
5. This separate record quotes the actual words and binds the exact request and
   proof artifacts.
6. No generated qualification, real/private path, network, EDF, target, model,
   score, device, release, other-project, or scientific-claim authority is
   inferred.

This decision is ineffective until its own exact commit passes Base Python and
Optional Neuro Readers on GitHub `main`.

## Exact Authorized Work

After this decision is remotely green, implement one additive standard-library
successor module, CLI, and tests. The predecessor source, CLI, tests, receipts,
and Stage H parser must remain byte-identical. Development may use only
generated EDF fixtures, temporary directories, generated proof records,
injected openers, mocked resource readers, and bounded unit-level failure cases.

The successor must start its protected transaction immediately after durable
marker creation and before staging, opener, request, or response capability.
It must own an explicit invocation path manifest, sanitize expected and
unexpected standard exceptions, independently account for response closure and
cleanup, downgrade H1 after teardown failure, emit one aggregate H0 after each
eligible post-marker failure, preserve no staging or unaccepted payload debris,
and isolate publication failure from cleanup.

The implementation may expose no usable real command. It may not consume the
registered H-L1R1 qualification. After the exact implementation is committed,
pushed, and remotely green, a new all-false activation and fresh decision are
required before the sole 43-case registered qualification. H-L2 and the real
14,805,604-byte EDF remain separately closed.

## Decision-Only Measurements

```text
fresh GitHub CI verification calls:             1
real/private path operations:                    0
real requests / network bytes / EDF bytes:       0 / 0 / 0
fixed-header / annotation / signal reads:        0 / 0 / 0
target or label reads:                           0
model loads / training / inference / scoring:    0 / 0 / 0 / 0
generated qualification attempts consumed:      0
provider / stream / device / hardware actions:   0 / 0 / 0 / 0
retries / reruns / releases / claim upgrades:    0 / 0 / 0 / 0
end-to-end latency measured:                     false
```

Engineering capability authorized: build and test the additive generated-only
H-L1R1 recovery implementation under the frozen transaction and resource
contract.

Scientific claim not established: this decision accesses no real EEG and
establishes no neural, decoding, unseen-person, peripheral-adjusted, live,
hardware, or clinical result.
