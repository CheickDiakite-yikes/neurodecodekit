# BNCI-C3C5-1 Cross-Participant EEG-Gain Authorization Decision

Date: 2026-08-24

Status: **Packet-bound short-form authorization recorded; ineffective until
this exact decision is committed, pushed, and both required CI jobs are green**

Machine decision:

- `registries/bnci_2014_001_cross_participant_eeg_gain_authorization_decision.v0.json`

## Maintainer Decision

After Codex identified `BNCI-C3C5-1` as the sole active Tier C packet and
reported its exact request and proof commits, both green CI jobs, staged scope,
resource envelope, scientific ceiling, and fresh-decision requirement, the
maintainer's next message was exactly:

```text
continue
```

Those eight UTF-8 bytes have SHA-256:

```text
e256ee8e7aff6957a781d8328f0f68e26996564c81fa458da59fbca2305138ad
```

Under the approved short-form packet rule, this message authorizes only the
unchanged remotely green BNCI-C3C5-1 packet by reference. It does not
fabricate a long authorization sentence, predeclare a positive result, or add
a participant, file, model, control, request, retry, resource, route, release,
or scientific claim.

## Exact Green Packet

```text
request commit:       3197390d45bfc8d19c9df2f3675166815f56f028
request CI:           32749812954
request Base job:     97503845918
request Optional job: 97503846151
proof commit:         9e7c70dcdecae8264ea988563c05e7b2f1da7fd0
proof CI:             32751503586
proof Base job:       97509285137
proof Optional job:   97509285462
```

The request binds nine immutable research, protocol, request, and test
artifacts totaling 87,813 bytes under canonical artifact-set SHA-256
`72e7ebf7de421b723a668ee2076c223c45c0ab5fca724457d66314951a1e3823`.
The proof commit adds three exact proof artifacts totaling 14,168 bytes, each
bound by path, size, SHA-256, and Git blob in the machine decision.

## Delayed Effect

Recording this decision performs zero network, payload, retained-data, MAT,
signal, event, target, label, model, prediction, score, release, or claim
operation. It becomes effective only after this exact decision commit is
pushed and both required CI jobs pass.

After that proof, only generated and mocked Stage G1 opens. Acquisition remains
closed until the exact generated implementation and sole qualification are
committed, pushed, and remotely green. Every later stage retains its own
registered barrier.

## Authorized Sequence After Green Decision

### G1: generated and mocked implementation

Implement and qualify the complete fixed pipeline using generated MAT fixtures
and mocked transport only. The pass must exercise integrity, structure,
capability, participant, split, causal preprocessing, feature, model, control,
prediction-freeze, scorer, replay, output, and resource boundaries. It may
retain no generated neural payload and may not access a real or ignored path.

Commit, push, and green the exact implementation and result before Stage A.

### A: one exact opaque acquisition

Acquire only the 18 registered original NEMAR `nm000139` v1.0.2 MAT files.
Final accepted bytes must equal `779,873,919`, and every file must match its
registered SHA-256. Transport may inspect only identity, response, range,
length, size, and digest fields. It may not parse MAT structure or content.

Freeze and remotely green the private manifest and aggregate receipt before Q.

### Q: one target-blind qualification

Open each verified MAT once through the registered target-isolation boundary.
Validate only the frozen structure, runs, trials, sampling, channels, indices,
windows, geometry availability, warnings, and capability lineage. Create
fold-scoped source-label capabilities and nine sealed held-out-E target sets;
the capability predicting a participant cannot receive that participant's
target identity.

Commit, push, and green the exact qualification before P.

### P: nine isolated fits and one prediction freeze

Run the nine outer folds sequentially. Each fold fits and selects only from the
other eight participants, uses zero held-out-person calibration, leaves held-
out session T unused, and predicts held-out session E once under all frozen
conditions. Freeze only aggregate hashes, completeness, operation counts,
resources, warnings, and claim boundaries publicly.

Commit, push, and green the aggregate prediction freeze before T.

### T: one target delivery and score

Deliver the same nine sealed held-out-E target sets once. Apply the frozen C3
and C5-partial gates and route once. No post-target fit, exclusion,
normalization, threshold, seed, feature, model, control, route, or amendment is
authorized.

## Fixed Scientific Ceiling

A maximum `BNCIC3C5-R5` result could establish only participant-independent
four-class BNCI protocol-condition prediction and incremental scalp-EEG sensor
information beyond the three recorded EOG channels under this protocol. It
cannot establish language or arbitrary-thought decoding, executed movement
intention, exclusive motor-cortex origin, freedom from unrecorded peripheral
or visual confounds, live decoding, portable hardware, home use, or clinical
utility.

## Fixed Resource Envelope

```text
CPU threads / workers / jobs: 1 / 1 / 1
registered payload files:     18
accepted payload bytes:       779,873,919 exactly
transport requests:           <= 54, <= 3 per file
payload network:              <= 2.5 GiB
incremental disk:             <= 2 GiB
free disk before acquisition: >= 5 GiB
private derivatives:          <= 512 MiB
public artifacts:             <= 4 MiB
peak RSS:                     <= 1 GiB
fits / prediction sets:       <= 540 / <= 900
target deliveries / scores:   1 / 1
scientific reruns / updates:   0 / 0
```

Cleanup is restricted to temporary files created by the active invocation. No
existing file, unrelated project, consumed state, user artifact, or prior
experiment output may be deleted, renamed, overwritten, repaired, or reused.

## Next Gate

Commit, push, and green this exact decision. Then implement and run the sole
generated/mock G1 qualification. Do not begin acquisition until that exact
implementation and result are also committed, pushed, and remotely green.

Engineering capability authorized after green decision: one complete,
proof-gated, target-firewalled unseen-participant EEG experiment may proceed
through the exact G1 -> A -> Q -> P -> T barriers.

Scientific claim not established: this decision is authorization rather than
a neural-data or model result and establishes no unseen-person generalization,
EEG gain beyond recorded EOG, or decoding performance.
