# COMM-P0-G Two-Child Full-Scale Rehearsal Preregistration

Recorded: 2026-08-28

Status: frozen generated-only Tier B rehearsal contract; implementation and
execution have not occurred.

Gate: `COMM-P0-G-FS2-v0`

Run identity: `COMM-P0-G-FS2-R0`

## Question

Can the exact remotely proven COMM-P0 coordinator complete two sequential,
isolated full-scale fictional replays within the already frozen 180-second
total wall-time cap while preserving every target-firewall, schedule,
determinism, resource, and publication invariant?

This is an engineering performance question. A pass does not activate or
consume the official qualification and has no scientific value.

## Immutable parents

The rehearsal binds:

- the frozen `COMM-P0-G-v0` qualification contract;
- Amendment 2's one-invocation timing interpretation;
- the immutable prior single-replay rehearsal result;
- exact coordinator head
  `669c858d9c35f33bb39d5a71aa886d645a832497`; and
- its remotely green proof closeout on GitHub `main` at
  `f9a8e8f6eb73081bbc0ec14ef9b52c3a8fbfb474`.

The green coordinator remains unchanged. The rehearsal wrapper must be
additive and must not modify its compile-time activation lock, official marker
schema, model, split, feature, threshold, scorer, or refusal rules.

## Ordered barriers

1. This registration and its test must be committed, pushed, remotely green,
   and present on GitHub `main`.
2. An additive wrapper and CLI may then be implemented and qualified only with
   reduced or mocked fictional fixtures. Implementation qualification may not
   run a 21-person-per-cohort replay.
3. The exact implementation and proof record must be committed, pushed,
   remotely green, and present on GitHub `main`.
4. One Tier B rehearsal attempt may then run. A durable, no-replace rehearsal
   receipt must be created before child 1. Failure, timeout, or refusal consumes
   the attempt. No retry, rerun, resume, or substitution is allowed.
5. The aggregate target-free result may be committed only after the invocation
   has terminated and every output and resource gate has passed.

No separate Tier C decision is required because the rehearsal uses generated
fictional data only. The Research Autonomy Charter does not permit this route
to open any real, private, human, device, release, or claim surface.

## Exact schedule

Each of two sequential replay children must contain 21 complete fictional
participants per cohort across discovery and independent replication. Across
both replays, exact required totals are:

| Operation | Per replay | Total |
|---|---:|---:|
| Prior fits | 42 | 84 |
| Residualizer fits | 84 | 168 |
| Classifier fits | 630 | 1,260 |
| Temperature-calibration fits | 630 | 1,260 |
| Model inference runs | 714 | 1,428 |
| Prediction sets | 1,428 | 2,856 |
| Prediction rows | 91,392 | 182,784 |
| Shortcut-fixture executions | 7 | 14 |
| Refusal observations | 70 | 140 |
| Cohort target deliveries | 2 | 4 |
| Cohort scores | 2 | 4 |
| Shortcut target deliveries | 14 | 28 |
| Shortcut scores | 14 | 28 |
| Post-target updates | 0 | 0 |

The two children must have distinct process IDs and isolated work directories.
They run sequentially under one shared absolute deadline. Canonical replay
surfaces must match after excluding only the three already registered volatile
fields: runtime, peak process-tree RSS, and observed free disk.

Complete prediction tables may not be materialized. At most 256 prediction
rows may be buffered. The exact 70-family parent refusal matrix must run once
per replay and return 140 correct observations with zero malformed acceptance,
wrong wrapper, or post-refusal mutation.

## Resource envelope

- one CPU thread, one worker, one numerical job;
- two replay children, strictly sequential, concurrency maximum one;
- 180 seconds total wall time including setup, both children, scoring,
  publication, and cleanup;
- 536,870,912-byte peak parent-plus-child process-tree RSS;
- 134,217,728 generated-input bytes maximum per replay;
- 134,217,728 private-generated-output bytes maximum per replay;
- 268,435,456-byte temporary-disk peak;
- 537,919,488-byte aggregate incremental-disk ceiling;
- 1,048,576-byte public target-free result ceiling;
- zero retained generated payload bytes after cleanup;
- zero network requests and bytes; and
- at least 22,012,755,968 free bytes before reservation, leaving at least
  21,474,836,480 bytes, exactly 20 GiB, free afterward.

The wrapper must measure the entire process tree continuously. Zero monitor
samples, missing descendants, resource-cap breach, disk-reservation failure,
or cleanup escape is a consumed refusal.

## One-shot receipt

The rehearsal receipt is a new no-replace schema and path. It must be created
durably before any replay child and rejected by the official activation loader.
It may contain only gate identity, attempt identity, parent hashes, start time,
and aggregate status. It must not contain targets, predictions, probabilities,
participant outcomes, private paths, keys, nonces, or generated payloads.

The wrapper may not read, create, replace, rename, or delete an official
qualification consumed marker. It may clean up only temporary files created by
its own invocation.

## Outcomes

`FS2-PASS` requires the exact schedule, two equivalent replay surfaces, all
resource gates, atomic no-replace publication, and total runtime at or below
180 seconds.

Any timeout, crash, mismatch, drift, forbidden operation, or cap breach routes
to `FS2-PARK`. The aggregate result must identify the failed public gate
without exposing a private topology or partial prediction surface. Either
outcome consumes `COMM-P0-G-FS2-R0`; neither permits a rerun.

## Zero counters and claim boundary

Official qualification invocations, activation reads, official marker
operations, real/private path operations, real signals, real targets or labels,
real-data training or inference, humans, devices, streams, microphones,
providers, network, releases, end-to-end device-latency measurements, retained
payload, post-target updates, and scientific-claim upgrades must all remain
zero.

Engineering capability this rehearsal can establish: the exact fictional
two-replay coordinator fits its frozen resource envelope deterministically.

Scientific claim this rehearsal cannot establish: no real EEG is accessed, so
it cannot demonstrate communication decoding, EEG information beyond
peripheral controls, unseen-person generalization, independent replication,
causal live decoding, hardware performance, or clinical value.
