# PhysioNet Motor Positive-Control Authorization Packet

Status: **Exact Tier C authorization requested; not yet granted**

Date: 2026-08-09

Registration commit:
`3c00557ecfb09c80e30843589ae295a09feec97c`

Green registration CI:
`31346882592`

Machine request:
`registries/physionet_motor_positive_control_authorization_request.v0.json`

## What This Decision Would Allow

The requested decision would allow NeuroDecodeKit to implement and run one
strict public EEG positive control over the nine files already acquired in work
order 8. It would not acquire more data.

The experiment has one scientific question: can a train-only-selected
classical pipeline recover left-versus-right motor-task information on held-out
run 11 while a separate motor-physiology assay and every mandatory confound
control pass?

The result is deliberately harder to pass than an accuracy-only benchmark.
PhysioNet's left/right action is cued by a left/right visual target. The maximum
verdict therefore requires predictive, physiology, and confound evidence
together.

## Proof Anchor

Registration `3c00557ecfb09c80e30843589ae295a09feec97c` froze:

- exactly S001-S003 and runs 03/07/11;
- exactly nine EDF files and 23,248,224 bytes;
- expected 135 task events, with 90 fit/selection and 45 sealed final events;
- causal 8-30 Hz preprocessing and fixed signal windows;
- CSP-LDA versus Riemannian MDM selection using only runs 03/07;
- one nonselecting low-frequency LDA comparator;
- 12 final primary/control prediction sets;
- a central mu/beta physiology assay;
- the ordered `WO9-V0` through `WO9-V3` router;
- one remote-green prediction freeze before one final score; and
- one-thread runtime, memory, storage, fit, prediction, and no-rerun caps.

Push CI `31346882592` passed Base Python job `93330354031` and Optional Neuro
Readers job `93330354047` at that exact commit.

## Ordered Authorization

Authorization is conditional and does not permit immediate real-data access.

### 1. Decision first

The exact maintainer sentence below must be recorded in separate human and
machine authorization-only artifacts. That decision commit must be pushed and
both required CI jobs must be green.

### 2. Fixture-only implementation second

Only after the decision is remotely green may Codex:

1. add a narrow optional `classical` dependency extra without changing the
   dependency-free base install;
2. create one isolated Git-ignored environment containing only the registered
   NumPy, SciPy, MNE, scikit-learn, and pyRiemann families;
3. implement the sequential reader, target firewall, causal filters, classical
   models, controls, prediction freezer, isolated scorer, resource monitor,
   receipts, and CLI;
4. qualify every behavior with generated synthetic arrays, synthetic reader
   objects, and malformed fixtures only; and
5. commit and push that exact implementation and obtain green Base Python and
   Optional Neuro Readers jobs.

Implementation may not stat or open the registered local PhysioNet bundle.

The one isolated environment creation is capped at 256 MiB network transfer,
512 MiB incremental disk, 900 seconds, one installer worker, and no Torch,
Braindecode, MOABB, foundation model, pretrained checkpoint, or base
dependency addition. The environment is tooling, not evidence, and cannot be
committed.

### 3. One real execution third

Only after the exact implementation is remotely green may one registered
execution:

1. no-follow verify the private acquisition-manifest hash and the exact nine
   regular files;
2. make one new sequential size/SHA-256 pass and one semantic MNE parse per
   EDF;
3. read the exact headers, all 64 EEG channels, available geometry, 160 Hz
   signal samples, and only `T0`/`T1`/`T2` EDF+ annotations;
4. stop at `WO9-V0` on any identity, header, channel, sampling, event-count,
   timing, finite-value, window, split, or resource mismatch;
5. create isolated target-firewalled fit, prediction, and sealed-target
   derivatives;
6. run at most 40 classical fits and 64 target-blind prediction sets under the
   registered split, preprocessing, models, and controls;
7. create a public aggregate hash-only prediction-freeze ledger without
   individual predictions, probabilities, targets, or participant outcomes;
8. commit and push that ledger and obtain both required green CI jobs; and
9. only then deliver the same 45 run-11 targets to the isolated scorer once,
   score once, and apply the frozen verdict router.

The real execution uses one CPU thread, one worker, one numerical job, at most
1,800 seconds, 805,306,368-byte peak RSS, 67,108,864 private generated bytes,
at least 2 GiB free disk, zero network bytes, zero new payload bytes, and zero
retry or rerun. Cleanup is limited to temporary files created by that
invocation under its isolated work root.

## Maximum Result

`WO9-V3` would establish only this:

> On three public EEGMMIDB participants, the frozen pipeline recovered
> held-out-run motor-task information and the registered sensorimotor-band
> physiology and proxy-control conjunction passed.

It would not establish brain-specific origin. The task has a class-correlated
visual cue and lacks separate EOG/EMG measurements. It would also not establish
unseen-person generalization, typing, language or thought decoding, real-time
performance, portable hardware, home use, assistive benefit, or clinical
utility.

A clean `WO9-V3` would justify a separately registered no-tuning replication
on more untouched public participants. It would not itself authorize that
acquisition or replication.

## Exact Authorization Sentence

The request becomes active only if the maintainer sends this sentence exactly:

> Authorize the work order 9 PhysioNet motor positive-control implementation and one registered execution exactly as scoped in docs/PHYSIONET_MOTOR_POSITIVE_CONTROL_PREREGISTRATION.md and registries/physionet_motor_positive_control_contract.v0.json. I bind registration commit 3c00557ecfb09c80e30843589ae295a09feec97c and green CI run 31346882592, including Base Python job 93330354031 and Optional Neuro Readers job 93330354047. I authorize, only after a separate authorization-only decision is committed, pushed, and remotely green, one generated-fixture-only implementation of the exact sequential EDF reader, target firewall, causal preprocessing, fixed classical families, controls, prediction freezer, isolated scorer, resource monitors, receipts, and CLI; addition of one narrow optional classical dependency extra without changing the dependency-free base install; and one isolated Git-ignored environment installation containing only the registered NumPy, SciPy, MNE, scikit-learn, and pyRiemann families under one installer worker, 900 seconds, 256 MiB network transfer, and 512 MiB incremental disk. I authorize, only after that exact implementation is committed, pushed, and remotely green, one no-network execution over only the nine acquired PhysioNet EEGMMIDB v1.0.0 S001-S003 runs 03/07/11 EDF files totaling exactly 23,248,224 bytes: one no-follow private-manifest verification, one sequential size and SHA-256 pass and one semantic MNE parse per EDF; reading their exact headers, all 64 EEG channels, available geometry, 160 Hz signal samples, and only T0, T1, and T2 EDF+ annotations; target-firewalled creation of the registered runs-03/07 fit derivative, target-blind run-11 prediction derivative, and sealed 45-target scorer input; up to 40 classical parameter-update fits and 64 target-blind prediction sets under the frozen family-selection, causal preprocessing, physiology, and confound contracts; and creation of one aggregate hash-only prediction-freeze ledger containing no individual prediction, probability, target, or participant outcome. I authorize, only after that freeze ledger is committed, pushed, and remotely green, one delivery and one scoring of the same 45 sealed run-11 targets and application of the frozen WO9-V0 through WO9-V3 router. The real execution is limited to one CPU thread, one worker, one numerical job, 1,800 seconds, 805,306,368-byte peak RSS, 67,108,864 private generated bytes, at least 2 GiB free disk, zero network bytes, zero new payload bytes, cleanup only of invocation-created temporary files, and zero retry or rerun. I do not authorize any .event sidecar; additional file, participant, run, dataset, download, or substitution; S20, S21, S24, S25, SpanishBCBL, raw FIF, or MAT access; row-random split, run-11 target use before the remotely green freeze, target-derived exclusion, evaluation-time fit, test-time adaptation, post-target update, hyperparameter search, larger or additional model, deep network, CML-v0, pretrained checkpoint, foundation or language model, provider, RW3, stream, device, hardware, individual protected output publication, release, replication cohort, or scientific claim beyond the registered WO9-V3 three-person motor-task EEG pilot ceiling.

## Current Counters

Preparing this packet performed zero local PhysioNet stats, opens, hashes,
parses, header reads, annotation reads, signal reads, target reads, dependency
installs, derivative or split operations, fits, inferences, freezes, scores,
downloads, provider calls, hardware operations, retries, or reruns.

Engineering capability if the future registered execution passes: one
leakage-resistant public EEG motor positive control can be run under a
prediction-freeze-protected and resource-bounded evidence order.

Scientific claim not established by this request: authorization planning
observes no EDF content and produces no motor-task, physiology, neural,
generalization, decoding, latency, device, or human-benefit evidence.
