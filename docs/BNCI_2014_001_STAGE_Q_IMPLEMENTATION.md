# BNCI-C3C5-1 Stage Q Implementation

Date: 2026-08-25
Lane: `BNCI-C3C5-1-Q`
Status: generated implementation qualification passed; live activation pending

## Purpose

Stage Q is the only bridge between the remotely green opaque Stage A bundle and
future modeling. It validates the exact 18 upstream MAT files once, produces a
target-free feature derivative, creates fold-scoped source-label capabilities,
and seals the nine held-out session-E target sets. It does not fit, select, run,
or score a model.

The remotely green umbrella decision already authorizes one Q execution after
the Stage A result is green. The exact Stage A result commit is
`96d7f0569a54b05f8031d2e3943658ef598e38a5`; CI `32814564120`, Base Python job
`97700176631`, and Optional Neuro Readers job `97700176787` passed. A separate
green implementation activation must still bind the exact Stage Q source,
tests, generated result, and CI proof before any private manifest or MAT open.

## Implementation

The generated-qualified semantic core is
`src/neurodecodekit/datasets/bnci_2014_001_stage_q.py`. Its original sidecar
exposes only planning and the consumed qualification command; its live
activation path will remain absent. The real operation is exposed only through
the additive live control
plane in `src/neurodecodekit/datasets/bnci_2014_001_stage_q_live.py` and
`src/neurodecodekit/bnci_c3c5_stage_q_live_cli.py`.

The live executor will:

1. fail closed unless the frozen public research, contract, decision, Stage A
   result, and a later green Stage Q activation remain exact, then use fresh
   Git remote and GitHub Actions metadata to prove that activation commit and
   both required jobs are green before the semantic executor begins;
2. prove the conservative one-copy layout fits its cap, require at least 2 GiB
   plus that bound free, exclusively reserve its temporary directory, and only
   then persist a consumed marker before the first ignored-path read;
3. validate the private Stage A manifest against all 18 registered members;
4. open each MAT once with no-follow semantics, read its exact bytes, verify its
   registered size and SHA-256, and parse that in-memory byte sequence;
5. recognize empty-trial calibration structs without reading their signal;
6. require six task runs, 48 trials per task run, 250 Hz, 25 columns, exact
   public class order after the frozen alias normalization, complete one-based
   windows, balanced targets, and binary artifact flags when available;
7. apply common average over exactly 22 EEG channels and fourth-order causal
   Butterworth filters with zero state reset once per run, never per trial;
8. emit one deterministic target-free feature shard for each of the 18
   participant/session pairs, storing every high-dimensional array exactly once;
9. emit nine tiny fold delivery manifests that reference only the other eight
   participants' T/E shards plus the fold participant's E shard, with a separate
   matching source-target capability and zero held-out-T delivery;
10. store held-out session-E labels as authenticated encrypted envelopes, with
    their scoring keys at a separate fixed private path outside every fold
    capability tree;
11. seal both artifact values and an explicit availability mask for later
    aggregate-only post-score description; and
12. write the private aggregate receipt into the unpromoted temporary output,
    apply the final runtime/RSS/byte gate, and atomically promote it with the
    derivative; followed later by a public
    aggregate result that contains no row, target, participant outcome, or
    derivative hash.

There is no global predictive archive and no nine-fold duplication of the
3,711 feature dimensions. For fold `Ai`, its delivery manifest allowlists the
other eight participants' signal shards, target-free signal for `AiE`, its
matching source-target capability, and zero `AiT` delivery. A future proof-bound
Stage P controller delivers only those exact listed bytes into an isolated fold
process and gives it no repository root or scoring-key path. Stage T scoring
keys remain unopened until the
prediction-freeze barrier is remotely green. The first trial has no previous
intertrial interval, so the timing control uses a frozen exact-zero sentinel
and reports that choice as a warning.

## Generated Qualification

Before activation, one generated-only qualification must verify:

- real-shaped task structs plus empty-trial calibration structs;
- six runs, 48 trials, 25 channels, 250 Hz, and all frozen windows;
- run-bound causal filtering and every feature dimension;
- deterministic NPZ replay;
- target mutation leaving target-free feature bytes unchanged;
- malformed identity refusal, output collision refusal, and one-thread/version
  enforcement; and
- zero private, real, network, model, training, prediction, delivery, or score
  operations.

The sole qualification passed all six registered case classes. It processed
one 40,069,562-byte generated MAT with three target-ineligible calibration
structs, six task runs, and 288 trials. Runtime was 3.6570341249462217 seconds,
peak process RSS was 498,434,048 bytes, and the deterministic target-free
feature archive was 644,708 bytes. Every private/real, network, model,
training, prediction, delivery, and score counter was zero. The generated run
is consumed and may not be repeated.

Its result and exact implementation must be committed, pushed, and green
before an activation record can enable the one real invocation.

The later activation must bind the implementation CI run's exact head SHA,
successful workflow conclusion, exact Base Python and Optional Neuro Readers
job names, IDs, and successful conclusions. Immediately before the zero-network
semantic executor, the live CLI performs metadata-only `git ls-remote` and
GitHub Actions checks for the activation commit itself. It then passes an
in-memory exact proof into the executor, which requires the local, remote, CI,
and activation head SHAs to agree before writing the consumed marker.

The generated run directly qualified the MAT identity/parser, calibration
recognition, run-bound feature core, target invariance, deterministic archive,
and malformed identity refusal. Additive adversarial tests qualify the live
control plane's exact activation schema, authenticated target envelopes,
one-copy signal shards, fold delivery manifests, symlink-safe writes, artifact
availability sealing, conservative storage preflight, progressive and final
resource checks. The generated semantic run is not claimed
to have exercised the future private 18-member orchestration.

After the generated result was written, independent review found that the
core's provisional live writer and caller-selected qualification output were
too permissive. A fail-closed safety amendment now makes the qualification
fixed-path and permanently consumed and makes the original core live entry
point always refuse. This amendment changes no qualified MAT parsing, feature,
target-invariance, or serialization computation. Only the separately tested
live control plane can later receive an activation.

## Resource Boundary

- one CPU thread, one worker, one numerical job;
- 18 semantic MAT content opens maximum;
- zero network bytes;
- 3,600 seconds maximum runtime;
- 1 GiB peak RSS maximum;
- 512 MiB private derivative maximum;
- 4 MiB public artifact maximum; and
- at least 2 GiB free disk plus the conservative one-copy layout bound before
  the consumed marker;
- zero fits, model runs, prediction sets, target deliveries, scores, retries,
  reruns, or post-result changes.

The semantic executor uses zero network bytes. Its mandatory preceding control
plane performs only Git remote and GitHub Actions metadata verification; it
does not request or upload a neural payload, derivative, target, or model.

Only invocation-created temporary output may be cleaned after a failure. The
consumed marker remains, and a Q failure permanently refuses this lane.

## Claim Boundary

This implementation can establish MAT readability, frozen task/channel/timing
semantics, and a target-firewalled feature capability for later modeling.

It cannot establish neural information, EEG advantage beyond EOG, unseen-person
generalization, decoding accuracy, language or thought decoding, motor-cortex
origin, live decoding, portable hardware, home use, or clinical value.
