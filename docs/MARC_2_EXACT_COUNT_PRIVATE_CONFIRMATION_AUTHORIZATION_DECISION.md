# MARC2-VR34P Exact-Count Private Confirmation Authorization Decision

Date: 2026-08-23

Lane: `MARC2-VR34P`

Status: **Packet-bound short-form authorization recorded; ineffective until
this exact decision is committed, pushed, and both required CI jobs are green**

Machine decision:
`registries/marc2_exact_count_private_confirmation_authorization_decision.v0.json`

## Maintainer Decision

After Codex identified `MARC2-VR34P` as the sole active Tier C packet and
reported its exact request, proof closeout, CI evidence, aggregate-only scope,
and fresh-decision boundary, the maintainer's next message was exactly:

```text
continue
```

The machine decision preserves those actual eight UTF-8 bytes and their
SHA-256. Under the approved Research Autonomy Charter short-form rule, this
message binds only the unchanged, remotely green VR34P packet by reference.
It does not fabricate a long-form user utterance or expand the scope.

## Green Packet Bound By This Decision

```text
request commit:       d4215c5aa5b8e43d91ff7ff26b8ea035648f3706
request CI:           32637387771
request Base job:     97189079380
request Optional job: 97189079311
proof commit:         45cc8f18fe8fc7f5c8b9675648a4cd358617808d
proof CI:             32638120718
proof Base job:       97190816942
proof Optional job:   97190817104
```

Both jobs passed for both commits. The request and proof artifact set contains
six unchanged files totaling 41,496 bytes. The unrelated untracked tracker
inspection NDJSON remains untouched.

## Delayed Effect

This decision does not become effective merely because these files exist.
Before generated implementation, qualification, readiness work, output-root
work, or private source access, this exact decision must pass local checks, be
committed and pushed, and pass both required remote CI jobs.

Recording the decision performs zero `.codex_work`, consumed-VR32P, private-
source, readiness, output-root, archive, neural, target, model, score, network,
device, release, or scientific-claim operation.

## Authorized Sequence After Green Decision

### Stage 1: generated exact-count wrapper

After this decision is remotely green, one additive standard-library wrapper
may be implemented and qualified using only generated structural fixtures,
injected readiness samples, mocked no-follow facts, and temporary output roots.

The wrapper must expose fixed `plan`, `qualify`, `inspect`, and proof-gated
`execute` surfaces. It may expose no generic path, output, URL, count,
threshold, difference, route, readiness-count, interval, timeout, retry,
resume, fallback, substitution, or resource override. It must not import,
call, patch, copy, modify, or inspect consumed VR32P or another consumed
private lane, source, certificate, marker, report, output, or executor.

Qualification must run the 15 exact cases in two orders and two exact replays.
Across 60 paths it must call unchanged VR33A exactly 60 times, make exactly
180 readiness-provider calls and 120 sleeper calls, and call unchanged VR31A
exactly 32 times only for `PPP`. Every non-`PPP` path must consume at R3 with
zero source construction and zero VR31A calls. Exact G1/G2/R1/R2/R3 counts
4/4/4/4/44 and at least 110 direct refusals must pass with zero retention and
every private or scientific counter at zero.

The exact implementation must then be committed, pushed, and pass both remote
jobs. A separate proof-only closeout must also be committed, pushed, and pass
both jobs before Stage 2.

### Stage 2: one target-free exact-count private confirmation

Only after the exact Stage 1 implementation and proof closeout are remotely
green may one command:

1. validate every exact proof and the one-thread environment;
2. call unchanged VR33A once for exactly three fresh readiness samples with
   exactly two fixed five-second sleeps;
3. write one fresh readiness certificate at the fixed VR34P path;
4. consume any non-`PPP` result at R3 with zero structural source opens;
5. for `PPP`, require the fixed output root to be absent and reject symlinks or
   aliases;
6. no-follow preflight only the registered target-free source;
7. write one consumed marker immediately before content open;
8. read, hash, and strict-parse exactly 418,755 bytes once;
9. call exact VR31A once without mutating the source; and
10. retain only one aggregate R1 or R2 answer, or one safe R3-R5 failure route.

Every route consumes the one invocation. No retry, rerun, resume, repair,
fallback, substitution, cleanup, private reinspection, or amendment is
authorized.

## Fixed Route Contract

| Route | Maximum engineering meaning |
|---|---|
| `MARC2VR34P-R1` | filtered eligible total is below 195 |
| `MARC2VR34P-R2` | filtered eligible total is above 195 |
| `MARC2VR34P-R3` | exact readiness or fresh fixed-path precondition refused |
| `MARC2VR34P-R4` | source identity or strict structural envelope refused |
| `MARC2VR34P-R5` | unexpected upstream, privacy, deterministic-output, or resource refusal |

Only R1 and R2 answer the registered question. Neither may retain or publish
the observed total, difference, distribution, predicate, value, row, path,
identity, participant, selection, reservation, or cohort. R5 parks rather
than guesses.

## Fixed Paths And Resources

```text
readiness certificate: .codex_work/marc2_machine_readiness/vr34p/readiness.v0.json
output root:           .codex_work/marc2_exact_count_private_confirmation/v0
private source:        .codex_work/marc1_central_directory/live_audit_v0/member_inventory.private.v0.json
source bytes:          418,755 at most once and only after PPP
source SHA-256:        2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031
CPU / workers / jobs:  1 / 1 / 1
readiness samples:     3 exactly
readiness sleeps:      2 exactly, 5 seconds each
generated runtime:     <= 90 seconds
private runtime:       <= 120 seconds
peak RSS:              < 256 MiB
minimum free disk:     >= 15 GiB
combined output:       <= 1 MiB
network/new payload:   0 / 0 bytes
archive/signal/target: 0 / 0 / 0 bytes
retry/rerun/resume:    0 / 0 / 0
```

## Explicitly Not Authorized

This decision does not authorize implementation before its own remote green
proof; Stage 2 before exact Stage 1 and closeout proof; consumed-state access;
another source, path, project, participant, session, run, or dataset; network
or downloads; archive members; EEG/MEG samples, events, channels, geometry,
quality, labels, targets, or outcomes; derivatives, caches, features, or
NeuroTokens; cohort freeze; training, inference, prediction, scoring, tuning,
or language models; FW2, CIL1, RW3, streams, devices, hardware, release,
publication, or any scientific or clinical claim upgrade.

## Next Gate

Commit, push, and green this exact decision. Then implement and generated-
qualify Stage 1. Only after that implementation and its proof-only closeout
are remotely green may the registered structural direction check run once.

Engineering capability authorized after green decision: one proven exact-
count fixed-path wrapper and one later proof-gated target-free structural read
may distinguish whether the eligible total is below or above 195 without
exposing the count.

Scientific claim not established: this decision is not neural data or a
result and establishes no neural effect, decoding performance, language
decoding, unseen-person generalization, or live decoding.
