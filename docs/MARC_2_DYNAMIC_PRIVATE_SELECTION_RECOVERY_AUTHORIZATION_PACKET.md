# MARC2-VR7P Dynamic Private Selection Recovery Authorization Packet

Date: 2026-08-16

Lane: `MARC2-VR7P`

Status: **All authorization fields false; no implementation, private path,
readiness invocation, output root, marker, structural read, cohort freeze,
payload, target, model, prediction, or score is authorized**

Request:
`registries/marc2_dynamic_private_selection_recovery_authorization_request.v0.json`

## Decision Requested

After this exact packet is committed, pushed, and both required CI jobs are
green, request one future two-stage sequence:

1. implement and qualify a new additive generated/mock-only fixed-path wrapper;
   and
2. only after that exact implementation is committed, pushed, and both CI jobs
   are green, run one machine-stable target-free structural cohort pass.

The current and earlier `continue` messages are not retroactive authority for
this packet. Once it is the sole named green Tier C gate, a fresh unambiguous
maintainer `continue`, `approve`, or `proceed` may bind it by reference under
the Research Autonomy Charter.

## Why This Lane Is New

VR4P is consumed and immutable. It opened and verified the 418,755-byte
structural source once, then lost the nested VR2 refusal inside one outer route.
VR5A localized the route collapse and fixture-identity defect without reopening
private data. Generated VR6 then replaced exact 16-subject, byte-total, and hash
expectations with dynamic invariants and passed both CI jobs at exact
implementation `482dad55e91e2abf48b6a59a417ebca191c0cd68`.

VR7P does not amend, retry, patch, inspect, or reuse VR4P. It proposes new code,
a new readiness path, a new output root, a new marker, and one future call to
the green VR6 interface. It binds VR6 closeout
`5b4dde3f56e8049f24a2df4cf4c3538bd5d71683`, Base job `95235147054`,
Optional job `95235147047`, and CI `31975867040`.

## Proposed Generated Stage

After a separate decision is remotely green, Stage 1 may implement only:

- exact request, decision, and future implementation-proof validation;
- a fixed-path machine-readiness state machine using generated fixtures;
- no-follow owner/mode/size/hash/race checks on generated source fixtures;
- one new-root and marker-before-open state machine;
- one strict JSON structural read and one VR6 adapter call on generated data;
- dynamic 12/14/16/18/19-subject selection outcomes in both row orders;
- private and aggregate output validation;
- at least 72 direct refusal mutations, deterministic replay, resource
  monitors, CLI help, and exact temporary cleanup.

The future wrapper must expose no generic path, URL, source, output-root, retry,
resume, fallback, or substitution argument. Generated qualification must not
stat, resolve, hash, open, alter, delete, or reuse a real `.codex_work` path.

## Proposed One Real Structural Pass

Only after the exact future implementation is remotely green may one command:

1. verify its exact proof record;
2. obtain three consecutive passing machine-readiness samples;
3. create a fresh mode-`0600` certificate only at
   `.codex_work/marc2_machine_readiness/vr7p/readiness.v0.json`;
4. require the new root
   `.codex_work/marc2_dynamic_private_selection_recovery/v0` to be absent;
5. no-follow preflight the registered structural source;
6. create one mode-`0600` consumed marker immediately before the content open;
7. open, read, hash, and strict-parse exactly 418,755 bytes once;
8. call the green VR6 adapter exactly once; and
9. write at most one private selection manifest and one aggregate-safe report.

The source identity remains:

```text
path:        .codex_work/marc1_central_directory/live_audit_v0/member_inventory.private.v0.json
mode:        0600
bytes:       418,755
SHA-256:     2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031
rows:        1,227
files:       1,025
directories: 202
bundles:     238 total / 195 eligible / 43 valid ineligible
```

This packet does not inspect that path. The identity is copied from committed,
remotely green structural records.

## Dynamic Cohort Boundary

The future result must not be compared with a generated subject count, reserved
byte total, or selection hash. It may succeed only when the selected cohort is:

- the frozen target-free rank's maximal contiguous prefix;
- between 12 and 19 subjects;
- exactly three session-1 fit and three session-2 heldout run bundles per
  subject, with four structural companions per bundle;
- split-disjoint and not row-random;
- at or below the unchanged 8 GiB metadata reservation cap; and
- either all 19 eligible subjects or followed by the exact next ranked subject
  whose measured reservation exceeds the remaining cap.

Selected count, selected reservation, source hash, selection hash, and private
manifest hash are measured future outputs. Target, label, quality, outcome,
signal, prediction, and score fields are forbidden.

## Aggregate Privacy

The aggregate report may retain counts, byte totals, hashes, route codes, and
resource measurements. It may not retain subject IDs, participant IDs, member
names, paths, offsets, CRCs, upstream reasons, failed private values, or private
manifest rows. On a VR6 refusal it may retain only the allowlisted VR6 route
code. Unknown routes fail closed without echoing their value.

## Resource Limits

```text
CPU threads / workers / numerical jobs:  1 / 1 / 1
generated qualification:                 <= 30 seconds
future real command:                     <= 650 seconds
peak RSS:                                < 256 MiB
minimum free disk before marker:         15 GiB
fresh readiness wait:                    <= 600 seconds
private source read:                     exactly 418,755 bytes once
network bytes:                           0
archive-member or payload bytes:          0
combined incremental output:             <= 4 MiB
selection reservation ceiling:           8 GiB metadata only
retries / reruns / resumes:               0 / 0 / 0
```

## Explicitly Not Requested

This packet does not request:

- any implementation or operation before a separate green decision;
- any stat, resolve, read, hash, unlink, rename, overwrite, cleanup, or reuse of
  a consumed certificate, marker, output root, executor, or private result;
- archive-member, ZIP range, EEG, MEG, channel, geometry, signal, event, onset,
  sentence, key, label, target, quality, or payload access;
- a download, network request, additional dataset, participant, session, file,
  or source substitution;
- derivative or split creation, training, parameter update, model or checkpoint
  access, inference, prediction, freeze, target delivery, scoring, threshold
  selection, or post-result tuning;
- a language model, provider, RW3, stream, device, or hardware operation;
- release, publication, or any scientific, decoding, neural, real-time,
  portable, home-use, assistive, or clinical claim upgrade; or
- FW2 or CIL1 execution.

## Verification

- 11 focused tests and 9 artifact-hash subtests pass.
- The complete primary process reports 3,905 passes and 35 skips plus the same
  three process-state failures as the pre-change baseline.
- The two state-sensitive gates pass together in a fresh process; the isolated
  timing-worker gate passes outside the sandbox, where its forkserver Unix
  socket is permitted.
- Ruff, all registry JSON parsing, and diff hygiene pass.
- No private path, readiness, source, output, archive, neural, target, model, or
  score operation occurred during packet preparation or verification.

## Success And Failure Meaning

Success would establish only that the registered target-free structural source
can be transformed into a dynamically measured real cohort identity under exact
proof, machine, privacy, and resource controls. It would make a separate FW2
preregistration eligible, not FW2 execution.

Failure before the marker performs zero private content opens and still permits
no retry under this packet. Failure after the marker consumes the one
structural pass with no retry, rerun, resume, repair, fallback, or private
reinspection.

Engineering capability requested: one proof-gated, machine-stable, target-free
structural pass that freezes a dynamically measured real cohort without
requiring generated outcome values.

Scientific claim not established: even a successful structural cohort freeze
would access no archive or neural payload, target, prediction, or score and
would establish no neural effect or decoding result.
