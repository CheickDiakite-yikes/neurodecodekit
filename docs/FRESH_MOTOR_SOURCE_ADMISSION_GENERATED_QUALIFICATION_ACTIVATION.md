# Fresh Motor Source Admission Generated Qualification Activation

Date: 2026-08-31

Protocol: `FMSR1-R1-G-v0`

Status: **activation recorded; not effective until this exact activation commit
and both required GitHub `main` jobs are green; official qualification not run**

The activation binds corrected implementation
`d9229c1d8e9c56e2ce31da0dec0dcf302fe73eee`, CI `33349080445`, Base Python
job `99358753480`, Optional Neuro Readers job `99358753302`, and the exact
bytes and SHA-256 identities of the four runtime artifacts checked by the
qualification loader.

After this activation reaches GitHub `main` and both required jobs pass, it
permits one invocation of `qualify-generated` at
`.codex_work/fmsr1-r1-g-v0-official`. The invocation remains generated-only,
uses one CPU thread and one worker, and may produce at most one aggregate
report under the registered resource envelope. The durable attempt root must
never be removed, repaired, resumed, or reused.

This activation grants no network or GitHub API request, source discovery,
real metadata or payload access, EEG read, target or label access, model run,
prediction, score, release, or scientific claim. The current frontier remains
scientifically unchanged until a later measured real-source operation occurs;
the generated result can establish only that the admission control plane works
as registered.

Engineering capability added: the exact-green admission implementation is
eligible for one generated-only qualification after this activation's own
remote proof.

Scientific claim not established: no real neural data has been accessed and
no neural effect, nuisance resistance, unseen-person generalization, or live
decoding result has been measured.
