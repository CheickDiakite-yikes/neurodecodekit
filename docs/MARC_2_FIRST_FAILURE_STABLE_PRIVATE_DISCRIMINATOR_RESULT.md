# MARC2-VR18P First-Failure-Stable Private Discriminator Result

Date: 2026-08-21

Lane: `MARC2-VR18P`

Status: consumed at `MARC2VR18P-R4`; no retry, rerun, resume, repair,
fallback, substitution, cleanup, or private reinspection

Machine result:
`registries/marc2_first_failure_stable_private_discriminator_result.v0.json`

## Proof Before Execution

Exact Stage 1 implementation `668812367acd8ca3ae9d0603dcde9b4b5aa02d58`
passed Base Python job `96756873128`, Optional Neuro Readers job
`96756873357`, and CI `32477528982`.

Proof-only closeout `535bd9bf8cb8cc48dd8369be7c2d5c51af1a7d63`
then passed Base Python job `96759728564`, Optional Neuro Readers job
`96759728379`, and CI `32478506443`. No qualification or private operation
was repeated during closeout.

## One-Shot Result

The registered command collected three fresh readiness samples, opened the
fixed 418,755-byte target-free structural source once, strict-parsed it once,
called VR16A once, consulted the frozen VR17C map once, and returned
`MARC2VR18P-R4`. It created no private cohort manifest.

Runtime was 10.053867666982114 seconds, peak RSS was 27,443,200 bytes,
combined output was 2,063 bytes, and network and new payload bytes were zero.
The result stayed far below the registered 650-second, 256 MiB RSS, and 2 MiB
output ceilings.

Under the frozen route table, R4 means only the generated-qualified core task
or identity class. It excludes the registered companion spelling,
normalized-collision, incomplete-set, and unknown-map classes for this exact
execution. It does not distinguish task from identity and does not reveal a
predicate, value, token, filename, path, row, participant, candidate,
selection, or cohort.

## Operation Boundary

The invocation wrote only its authorized readiness certificate, consumed
marker, and aggregate report. The ignored source and output were not listed,
reopened, rehashed, or inspected after execution. This record uses only the
command's returned aggregate JSON and the already committed route table.

Archive members, neural payloads, signals, events, channels, geometry,
targets, labels, caches, features, splits, models, training, inference,
predictions, scoring, FW2/CIL1, network, providers, streams, devices,
hardware, releases, and other projects were untouched.

## What Comes Next

The next safe task is a separately frozen artifact-only and generated-only
decomposition of the core task-or-identity class. It may distinguish exact
generated predicate classes without touching this consumed source. Any later
private read, cohort freeze, archive or neural payload access, target/model/
score work, FW2, or CIL1 remains a new Tier C packet and decision.

Engineering capability added: a proof-separated one-shot localized the real
target-free structural refusal to one frozen task-or-identity class without
retaining private identity details.

Scientific claim not established: no neural payload, target, model,
prediction, or score was accessed, so this establishes no neural effect,
decoding performance, language decoding, live decoding, or thought-to-text
capability.
