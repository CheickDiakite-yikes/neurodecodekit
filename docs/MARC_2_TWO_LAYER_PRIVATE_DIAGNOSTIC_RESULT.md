# MARC2-VR9P Two-Layer Private Diagnostic Result

Date: 2026-08-17

Lane: `MARC2-VR9P`

Status: **Consumed after one target-free structural read; the exact nested
route is `MARC2VR2-F03`, no cohort was emitted, and no retry or rerun is open**

Machine result:
`registries/marc2_two_layer_private_diagnostic_result.v0.json`

## Green Execution Proof

Exact implementation commit
`0dd113a9a0259b6192e3997eae62369b9cc5a85b` passed Base Python job
`95285174846`, Optional Neuro Readers job `95285174911`, and CI
`31995078475` before execution.

The command was invoked once with all five numerical-thread variables fixed to
one and only the four registered proof identifiers. It exposed no path, root,
source, output, threshold, retry, dataset, participant, cohort, or model
override. The unrelated untracked tracker-inspection sidecar remained
unstaged and untouched.

## Final Route

The one invocation retained only the registered aggregate classes:

```text
MARC2VR9P-R1
outer VR6 route:  MARC2VR6-F02
nested VR2 route: MARC2VR2-F03
```

Committed route accounting defines `MARC2VR2-F03` as the row-path, ZIP/BIDS,
run-companion, or structural-grouping class. This localizes the current blocker
to that layer and excludes the later `MARC2VR2-F04` taxonomy/arithmetic layer
for this exact source and validator. It does **not** reveal which F03 predicate
failed, which value triggered it, or which row, path, run, participant, or
candidate was involved.

## Proven State-Machine Boundary

The registered sequence proves these operations completed in order:

1. exact implementation, clean tracked HEAD, green CI, decision ancestry, and
   shared proof-record validation;
2. one fresh machine-readiness invocation with three consecutive passing
   samples and one new mode-`0600` certificate;
3. one thread/RSS/disk recheck and one no-follow source preflight;
4. creation of one fresh output root and one mode-`0600` consumed marker;
5. one immediately following 418,755-byte structural content open, registered
   integrity match, and strict duplicate-key-controlled JSON parse;
6. exactly one call to the frozen VR6 adapter and its nested VR2 validator; and
7. one mode-`0644` aggregate report containing only the allowlisted route
   classes, measurements, zero counters, warnings, and claim boundary.

No archive member, header, neural payload, signal sample, event, target,
derivative, checkpoint, model, prediction, freeze, or score was opened or run.

## Measured Boundary

```text
route:                              MARC2VR9P-R1
source structural bytes:                 418,755
runtime:                      10.044833041 sec
peak RSS:                         39,075,840 bytes
readiness samples:                            3
readiness wait:                10.017770000 sec
maximum normalized load:        0.9684244792
minimum free disk:             146,429,759,488 bytes
readiness certificate:                   3,237 bytes
consumed marker:                            426 bytes
aggregate report:                         3,011 bytes
combined generated output:                6,674 bytes
CPU threads / workers / jobs:             1 / 1 / 1
raw-data reads / real-cache reads:        0 / 0
model runs / training runs:               0 / 0
producer causal:       not applicable, structural metadata only
end-to-end latency measured:                  no
```

Network, consumed-path, archive-member, signal, event, target, derivative,
model, training, prediction, score, provider, hardware, FW2/CIL1,
other-project, retry, release, and claim-upgrade counters were all zero.

## Disposition

`MARC2-VR9P` is consumed. There is no retry, rerun, resume, repair, fallback,
substitution, source reinspection, or output-root reuse. Do not open, inspect,
hash, parse, rename, delete, overwrite, or reuse the private structural source,
fresh certificate, consumed marker, or output root under this lane.

No real cohort identity is available, so `MARC2-FW2` and `MARC2-CIL1` remain
ineligible. The next safe work is a separately named artifact-only F03
predicate-decomposition and generated-coverage pass using committed code and
generated fixtures. Any further private structural read requires a new frozen
packet and fresh Tier C decision. Archive payload, neural data, training,
prediction freezing, target delivery, scoring, and live decoding remain later
separate gates.

Engineering capability added: the remotely green wrapper safely distinguished
the private source's nested F03 structural class from F04 while retaining no
private row, path, identity, value, candidate, selection, or cohort output.

Scientific claim not established: no neural payload, target, model,
prediction, or score was accessed, so this result establishes no neural effect,
decoding accuracy, language decoding, live decoding, or thought-to-text
capability.
