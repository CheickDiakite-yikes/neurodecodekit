# MARC2-VR26P Selection-Boundary Private Confirmation Result

Date: 2026-08-22

Lane: `MARC2-VR26P`

Status: consumed at `MARC2VR26P-R5`; no retry, rerun, resume, repair,
fallback, substitution, cleanup, amendment, or private reinspection

Machine result:
`registries/marc2_selection_boundary_private_confirmation_result.v0.json`

## Proof Before Execution

Packet-bound decision `8b9ce248f6cbce1205addf113f97325a98c1992e`
passed Base Python job `97116976065`, Optional Neuro Readers job
`97116976016`, and CI `32608318342` before Stage 1.

Exact Stage 1 implementation `d5c5abd8fde15f0557101fa2aa1135382819ea4e`
passed Base Python job `97119373912`, Optional Neuro Readers job
`97119373779`, and CI `32609236180`. Proof-only closeout
`bb6c52bb7217edb79eec5c3f09c14ba50776c2c6` then passed Base Python job
`97120986292`, Optional Neuro Readers job `97120986207`, and CI
`32609855945`. No qualification or private operation was repeated during
closeout.

## One-Shot Result

The registered command collected three fresh readiness samples, opened the
fixed 418,755-byte target-free structural source once, strict-parsed it once,
and called VR25A once. It returned `MARC2VR26P-R5` and created no private
cohort manifest. Executor runtime was 0.0482423750218004 seconds, peak RSS was
29,622,272 bytes, combined output was 2,923 bytes, and network and new payload
bytes were zero.

Under the frozen route table, R5 means participant taxonomy or the exact
eligible inventory refused. The source passed the earlier readiness,
fixed-path, source-identity, row, entry-kind, and companion-integrity classes.
The result does not distinguish eligible count/distribution drift from an
unknown recognized participant, identify a failed predicate or value, expose
an observed count or direction, or retain a member name, path, row, identity,
participant, selection, reservation, or cohort.

## Operation Boundary

The invocation wrote only its authorized readiness certificate, consumed
marker, and aggregate report. This tracked result was created from the
aggregate JSON returned by `execute`; the public report was not inspected
afterward. No private cohort manifest was created or inspected, and the
private source was not reopened, rehashed, listed, or otherwise reinspected.

Archive members, neural payloads, signals, events, channels, geometry,
targets, labels, caches, features, splits, models, training, inference,
predictions, scoring, FW2/CIL1, network, providers, streams, devices,
hardware, releases, and other projects were untouched.

## What Comes Next

The next safe task is an artifact-only and generated-only R5 decomposition
that separates VR25A R1 eligible inventory/distribution from VR25A R2 unknown-
participant taxonomy without touching the consumed source. Any later private
discriminator, cohort freeze, archive or neural payload access,
target/model/score work, FW2, or CIL1 remains a new Tier C packet and decision.

Engineering capability added: one proof-separated target-free structural
invocation localized the remaining cohort blocker to participant taxonomy or
exact eligible-inventory validation without exposing private detail.

Scientific claim not established: no neural signal, target, model,
prediction, or score was accessed, so this establishes no neural effect,
decoding performance, language decoding, unseen-person generalization, live
decoding, or thought-to-text capability.
