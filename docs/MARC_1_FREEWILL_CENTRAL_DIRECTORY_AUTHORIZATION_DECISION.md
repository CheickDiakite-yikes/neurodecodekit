# MARC1-CD1A Live Archive Audit Authorization Decision

Date: 2026-08-11

Status: **Authorized only after this decision is tested, committed, pushed,
and remotely green; no live-wrapper implementation or public request has
occurred**

Machine decision:
`registries/marc1_freewill_central_directory_authorization_decision.v0.json`

Frozen request commit:
`950796d123272a459eedf1e431ba99f22a0c582e`

Frozen packet:
`docs/MARC_1_FREEWILL_CENTRAL_DIRECTORY_AUTHORIZATION_PACKET.md`

## Actual Maintainer Decision

After Codex identified MARC1-CD1A as the sole active Tier C packet, named
request commit `950796d123272a459eedf1e431ba99f22a0c582e`, green CI
`31513578445`, both required green jobs, the generated-wrapper-first order,
the three-body 17,039,360-byte live-response cap, the no-whole-download
boundary, and the need for a fresh decision, the maintainer said:

> keep going, move the needle, continue, you approved to go on

This record preserves those exact 60 UTF-8 bytes. It does not claim that the
maintainer typed the packet's long scope. The short instruction incorporates
the immutable, remotely green packet by reference without widening it.

Every provider, identity, range, response, resource, privacy, no-member,
no-retry, no-rerun, and claim boundary remains unchanged.

## Why The Short Form Is Valid

All fail-closed conditions were satisfied before the instruction:

1. MARC1-CD1A was the sole active Tier C packet.
2. The packet and all-false request were committed and pushed at `950796d`.
3. Base Python job `93853089748` and Optional Neuro Readers job `93853089786`
   were green in CI `31513578445`.
4. Codex named the packet, commit, CI proof, exact archive-audit scope, and
   decision boundary.
5. The maintainer then unambiguously included `continue` in a direct
   instruction to keep this work moving.
6. This separate record quotes the actual message and binds every immutable
   scope artifact.
7. No whole-archive download, member access, participant acquisition, neural
   read, model, score, release, or scientific upgrade is inferred.

The decision is ineffective until its own commit passes both remote CI jobs.

## Bound Evidence

```text
authorization parent:  950796d123272a459eedf1e431ba99f22a0c582e
request push CI:        31513578445
Base Python job:        93853089748
Optional Neuro job:    93853089786
contract SHA-256:      900417812d978030b7b92ad04befa4a7ce7f9e54fe479cd8c2c43ccfab1b8d69
implementation SHA:    9cd010cd7bae25bb43c01e25c46169439103c66bf20292485f80da2ee701ee7a
generated result SHA:  00ca215a9ad236f16823a3f864f0215d2d2fb803b784423201f0a4018faaf804
request SHA-256:       67dd062df8189f4a5742da9ad9986dd12641d146df7aa3f12a50ca3b845d25fd
packet SHA-256:        88801e7dab7f548eca8254c201a454479934566f8401d310ef0945d655e407b0
user-message SHA-256:  c97c7d04ef3fb6e70265325d4805026948a1474554de1725374ae47c64a19371
```

The research, registration, generated parser, generated result, packet,
request, and tests remain immutable snapshots. Their pending fields are not
rewritten after approval. This additive record supplies only the packet-bound
permission.

## Ordered Authorization

### Gate 1: this decision must become remotely green

This exact decision must be tested, committed, pushed, and pass Base Python
and Optional Neuro Readers CI. Until then, implementation and access remain
closed.

### Gate 2: generated and mocked live-wrapper qualification

After Gate 1, Codex may implement the one additive standard-library wrapper
named by the packet. It may use only generated ZIP fixtures, generated
metadata, injected DNS answers, and mocked HTTP responses. It must import the
green central-directory parser, expose no user-selected URL or archive path,
validate the exact request and decision proofs before enabling its fixed live
route, implement the pre-consumption machine gate, and preserve the frozen
transport, output, privacy, and refusal contracts.

The existing environment may be reused. Dependency installation, public
Figshare access, real archive access, member access, participant acquisition,
and neural/model work remain closed during implementation. The exact wrapper
commit must pass both remote CI jobs before Gate 3.

### Gate 3: one public metadata/tail/directory sequence

After Gate 2 is remotely green, first require all numerical thread values to
equal one, at least 12,884,901,888 bytes free, and normalized one-minute load
no greater than `1.0` per logical CPU. Any unavailable or failed measure
refuses before a new private consumed marker.

One no-retry invocation may then:

1. create the new isolated Git-ignored root and consumed marker;
2. read one bounded version-files metadata response for Figshare record
   `28632599`, version `1`;
3. select only file `57518986` with the frozen name, size, URL, MD5, and
   license identity;
4. request exactly `bytes=13591416976-13591548047`, following at most two
   bodyless globally routable HTTPS redirects;
5. parse EOCD and complete in-tail ZIP64 structures from that exact 131,072-
   byte body in memory;
6. conditionally request the exact central-directory range, no larger than
   16 MiB, from the same terminal URL without redirect;
7. parse only central-directory metadata, emit one private exact manifest and
   one aggregate public report, discard response bodies, and stop.

There may be exactly three accepted body responses totaling no more than
17,039,360 bytes and at most five HTTP attempts. Any failure after the marker
consumes MARC1-CD1A. There is no HEAD, retry, rerun, resume, restart, alternate
provider, exploratory range, whole download, local-header read, member-payload
read, or follow-on acquisition.

## Exact Limits

```text
provider / record / version:          Figshare / 28632599 / 1
DOI / file id:                        10.6084/m9.figshare.28632599.v1 / 57518986
registered archive bytes:             13,591,548,048
tail body:                            exactly 131,072 bytes
conditional directory body:          <= 16,777,216 bytes
accepted bodies / total bytes:        3 / <= 17,039,360
HTTP attempts / bodyless redirects:   <= 5 / <= 2
HEAD / retries / reruns:              0 / 0 / 0
whole downloads / member requests:    0 / 0
live execution wall / RSS:            120 sec / 268,435,456 bytes
aggregate / combined output:          <= 1,048,576 / <= 8,388,608 bytes
incremental disk / minimum free:      <= 33,554,432 / >= 12,884,901,888 bytes
threads / workers / jobs:             1 / 1 / 1
participants / models / scores:       0 / 0 / 0
```

The registered archive size is source identity, not an authorized transfer.
This decision expressly forbids downloading the 13.59 GB archive as a whole.

## Decision-Only Measurements

```text
GitHub CI verification calls:                   1
Figshare metadata / tail / directory requests:  0 / 0 / 0
accepted public response bodies / bytes:         0 / 0
DNS queries / redirects:                         0 / 0
whole archive / local header / member bytes:     0 / 0 / 0
private markers / manifests / public reports:    0 / 0 / 0
participant / signal / event / target reads:     0 / 0 / 0 / 0
models / predictions / freezes / scores:         0 / 0 / 0 / 0
dependency installs / cleanup operations:        0 / 0
provider-model / hardware / release operations:  0 / 0 / 0
scientific claim upgrades:                       0
end-to-end latency measured:                     false
```

## Claim Boundary

Engineering capability authorized for testing: one exact standard-library,
machine-gated wrapper may be qualified and, after its own green proof,
inventory a 13.59 GB public ZIP through at most 17,039,360 accepted body bytes
without opening a member payload.

Scientific claim not established: this decision is not EEG data or a result.
Even a future metadata-only pass would establish no neural effect, decoding
accuracy, brain-specific origin, generalization, language or thought
decoding, real-time operation, portable hardware, home use, assistive benefit,
or clinical utility.
