# DREYER-C5R-1 Stage H Live Preflight Decision

Date: 2026-08-28

Status: **Packet-bound authorization recorded. It is effective only after this
exact decision is tested, committed, pushed, and both required CI jobs are
remotely green. No real-data operation occurred while recording it.**

Machine decision:

- `registries/dreyer_c5r_1_stage_h_live_preflight_decision.v0.json`

Packet ID: `DREYER-C5R-1-HL`

## Actual Maintainer Decision

After Codex identified `DREYER-C5R-1-HL` as the sole active Tier C packet,
named all-false request commit
`5191e79e2ccfa50b5042a24357c0c22d68d8f088`, proof commit
`821fad17e06914375c50a7d0dd7017458b2df838`, green CI
`32936247679`, the exact 14,805,604-byte member, and the H-L1 then H-L2
decision boundary, the maintainer said:

> continue, make a deep push

This record preserves those words exactly. It does not claim that the
maintainer typed the packet's long scope. The short instruction incorporates
the immutable, remotely green packet by reference without widening it.

## Why The Short Form Is Valid

1. `DREYER-C5R-1-HL` was the sole active Tier C packet.
2. The all-false request and proof-only closeout were already committed,
   pushed, and remotely green.
3. Base Python job `98077895278` and Optional Neuro Readers job `98077895460`
   were green in CI `32936247679` at proof commit `821fad17`.
4. Codex named the packet, commits, CI, exact object, ordered barriers, and
   exclusions before the maintainer's fresh message.
5. The maintainer then unambiguously directed continuation.
6. This separate record quotes the actual words and binds the frozen request,
   proof, and static safety review.
7. No bulk acquisition, signal, annotation, target, model, score, release,
   destructive action, other-project access, or claim upgrade is inferred.

The decision is ineffective until its own exact commit passes both required
remote CI jobs.

## Exact Authorized Sequence

After this decision is remotely green, H-L1 may implement one additive
standard-library live wrapper around the byte-identical generated Stage H
verifier. H-L1 may use only generated payloads and an injected mock opener for
one registered generated qualification. It must implement all nine controls in
the static safety review, including proof-before-capability, a durable consumed
marker before opener construction, capability-safe paths, proxy-free verified
TLS, no-replace publication, continuous resource enforcement, aggregate-only
output, and exact EDF header-to-payload geometry.

The exact H-L1 implementation, generated qualification, and activation must
then be committed, pushed, and pass both required remote CI jobs. Only after
that second barrier may H-L2 run once. The one H-L2 invocation may:

1. require at least 10 GiB free disk without scanning another project;
2. durably consume its one-shot marker before constructing the opener;
3. issue exactly one direct, proxy-free HTTPS GET for the registered member;
4. stream, size-check, and SHA-256 verify exactly 14,805,604 body bytes;
5. parse only the fixed EDF header;
6. apply the frozen 27 EEG, three EOG, two EMG, annotation-count, unique-label,
   512 Hz, and header-geometry gates;
7. retain the complete private payload only if every H1 gate passes; and
8. publish one bounded aggregate H1 or H0 result and stop.

Any outcome after the H-L2 consumed marker is final. There is no retry, rerun,
resume, repair, redirect, substitution, fallback, or post-result amendment.
H1 permits only a future narrowing amendment for the observed EOG/EMG names;
it does not authorize acquisition of the other 119 files. H0 parks the lane.

## Exact Member And Limits

```text
dataset / revision:       NEMAR nm000250 / v1.0.4
member:                   sourcedata/sub-01/eeg/sub-01_task-R1acquisition_eeg.edf
body bytes / SHA-256:     14,805,604 / a678fe6d37e0496eb381dcac6b877b047d02dfffc659ae4cfc38226f4850e185
requests / redirects:     1 / 0
retries / reruns:         0 / 0
threads / workers / jobs: 1 / 1 / 0
wall time / peak RSS:     300 sec / 268,435,456 bytes
network / disk / output:  16 MiB / 32 MiB / 1 MiB
stream chunk maximum:     1 MiB
minimum free disk:        10 GiB
```

## Decision-Only Measurements

```text
fresh GitHub CI verification calls:             1
real requests / network bytes / EDF bytes:      0 / 0 / 0
real fixed-header / annotation / signal reads:  0 / 0 / 0
target or label reads:                          0
model loads / training / inference / scoring:   0 / 0 / 0 / 0
payloads retained / private paths opened:       0 / 0
provider / stream / device / hardware actions:  0 / 0 / 0 / 0
retries / reruns / releases / claim upgrades:   0 / 0 / 0 / 0
end-to-end latency measured:                     false
```

## Claim Boundary

**Engineering capability authorized for testing:** build and generated-qualify
one proof-bound live wrapper, then use it once to verify the exact public
object identity and fixed sensor-header assumptions before bulk acquisition.

**Scientific claim not established:** this decision is not an EEG result and
establishes no neural effect, EEG-beyond-peripheral advantage, unseen-person
generalization, spontaneous movement intention, motor-cortex causation,
thought or language decoding, live decoding, hardware result, or clinical
utility.
