# MARC2-VR32P Eligible-Total Direction Private Discriminator Authorization Decision

Date: 2026-08-23

Lane: `MARC2-VR32P`

Status: **Packet-bound short-form authorization recorded; ineffective until
this exact decision is committed, pushed, and both required CI jobs are green**

Machine decision:
`registries/marc2_eligible_total_direction_private_discriminator_authorization_decision.v0.json`

## Maintainer Decision

After Codex identified `MARC2-VR32P` as the sole active Tier C packet and
reported its exact request, proof closeout, CI evidence, aggregate-only scope,
and fresh-decision boundary, the maintainer's next message was exactly:

```text
continuecontinue
```

The machine decision preserves those actual 16 UTF-8 bytes and their SHA-256.
Under the approved Research Autonomy Charter short-form rule, this message
binds only the unchanged, remotely green VR32P packet by reference. It does
not fabricate a long-form user utterance or expand the scope.

## Green Packet Bound By This Decision

```text
request commit:       9dc13cb29804a7adfeaa45aa821e36e160a0f6ee
request CI:           32629224038
request Base job:     97169278061
request Optional job: 97169278037
proof commit:         41d7ef7f5ae7f2288ce3af870ae786652d0aade3
proof CI:             32630153639
proof Base job:       97171587597
proof Optional job:   97171587706
```

Both jobs passed for both commits. The request and proof artifact set contains
six unchanged files totaling 36,522 bytes. The unrelated untracked tracker
inspection NDJSON remains untouched.

## Delayed Effect

This decision does not become effective merely because these files exist.
Before generated implementation, readiness work, output-root work, or private
source access, this exact decision must pass local checks, be committed and
pushed, and pass both required remote CI jobs.

Recording the decision performs zero `.codex_work`, private-source, readiness,
output-root, archive, neural, target, model, score, network, device, release,
or scientific-claim operation.

## Authorized Sequence After Green Decision

### Stage 1: generated fixed-path wrapper

After this decision is remotely green, one additive standard-library wrapper
may be implemented and qualified using only generated fixtures, injected
readiness samples, mocked no-follow facts, and temporary output roots.

The wrapper must expose fixed `plan`, `qualify`, `inspect`, and proof-gated
`execute` surfaces. It may expose no generic path, output, URL, count,
threshold, difference, route, retry, resume, fallback, substitution, or
resource override. It must not import, call, patch, copy, modify, or inspect
any consumed private lane or its source, certificate, marker, output, or
executor.

Qualification must replay the eight exact VR31A cases in two orders and two
exact replays. Each of the 32 paths must call unchanged VR31A once. Exact
G1/G2/R1/R2/R3 counts 4/4/4/4/16 and at least 90 direct refusals must pass
with zero retention and every private or scientific counter at zero.

The exact implementation must then be committed, pushed, and pass both remote
jobs. A separate proof-only closeout must also be committed, pushed, and pass
both jobs before Stage 2.

### Stage 2: one target-free private direction check

Only after the exact Stage 1 implementation and proof closeout are remotely
green may one command:

1. validate every exact proof and the one-thread environment;
2. obtain three consecutive passing machine-readiness samples;
3. write one fresh mode-`0600` certificate at the fixed VR32P path;
4. require the fixed output root to be absent and reject symlinks or aliases;
5. no-follow preflight only the registered target-free source;
6. write one mode-`0600` consumed marker immediately before content open;
7. read, hash, and strict-parse exactly 418,755 bytes once;
8. call exact VR31A once without mutating the source;
9. retain only the mapped R1 or R2 aggregate answer; and
10. write one aggregate-safe mode-`0644` R1-R5 report with no private detail.

Every route consumes the one invocation. No retry, rerun, resume, repair,
fallback, substitution, cleanup, private reinspection, or amendment is
authorized.

## Fixed Route Contract

| Route | Maximum engineering meaning |
|---|---|
| `MARC2VR32P-R1` | filtered eligible total is below 195 |
| `MARC2VR32P-R2` | filtered eligible total is above 195 |
| `MARC2VR32P-R3` | readiness, fixed-path, or output precondition refused |
| `MARC2VR32P-R4` | source identity or strict structural envelope refused |
| `MARC2VR32P-R5` | unexpected upstream, privacy, deterministic-output, or resource refusal |

Only R1 and R2 answer the registered question. Neither may retain or publish
the observed total, difference, distribution, predicate, value, row, path,
identity, participant, selection, reservation, or cohort. R5 parks rather
than guesses.

## Fixed Paths And Resources

```text
readiness certificate: .codex_work/marc2_machine_readiness/vr32p/readiness.v0.json
output root:           .codex_work/marc2_eligible_total_direction_private_discriminator/v0
private source:        .codex_work/marc1_central_directory/live_audit_v0/member_inventory.private.v0.json
source bytes:          418,755 exactly once
source SHA-256:        2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031
CPU / workers / jobs:  1 / 1 / 1
generated runtime:     <= 60 seconds
private runtime:       <= 650 seconds
peak RSS:              < 256 MiB
minimum free disk:     >= 15 GiB
combined output:       <= 1 MiB
network/new payload:   0 / 0 bytes
archive/signal/target: 0 / 0 / 0 bytes
retry/rerun/resume:    0 / 0 / 0
```

## Explicitly Not Authorized

This decision does not authorize implementation before its own remote green
proof; Stage 2 before exact Stage 1 and closeout proof; consumed-state access;
another source, path, project, participant, session, run, or dataset; network
or downloads; archive members; EEG/MEG samples, events, channels, geometry,
quality, labels, targets, or outcomes; derivatives, caches, features, or
NeuroTokens; cohort freeze; training, inference, prediction, scoring, tuning,
or language models; FW2, CIL1, RW3, streams, devices, hardware, release,
publication, or any scientific or clinical claim upgrade.

## Next Gate

Commit, push, and green this exact decision. Then implement and generated-
qualify Stage 1. Only after that implementation and its proof-only closeout
are remotely green may the registered structural direction check run once.

Engineering capability authorized after green decision: one proven fixed-path
wrapper and one later proof-gated target-free structural read may distinguish
whether the eligible total is below or above 195 without exposing the count.

Scientific claim not established: this decision is not neural data or a
result and establishes no neural effect, decoding performance, language
decoding, unseen-person generalization, or live decoding.
