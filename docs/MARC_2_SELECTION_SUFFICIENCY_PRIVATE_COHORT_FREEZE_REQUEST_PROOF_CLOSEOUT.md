# MARC2-VR39P Terminal Cohort Request Proof Closeout

Date: 2026-08-23

Lane: `MARC2-VR39P`

Status: **Request proof closeout pending its own remote-green barrier**

Machine proof:

- `registries/marc2_selection_sufficiency_private_cohort_freeze_request_proof.v0.json`

## Exact Request Proof

Exact all-false request commit
`6c805a817fa44375b7b0e120abcb2c748c78ca07` passed:

```text
CI run:                 32675925646
Base Python job:        97283907786
Optional Neuro job:     97283907932
both required jobs:     green
```

The remotely green request contains exactly these request artifacts:

| Artifact | Bytes | SHA-256 | Git blob |
|---|---:|---|---|
| authorization packet | 9,406 | `9ec62f2fd89d1310c8d8d64e5bca4aafc6bfb314002432d3526a11ffbf83fa37` | `1f79c7223cc74d590458f2ee36336c0c8467467a` |
| machine request | 23,662 | `313a1fe1a3cfb9002b636f1e992ce3981804c274fb6d486e84e3f143f7eb9a46` | `110224933c4eb21f7e0757ae050ae770b3b59cdf` |
| request test | 12,674 | `0d4a8d3478dd08ac54bf3020245f7229085066ee65ebed96e8824c30aa6500d4` | `890eece4159b5eeec6846e8cc1840e104d9868b8` |

The artifact set totals 45,742 bytes. The two-stage sequence, 168-path
generated matrix, R1/R2 terminal map, 12-19 cohort range, HMAC commitment,
storage limits, fixed paths, strict public allowlist, decision protocol, and
claim boundary are unchanged.

## Metadata-Only Closeout

This closeout does not implement the wrapper or run generated qualification.
It does not collect readiness, traverse or inspect `.codex_work` descendants,
touch consumed state, open the private structural source, create an output root
or marker, freeze a cohort, generate a commitment key, access an archive member
or neural signal, read a target, run a model, create a prediction, score, call
a provider, connect a device, execute FW2/CIL1, publish, release, or upgrade a
scientific claim.

Every authority flag remains false. One repository-root `ls -la` occurred
during local environment discovery and exposed the `.codex_work` root entry's
metadata. No observed mode, owner, size, or timestamp is retained here. This
is recorded as one root-directory listing and one ignored-root-entry metadata
observation, not hidden inside a zero-operation claim.

No `.codex_work` descendant was traversed, listed, resolved, statted, hashed,
opened, read, parsed, written, renamed, or deleted. No private source or
consumed-state content was accessed. This metadata-only protocol deviation
does not authorize implementation or private access and does not consume
Stage 1 or Stage 2. The unrelated tracker-inspection NDJSON remains untracked
and untouched.

Measured closeout verification passed 23 focused request/proof tests and all
5,620 dependency-light tests with 204 expected skips. Pinned Ruff 0.15.20,
targeted formatting, targeted compilation, all 431 registry parses, exact
request-artifact immutability, and diff hygiene pass.

A secondary local optional-neuro run reached 5,690 tests with 35 skips and
reported the same three environmental/load-sensitive failures seen at the
prior milestone: one sandbox-blocked forkserver bind and two timing gates.
All three passed in isolated runs; only the forkserver check required an
outside-sandbox local-socket run. Remote Optional Neuro CI remains the
authoritative complete optional-environment gate. No requested research
operation was performed during verification.

## Delayed Effect

This closeout is ineffective until its own exact commit is pushed and both
required CI jobs are green. Only afterward may VR39P be identified as the sole
active Tier C packet. A fresh unambiguous maintainer message after that
identification may authorize the unchanged request by reference. The current
or any earlier `continue`, `approve`, or `lets go` is not retroactive
authority.

Even after a future decision, Stage 1 implementation must become remotely
green and receive its own remotely green proof-only closeout before the one
private invocation. The packet and decision alone cannot open private data.

Engineering capability requested: a proof-gated terminal structural attempt
can privately commit one target-free 12-19 participant cohort or permanently
park the Freewill/CIL1 lane.

Scientific claim not established: this proof closeout performs no real or
private read, neural access, model run, prediction, or score and establishes
no neural effect, decoding performance, advantage over no-signal or peripheral
controls, movement intention, language decoding, unseen-person generalization,
or live decoding.
