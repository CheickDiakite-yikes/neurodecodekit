# IACKD-2 Role-Aware Dual-Reversal Authorization Decision

Date: 2026-08-10

Status: **Authorized only after this decision is tested, committed, pushed,
and remotely green; no executor implementation, dependency operation, public
request, local IACKD operation, model run, target delivery, or score has
occurred**

Machine decision:
`registries/iackd_role_aware_dual_reversal_authorization_decision.v0.json`

Frozen request commit:
`862141f6729182f36accce38ce42a3631feb7232`

Frozen packet:
`docs/IACKD_ROLE_AWARE_DUAL_REVERSAL_AUTHORIZATION_PACKET.md`

## Actual Maintainer Decision

After Codex identified IACKD-2 as the sole active Tier C packet, named request
commit `862141f6729182f36accce38ce42a3631feb7232`, green CI
`31454131606`, both required green jobs, the exact streaming, fit, freeze, and
score scope, and the need for a fresh decision, the maintainer said:

> continue

This record preserves that word exactly. It does not claim that the maintainer
typed the packet's long scope. The short instruction incorporates the
immutable, remotely green packet by reference without widening it.

The procedural recital is waived for this decision. Every substantive object,
byte, run group, model, target-firewall, resource, no-retry, no-rerun, and
claim boundary remains unchanged.

## Why The Short Form Is Valid

All fail-closed conditions were satisfied before the instruction:

1. IACKD-2 was the sole active Tier C packet.
2. The packet and all-false request were committed and pushed at `862141f`.
3. Base Python job `93664349787` and Optional Neuro Readers job `93664349786`
   were green in CI `31454131606`.
4. Codex named the packet, commit, CI proof, exact scope, and decision gate.
5. The maintainer then unambiguously said `continue`.
6. This separate record quotes the actual word and binds every immutable scope
   artifact.
7. No release, hardware, destructive action, scientific upgrade, or other
   scope is inferred.

The decision is ineffective until its own commit passes both remote CI jobs.

## Bound Evidence

```text
authorization parent: 862141f6729182f36accce38ce42a3631feb7232
request push CI:       31454131606
Base Python job:       93664349787
Optional Neuro job:   93664349786
contract SHA-256:     f3b38cb2c5bf0a55e0816072ef654cc87bd2e2f36bab50df19947d66d2abdb7f
request SHA-256:      4677076260bfecc0889843513b28e3d03bc55e5933376a96d7694d02dd7ce1e2
packet SHA-256:       8a0a29dfa6e69b3b2c4a9e939c9e4ab706175c9731908f154661d8c5d255d8e8
user-message SHA-256: e256ee8e7aff6957a781d8328f0f68e26996564c81fa458da59fbca2305138ad
```

The preregistration, contract, generated implementation, generated closeout,
packet, request, inventory, and invariant tests remain immutable snapshots.
Their pending fields are not rewritten after approval. This additive record
supplies the packet-bound permission.

## Ordered Authorization

### Gate 1: this decision must become remotely green

This exact decision must be tested, committed, pushed, and pass Base Python
and Optional Neuro Readers CI. Until then, implementation and access remain
closed.

### Gate 2: generated qualification of a separate real executor

After Gate 1, Codex may implement and qualify the registered dry-run-first
executor, mocked transport, one-run-at-a-time BrainVision/event/ball/Leap
readers, source-role reconciliation, causal preprocessing, target firewall,
motion guard, fixed low-frequency model and controls, prediction freezer,
isolated scorer, resource guards, receipts, and module CLI using generated
fixtures only.

The existing Git-ignored numerical environment may be reused only if it
reports NumPy 2.5.2, SciPy 1.18.0, MNE 1.12.1, and scikit-learn 1.9.0. No
dependency installation, version substitution, tooling network, public URL
request, or local IACKD path operation is authorized. The exact executor
implementation must be committed, pushed, and remotely green before any
public-content operation.

### Gate 3: one fresh stream and derivative build

After Gate 2 is green, one no-retry invocation may reverify the four frozen
OpenNeuro metadata surfaces and stream exactly 1,340 allowlisted objects
totaling 7,249,113,684 bytes into a new isolated invocation root.

Only one of 128 ten-object raw run groups may exist at a time. Each group must
be verified and parsed once, promoted into bounded private derivatives, and
then only that invocation-created temporary raw group may be removed. The old
retained IACKD bundle and every preexisting path are forbidden. Final target
views must remain structurally sealed from predictive code.

### Gate 4: one target-blind fit, prediction, and freeze

After the derivative build passes, one no-network execution may complete
exactly 660 registered parameter-update fits and 900 target-blind prediction
sets. It must emit one aggregate hash-only freeze containing no individual
prediction, probability, target, trajectory, coefficient, or participant
outcome, then stop with both final target views sealed.

The freeze must be committed, pushed, and green in both CI jobs before either
final target view opens.

### Gate 5: one combined score

After the freeze is remotely green, the isolated scorer may receive the
actual-action and exact-opposite cue-surrogate views together once, verify all
frozen identities and hashes, score both reversal arms, apply the frozen
`IACKD2-R0` through `IACKD2-R5` router, emit aggregate output, and stop.

No refit, normalization update, threshold change, exclusion, second target
delivery, second score, retry, rerun, or post-target update is open.

## Exact Limits

```text
dataset / version:                 OpenNeuro ds006840 / 1.0.0
participants / hand units:         15 / 30
run groups / objects / bytes:      128 / 1,340 / 7,249,113,684
raw groups resident concurrently:  1
largest raw run group:             82,064,564 bytes
model family:                      fixed causal 0.5-4 Hz shrinkage LDA
fits / prediction sets:            exactly 660 / exactly 900
acquisition wall / RSS:            7,200 sec / 2,147,483,648 bytes
analysis wall through freeze:      10,800 sec
peak incremental disk:             1,073,741,824 bytes
minimum free disk:                 10,737,418,240 bytes
private derivatives / output:      536,870,912 / 536,870,912 bytes
public receipt / freeze / result:  at most 4,194,304 bytes each
threads / workers / jobs:          1 / 1 / 1
final deliveries / scores:         1 / 1
retries / reruns / updates:        0 / 0 / 0
```

## Decision-Only Measurements

```text
GitHub CI verification calls:                    1
OpenNeuro metadata / payload requests:           0 / 0
payload / network bytes:                         0 / 0
old-bundle / local IACKD operations:             0 / 0
EEG / EOG / marker / trajectory / target reads:  0 / 0 / 0 / 0 / 0
dependency installs / executor operations:       0 / 0
derivatives / fits / predictions / freezes:      0 / 0 / 0 / 0
final target deliveries / scores:                0 / 0
provider / stream / device / hardware operations: 0 / 0 / 0 / 0
generated experiment artifacts:                  0
end-to-end latency measured:                      false
```

## Claim Boundary

**Engineering capability authorized for testing:** one exact, storage-safe,
role-aware, target-firewalled public IACKD dual reversal may proceed through
the ordered remote-green gates.

**Scientific claim not established:** this decision is not EEG data or a
result. Even future `IACKD2-R5` would not establish absolute brain-specific
origin, independent replication, unseen-person generalization, typing,
language or thought decoding, end-to-end real-time operation, portable
hardware, home use, assistive benefit, or clinical utility.
