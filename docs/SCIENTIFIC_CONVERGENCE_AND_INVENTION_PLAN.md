# Scientific Convergence And Invention Plan

Date: 2026-08-29

Status: active strategic plan; Tier A analysis only; no new data authority

Canonical directive:
[`SCIENTIFIC_DISCOVERY_AND_INVENTION_CONSTITUTION.md`](SCIENTIFIC_DISCOVERY_AND_INVENTION_CONSTITUTION.md)

Machine knowledge source:
[`scientific_knowledge_ledger.v0.json`](../registries/scientific_knowledge_ledger.v0.json)

Current machine frontier:
[`current_research_frontier.v7.json`](../registries/current_research_frontier.v7.json)

## Current Addendum: Transport Admission Before Source Promotion

The exact Ofner result proof passed Base Python `99174411928`, Optional Neuro
Readers `99174412006`, and CI `33280371097` on GitHub `main`. The Ofner and
Dreyer lanes are both consumed and parked before any neural payload or sensor
header was accepted. Neither result is a biological null.

Artifact-only comparison with the successful BNCI signed-object recovery and
the IACKD framing analysis identifies the shared process weakness: live
transport admissibility remained unknown until an irreversible scientific
header checkpoint. The aggregate artifacts do not establish one common NEMAR,
redirect, range, Python, or server cause.

`NPA1-v0` is now the reversible bridge. It orders scientific source identity,
transport capability, HTTP framing, bounded content identity, and semantic
sensor eligibility. Generated `NPA1-G` is next. A fresh source will be promoted
only after it meets the nuisance-control, participant, geometry, license,
20 GiB, and transport-admission rules. Any later real 256-byte opaque canary is
a separately governed Tier C operation and has no scientific value by itself.
No fresh source, network request, payload, model, score, or claim is activated
by this plan update.

## Current Addendum: Ofner Source Reselection

The Dreyer route described below is retained as the rationale and frozen
failure history, but it is no longer the active prospective source. Its sole
H-L2 attempt consumed at transport H0 before any EDF body or header read.

The next prospective flagship is `OFNER-C6R-1`. Metadata-only research selected
the original NEMAR `nm000173` `v1.0.3` motor-imagery GDF surface: 15 people, 150
files, 13,748,417,608 bytes, and reported 61 EEG, three EOG, 19 glove, and 13 arm
channels at 512 Hz. The smaller BIDS derivative is rejected because its public
channel sidecar exposes only EEG and therefore cannot support the nuisance-
controlled question. The new claim asks whether central EEG adds held-out
seven-class imagined-movement information beyond EOG, recorded kinematics,
posterior EEG, cue/timing, and matched derangement in unseen people.

No real Ofner payload or header has been accepted. The generated-only
selector/acquisition milestone passes two complete
150-member fixture replays and 20 fail-closed cases while separating stable
scientific identity from expiring signed transport capabilities; its exact
implementation and proof are remotely green. The follow-on generated GDF 2.x
header parser and two-range firewall pass two 24,832-byte replays and 41
refusals, recover the frozen 96-role synthetic roster at 512 Hz, and reject any
trailing data bytes. It remains generated engineering, not EEG evidence.

The exact decision, live wrapper, and activation later became remotely green.
The sole `OFNER-C6R-1-HL-R0` invocation consumed at
`OFNER-H0-TRANSPORT`: manifest identity selection completed, then the first
256-byte GDF range yielded zero accepted GDF body bytes. No real GDF header,
sensor roster, geometry, event, annotation, signal, target, model, prediction,
or score was observed. This is a transport refusal, not a biological null, and
the attempt cannot be retried, rerun, repaired, resumed, substituted, or
reinterpreted. No Tier C packet is active. The ordered next steps are a Tier A
artifact-only transport postmortem, fresh transport-verified source selection,
and only then a separately named Tier C proposal. Because no EMG was recorded,
even a future Ofner pass could not claim independence from all muscle activity.

## Decision In One Page

NeuroDecodeKit will optimize for belief-changing evidence rather than proof
machinery. The flagship question remains whether central EEG adds predictive
information beyond recorded peripheral physiology, posterior EEG, cue and
timing structure, and matched derangement in completely unseen people. No
dataset is currently promoted for that confirmation.

The earliest checkpoint is generated `NPA1-G`, followed by fresh public source
research. A future source must pass scientific eligibility and one separately
governed opaque live transport canary before a semantic sensor-header check.
Only after a green header may the scientific path proceed through bounded
acquisition, target-blind semantics, prediction freeze, and one score.

The generated COMM-P0 FS3 lane is paused after its green wrapper and proof.
It remains available as an adjacent diagnostic asset, but another generated
activation or rehearsal does not outrank transport admission and fresh source
selection for the flagship question.

## A. Current Scientific Knowledge

### Established

- The consumed BNCI-C3C5-1 experiment completed a real, target-firewalled,
  held-out-person score over nine participants.
- Selected EEG recovered protocol-condition information: participant-macro
  balanced accuracy was `0.38349`, versus `0.25000` no-signal and `0.29668`
  timing-only.
- Recorded EOG was strongly predictive (`0.60031` balanced accuracy), so eye
  information is a mandatory comparator rather than a minor artifact check.
- The pipeline can execute semantic parsing, participant-held-out fitting,
  target-blind prediction freezing, and one aggregate score without exposing
  held-out targets to model selection.

### Refuted On The Tested Surface

- The BNCI selected-EEG representation was not spatially specific: posterior
  EEG reached `0.39236`, slightly above selected EEG.
- The tested S21 cross-session tiny CTC model did not beat its no-signal prior;
  it was worse by `0.142491` corpus CER.
- The tested S7 nearest-centroid EEG classifier did not beat its train-only
  prior; it was worse by `0.113636` exact-label accuracy.
- Larger architecture alone is not currently supported as the leading remedy.

### Inconclusive Or Confounded

- Adding selected EEG to EOG improved BNCI log loss by `0.02552`, and beat the
  EOG plus deranged-EEG condition by `0.01843`. Both were directionally useful
  but below the frozen `0.03` effects, positive in only 6/9 people, and not
  significant. Incremental EEG information is therefore inconclusive.
- The above-chance BNCI EEG score is confounded by posterior/visual and task
  structure information and by poor probability calibration.
- The five-sentence S21 session-1 CTC result was a near-null estimate with
  uncertainty crossing zero, not a positive language-decoding result.

### Unknown

- Whether the Dreyer source EDF preserves the nuisance sensors reported by the
  primary paper.
- Whether nuisance-residualized central EEG adds at least `0.020` nats/trial
  over nuisance-only and matched deranged-residual controls in 60 unseen people.
- Whether any observed increment is spatially and temporally specific enough
  to support a central-EEG attribution rather than only protocol association.
- Whether communication-related EEG adds information beyond synchronized eye
  and oral-muscle activity.
- Whether any useful decoder generalizes to an unseen dataset, runs
  continuously, self-endpoints, or operates live.

### Blocker Classification

| Claim | Current blocker |
|---|---|
| Dreyer nuisance-controlled unseen-person effect | `BLOCKED BY DATA`: source header and payload remain unopened |
| Cross-person alignment mechanism | `BLOCKED BY METHOD` and development data |
| Communication beyond peripheral physiology | `BLOCKED BY DATA` and measurement design |
| Causal continuous/live decoding | `BLOCKED BY MEASUREMENT` and system integration |
| Open-vocabulary thought/language decoding | `BLOCKED BY DATA`, task identifiability, and method |

No physical or information-theoretic impossibility conclusion is supported.

## B. Current Engineering Assets

### Keep And Reuse

- Dataset adapters and bounded streaming readers.
- Explicit participant/session split identities.
- Source-only nuisance residualization and calibration primitives.
- First-class no-signal, timing, EOG, EMG, posterior, derangement, cue, and
  pre-cue conditions.
- Blind target delivery, prediction freeze, aggregate scorer, and consumed
  evaluation protections.
- Resource monitors, optional heavy dependencies, and selective-download caps.
- Existing Dreyer generated parser/firewall/model/scorer qualifications.

### Safeguards That Protect Real Failure Modes

- Held-out targets remain technically unavailable until predictions freeze.
- Consumed BNCI, S21 session 2, and other final evaluations cannot become
  development data.
- One-shot download and score boundaries prevent outcome-driven retries.
- Storage, RSS, and thread limits protect the maintainer's computer.
- License, privacy, and no-clinical-claim boundaries remain mandatory.

### Procedural Debt

The current checkout contains 628 documentation files, 575 registries, 747
test modules, and 22 sidecar CLI modules. Several public status documents have
already drifted. This history is not deleted in this milestone, but it stops
driving the research plan.

Stop adding a protocol name, proof closeout, CLI, and duplicated status update
for every reversible step. Wrap the existing Dreyer source, split, condition,
estimator, and scorer implementations. Generate future summaries from the
scientific ledger and standardized evidence bundles.

## C. Historical Dreyer Scientific Question

This section preserves the exact scientific design that motivated the current
fresh-source criteria. Its Dreyer lane is consumed and is not the active
execution plan.

In completely unseen participants performing visually cued left-versus-right
hand motor imagery, do predeclared central EEG features add held-out predictive
information beyond recorded EOG, wrist EMG, posterior EEG, trial timing, and a
matched deranged-EEG control?

This matters because it distinguishes a genuine incremental brain-sensor
contribution from the cheaper explanations that defeated the prior BNCI claim.
The [Dreyer primary paper](https://doi.org/10.1038/s41597-023-02445-z)
reports 60 Dataset A participants, 27 EEG channels, three EOG electrodes, two
wrist-EMG electrodes, and 512 Hz sampling. The exact selected R1/R2 surface has
4,800 trials and a manageable 1.6575 GiB payload.

The strongest competing explanations are lateralized visual cue/feedback,
eye movement, covert or overt wrist muscle activity, trial timing, posterior
visual activity, cross-person sensor misalignment, and probability
miscalibration.

## D. Historical Dreyer Discovery And Confirmation Boundary

### Discovery

- Generated Stage G/H fixtures and mock transport.
- Public aggregate BNCI result and postmortem only.
- Existing non-consumed, explicitly development-only surfaces when a governing
  contract permits them.
- Method invention, error hypotheses, literature collision checks, and cheap
  synthetic sensitivity tests.

Discovery may change representations, objectives, and models, but every tried
variant must enter compact experiment history. Discovery results cannot repair
or reinterpret the frozen Dreyer R1/R2 confirmation.

### Confirmation

- Exact cohort: Dreyer Dataset A `sub-01` through `sub-60`.
- Exact runs: R1 and R2 only; R3-R6 stay untouched.
- Exact outer split: 60 leave-one-participant-out folds with zero held-out
  calibration or adaptation.
- Exact target, conditions, model, source-only calibration, primary effects,
  tests, thresholds, exclusions, and route map in the existing preregistration.

Already observed: paper/manifest metadata and generated-only behavior.

Still sealed: every real Dreyer EDF header, annotation, signal sample, target,
prediction, participant result, and score.

Stage H may only verify the source measurement contract and, after H1, narrow
the observed EOG/EMG names without changing their count or scientific role.

## E. Historical Dreyer Decisive Experiment

### Hypotheses

- `H0`: nuisance-residualized central EEG contributes no reliable held-out
  information beyond nuisance signals and matched derangement.
- `H1`: central EEG contributes at least `0.020` nats/trial against both
  nuisance-only and nuisance-plus-deranged-residual comparators, with
  participant consistency and calibrated above-baseline performance.
- `A_visual`: posterior/cue information explains the apparent effect.
- `A_peripheral`: EOG or wrist EMG explains the apparent effect.
- `A_timing`: run/trial structure explains the apparent effect.
- `A_alignment`: within-person information exists but fixed cross-person
  representation destroys it.
- `A_calibration`: discrimination exists but probability reliability fails.

### Frozen Test

- Representation: causal one-second 8-12, 12-20, and 20-30 Hz relative-power
  features; nine central homologous pairs; source-only nuisance residuals.
- Estimator: one source-standardized L2 logistic family with source-only
  temperature calibration.
- Generalization: zero-calibration held-out participant.
- Primary endpoint: participant-macro binary log loss.
- Co-primary effects: `LL(N)-LL(N+R)` and
  `LL(N+D(R))-LL(N+R)`, each at least `0.020`.
- Consistency: each effect positive in at least 39/60 participants with exact
  one-sided sign-test `p <= 0.025`.
- Mandatory checks: posterior removal, pre-cue and cue windows, equal prior,
  timing, EOG, EMG, posterior EEG, label rotation, balanced accuracy, log loss,
  calibration error, completeness, and zero post-target updates.

### Outcome Map

| Pattern | Belief update |
|---|---|
| `N+R` beats `N` and `N+D(R)` with consistency, while cue/pre controls fail | Supports incremental late central-EEG information in unseen participants |
| `N+R` beats `N` but not `N+D(R)` | Association exists, but matched EEG identity is not established |
| Posterior or cue conditions match the primary | Visual/task explanation remains viable; no central specificity |
| EOG/EMG dominate and residual EEG adds nothing | Peripheral protocol information explains performance |
| Accuracy improves but log loss/calibration fail | Discrimination without reliable probabilities; claim gate fails |
| Source/within-person checks work but LOSO fails | Prioritize alignment or calibration-law discovery |
| Positive controls fail | Pipeline or measurement sensitivity failure; do not infer signal absence |
| All primary comparisons are narrow around zero with positive controls intact | Evidence against a practically meaningful effect under this measurement/model class |

### Claim Coordinates At Maximum Pass

- Generalization: unseen participant, same dataset and task.
- Attribution: predictive increment beyond recorded nuisance sensors and
  spatial/temporal controls; not exclusive cortical causation.
- Temporal regime: sample-causal event-locked offline decision at eight seconds.
- Output: binary instructed protocol condition.
- Calibration: zero held-out-person calibration.
- Setting: curated laboratory.

## F. Invention Opportunities

These are discovery candidates, not changes to the frozen Dreyer confirmation.
No novelty claim is made before a dedicated literature and implementation
collision check.

### Candidate 1: Geometry-Normalized Residual Dynamics

- **Observed failure:** BNCI posterior EEG beat the selected representation,
  and participant consistency was weak.
- **Mechanism:** fixed sensor pairs may not align homologous sources across
  anatomy and cap placement.
- **Intervention:** align source-participant covariance or geometry before
  computing nuisance-orthogonal lateralized dynamics.
- **Expected signature:** cross-person gain rises while posterior, timing, and
  deranged controls do not.
- **Falsifier:** alignment raises every condition equally or only helps
  posterior/control channels.
- **Simplest comparator:** fixed homologous-pair residual features.
- **Closest analogues:** He and Wu's
  [Euclidean Alignment](https://doi.org/10.1109/TBME.2019.2913914) and
  [tangent-space alignment](https://doi.org/10.3389/fnhum.2022.1049985).
- **True novelty:** currently unverified; any value would need to come from
  combining geometry with explicit nuisance-orthogonal attribution, not from
  alignment alone.
- **Minimum test:** source-only grouped cross-validation on an authorized
  development cohort; one fixed aligned arm versus one unaligned arm.
- **Failure value:** distinguishes misalignment from absent incremental signal.

### Candidate 2: Reliability-Gated Neural Increment

- **Observed failure:** selected EEG ranked classes above chance but had log
  loss worse than the equal prior.
- **Mechanism:** cross-person residual EEG is heteroscedastic; a fixed fusion
  overweights unreliable participant/epoch evidence.
- **Intervention:** estimate source-only residual reliability and shrink only
  the neural logit increment toward zero when uncertainty is high.
- **Expected signature:** log loss and calibration improve without increasing
  deranged-EEG or posterior-control gains.
- **Falsifier:** gains vanish under matched derangement or reduce only accuracy.
- **Simplest comparator:** existing source-only scalar temperature.
- **Closest analogue:** probability calibration and mixture-of-experts
  reliability weighting; no new scientific principle is claimed.
- **True novelty:** unverified integration at the neural-increment level.
- **Minimum test:** generated heteroscedastic fixture followed by one
  development-only grouped comparison.
- **Failure value:** tells us whether calibration, rather than representation,
  explains the prior gate failure.

### Candidate 3: Calibration Law Instead Of Zero-Shot Dogma

- **Observed failure:** aggregate signal can coexist with weak unseen-person
  consistency.
- **Mechanism:** a small participant-specific alignment sample may remove a
  large domain shift even when zero-shot decoding is brittle.
- **Intervention:** measure fixed 0, 1, 5, 10, and 20 labeled-trial calibration
  curves, with all global choices learned from source participants.
- **Expected signature:** a steep early gain with stable nuisance attribution
  identifies a favorable practical calibration law.
- **Falsifier:** calibration gains are no larger than nuisance-only gains or
  require impractical trial counts.
- **Simplest comparator:** zero-shot model and participant-only linear baseline.
- **Closest analogue:** weighted adaptation and broad BCI transfer-learning
  work; calibration reduction is established prior art.
- **True novelty:** none claimed. The scientific contribution would be an
  honest attribution-aware scaling law on the selected task.
- **Minimum test:** one authorized development cohort with nested participant
  splits; never the frozen Dreyer R1/R2 confirmation.
- **Failure value:** separates an invariance limit from a general signal limit.

### Unconventional Measurement Option

If cue/posterior confounding persists, change the task rather than the model:
orthogonalize visual cue side from instructed hand, remove lateralized visual
feedback, require central fixation, and record synchronized EOG and bilateral
EMG. This challenges the premise that legacy cued datasets can identify the
desired mechanism. It requires a future consented acquisition design and human
approval; it is not an immediate operation.

## G. Architecture Convergence

Minimum flagship path:

```text
SOURCE -> SPLIT -> TRANSFORM -> CONDITION -> ESTIMATOR
       -> EVALUATION -> EVIDENCE -> KNOWLEDGE
```

| Layer | Thin migration now |
|---|---|
| Source | Wrap the existing Dreyer manifest/reader; preserve provenance and license |
| Split | Reuse the exact 60-fold participant split and target-access rules |
| Transform | Adapt existing spectral and nuisance-residualization functions behind a small callable graph later |
| Condition | Reuse the 17 frozen conditions as data, not bespoke orchestration branches |
| Estimator | Keep the compact logistic/calibration interface |
| Evaluation | Reuse participant-first metrics and exact tests |
| Evidence | Introduce one standardized evidence-bundle contract only when the real vertical path reaches a result |
| Knowledge | Add the compact scientific ledger in this milestone |

Do not rewrite dataset readers, model workers, target vaults, or scorers before
the header checkpoint. Do not migrate historical lanes. An interface becomes
permanent only after serving Dreyer and at least one second concrete use.

## H. Research Portfolio

### Flagship: 70%

Dreyer nuisance-controlled unseen-person test. First falsifier: fixed-header
sensor contract. Reassess immediately on H0 or H1, and after any failure in
positive-control sensitivity.

### Adjacent Diagnostic: 20%

Source-only alignment, calibration, and sensitivity tools that directly
explain a Dreyer failure. FS3 remains paused unless a real communication cohort
is imminent and the rehearsal is the last concrete blocker.

### Moonshot: 10%

An orthogonalized communication or motor task with synchronized EEG, EOG,
bilateral oral/wrist EMG, gaze constraint, and causal self-endpointing. First
falsifier: a generated task/measurement identifiability simulation plus a
power and storage estimate. Expire or redesign if it cannot separate target
intent from cue and peripheral physiology before participant recruitment.

The generated checkpoint now establishes a narrower design conclusion. Cue
content makes the 64 prompted trials non-identifying for intention: nuisance
features recover the target perfectly and target identity adds zero design
degrees of freedom. The 64 free-choice trials preserve all three four-command
target degrees of freedom beyond the generated joint cue, eye, oral-EMG, and
timing schedule, whose nuisance-only ceiling is `0.25`. This validates free
choice as the non-rescuable primary endpoint, but says nothing about whether
real EEG contains command information or whether recorded peripheral channels
capture every contaminating source.

The exact participant-consistency sensitivity also makes the planning
assumption visible. With 21 complete people per cohort and a 15-of-21
one-sided gate, two-cohort joint power is `0.3031` when the true positive-person
probability is `0.70`, versus `0.7948` at `0.80` and `0.8108` at `0.804`. The
existing 21-person design therefore depends on the stronger approximately
`0.80` planning assumption; it is not generically high-powered at the bare
`0.70` acceptance boundary. No frozen protocol is changed by this audit.

## I. Stop-Doing List

- Stop treating green CI, hashes, or generated replays as scientific progress.
- Stop creating a new protocol name and sidecar CLI for every reversible step.
- Stop duplicating current status across README, START_HERE, AGENTS, handoff,
  trackers, and multiple result documents.
- Stop extending FS3 until it is the final blocker to a real communication
  experiment.
- Stop adding datasets or larger models before a discriminating failure test.
- Stop testing incidental serialization and naming that do not protect a
  scientific invariant.
- Stop reporting only aggregate accuracy without calibration, participant
  consistency, nuisance comparisons, and claim coordinates.
- Stop writing long procedural closeouts when a ledger update and evidence
  bundle capture the material facts.

Historical artifacts remain intact. Simplification will be additive and
gradual until a safe archive map exists.

## J. First Empirical Checkpoint

**Checkpoint:** one exact Dreyer `sub-01` R1 EDF fixed-header observation.

**Question:** does the source file expose exactly the required 27 EEG channels,
three unambiguous EOG channels, two unambiguous wrist-EMG channels, and 512 Hz
sampling with valid EDF geometry?

**Earliest outcomes:**

- `H1`: measurement surface verified; narrow EOG/EMG names and proceed toward
  the 119-file acquisition under a future gate.
- `H0`: measurement surface absent or ambiguous; park Dreyer before spending
  the cohort budget and promote the next measurement/data solution.

**Current gate:** `DREYER-C5R-1-HL` is exact-green and all-false. The next
fresh maintainer `approve`, `continue`, or `proceed`, after this packet and its
boundary are explicitly identified, may bind only its unchanged H-L1 then
H-L2 sequence. Until that decision becomes remotely green, no wrapper
implementation, request, EDF byte, or header read is authorized.

## Cycle Update Format

Future research updates should contain only:

1. scientific question;
2. evidence produced;
3. belief changed;
4. uncertainty remaining;
5. next decisive experiment; and
6. infrastructure created and why it was necessary.
