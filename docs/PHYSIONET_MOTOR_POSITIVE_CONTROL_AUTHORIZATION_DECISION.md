# PhysioNet Motor Positive-Control Authorization Decision

Date: 2026-08-09

Status: **Authorized only after this record is tested, committed, pushed, and
remotely green; no dependency installation, implementation, local PhysioNet
operation, EDF parse, model run, prediction freeze, target delivery, or score
has occurred yet**

Machine decision:
`registries/physionet_motor_positive_control_authorization_decision.v0.json`

Frozen request:
`registries/physionet_motor_positive_control_authorization_request.v0.json`

Frozen contract:
`registries/physionet_motor_positive_control_contract.v0.json`

## Exact User Decision

The maintainer supplied the registered sentence verbatim:

> Authorize the work order 9 PhysioNet motor positive-control implementation and one registered execution exactly as scoped in docs/PHYSIONET_MOTOR_POSITIVE_CONTROL_PREREGISTRATION.md and registries/physionet_motor_positive_control_contract.v0.json. I bind registration commit 3c00557ecfb09c80e30843589ae295a09feec97c and green CI run 31346882592, including Base Python job 93330354031 and Optional Neuro Readers job 93330354047. I authorize, only after a separate authorization-only decision is committed, pushed, and remotely green, one generated-fixture-only implementation of the exact sequential EDF reader, target firewall, causal preprocessing, fixed classical families, controls, prediction freezer, isolated scorer, resource monitors, receipts, and CLI; addition of one narrow optional classical dependency extra without changing the dependency-free base install; and one isolated Git-ignored environment installation containing only the registered NumPy, SciPy, MNE, scikit-learn, and pyRiemann families under one installer worker, 900 seconds, 256 MiB network transfer, and 512 MiB incremental disk. I authorize, only after that exact implementation is committed, pushed, and remotely green, one no-network execution over only the nine acquired PhysioNet EEGMMIDB v1.0.0 S001-S003 runs 03/07/11 EDF files totaling exactly 23,248,224 bytes: one no-follow private-manifest verification, one sequential size and SHA-256 pass and one semantic MNE parse per EDF; reading their exact headers, all 64 EEG channels, available geometry, 160 Hz signal samples, and only T0, T1, and T2 EDF+ annotations; target-firewalled creation of the registered runs-03/07 fit derivative, target-blind run-11 prediction derivative, and sealed 45-target scorer input; up to 40 classical parameter-update fits and 64 target-blind prediction sets under the frozen family-selection, causal preprocessing, physiology, and confound contracts; and creation of one aggregate hash-only prediction-freeze ledger containing no individual prediction, probability, target, or participant outcome. I authorize, only after that freeze ledger is committed, pushed, and remotely green, one delivery and one scoring of the same 45 sealed run-11 targets and application of the frozen WO9-V0 through WO9-V3 router. The real execution is limited to one CPU thread, one worker, one numerical job, 1,800 seconds, 805,306,368-byte peak RSS, 67,108,864 private generated bytes, at least 2 GiB free disk, zero network bytes, zero new payload bytes, cleanup only of invocation-created temporary files, and zero retry or rerun. I do not authorize any .event sidecar; additional file, participant, run, dataset, download, or substitution; S20, S21, S24, S25, SpanishBCBL, raw FIF, or MAT access; row-random split, run-11 target use before the remotely green freeze, target-derived exclusion, evaluation-time fit, test-time adaptation, post-target update, hyperparameter search, larger or additional model, deep network, CML-v0, pretrained checkpoint, foundation or language model, provider, RW3, stream, device, hardware, individual protected output publication, release, replication cohort, or scientific claim beyond the registered WO9-V3 three-person motor-task EEG pilot ceiling.

This is one exact Tier C decision for the frozen work-order-9 protocol. It does
not authorize a second execution, retry, additional participant, additional
run, `.event` sidecar, model-family expansion, post-result tuning, or
replication cohort.

## Bound Evidence

```text
authorization parent: c62b10a6e9dae8d92e5ff54d17403e1054a0ac76
registration commit:  3c00557ecfb09c80e30843589ae295a09feec97c
contract SHA-256:     4f00f8e2cb257e912a947b49268c1476554f3e671eb9322926592df4908b144e
request SHA-256:      63e8bec4f5b94dfe8e0f0a079184afa6bb4ac62c1fb97ae1c816255ba3f88484
packet SHA-256:       92e932e9c9939ca592edeb7929e95f5cede508b3fe1e49f3ae7c2c00c8855677
parent push CI:       31347209691
Base Python job:      93331241434
Optional Neuro job:  93331241411
```

The preregistration, contract, packet, request, and their tests remain
immutable snapshots. Their pending and false authorization fields are not
edited; this later decision records the exact permission.

## Ordered Authorization

### Gate 1: this decision must become remotely green

Before implementation or installation, this exact decision must be tested,
committed, pushed, and pass both Base Python and Optional Neuro Readers jobs.

### Gate 2: generated-fixture-only implementation

After Gate 1, Codex may implement and qualify the sequential reader, target
firewall, causal views, exact classical families, controls, freezer, scorer,
resource monitors, receipts, and CLI using only generated fixtures. One narrow
Git-ignored optional environment may be installed under its separate 900-second,
256 MiB network, and 512 MiB incremental-disk caps. The base install must stay
dependency-free. The implementation may not stat, open, hash, or parse the
registered local PhysioNet bundle.

The exact implementation must then be committed, pushed, and pass both
required CI jobs.

### Gate 3: one target-firewalled real execution through prediction freeze

Only after Gate 2 is remotely green may the executor verify the private
manifest and nine exact regular non-symlink EDFs, make one sequential size and
SHA-256 pass and one semantic MNE parse per EDF, and read only the registered
headers, 64 EEG channels, available geometry, 160 Hz samples, and `T0`/`T1`/`T2`
annotations. Runs 03/07 targets may enter the fit derivative. Run-11 signal may
enter the target-blind prediction derivative. Run-11 targets must remain sealed.

All 12 registered target-blind final prediction sets must complete before one
aggregate hash-only freeze ledger is emitted. That ledger may bind hashes,
counts, selected-family identity, dependency versions, and provenance, but no
individual prediction, probability, target, or participant outcome.

The freeze ledger must be committed, pushed, and pass both required CI jobs.

### Gate 4: one target delivery and one score

Only after Gate 3 is remotely green may the same 45 sealed run-11 targets be
delivered once to the isolated scorer and scored once. The frozen `WO9-V0`
through `WO9-V3` router must be applied without update, retry, substitution, or
rerun. Individual protected outputs remain private and uncommitted.

## Exact Inventory And Limits

```text
provider / dataset / version:   PhysioNet / eegmmidb / 1.0.0
subjects / runs:                S001-S003 / 03, 07, 11
named EDF files / bytes:        9 / 23,248,224
expected task events:           135
fit-selection / sealed final:   90 / 45
candidate families:             CSP-LDA / Riemannian MDM
nonselecting comparator:        low-frequency shrinkage LDA
maximum fits / predictions:     40 / 64
real executions / retries:      1 / 0
CPU threads / workers / jobs:   1 / 1 / 1
real wall time / peak RSS:      1,800 sec / 805,306,368 bytes
private generated output:       <= 67,108,864 bytes
minimum free disk:              >= 2 GiB
network / new payload:          0 / 0 bytes
final target deliveries/scores: 1 / 1
reruns:                         0
```

## Authorization-Only Measurements

```text
GitHub CI verification calls:                           1
dependency installs / implementation operations:       0 / 0
local PhysioNet stats / opens / hashes / parses:        0 / 0 / 0 / 0
header / annotation / signal / target reads:            0 / 0 / 0 / 0
derivative / split / fit / inference operations:        0 / 0 / 0 / 0
prediction freezes / target deliveries / scores:        0 / 0 / 0
network / provider / stream / device / hardware ops:    0 / 0 / 0 / 0 / 0
generated experiment artifacts:                        0
end-to-end latency measured:                            false
```

The one GitHub API read verified the green request commit. It was not a
dataset, provider-model, or EEG-payload request.

## Claim Boundary

**Engineering capability authorized for testing:** one leakage-resistant,
resource-bounded public EEG motor positive control may proceed through the
four ordered remote-green gates.

**Scientific claim not established:** this decision contains no EDF
observation, physiology, prediction, or score. Even a future `WO9-V3` can
establish only a three-person held-out-run motor-task EEG pilot with
motor-compatible physiology under proxy controls; it cannot establish
brain-specific origin, unseen-person generalization, typing, language or
thought decoding, real-time performance, portable hardware, home use,
assistive benefit, or clinical utility.
