# MARC2-VR15P Tracked-Clean Activation Proof

Date: 2026-08-21

Lane: `MARC2-VR15P`

Status: **Activation has delayed effect until this exact commit is remotely
green**

Machine record:
`registries/marc2_suffix_identity_private_discriminator_implementation_proof.v0.json`

## Bound Green Barriers

Corrected Stage 1 `28a734df3fb0cb83c3cddb4994b76d8c9453830b`
passed Base Python job `96688236516`, Optional Neuro Readers job
`96688236752`, and CI `32454196219`.

Proof closeout `2acfb3318beb46ade294fdc3ff0fc21765e3ea17` passed Base
Python job `96690180933`, Optional Neuro Readers job `96690181096`, and CI
`32454892777`. This activation binds its three exact artifacts totaling 9,513
bytes by path, byte count, SHA-256, and Git blob.

## One Future Command

Only after this exact activation commit is pushed and both required jobs are
green may the fixed command be explicitly armed. It may collect the registered
readiness samples, read and strict-parse the exact 418,755-byte target-free
structural source once, call VR15A once with one nested unchanged VR12A call,
and retain one aggregate R1-R16 route. It cannot freeze a cohort.

No source path, output path, route, threshold, retry, resume, fallback, or
substitution override exists. The command is one-shot and any route consumes
the lane.

## Zero-Operation Transition

This activation record does not inspect readiness or touch `.codex_work`, the
private source, prior consumed output, an archive member, neural data, targets,
models, predictions, scores, FW2/CIL1, network, hardware, another project, or
a release surface. All activation counters are zero.

Engineering capability added: a tracked-clean proof record can arm one fixed,
aggregate-only target-free structural discriminator after exact remote proof.

Scientific claim not established: activation accesses no neural payload,
target, model, prediction, or score and establishes no neural effect or
decoding performance.
