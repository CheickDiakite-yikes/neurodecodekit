# Fresh Motor Source Identity Witness Live Implementation

Date: 2026-08-31

Implementation: `FMSR1-R1-W-I1`

Qualification: `FMSR1-R1-W-I1-Q0`

Status: **sole zero-network generated qualification passed; exact implementation
pending this commit and both required GitHub `main` jobs; no real source or
neural result**

Machine records:

- `registries/fresh_motor_source_identity_witness_live_implementation.v0.json`
- `registries/fresh_motor_source_identity_witness_live_qualification_result.v0.json`

## Why This Exists

The scientific lane needs a fresh, complete motor EEG cohort before another
model can be informative. The registered five-index witness is the final source
identity gate: it establishes a replayable byte identity for every frozen
official index without semantically reading candidate records. The generated
core already proved the hash tree and pagination contract; this additive live
adapter supplies the missing bounded transport, one-shot marker, and remotely
green implementation check.

This implementation does not select a dataset and cannot read EEG. The public
entry point is parameterless and binds the repository root, process environment,
system TLS transport, official output root, packet, workflow, implementation
decision, and future execution decision. Tests can exercise internal adapters
with generated fixtures, but callers cannot inject those fixtures into the
production entry point.

## Frozen Live Surface

The implementation binds exact-green decision `FMSR1-R1-W-I1-D0` at commit
`0c50299dc5223ba0f2b1f337beded51038bffd4d`, CI `33364407489`, Base Python
job `99401989268`, and Optional Neuro Readers job `99401989133`.

Before any official-index contact, a future execution must prove all of these
conditions in the same process:

1. The exact execution decision is tracked on clean local `main` and matches
   remote GitHub `main`.
2. Exactly the registered Base Python and Optional Neuro Readers checks are
   successful for that commit.
3. The repository, workflow blob, packet, implementation record, decision, and
   one-shot output reservation all match their frozen identities.
4. A durable no-follow attempt marker is created and verified before every
   possible contact.

The CI proof uses exactly three GitHub API calls. The source witness then allows
at most 125 source requests, 128 requests total, 32 MiB each of cumulative
source wire and entity bytes, 8 MiB per page, 2 MiB temporary bytes, 1 MiB
retained bytes, 300 seconds, 256 MiB peak RSS, one thread, one worker, and one
numerical job. DNS, connect, TLS, response headers, transfer framing, redirects,
server time, content encoding, and cumulative caps fail closed.

Local Git proof is restricted to five read-only subcommands through the
root-owned `/usr/bin/git`. It runs with a closed environment, system and global
configuration disabled, filesystem monitoring and hooks disabled, credentials
disabled, partial-clone lazy fetch disabled, and no inherited loader or Git
variables. This prevents a PATH shim, configured hook, credential helper, or
promisor-remote fetch from creating an unmarked side effect during preflight.

Only registered pagination controls are decoded. Candidate values are
stream-skipped and may contribute only byte counts and SHA-256 digests to the
public page-to-root-to-profile-to-global ledger. The adapter has no candidate
selector, payload reader, model, target reader, prediction path, or scorer.

## Sole Generated Qualification

The one authorized generated qualification completed successfully and is now
consumed. It must not be rerun, repaired, resumed, or substituted. The shipped
`qualify-generated` entry point now returns a sanitized authority refusal before
any replay, marker, filesystem fixture, DNS, socket, or official-index action.

| Measurement | Observed |
|---|---:|
| Deterministic replays | 2 |
| Profiles / roots / pages | 5 / 17 / 34 |
| Generated transport calls | 76 |
| Candidate semantic accesses | 0 |
| Opaque members skipped | 116 |
| Refusal observations | 16 |
| Direct transport refusal observations | 6 |
| Runtime | 0.1274927919730544 s |
| Peak RSS | 40,583,168 bytes |
| Generated input / retained bytes | 19,898 / 0 |
| Temporary peak / report bytes | 737 / 3,018 |
| CPU threads / workers / numerical jobs | 1 / 1 / 1 |

Both replays produced global ledger digest
`9f567e28e2424ad71aa724834f82b89b182f27e36df0f4c7d04e33a29b73c97f`.
The generated `CI-W0` receipt digest was
`028bca65f22f88b0084eb1efbbea1617b517c614e6b74b1f23f7f236741889a9`.

The qualification covered exact request serialization, marker-before-contact,
system TLS policy, redirects, chunked and connection-close framing, conflicting
framing, truncated bodies, page and wire caps, path and output confinement,
authority checks, and candidate nonaccess. Network, official-index,
real-source, neural, target, model, training, prediction, score, release, and
claim operations were all zero.

## Next Gate

This exact implementation and its artifact set must first be committed, pushed
to GitHub `main`, and pass both required jobs. Only then can fresh maintainer
words be bound into a separate execution decision with the exact implementation
commit, artifacts, CI proof, repository, workflow, packet, and `CI-W0` contract.
That decision must itself become remotely green before the sole live witness can
run. No current instruction can pre-authorize that future execution decision.

Engineering capability added: a dependency-free, one-shot, fail-closed live
adapter can prove remote CI identity and construct a complete target-opaque
source-index hash ledger under strict transport and resource limits.

Scientific claim not established: no official index or EEG was accessed and no
model was run, so this establishes no neural advantage, nuisance resistance,
movement intention, unseen-person generalization, language decoding, or live
neural operation.
