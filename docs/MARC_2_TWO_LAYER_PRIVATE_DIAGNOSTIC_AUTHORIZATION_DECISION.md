# MARC2-VR9P Two-Layer Private Diagnostic Authorization Decision

Date: 2026-08-17

Lane: `MARC2-VR9P`

Status: **Packet-bound short-form authorization recorded; ineffective until
this decision is committed, pushed, and both required CI jobs are green; no
readiness, private path, structural source, archive, neural, target, model,
prediction, score, or claim operation occurred while recording this decision**

Machine decision:
`registries/marc2_two_layer_private_diagnostic_authorization_decision.v0.json`

## Maintainer Decision

After Codex identified `MARC2-VR9P` as the sole active Tier C packet and named
its remotely green request and proof commits, CI runs, two required jobs,
two-stage order, exact structural-only scope, privacy firewall, and fresh-
decision boundary, the maintainer's next message was exactly:

```text
continue
```

The machine record preserves those actual eight UTF-8 bytes and their SHA-256.
It does not fabricate the packet's long scope as a maintainer utterance. Under
the approved Research Autonomy Charter, the fresh `continue` binds only the
immutable, remotely green `MARC2-VR9P` packet by reference, without expansion.

## Green Packet Bound By This Decision

```text
request commit:       de8e6dcfb60d78b52429d32c6bdd5f9656ab2d58
request CI:           31992178980
request Base job:     95277554517
request Optional job: 95277554619
proof commit:         ddc5e8529f86d05d9f3edefb4595106e3959477f
proof CI:             31992563746
proof Base job:       95278576871
proof Optional job:   95278576905
```

Both required jobs passed for both exact commits. The immutable request
document, registry, and test hashes are preserved separately from the additive
green proof snapshot. The pre-existing untracked tracker inspection sidecar was
not opened, modified, staged, or deleted.

## Delayed Effect

This decision is not effective merely because these files exist. Before any
wrapper implementation, readiness invocation, output-root operation, or
private-source path check, this exact decision must:

1. pass focused local verification;
2. be committed as one coherent decision-only milestone;
3. be pushed on the current branch; and
4. pass both required remote CI jobs.

Until all four conditions hold, implementation and private access remain
closed.

## Authorized Sequence After Green Decision

### Stage 1: additive generated/mock wrapper

After this exact decision is remotely green, one additive standard-library
wrapper may be implemented and qualified using only invocation-created
generated source fixtures, injected machine samples, mocked no-follow
filesystem facts, and temporary generated output roots.

The wrapper must expose no generic source, output-root, path, threshold, retry,
resume, fallback, cleanup, substitution, or arbitrary execute override. It must
preserve the fixed VR9P paths, fresh-readiness state machine, marker-before-open
transition, one strict JSON read, one exact VR6 adapter call, the VR8B two-layer
route firewall, deterministic replay, resource monitors, and zero-retry
semantics.

Qualification must exercise generated F03 and F04 cases in canonical and
reversed order, replay each exact case twice, make exactly one VR6 call per
diagnostic path, and cover at least 64 direct refusal mutations. It may not
stat, resolve, hash, open, alter, delete, or reuse a real `.codex_work` path.

The exact Stage 1 implementation and its proof record must then be committed,
pushed, and pass both remote CI jobs before any private operation begins.

### Stage 2: one target-free structural diagnosis

Only after the exact Stage 1 implementation is remotely green may one command:

1. verify its exact green proof record;
2. obtain three consecutive passing machine-readiness samples and write one
   fresh mode-`0600` certificate at the fixed VR9P path;
3. require the new output root to be absent and recheck disk, thread, load, and
   RSS safety immediately before the irreversible boundary;
4. no-follow validate the exact registered structural source;
5. atomically write one mode-`0600` consumed marker immediately before the
   source content open;
6. read, hash, and strict-parse exactly 418,755 structural bytes once;
7. call the exact green VR6 adapter exactly once; and
8. write at most one aggregate-safe report.

The report may retain only outer `MARC2VR6-F02` and nested `MARC2VR2-F03` or
`MARC2VR2-F04`, plus proof identity, bounded measurements, zero-operation
counters, warnings, unavailable fields, and claim boundaries. Nested F02,
success, an unknown nested route, or any other route consumes and parks the
lane.

No reason, exception, predicate, failed value, source row, member name, path,
offset, CRC, private hash, participant identity, session, run, companion,
candidate, selection, or cohort may be retained. There is no private manifest
or cohort-output file in this authorization.

## Resources And Failure Semantics

```text
CPU threads / workers / numerical jobs: 1 / 1 / 1
generated qualification:                <= 30 seconds
future private command:                 <= 650 seconds
peak RSS:                               < 256 MiB
minimum free disk before marker:        >= 15 GiB
fresh readiness wait:                   <= 600 seconds
private structural input:               exactly 418,755 bytes once
network / archive / signal bytes:       0 / 0 / 0
combined incremental output:            <= 1 MiB
retry / rerun / resume / fallback:      0 / 0 / 0 / 0
```

A refusal before the marker performs zero private structural content opens and
still consumes the one registered invocation. Every route after the marker
consumes the one diagnostic, also without retry, rerun, resume, repair,
substitution, or fallback. Cleanup authority is limited to invocation-created
temporary files; no other project, consumed root, certificate, marker, or
artifact is in scope.

## Explicitly Not Authorized

This decision does not authorize implementation before this decision is
remotely green; private operation before the exact wrapper is remotely green;
any operation on a named consumed path; another path, source, output root,
project, dataset, participant, session, or file; network or download; archive-
member, ZIP-range, EEG, MEG, signal, event, onset, channel, geometry, sentence,
key, label, target, response, trial, quality, or payload access; cohort freeze,
derivative or split creation; training, parameter updates, checkpoint access,
inference, prediction, freeze, target delivery, scoring, or tuning; `MARC2-FW2`
or `MARC2-CIL1`; a language model, provider, RW3, stream, device, or hardware
operation; release or publication; or any scientific, decoding, neural, real-
time, portable, home-use, assistive, or clinical claim upgrade.

## Next Gate

After this decision is remotely green, implement and generated-qualify the
exact fixed-path wrapper. After that exact implementation is remotely green,
perform the one registered structural diagnosis. F03 or F04 may select the
subject of a later prospective repair, but neither result authorizes that
repair. `MARC2-FW2` and `MARC2-CIL1` remain closed until a separately designed,
authorized, and successful real cohort freeze exists.

Any future archive payload, neural derivative, model fit, prediction freeze,
target delivery, score, or live decoding run requires its own immutable all-
false Tier C packet and fresh decision.

## Claim Boundary

Engineering capability authorized after green decision: one newly proven,
machine-stable wrapper may reveal which of two frozen structural refusal
classes the exact target-free manifest reaches while retaining no private
reason, row, path, identity, value, selection, or cohort.

Scientific claim not established: this decision is not neural data or a result
and establishes no neural effect, decoding performance, brain-specific origin,
language decoding, live decoding, or thought-to-text capability.
