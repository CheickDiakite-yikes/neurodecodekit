# Ofner 2017 Range-Only Header Decision

Date: 2026-08-29

Status: **Packet-bound authorization recorded. It becomes effective only after
this exact decision is tested, committed, pushed, and both required CI jobs are
remotely green. No source or GDF operation occurred while recording it.**

Machine decision:

- `registries/ofner_2017_motor_imagery_range_header_decision.v0.json`

Packet ID: `OFNER-C6R-1-HL`

## Actual Maintainer Decision

After Codex identified `OFNER-C6R-1-HL` as the sole active Tier C packet,
reported request commit `af9d0247bb816c3432a9eb407fadcd286f84d87c`, proof
commit `2c313d57ca3acebeea985fb788593be88877f68e`, frontier commit
`845b39ef88cd747b329be49e56efa5cdf999a40d`, green CI `33274520086`, the
exact subject 1/run 1 GDF member, the 65,536-byte GDF ceiling, and the ordered
decision, implementation, activation, and one-shot barriers, the maintainer
said:

> continue

This record preserves that word exactly. It does not claim that the maintainer
typed the packet's long scope. The short instruction incorporates the
immutable, remotely green packet by reference without widening it.

## Why The Short Form Is Valid

1. `OFNER-C6R-1-HL` was the sole active Tier C packet.
2. Its all-false request and proof-only closeout were already committed,
   pushed, and remotely green.
3. The frontier transition at `845b39e` passed Base Python `99158792935` and
   Optional Neuro Readers `99158792997` in CI `33274520086`.
4. Codex named the packet, proof chain, exact member, request ceiling, ordered
   barriers, and exclusions before the maintainer's fresh message.
5. The maintainer then unambiguously directed continuation.
6. This separate record quotes the actual word and binds seven exact request,
   proof, test, and frontier artifacts totaling 36,105 bytes.
7. No full acquisition, event, annotation, signal, target, model, score,
   release, destructive action, other-project access, or claim upgrade is
   inferred.

The decision is ineffective until its own exact commit passes both required
remote CI jobs.

## Exact Authorized Sequence

After this decision is remotely green, H-L1 may implement one additive
standard-library range-only wrapper around the byte-identical generated GDF
2.x parser. H-L1 may use only generated fixtures and injected mock responses
for one registered generated qualification. It must expose no usable real
command before a separate exact activation is green.

The exact H-L1 implementation and generated qualification must be committed,
pushed, and pass both required remote CI jobs. Only then may an H-L2 activation
record be created, committed, pushed, and proved remotely green. Only after
that final barrier may one irreversible H-L2 invocation:

1. verify at least 2 GiB free disk without scanning another project;
2. durably consume its unique marker before constructing an opener;
3. refresh the exact NEMAR manifest once in memory;
4. issue `bytes=0-255` and then the exact remaining-header range;
5. accept at most 65,536 combined GDF body bytes and no trailing byte;
6. decode only allowlisted fixed-header fields with the proven parser;
7. require the frozen 96-role, 512 Hz representation for H1; and
8. publish one bounded aggregate H1 or H0 result and stop.

Any terminal route consumes the packet. There is no retry, rerun, resume,
repair, redirect, substitution, fallback, or post-result amendment. H1 permits
only a future full-cohort acquisition preregistration; it does not authorize
acquisition, events, signals, targets, models, or scoring.

## Exact Member And Limits

```text
dataset / revision:       NEMAR nm000173 / v1.0.3
member:                   sourcedata/motorimagination_subject1_run1.gdf
declared bytes / SHA-256: 105,365,484 / ec334466272a936986a50c120c52c57634801f028acb0fee30705f8a2dee3087
manifest / range GETs:    1 / 2 on success
GDF body bytes maximum:   65,536
threads / workers / jobs: 1 / 1 / 0
wall time / peak RSS:     120 sec / 268,435,456 bytes
response / disk / output: 2,162,688 / 4 MiB / 1 MiB
minimum free disk:        2 GiB
redirects / retries:      0 / 0
reruns:                   0
```

The full 105,365,484-byte payload is not requested, and its manifest-declared
SHA-256 cannot be recomputed or described as verified by this header-only
checkpoint.

## Decision-Only Measurements

```text
fresh GitHub CI verification calls:                  1
source requests / response bytes / GDF bytes:        0 / 0 / 0
real header / event / annotation / signal reads:     0 / 0 / 0 / 0
target or label reads:                               0
model loads / training / inference / scoring:        0 / 0 / 0 / 0
generated qualification / activation / real runs:   0 / 0 / 0
provider / stream / device / hardware actions:       0 / 0 / 0 / 0
cleanup / deletion / releases / claim upgrades:      0 / 0 / 0 / 0
end-to-end latency measured:                          false
```

## Claim Boundary

**Engineering capability authorized for testing:** build and generated-qualify
one proof-bound range-only wrapper, then use it once to verify one exact public
GDF fixed-header representation before spending 13.75 GB on the cohort.

**Scientific claim not established:** this decision is not an EEG result and
establishes no neural effect, EEG beyond EOG or kinematics, unseen-person
generalization, movement intention, motor-cortex causation, thought or language
decoding, live decoding, hardware result, or clinical utility.
