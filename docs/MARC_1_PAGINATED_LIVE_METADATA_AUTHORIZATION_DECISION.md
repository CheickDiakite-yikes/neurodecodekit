# MARC-1 Paginated Live Metadata Authorization Decision

Date: 2026-08-12

Lane: `MARC1-LM1`

Status: **Authorized only after this decision is tested, committed, pushed,
and remotely green; no implementation or live access has occurred**

Machine decision:
`registries/marc1_paginated_live_metadata_authorization_decision.v0.json`

Frozen request commit:
`4d3eb19690676d48ce42ed16c5c00cc041d8bb4b`

Frozen packet:
`docs/MARC_1_PAGINATED_LIVE_METADATA_AUTHORIZATION_PACKET.md`

## Actual Maintainer Decision

After Codex identified `MARC1-LM1` as the sole active Tier C packet, named
request commit `4d3eb19690676d48ce42ed16c5c00cc041d8bb4b`, green CI
`31603530015`, both required green jobs, the generated-wrapper-first order,
the one-response metadata scope, the 2 MiB body cap, the zero-payload boundary,
and the need for a fresh decision, the maintainer's active instruction was:

> approved, continue, achieve a scientific claim, achieve thought to text 😎

This record preserves those exact 76 UTF-8 bytes. It does not claim that the
maintainer typed the packet's long scope. The short approval incorporates the
immutable remotely green packet by reference without widening it.

Achieving a scientific claim and thought-to-text remains the research
objective. It is not permission to announce an outcome before evidence, skip
controls, open a participant payload under this metadata gate, or substitute
movement metadata for held-out language decoding.

## Why The Short Form Is Valid

All fail-closed conditions were satisfied before this decision:

1. `MARC1-LM1` was the sole active Tier C packet.
2. The packet and all-false request were committed and pushed at `4d3eb19`.
3. Base Python job `94136577454` and Optional Neuro Readers job `94136577639`
   were green in CI `31603530015`.
4. Codex named the packet, commit, CI proof, exact one-response scope,
   zero-payload boundary, and fresh-decision gate.
5. The maintainer unambiguously approved and directed continuation.
6. This separate record quotes the complete actual instruction and binds the
   immutable request SHA-256.
7. No payload, neural, target, model, score, release, or scientific-claim
   operation is inferred from the aspiration to reach thought-to-text.

The decision is ineffective until its own commit passes both remote CI jobs.

## Bound Evidence

```text
authorization parent: 4d3eb19690676d48ce42ed16c5c00cc041d8bb4b
request push CI:       31603530015
Base Python job:       94136577454
Optional Neuro job:   94136577639
request SHA-256:       798d05a52e86891467b54a6807475602d9f3530468bd5a4005768e9f966dac9d
packet SHA-256:        f7158aa6e73693e99d3a61433523f6cf24111f778d1a02db31ce5d213a67ddeb
user-message SHA-256:  aedb564a57be18493fc20376676ef404794ca169d02985228aa8424cd7f7e6e8
```

The packet, request, tests, output-capability result, pagination source, and
consumed failures remain immutable snapshots. Their pending fields are not
rewritten. This additive record supplies only the packet-bound permission.

## Ordered Authorization

### Gate 1: this decision becomes remotely green

This exact decision must be tested, committed, pushed, and pass Base Python
and Optional Neuro Readers CI. Until then, implementation and access remain
closed.

### Gate 2: generated and mocked wrapper qualification

After Gate 1, Codex may implement the additive standard-library wrapper named
by the packet at
`src/neurodecodekit/datasets/marc1_paginated_live_metadata.py`.

Qualification may use only generated 55-row inventories, mocked response
objects, injected failures, and fresh temporary nonregistered paths. It must
prove:

- capability acquisition before repository, contract, transport, fixture,
  selection, or output work;
- exact decision, request, source, implementation, and query binding;
- one uncoded terminal JSON body bounded by a cap-plus-one read;
- safe absent, decimal-length, and exact chunked-framing handling;
- refusal of redirects, content coding, conflicting framing, overflow,
  duplicate JSON keys, schema drift, target-like fields, wrong inventory,
  cohort or split drift, output races, overwrite, fallback, retry, and second
  invocation;
- exact target-free 12-subject Wrist selection;
- one consumed marker, one private manifest, one aggregate report, privacy
  separation, deterministic replay, and bounded resources; and
- zero real, network, payload, neural, target, model, score, provider,
  hardware, other-project, and claim counters.

The existing environment may be reused. Dependency installation, public
Figshare access, private paths, consumed roots, payloads, signals, targets,
models, scores, and claim work remain closed during implementation. The exact
wrapper commit must pass both remote CI jobs before Gate 3.

### Gate 3: one live metadata request

After Gate 2 is remotely green, require all numerical thread settings to equal
one, at least 10,737,418,240 bytes free, and normalized one-minute load no
greater than `1.0` per logical CPU. Any unavailable or failed measure refuses
before a consumed marker.

One no-retry invocation may then:

1. acquire a held no-follow output-parent capability before repository or
   network work;
2. validate every immutable proof and the exact absent output path;
3. create only
   `/private/tmp/neurodecodekit-marc1lm1-live-metadata-20260812` and one
   mode-`0600` consumed marker;
4. make exactly one unauthenticated HTTPS `GET` to
   `https://api.figshare.com/v2/articles/29666735/versions/3/files?page=1&page_size=1000`;
5. accept one terminal `200` JSON body no larger than 2,097,152 bytes under
   the frozen uncoded and bounded-framing rules;
6. require exactly 55 rows, 45 participant archives, ten supplementary rows,
   3,683,416,050 declared bytes, and the frozen seven-field schema;
7. apply only the target-free 12-subject Wrist selection with runs 1-6 fit and
   runs 7-8 held out;
8. write one mode-`0600` private metadata manifest and one aggregate report
   through held descriptors; and
9. discard the raw response body and stop.

Any failure after the consumed marker consumes `MARC1-LM1`. There is no
redirect, second request, second page, retry, rerun, resume, restart,
substitution, fallback parser, expectation change, participant archive,
payload, signal, target, model, prediction, or score.

## Exact Limits

```text
metadata request attempts / redirects:  1 / 0
accepted bodies / bytes:                1 / <= 2,097,152
expected rows / selected subjects:      55 / 12
payload requests / bytes:               0 / 0
live execution wall / RSS:              30 sec / 268,435,456 bytes
aggregate / combined output:            <= 1,048,576 / <= 2,097,152 bytes
incremental disk / minimum free:         <= 4,194,304 / >= 10,737,418,240 bytes
threads / workers / jobs:                1 / 1 / 1
retries / reruns:                        0 / 0
signals / targets / models / scores:     0 / 0 / 0 / 0
```

## Decision-Only Measurements

```text
private path operations:                   0
DNS queries / network requests / redirects: 0 / 0 / 0
metadata bodies / bytes / parses:           0 / 0 / 0
real participant selections:                0
markers / private manifests / reports:      0 / 0 / 0
payload requests / bytes:                    0 / 0
signal / event / target reads:               0 / 0 / 0
models / predictions / freezes / scores:     0 / 0 / 0 / 0
dependency installs / cleanup operations:    0 / 0
provider / hardware / release operations:    0 / 0 / 0
scientific claim upgrades:                   0
end-to-end latency measured:                 false
```

## Local Verification

```text
decision tests:                       13 / 13
request plus decision tests:          26 / 26
all MARC tests:                      625 / 625
MARC runtime / external peak RSS:    6.452 sec / 68,485,120 bytes
dependency-light suite:              2,764 passed / 204 skipped
dependency-light runtime / peak RSS: 22.590 sec / 259,768,320 bytes
optional-neuro suite:                2,835 passed / 35 skipped
optional-neuro runtime / peak RSS:   60.790 sec / 726,024,192 bytes
```

The decision adds thirteen tests and zero skips over the green request.
Repository-wide Ruff, source and test compilation, all 194 registry JSON
parses, artifact hash replay, and `git diff --check` pass. Verification did
not implement the wrapper, open the registered path, contact Figshare, or make
any private, payload, neural, target, model, score, or claim operation.

## Claim Boundary

Engineering capability authorized for testing: one exact additive,
standard-library, capability-first wrapper may be qualified and, after its own
green proof, bind the complete public Wrist metadata inventory through one
paginated response without opening a neural payload.

Scientific claim not established: this decision is not neural data or a
result. Even clean metadata would establish no neural effect, decoding
accuracy, language decoding, or thought-to-text capability.

Research objective preserved: this is a cohort-integrity checkpoint on the
same path toward a controlled neural effect and properly held-out
thought-to-text result, not a pivot or substitute experiment.
