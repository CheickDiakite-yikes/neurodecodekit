# COMM-P0-G Qualification Hardening Implementation V1

Date: 2026-08-28

Status: **generated-only implementation and full-scale rehearsal pending exact
commit, push, and remote CI; official qualification remains inactive**

Machine records:

- `registries/communication_eeg_prospective_generated_qualification_hardening_implementation.v1.json`
- `registries/communication_eeg_prospective_generated_full_scale_rehearsal_result.v0.json`

Supersession note: branch commit
`0097cb85835498494ef65b08f7ad9850eb0e6f5e` is not on `main`. Base job
`98780682764` in CI `33150365280` refused because its historical V0 artifact
table still bound the previous score-worker hash. No qualification, real-data,
network, target, model, score, device, or claim operation occurred in CI. This
V1 record is the fresh successor and requires its own complete remote proof.

## What Changed

The generated communication-study harness now closes the three concrete
engineering gaps left by V0:

1. Fold predictions are validated in canonical order and appended directly to
   one no-replace stream. The coordinator never stores the complete prediction
   collection and deletes only its own temporary fold inputs after each fold.
2. The score child makes one target-free verification pass over the frozen
   descriptor and one post-delivery scoring pass over the same inode. It retains
   a compact duplicate bitset and participant-level accumulators, with a measured
   high-water mark of one raw prediction row.
3. All seven registered shortcut routes are actual numerical executions. The
   generated residual-central-EEG positive control passes; EOG, oral EMG,
   microphone, cue, timing, and language-only routes fail the neural-evidence
   router while their injected shortcut controls are detected.
4. The future activated entry creates and fsyncs the durable consumed marker
   before any official fixture or model action. The compile-time activation lock
   remains false.
5. Exact sign flipping is now reserved for the two confirmatory free-choice
   cohort tests, matching the frozen `4,194,304`-assignment schedule. Prompted
   results remain directional and cannot rescue free-choice failure.

## Reduced Replay Measurement

Two isolated reduced replays used three fictional participants per cohort and
matched exactly across their complete 15-digest surfaces.

| Measure | Result |
|---|---:|
| Prediction rows / sets per replay | 13,056 / 204 |
| Maximum raw prediction rows buffered | 1 |
| Complete prediction collection materialized | no |
| Numerical shortcut executions per replay | 7 |
| Shortcut prediction rows per replay | 91,392 |
| Main target deliveries / scores | 2 / 2 total |
| Shortcut target deliveries / scores | 14 / 14 per replay |
| Refusal observations | 140 total |
| Runtime | 42.6944 s |
| Peak process-tree RSS | 211,288,064 bytes |
| Maximum private generated bytes | 4,896,930 bytes |
| Network / real-data / device operations | 0 / 0 / 0 |
| Retained generated payload | 0 bytes |

## Full-Scale Nonofficial Rehearsal

One bounded rehearsal exercised the complete 42-person fictional structure:

| Measure | Result |
|---|---:|
| Fictional participants | 42 |
| Prediction rows / sets | 91,392 / 1,428 |
| Maximum raw prediction rows buffered | 1 |
| Numerical shortcut routes | 7 |
| Runtime | 101.5826 s |
| Peak process-tree RSS | 283,394,048 bytes |
| Private temporary bytes | 34,259,372 bytes |
| Process-monitor samples | 617 |
| Main target deliveries / scores | 1 / 1 |
| Shortcut target deliveries / scores | 14 / 14 |
| Network / real-data / device operations | 0 / 0 / 0 |
| Retained generated payload | 0 bytes |

The first sandboxed attempt refused before its first fold because `ps` was not
available to the mandatory process-tree monitor. It produced no result, target
delivery, score, retained payload, network activity, or official consumption.
The measured rehearsal above ran with the mandatory monitor available.

## Remaining Gate

The engineering implementation is ready for exact artifact binding and remote
CI. The official two-replay generated qualification remains separately locked;
it requires its own exact-green activation before the one consumed invocation.
No real-data or scientific authority changes here.

Engineering capability added: NeuroDecodeKit can execute the complete fictional
communication protocol with bounded streaming predictions, isolated two-pass
scoring, numerical shortcut discrimination, durable future consumption, and
full-scale resource evidence.

Scientific claim not established: these procedural fictional signals do not
show that EEG decodes communication, adds information beyond eyes or mouth,
generalizes to unseen people, works live on a device, or provides clinical value.
