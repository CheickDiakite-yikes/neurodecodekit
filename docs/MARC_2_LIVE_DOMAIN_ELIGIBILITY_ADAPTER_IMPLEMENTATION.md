# MARC2-VR2 Live-Domain Eligibility Adapter Implementation

Date: 2026-08-16

Lane: `MARC2-VR2`

Status: **Generated implementation ready; exact remote proof pending**

Machine record:
`registries/marc2_live_domain_eligibility_adapter_implementation.v0.json`

## Green Registration Barrier

Implementation began only after exact registration commit
`384373e0ffcfe999ae0ae188087f7e84f09720ca` passed both required jobs:

```text
CI run:               31945086852
Base Python job:      95159734989
Optional Neuro job:  95159734967
```

The registration and its authority boundary are unchanged. No private path,
consumed root, archive member, payload, neural value, target, model, score, or
network operation was opened.

## Added Capability

The standard-library module
`neurodecodekit.datasets.marc2_live_domain_eligibility_adapter` exposes only:

```text
plan
qualify
inspect
```

Its public in-memory adapter takes one mapping object. It accepts no path, URL,
output root, credential, archive reader, signal reader, target reader, trainer,
predictor, or scorer.

The adapter applies this order:

1. Verify the frozen VR2 contract, green VR1 lineage, and bounded surface.
2. Validate the exact live-shaped source envelope and transport hashes.
3. Validate all 1,227 structural rows and 238 complete four-file bundles.
4. Classify every bundle from the frozen public participant taxonomy.
5. Require exactly 195 eligible and 43 valid ineligible bundles.
6. Verify the exact public eligible participant/session counts.
7. Apply the green VR1 rank, split, reservation, and contiguous-prefix logic.
8. Emit aggregate generated evidence without individual ineligible identities.

The source never supplies a generated profile name to the adapter. Profile
identity therefore cannot become a live acceptance requirement.

## Variable-Domain Qualification

The generated builder creates four distinct ineligible distributions, each in
canonical and reversed row order:

| Profile | Single-session | Sampling-tier | Extra-session |
|---|---:|---:|---:|
| A | 12 | 24 | 7 |
| B | 8 | 20 | 15 |
| C | 16 | 12 | 15 |
| D | 4 | 4 | 35 |

All eight paths preserve:

```text
eligible bundles:          195
selected subjects:          16
selected run bundles:       96
selected core members:     384
selected reservation: 8,105,207,776 bytes
selection identity: dee065bfdb5f8439fe711042eaadbea0dca3d83f8be0d6b7b9d1637e84d9f641
ineligible selected:          0
```

Canonical/reversed source hashes agree within each profile. Source hashes may
differ between profiles because the generated ineligible identities differ:

```text
A: 8ef7bad415058fb5b3e247c269a5cdc33795dcb92b0e6212e1a1182d28a45f48
B: 312dec1cb43b58ba73c6d12e44cf8336b2bcaafc59cd2a695307e77840647fcd
C: 9663e3abc655a088303e2872a12eaf7425f53947278040cb6a1d3fbeeb7b37fc
D: cbb06abe86a7c05edf85172087b8b658552078427a97f3c860091b45ebc2506c
```

## Refusal Matrix

All 58 registered mutations refuse. Route coverage from the final development
preflight is:

| Route | Refusals |
|---|---:|
| `MARC2VR2-F01` | 4 |
| `MARC2VR2-F02` | 13 |
| `MARC2VR2-F03` | 18 |
| `MARC2VR2-F04` | 8 |
| `MARC2VR2-F05` | 2 |
| `MARC2VR2-F06` | 4 |
| `MARC2VR2-F07` | 3 |
| `MARC2VR2-F08` | 6 |

This includes unknown and overlapping participant taxonomy, 237/239 source
bundles, 42/44 ineligible arithmetic, exact-breakdown overconstraint,
prefilter equality, rank and reservation drift, ineligible selection,
target/quality leakage, individual identity leakage, resource drift, and a
forbidden private or execute surface.

## Development Measurement

One non-closeout development preflight used:

```text
success paths:             8
refused mutations:        58
generated input:   3,435,280 bytes
runtime:             0.511378 seconds
peak RSS:          35,356,672 bytes
threads/workers/jobs:      1 / 1 / 1
retained output:            0 bytes
network/private bytes:      0 / 0
```

This is not the registered measured closeout. The `qualify` command must not be
used for closeout until this exact implementation commit is pushed and both CI
jobs are green.

## Verification State

Thirty-two focused registration, behavior, and implementation-record tests
pass. The complete dependency-light suite passes 3,575 tests with 204 expected
skips. Fresh optional A-M and N-Z processes pass 3,133 tests with 28 skips and
513 tests with seven skips, respectively, for 3,646 optional-enabled tests
with 35 skips. Ruff, compilation, all 240 registry JSON documents, CLI help,
`plan`, `inspect`, and diff hygiene pass. The registered `qualify` closeout is
still pending exact remote-green implementation proof.

## Next Gate

Commit and push the exact implementation, then require Base Python and Optional
Neuro Readers to pass remotely. Only after that proof may the one registered
generated qualification run. A later private structural attempt still needs a
separately named executor, all-false Tier C request, and fresh packet-bound
maintainer decision.

Engineering capability added: NeuroDecodeKit can validate a live-shaped full
source domain without requiring a synthetic exclusion breakdown and can
preserve the exact target-free eligible selection across variable valid
ineligible layouts.

Scientific claim not established: no private archive, neural payload, target,
prediction, or score was accessed, so this implementation establishes no
neural effect, decoding accuracy, language decoding, or thought-to-text
capability.
