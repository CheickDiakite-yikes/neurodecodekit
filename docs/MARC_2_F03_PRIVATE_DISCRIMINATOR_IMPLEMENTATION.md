# MARC2-VR11P F03 Private Discriminator Implementation

Date: 2026-08-17

Lane: `MARC2-VR11P`

Status: **Generated/mock implementation prepared; private execution remains
locked pending exact remote-green implementation proof**

Machine implementation record:
`registries/marc2_f03_private_discriminator_implementation.v0.json`

## Green Decision Gate

Decision commit `4fa277121f24dde3f6f7c917ef6c2bb7506134d6` passed Base
Python job `95414004791`, Optional Neuro Readers job `95414004814`, and CI
`32038683203` before implementation began. The decision quotes the
maintainer's exact 18-byte message `okay lets continue` and binds only the
unchanged green VR11P packet.

## Added Surface

`src/neurodecodekit/datasets/marc2_f03_private_discriminator.py` adds:

- exact decision, implementation, and tracked-artifact identity checks;
- 24 generated route paths through the exact green VR6 and VR10B functions;
- one temporary exact-size generated fixture that qualifies readiness, fresh
  directories, mode-`0600` marker-before-read ordering, no-follow content
  access, strict JSON, and mode-`0644` aggregate output;
- deterministic G1/R1-R5 translation with no failed value or source identity;
- aggregate leakage, resource, thread, and forbidden-operation firewalls;
- at least 80 direct refusals, including inherited VR10B mutations; and
- fixed `plan`, `qualify`, `inspect`, and `execute` commands with no generic
  path, URL, output, threshold, retry, resume, fallback, or substitution
  option.

The module does not import, call, patch, copy, or modify the consumed VR9P
executor. Generated qualification uses temporary fixtures only and retains
zero output.

## Proof Gate

The implementation registry intentionally records
`remote_implementation_proof: null`. `execute_registered()` validates that
record and refuses before readiness, output, or private-source operations.

After this exact implementation is committed, pushed, and both remote CI jobs
are green, a separate proof-only closeout may bind those immutable identifiers
and open the already-authorized one-shot Stage 2 command. The proof closeout
must not run the generated qualification again or touch any private path.

## Stage 2 Boundary

Even after proof, the fixed executor can read only the registered 418,755-byte
target-free structural manifest once, require outer `MARC2VR6-F02` plus nested
`MARC2VR2-F03`, call VR10B once, and retain one aggregate R1-R5 route. It has
no arbitrary path arguments and no retry, rerun, resume, repair, fallback, or
substitution surface.

No archive member, neural signal, event, channel, target, model, prediction,
or score is part of this implementation. FW2 and CIL1 remain closed.

Engineering capability added: a proof-gated fixed-path wrapper can qualify the
five-class F03 discriminator and its one-shot filesystem protocol without
touching the registered private source.

Scientific claim not established: generated structural qualification uses no
neural payload, target, model, prediction, or score and therefore establishes
no neural effect or decoding performance.
