# MARC2-VR20P Published-Task Private Confirmation Authorization Decision

Date: 2026-08-22

Lane: `MARC2-VR20P`

Status: **Packet-bound short-form authorization recorded; ineffective until
this exact decision is committed, pushed, and both required CI jobs are green**

Machine decision:
`registries/marc2_published_task_private_confirmation_authorization_decision.v0.json`

## Maintainer Decision

After Codex identified `MARC2-VR20P` as the sole active Tier C packet and
reported its request commit, proof-closeout commit, CI evidence, two-stage
order, and fresh-decision boundary, the maintainer's next message was exactly:

```text
conitnue
```

The machine decision preserves those actual eight UTF-8 bytes and their
SHA-256. In the immediately preceding single-packet context, `conitnue` is an
unambiguous transposition typo for the requested short-form word `continue`.
The record does not normalize the quoted message or fabricate the packet's
long scope as a maintainer utterance.

Under the approved Research Autonomy Charter short-form rule, this message
binds only the unchanged, remotely green `MARC2-VR20P` packet by reference. It
cannot expand the packet by inference.

## Green Packet Bound By This Decision

```text
request commit:       bef2391d8edf92c5edf8a3624831e50430636626
request CI:           32489589922
request Base job:     96793861959
request Optional job: 96793861717
proof commit:         88b3b4aaa4436655ce6f4de65215982e2b8ff9de
proof CI:             32490587975
proof Base job:       96797011698
proof Optional job:   96797011783
```

Both jobs passed for both exact commits. The proof closeout binds six
unchanged packet artifacts totaling 46,756 bytes. The unrelated untracked
tracker inspection NDJSON was not opened, modified, staged, or deleted.

## Delayed Effect

This decision does not become effective merely because these files exist.
Before wrapper implementation, readiness work, output-root work, or private
source path access, this exact decision must pass focused local checks, be
committed, pushed, and pass both required remote CI jobs.

Recording this decision performs zero `.codex_work`, private-source,
readiness, output-root, archive, neural, target, model, score, network,
hardware, release, or scientific-claim operation.

## Authorized Sequence After Green Decision

### Stage 1: generated fixed-path wrapper

After this decision is remotely green, one additive standard-library wrapper
may be implemented and qualified using only invocation-created generated
fixtures, injected machine samples, mocked no-follow facts, and temporary
output roots.

The wrapper must expose fixed `plan`, `qualify`, `inspect`, and proof-gated
`execute` surfaces. It may expose no generic source, output, path, URL, task,
run, threshold, cap, route, retry, resume, fallback, or substitution override.
It must not touch any `.codex_work` path during qualification or import, call,
patch, copy, modify, or inspect consumed VR18P state.

Qualification must replay one exact VR20A success source and five aggregate
refusal groups in two source orders and two exact replays. It must make exactly
one VR20A call per path. All 24 paths and at least 90 direct refusal mutations
must pass under the frozen caps, with zero retained generated output and every
private or scientific counter at zero.

The exact implementation must then be committed, pushed, and pass both remote
jobs. A separate proof-only closeout must also be committed, pushed, and pass
both jobs before Stage 2.

### Stage 2: one target-free structural confirmation

Only after the exact Stage 1 implementation and proof closeout are remotely
green may one command:

1. validate every exact proof and the one-thread environment;
2. obtain three consecutive passing machine-readiness samples;
3. write one fresh mode-`0600` certificate at the fixed VR20P path;
4. require the fixed output root to be absent and reject symlinks or aliases;
5. no-follow preflight only the registered target-free source;
6. write one mode-`0600` consumed marker immediately before content open;
7. read, hash, and strict-parse exactly 418,755 bytes once;
8. call the exact green VR20A adapter once without source mutation;
9. on R1 only, write one mode-`0600` source-exact private cohort manifest; and
10. write one aggregate-safe mode-`0644` R1-R6 report.

Every route consumes the one invocation. No retry, rerun, resume, repair,
fallback, substitution, private reinspection, cleanup, or post-result
amendment is authorized.

## Fixed Route Contract

| Route | Maximum engineering meaning |
|---|---|
| `MARC2VR20P-R1` | exact published-task adapter accepted and froze one bounded structural cohort |
| `MARC2VR20P-R2` | proof, fixed-path, readiness, or output precondition refused |
| `MARC2VR20P-R3` | source identity or strict structural envelope refused |
| `MARC2VR20P-R4` | published task, identity, path, or companion validation refused |
| `MARC2VR20P-R5` | taxonomy, selection, split, rank, or reservation refused |
| `MARC2VR20P-R6` | privacy, determinism, resource, or unknown refusal |

R1 alone may freeze a maximal contiguous 12-19-subject structural cohort with
72-114 selected run bundles, 288-456 source-exact core members, equal fit and
held-out counts, exact task label `reachingandgrasping`, and no more than
8 GiB reserved. R1 may make a separate FW2 preregistration eligible; it does
not authorize FW2 or CIL1.

## Fixed Paths And Resources

```text
readiness certificate: .codex_work/marc2_machine_readiness/vr20p/readiness.v0.json
output root:           .codex_work/marc2_published_task_private_confirmation/v0
private source:        .codex_work/marc1_central_directory/live_audit_v0/member_inventory.private.v0.json
source bytes:          418,755 exactly once
source SHA-256:        2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031
CPU / workers / jobs:  1 / 1 / 1
generated runtime:     <= 60 seconds
private runtime:       <= 650 seconds
peak RSS:              < 256 MiB
minimum free disk:     >= 15 GiB
combined output:       <= 2 MiB
network/new payload:   0 / 0 bytes
archive/signal/target: 0 / 0 / 0 bytes
retry/rerun/resume:    0 / 0 / 0
```

Public output may contain only aggregate route, counts, hashes, byte totals,
proof identity, resources, warnings, unavailable fields, zero counters, and
claim boundaries. It may not contain member names, source paths, participant
identity, sessions, runs, companions, offsets, CRCs, rows, reasons, exception
text, labels, targets, predictions, scores, or outcomes.

## Explicitly Not Authorized

This decision does not authorize implementation before its own remote green
proof; Stage 2 before exact Stage 1 and closeout proof; consumed VR18P access;
another source, path, project, participant, session, run, or dataset; network
or download; archive members; EEG or MEG samples, events, channels, geometry,
quality, labels, targets, or outcomes; derivatives, caches, features, or
NeuroTokens; training, inference, prediction, scoring, tuning, or language
models; FW2, CIL1, RW3, streams, devices, hardware, release, publication, or
any scientific or clinical claim upgrade.

## Next Gate

Commit, push, and green this exact decision. Then implement and
generated-qualify Stage 1. Only after that implementation and its proof-only
closeout are remotely green may the single registered structural confirmation
run once.

Engineering capability authorized after green decision: one proven fixed-path
wrapper may confirm the published-task selector and freeze a bounded,
source-exact target-free structural cohort without opening neural payloads.

Scientific claim not established: this decision is not neural data or a
result and establishes no neural effect, decoding performance, language
decoding, unseen-person generalization, live decoding, or thought-to-text
capability.
