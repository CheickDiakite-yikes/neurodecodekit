# MARC2-VR36P Task-Aware Private Cohort Confirmation Authorization Packet

Date: 2026-08-23

Lane: `MARC2-VR36P`

Status: **All-false Tier C request; no implementation or private access authorized**

Machine request:

- `registries/marc2_task_aware_private_cohort_confirmation_authorization_request.v0.json`

## Why This Is The Next Gate

Protocol-conforming private route `MARC2VR34P-R2` established only that the
task-blind eligible total is above 195. Generated-only VR35A then proved that
omitting task identity can create exactly that class of surplus and that exact
`reachingandgrasping` projection repairs it without changing the baseline
semantic cohort. It also preserves genuine target-task surplus and deficit as
separate failure routes.

VR35A closeout `6744568115f1f4871b68f4605d14732bf3af38f8` passed Base
Python job `97209528132`, Optional Neuro Readers job `97209528186`, and CI
`32645704669`. This makes a separately authorized target-free private cohort
confirmation technically eligible. It does not authorize that confirmation.

## Requested Two-Stage Sequence

### Stage 1: generated/mock fixed-path wrapper

Only after a fresh packet-bound decision is committed, pushed, and remotely
green, implement a standard-library wrapper with fixed `plan`, `qualify`,
`inspect`, and `execute` commands. Generated qualification must use injected
temporary paths and readiness providers only. It must cover the five exact
VR35A cases in ready and not-ready states, two row orders, and two replays,
with at least 100 direct refusals and zero retained generated output.

Stage 1 may not stat, resolve, hash, list, open, or parse `.codex_work`, a
private source, consumed output, archive member, or neural payload. Its exact
implementation and result must be committed, pushed, and remotely green, then
receive a separately green proof-only closeout before Stage 2.

### Stage 2: one target-free private confirmation

Only after every proof barrier, request one fixed-path invocation that:

1. collects exactly three readiness samples through unchanged VR33A, with
   exactly two fixed five-second sleeps;
2. creates one new output root and consumed marker without overwrite;
3. opens and strict-parses only the registered 418,755-byte structural JSON
   source once;
4. calls unchanged VR35A once;
5. on VR35A G1 or G2 only, writes one mode-0600 source-bound private cohort
   manifest containing the exact 16 subjects, 96 run bundles, and 384
   source-exact structural rows already produced by the target-free selector;
6. writes one aggregate public report containing no private identity or row.

Not-ready consumes the invocation and opens zero source bytes. Every outcome
consumes the invocation. Retry, rerun, resume, repair, fallback, substitution,
overwrite, and private reinspection are excluded.

## Frozen Aggregate Routes

| Route | Maximum meaning |
|---|---|
| `MARC2VR36P-R1` | Exact target-task inventory passed directly and one target-free cohort was frozen. |
| `MARC2VR36P-R2` | Exact task projection removed mixed-task surplus, then one target-free cohort was frozen. |
| `MARC2VR36P-R3` | Exact target-task eligible total remained above 195; no cohort was frozen. |
| `MARC2VR36P-R4` | Exact target-task eligible total was below 195; no cohort was frozen. |
| `MARC2VR36P-R5` | Structural, task, taxonomy, selection, split, storage, or privacy validation refused. |
| `MARC2VR36P-R6` | Readiness, fixed-path, output, deterministic-replay, or resource precondition refused. |

No route may expose the count, difference magnitude, task distribution,
member name, path, participant, subject, session, run, reservation, private
hash, row, or exception text. R2 would identify mixed-task surplus only as the
aggregate repaired mechanism; it would not expose which task or item caused
it.

## Fixed Paths And Limits

The future private source identity is copied only from committed records:

```text
.codex_work/marc1_central_directory/live_audit_v0/member_inventory.private.v0.json
bytes:  418755
sha256: 2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031
```

Future readiness and output paths are new VR36P-only locations. Every
consumed VR20P through VR34P lane remains forbidden. The private invocation is
limited to one CPU thread, one worker, one numerical job, 120 seconds, less
than 256 MiB peak RSS, at least 15 GiB free disk, 2 MiB combined incremental
output, zero network bytes, zero new payload bytes, and zero archive, signal,
target, or model bytes.

## Current Authorization State

Every authority flag is false. Packet preparation performed zero readiness,
private-path, source, output-root, consumed-state, archive, neural, target,
model, prediction, score, network, provider, device, FW2/CIL1, release, or
claim operation.

The request and a non-scope-changing proof closeout must each be committed,
pushed, and both remotely green. Only then may VR36P be identified as the sole
active Tier C packet. A fresh unambiguous maintainer message after that
identification may authorize the unchanged packet by reference. The current
or any earlier `continue` is not retroactive authority.

Engineering capability requested: one proof-gated target-free read can test
the task-aware structural repair and freeze a deterministic source-bound
cohort without opening an archive member or neural payload.

Scientific claim not established: this request performs no real/private read,
cohort freeze, neural access, model run, prediction, or score and establishes
no neural effect, decoding accuracy, language decoding, unseen-person
generalization, or live decoding.
