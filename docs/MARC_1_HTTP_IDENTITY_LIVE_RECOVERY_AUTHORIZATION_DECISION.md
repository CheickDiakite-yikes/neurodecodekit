# MARC1-HT1A Live Metadata Recovery Authorization Decision

Date: 2026-08-12

Status: **authorized only after this decision is tested, committed, pushed,
and remotely green; no wrapper implementation or metadata access has occurred**

Machine decision:
`registries/marc1_http_identity_live_recovery_authorization_decision.v0.json`

Frozen request commit:
`27f39aee5f056eafc81b615cec4a178a41a6c5d2`

Frozen packet:
`docs/MARC_1_HTTP_IDENTITY_LIVE_RECOVERY_AUTHORIZATION_PACKET.md`

## Actual Maintainer Decision

After Codex identified `MARC1-HT1A` as the sole active Tier C packet, named
request commit `27f39aee5f056eafc81b615cec4a178a41a6c5d2`, green CI
`31586256906`, both required green jobs, the generated-wrapper-first order,
the one-private-plus-one-public metadata scope, the zero-payload boundary, and
the need for a fresh decision, the maintainer's active instruction was:

> approved, continue, achieve a scientific claim, achieve thought to text 😎

This record preserves those exact 76 UTF-8 bytes. It does not claim that the
maintainer typed the packet's long scope. The short approval incorporates the
immutable, remotely green packet by reference without widening it.

Achieving a scientific claim and thought-to-text remains the research
objective. It is not permission to announce an outcome before evidence, skip
controls, access a payload under this metadata gate, or replace the eventual
language-specific held-out experiment with movement metadata.

## Why The Short Form Is Valid

All fail-closed conditions were satisfied before this decision:

1. `MARC1-HT1A` was the sole active Tier C packet.
2. The packet and all-false request were committed and pushed at `27f39ae`.
3. Base Python job `94080678529` and Optional Neuro Readers job `94080678738`
   were green in CI `31586256906`.
4. Codex named the packet, commit, CI proof, exact metadata scope, zero-payload
   boundary, and fresh-decision gate.
5. The maintainer unambiguously approved and directed continuation.
6. This separate record quotes the complete actual instruction and binds the
   immutable request SHA-256.
7. No payload, neural, target, model, score, release, or scientific-claim
   operation is inferred from the aspiration to reach thought-to-text.

The decision is ineffective until its own commit passes both remote CI jobs.

## Bound Evidence

```text
authorization parent: 27f39aee5f056eafc81b615cec4a178a41a6c5d2
request push CI:       31586256906
Base Python job:       94080678529
Optional Neuro job:   94080678738
request SHA-256:       9d2249005a9cfe3437c0914f471cdc830c2967da25291964cc29357c8d5091f8
packet SHA-256:        1ed48223c0bc05fd52c59c338649c0ed471083495ac6e8fc6446ba379317ec2b
HT1 result SHA-256:    2ac67d4fb4a92cb1c3a4ef86983a59ec4be17d17c26267960f75a3487c1d448c
user-message SHA-256:  aedb564a57be18493fc20376676ef404794ca169d02985228aa8424cd7f7e6e8
```

The packet, request, tests, generated semantics, frozen selector, upstream
inventory result, and consumed prior result remain immutable snapshots. Their
pending fields are not rewritten. This additive record supplies only the
packet-bound permission.

## Ordered Authorization

### Gate 1: this decision becomes remotely green

This exact decision must be tested, committed, pushed, and pass Base Python
and Optional Neuro Readers CI. Until then, implementation and access remain
closed.

### Gate 2: generated and mocked additive-wrapper qualification

After Gate 1, Codex may implement the additive standard-library wrapper named
by the packet. It may import the green target-free selector and HTTP-semantics
module. It may not import, call, modify, or expose the consumed
`marc1_pilot_selection_live.py` executor.

Qualification may use only generated private manifests, generated Wrist rows,
fresh temporary paths, injected DNS answers, and mocked HTTP responses. It
must prove:

- exact decision, request, source, and implementation hash binding;
- absent and lone case-insensitive `identity` acceptance;
- refusal of every actual coding, duplicate, list, transfer coding, decoding,
  decompression, malformed response, overflow, alias, target-like field,
  wrong source identity, symlink, mode mismatch, old-root access, overwrite,
  fallback, retry, and consumed second invocation;
- one no-follow private reader, one-shot marker, machine gate, target firewall,
  exact target-free selection, private/public output split, and aggregate
  failure routing; and
- one thread, bounded time, bounded memory, bounded output, deterministic
  replay, and zero real, network, payload, neural, model, score, and claim
  counters.

The current environment may be reused. Dependency installation, the retained
Freewill path, public Figshare access, archive/member payload access, signal,
target, model, score, and claim work remain closed during implementation. The
exact wrapper commit must pass both remote CI jobs before Gate 3.

### Gate 3: one live metadata selection

After Gate 2 is remotely green, require all numerical thread settings to equal
one, at least 12,884,901,888 bytes free, and normalized one-minute load no
greater than `1.0` per logical CPU. Any unavailable or failed measure refuses
before a consumed marker.

One no-retry invocation may then:

1. create only the new Git-ignored
   `.codex_work/marc1_http_identity/live_recovery_v0` root and consumed marker;
2. refuse any operation on the old
   `.codex_work/marc1_pilot_selection/live_selection_v0` root;
3. no-follow validate the registered upstream Freewill manifest as one regular
   418,755-byte mode-`0600` file;
4. make exactly one bounded content open, SHA-256 pass, and strict JSON parse,
   requiring digest
   `2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031`;
5. make one unauthenticated `GET` to the exact Wrist Figshare v3 files
   endpoint, following at most two bodyless globally routable HTTPS redirects;
6. accept exactly one terminal `200` JSON body no larger than 2,097,152 bytes
   only when `Content-Encoding` is absent or one case-insensitive `identity`;
7. require 55 rows, 45 unique participant archives, ten supplementary rows,
   3,683,416,050 total declared bytes, and the frozen participant-name rule;
8. run only the preregistered target-free 12-person selection and split logic;
9. emit one mode-`0600` private selection manifest and one aggregate public
   report, discard the public response body, and stop.

Any failure after the new marker consumes `MARC1-HT1A`. There is no second
manifest open, second accepted metadata body, retry, rerun, resume, restart,
fallback parser, alternate endpoint, participant substitution, old-root
access, local-header or payload open, acquisition, signal read, target read,
model, prediction, or score.

## Exact Limits

```text
Freewill private input:                exactly 418,755 bytes / one content open
Wrist metadata bodies / bytes:         1 / <= 2,097,152
HTTP attempts / bodyless redirects:    <= 3 / <= 2
selected participants per axis:        12
Freewill bundles / members:            72 / 288
Wrist archives:                        12
payload requests / bytes:              0 / 0
live execution wall / RSS:             30 sec / 268,435,456 bytes
aggregate / combined output:           <= 1,048,576 / <= 2,097,152 bytes
incremental disk / minimum free:       <= 4,194,304 / >= 12,884,901,888 bytes
threads / workers / jobs:              1 / 1 / 1
retries / reruns:                      0 / 0
signals / targets / models / scores:   0 / 0 / 0 / 0
```

The 6-GiB Freewill, 2-GiB Wrist, and 8-GiB joint payload values are only future
reservation ceilings. This decision authorizes moving zero payload bytes.

## Decision-Only Measurements

```text
GitHub CI verification calls:                    1
private path/open/read/hash/parse:                0 / 0 / 0 / 0 / 0
Wrist requests/bodies/bytes/parses:               0 / 0 / 0 / 0
DNS queries / redirects:                          0 / 0
old consumed-root operations:                     0
real participant/member/archive selections:       0 / 0 / 0
local headers / payload requests / payload bytes: 0 / 0 / 0
private markers/manifests / public reports:       0 / 0 / 0
signal / event / target reads:                    0 / 0 / 0
models / predictions / freezes / scores:          0 / 0 / 0 / 0
dependency installs / cleanup operations:         0 / 0
provider-model / hardware / release operations:   0 / 0 / 0
scientific claim upgrades:                        0
end-to-end latency measured:                      false
```

## Local Verification

Twelve decision tests and 24 combined request-plus-decision tests pass, as do
all 424 MARC tests. The dependency-light suite passes 2,563 tests with 204
expected skips in 21.337 seconds at 277,233,664-byte external maximum RSS. The
optional-neuro suite passes 2,634 tests with 35 expected skips in 57.365
seconds at 736,804,864-byte external maximum RSS. Each complete suite adds
exactly 12 tests and zero skips over the green request baseline. Ruff,
compilation, all 182 registry JSON files, and `git diff --check` pass.

## Claim Boundary

Engineering capability authorized for testing: one exact additive,
standard-library, machine-gated wrapper may be qualified and, after its own
green proof, bind the frozen two-axis real pilot from no more than 2,515,907
metadata bytes without opening a neural payload.

Scientific claim not established: this decision is not neural data or a
result. Even a clean future metadata selection would establish no neural
effect, movement decoding, source attribution, language decoding, or
thought-to-text capability.

Research objective preserved: this is a control and attribution checkpoint on
the same path toward a properly controlled held-out thought-to-text result,
not a pivot or a substitute for the eventual language-specific experiment.
