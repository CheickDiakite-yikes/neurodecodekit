# Fresh Motor Source Identity Witness Implementation Decision

Date: 2026-08-31

Decision: `FMSR1-R1-W-I0-D0`

Status: **packet-bound generated-implementation authority recorded; effective
only after this exact decision is tested, committed, pushed, and both required
GitHub CI jobs are remotely green**

Machine decision:

- `registries/fresh_motor_source_identity_witness_implementation_decision.v0.json`

Packet: `FMSR1-R1-W-v0`

## Actual Maintainer Decision

After Codex identified `FMSR1-R1-W-v0`, its exact green commit and CI, and the
fact that the first decision can authorize only generated implementation and
qualification, the maintainer said:

> need real results, real scientific results, focus on that and only that and keep going

These words are preserved exactly. They incorporate only the single named
packet by reference. The demand for real results sets priority; it does not
skip the packet's ordered proof barriers or expand this first decision into
live network, candidate, payload, neural, model, score, release, or claim
authority.

## Bound Green Packet

The all-false packet is exact-green on GitHub `main` at commit
`d4ae388d883b8fb04fc75546e6a30aec2fbfa6f2`, CI `33357313608`, Base
Python job `99381828165`, and Optional Neuro Readers job `99381828024`.
Codex freshly reverified that exact run before recording this decision.

The machine decision binds the human packet, machine request, packet test,
frontier v21, and frontier test by exact byte count, SHA-256, and Git-blob
identity.

## Exact Ordered Authorization

Only after this decision is remotely green:

1. implement one additive standard-library witness surface with no candidate
   parser, source selector, payload reader, model, or scorer capability;
2. expose generated-fixture planning, inspection, and qualification only;
3. qualify exact HTTP identity, pagination-only control decoding, redirects,
   page/root/profile/global hashing, durable marker ordering, CI proof routing,
   resource caps, output caps, and registered refusals using generated fixtures
   and mock transports only;
4. commit and push that exact implementation and require both GitHub CI jobs
   green; and
5. stop before every GitHub API or official-index request.

No live witness command may exist in this implementation milestone. Exact code,
artifact hashes, and implementation CI must be known before the maintainer can
provide fresh second execution-bound words. A separate authority-bearing
decision must then become remotely green before one consumed live witness.

## Explicit Exclusions

This decision does not authorize DNS, sockets, GitHub API calls, official-index
requests, candidate parsing, source ranking or selection, publication reads,
payload URLs, downloads, archives, members, headers, EEG, EOG, EMG, events,
annotations, targets, labels, acquisition, derivatives, models, checkpoints,
training, inference, prediction freezing, scoring, language models, providers,
streams, devices, hardware, another project, cleanup, deletion, release,
publication, rerun, substitution, or scientific-claim upgrade.

## Decision-Only Measurements

```text
fresh GitHub CI verification calls:             1
implementation or generated qualification:      0
network requests / bytes:                        0 / 0
candidate or source selections:                  0
payload/header/neural/target reads:               0
model/training/prediction/score operations:       0
provider/stream/device/hardware operations:       0
release/deletion/claim upgrades:                  0
end-to-end latency measured:                      false
```

Engineering capability authorized: after this decision is remotely green, the
project may implement and generated-qualify the exact source-identity witness
needed immediately before one bounded live witness.

Scientific claim not established: this decision reads no neural measurement
and establishes no neural advantage, movement-intention decoding, unseen-person
generalization, language decoding, live operation, hardware result, or clinical
value.
