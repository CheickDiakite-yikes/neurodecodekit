# DREYER-C5R-1 H-L1R1 Qualification Authorization Packet

Date: 2026-08-29

Status: **request only; every authority flag remains false**

Machine request:

- `registries/dreyer_c5r_1_stage_h_live_recovery_qualification_authorization_request.v0.json`

Request ID: `DREYER-C5R-1-HL1R1-QA0`

## Green Basis

Generated-only recovery implementation `6a0bc7749dd6c36b4d8db019cc6e78acf653c83d`
passed both required jobs. Its proof-only closeout
`41644a55ada938b0d1d3a042264e8c0b7a48384b` passed Base Python
`99093675279`, Optional Neuro Readers `99093675204`, and CI `33249903090`,
then reached GitHub `main`.

This request binds the proof closeout document, registry, and test as three
exact artifacts totaling 8,632 bytes. It requests the next generated-only
barrier and grants no authority now.

## Requested Work

Only after this exact request and its proof-only closeout are remotely green,
and only after fresh packet-bound maintainer words are recorded in a separate
decision that itself becomes remotely green, may the project:

1. add a narrow generated-only qualification coordinator and CLI that call the
   exact green recovery implementation without modifying it;
2. qualify that coordinator with generated fixtures and refusal-only unit
   checks;
3. commit and remotely green the exact coordinator before execution; and
4. execute exactly one registered generated qualification.

The single qualification must produce two byte-deterministic H1 replays, run
the inherited two valid Stage H cases and 18 Stage H refusals, and run the 43
ordered H-L1R1 refusal identities without substitution. It must prove marker-
before-capability order, exactly one opener and request on H1, response closure,
manifest-contained cleanup, no staging debris, no unaccepted payload, aggregate
H0 behavior, no-replace publication, and consumed-rerun refusal.

The attempt is consumed whether it passes or fails. There is no retry, rerun,
resume, repair, threshold change, case substitution, or post-result amendment.
The coordinator may expose no real path, URL, network, or H-L2 command.

## Resource Envelope

| Resource | Frozen maximum |
|---|---:|
| CPU threads / workers / numerical jobs | 1 / 1 / 0 |
| Wall time | 30 seconds |
| Peak process-tree RSS | 256 MiB |
| Generated input plus output | 8 MiB |
| Incremental temporary disk | 16 MiB |
| Public result | 1 MiB |
| Network bytes / HTTP requests | 0 / 0 |
| Registered qualifications | 1 |

## Explicit Exclusions

This request does not authorize the qualifier implementation, generated
qualification, H-L2 activation, the real 14,805,604-byte EDF, another Dreyer
object, any network request, real/private path, EDF/MAT/EEG content, header,
annotation, signal, target, label, participant, model, training, inference,
prediction, score, language model, provider, stream, device, hardware, release,
other project, or claim upgrade.

No short-form instruction predating this packet may activate it. The packet
must cross its own commit, push, two-job CI, and proof barriers before a fresh
maintainer decision can have any effect.

Engineering capability requested: add and execute one bounded generated-only
qualification that can establish whether the recovery wrapper fails closed
across its complete frozen matrix.

Scientific claim not established: this all-false request performs no real EEG
operation and establishes no neural, decoding, unseen-person,
peripheral-adjusted, live, hardware, or clinical result.
