# MARC2-VR3 Variable-Domain Private Recovery Authorization Decision

Date: 2026-08-16

Lane: `MARC2-VR3`

Status: **Packet-bound short-form authorization recorded; ineffective until
this decision is committed, pushed, and both required CI jobs are green; no
executor, private path, archive, neural, target, model, or score operation
occurred while recording this decision**

Machine decision:
`registries/marc2_variable_domain_private_recovery_authorization_decision.v0.json`

## Maintainer Decision

After Codex identified `MARC2-VR3` as the sole active Tier C packet and named
its green request commit, CI run, two required jobs, hashes, two-stage scope,
and fresh-decision boundary, the maintainer's next message was exactly:

```text
continue; Run one structural-manifest pass to freeze the real cohort.
Preregister a bounded FW2 neural experiment with signal, derangement, timing, and no-signal controls.
Train target-firewalled models, freeze predictions, then score once.
```

The machine record preserves those actual 240 UTF-8 bytes and their SHA-256.
It does not fabricate the packet's long scope as a maintainer utterance. Under
the approved Research Autonomy Charter, the fresh `continue` and explicit
structural-manifest instruction bind only the immutable, remotely green
`MARC2-VR3` packet by reference.

The two later sentences are also preserved as the maintainer's requested
research direction. They authorize Tier A preregistration work after the real
cohort is frozen. They do not authorize an undefined future neural execution:
no `MARC2-FW2` contract, immutable cohort identity, model matrix, resource cap,
target firewall, prediction-freeze protocol, or scoring boundary existed when
this message was sent. A future all-false FW2 request must be separately
committed, pushed, remotely green, identified as the sole active Tier C gate,
and followed by fresh unambiguous maintainer words.

## Green Request Bound By This Decision

```text
request commit:       328faa845d894459a658b6ad62d078a00f539e9e
CI run:               31947928896
Base Python job:      95166799271
Optional Neuro job:  95166799305
packet SHA-256:       1f546f25372b6212d5e86518b30acd3d42fe664965394fa5a72d5b043b4a709a
request SHA-256:      f665d66f3b6c5e97c88d8302de0b87d63e0383f45bdab54d34a90f80f4ce248b
```

The local branch and request commit matched before this decision was written.
GitHub reported both required jobs successful. The pre-existing untracked
tracker inspection sidecar was not opened, modified, staged, or deleted.

## Delayed Effect

This decision is not effective merely because these files exist. Before any
executor implementation or retained-path operation, this exact decision must:

1. pass focused and complete local verification;
2. be committed as one coherent decision-only milestone;
3. be pushed on the current branch; and
4. pass both required remote CI jobs.

Until all four conditions hold, implementation and private access remain
closed.

## Authorized Sequence After Green Decision

### Stage 1: generated/mock wrapper

After this exact decision is remotely green, one additive standard-library
module may be implemented and qualified:

```text
neurodecodekit.datasets.marc2_variable_domain_private_recovery
```

Development may use only generated manifests, mocked no-follow filesystem
facts, injected readers, and invocation-created temporary outputs. The module
must use the exact shared proof validator and call VR2's
`adapt_live_domain_source` exactly once. It must not import, call, edit, copy,
alias, or expose any consumed private executor or consumed output root.

The implementation must create a native `MARC2-VR3` registry and a distinct
`MARC2-FW1B`-format proof certificate accepted by the exact shared validator.
Both records must bind the future wrapper HEAD, this decision, module, tests,
green VR2 artifacts, shared validator, selector lineage, and consumed LA2
boundary. Generated qualification must exercise at least 32 proof-certificate
mutations and 32 wrapper mutations, four valid exclusion profiles in canonical
and reversed order, exact one-call integration, deterministic replay,
malformed-source refusals, no-follow refusals, privacy, caps, and forbidden
consumed-dependency operations.

The exact Stage 1 implementation must then be committed, pushed, and pass both
remote jobs. Stage 1 may not inspect the retained manifest.

### Stage 2: one target-free structural pass

Only after the exact Stage 1 implementation is remotely green may one
no-retry invocation operate on this literal retained file:

```text
.codex_work/marc1_central_directory/live_audit_v0/member_inventory.private.v0.json
```

Its registered identity is 418,755 bytes, mode `0600`, SHA-256
`2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031`,
schema `neurodecodekit.marc1_central_directory_private_manifest` version
`0.1.0`, 1,227 entries, 1,025 regular entries, 202 directories, and 238
complete source run bundles before classification.

The future executor must reject resolution, globbing, directory listing,
sibling inspection, symlink components, a symlink final path, non-regular
files, wrong ownership or mode, and alternate paths. It may perform one
no-follow content open, one open/fstat reconciliation, one bounded sequential
read, one SHA-256 pass, and one strict duplicate-key-controlled JSON parse.

After proof, machine, output, and path preflight but before the private content
open, it must atomically write one mode-`0600` consumed marker beneath:

```text
.codex_work/marc2_live_domain_private_recovery/v0
```

Every later route is final. The executor must validate all 1,227 rows and 238
bundles before filtering, dynamically reconcile exactly 195 eligible plus 43
valid ineligible bundles without assuming their predicate breakdown, preserve
source immutability and mutable-object independence, and accept only the
unchanged target-free selector result returned by VR2.

At most a consumed marker, private structural selection manifest, and
aggregate report may be written. The aggregate report must not expose member
names, offsets, CRCs, retained paths, private rows, source bodies, siblings, or
private output paths.

## Routes And Resources

Routes remain `MARC2VDR-F00` through `MARC2VDR-F06`, then success
`MARC2VDR-R1`. Every route consumes the invocation. Success freezes structural
cohort membership only; it does not authorize an archive member, payload,
`MARC2-FW2`, model, target, score, or scientific claim.

```text
CPU threads / workers / numerical jobs: 1 / 1 / 1
runtime:                                 <= 30 seconds per stage
peak RSS:                                <= 256 MiB
private input opens / bytes:             1 / 418,755 exact
network requests / bytes:                0 / 0
archive local-header/member bytes:       0
combined output:                         <= 2 MiB
incremental disk:                        <= 4 MiB
minimum free disk:                       >= 15 GiB
selected future reservation:             <= 8 GiB accounting only
retry / rerun / resume:                  0 / 0 / 0
```

## Explicitly Not Authorized

This decision does not authorize implementation before this decision is
remotely green; private access before the exact wrapper is remotely green;
operation on consumed executors or roots; another path, sibling, listing,
network request, or download; archive headers, members, payloads, extraction,
or range requests; EEG samples, signals, events, channels, geometry, targets,
labels, quality, onsets, responses, trials, or outcomes; derivatives, caches,
features, split payloads, or NeuroTokens; training, parameter updates,
checkpoints, inference, predictions, freezes, target delivery, or scoring;
`MARC2-FW2`, `MARC2-CIL1`, `MARC2-ORTH1`, or `NDK-LANG1`; providers,
language models, RW3, streams, devices, or hardware; retries, reruns, resumes,
substitutions, fallbacks, or amendments; pre-existing cleanup, overwrite,
move, rename, or deletion; operation on another project; release,
publication, participant-level protected output, or scientific claim upgrade.

## Next Gate

After a successful one-shot structural pass, Tier A work may use only its
aggregate and private structural selection identities to preregister a bounded
`MARC2-FW2` experiment. That future contract should freeze the selected cohort,
session split, causal signal path, derangement control, timing-only control,
no-signal control, model families, exact fit and prediction counts, target
firewall, prediction-freeze ledger, one score, and machine/storage caps.

Real archive-member access, neural derivatives, model fitting, prediction
freezing, target delivery, and scoring remain closed until that exact FW2
packet receives its own green proof and fresh packet-bound decision.

## Claim Boundary

Engineering capability authorized after green decision: one newly proven
wrapper may derive a frozen real cohort from one exact private structural
manifest without opening an archive member.

Scientific claim not established: this decision is not neural data or a
result and establishes no neural effect, decoding accuracy, brain-specific
origin, language decoding, or thought-to-text capability.
