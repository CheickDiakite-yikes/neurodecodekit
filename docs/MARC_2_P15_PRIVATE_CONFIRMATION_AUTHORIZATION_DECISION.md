# MARC2-VR12P P15 Private Confirmation Authorization Decision

Date: 2026-08-18

Lane: `MARC2-VR12P`

Status: **Packet-bound short-form authorization recorded; ineffective until
this decision is committed, pushed, and both required CI jobs are green; no
implementation, readiness, private path, structural source, archive, neural,
target, model, prediction, score, or claim operation occurred while recording
this decision**

Machine decision:
`registries/marc2_p15_private_confirmation_authorization_decision.v0.json`

## Maintainer Decision

After Codex identified `MARC2-VR12P` as the sole active Tier C packet and named
its green request and proof-closeout commits, CI runs and jobs, two-stage order,
exact target-free structural scope, privacy firewall, and fresh-decision
boundary, the maintainer's next message was exactly:

```text
continue
```

The machine record preserves those actual eight UTF-8 bytes and their SHA-256.
It does not fabricate the packet's long scope as a maintainer utterance. Under
the approved Research Autonomy Charter, this fresh short form binds only the
immutable, remotely green `MARC2-VR12P` packet by reference, without expansion.

## Green Packet Bound By This Decision

```text
request commit:       816589473eafabdebe66be2b4e921b005f04a959
request CI:           32171993061
request Base job:     95825074164
request Optional job: 95825073430
proof head:           5e4354a2928d0b4d99ff686585878751b94b6605
proof CI:             32173596281
proof Base job:       95830254383
proof Optional job:   95830254539
```

Both required jobs passed for both exact commits. Proof attempt `3eddbd8`
failed only because one test required parent Git history unavailable in the
default shallow CI checkout. Final proof head `5e4354a` replaced that check
with the same literal byte, SHA-256, and Git-blob bindings; it changed no
packet scope and performed no private or scientific operation.

The unrelated untracked tracker inspection sidecar was not opened, modified,
staged, or deleted.

## Delayed Effect

This decision is not effective merely because these files exist. Before any
wrapper implementation, readiness invocation, output-root operation, or
private-source path check, this exact decision must:

1. pass focused local verification;
2. be committed as one coherent decision-only milestone;
3. be pushed on the current branch; and
4. pass both required remote CI jobs.

Until all four conditions hold, implementation and private access remain
closed.

## Authorized Sequence After Green Decision

### Stage 1: additive generated/mock wrapper

After this exact decision is remotely green, one additive standard-library
wrapper may be implemented and qualified using only invocation-created
generated source fixtures, injected machine samples, mocked no-follow
filesystem facts, and temporary generated output roots.

The wrapper must expose fixed `plan`, `qualify`, `inspect`, and proof-gated
`execute` surfaces with no generic source, output-root, path, URL, threshold,
retry, resume, fallback, cleanup, substitution, or arbitrary execute override.
It must preserve the fixed VR12P paths, fresh-readiness state machine, and
marker-before-open transition with one strict JSON read and one exact VR12A adapter call.
It must also preserve deterministic replay, resource monitors, and zero-retry
semantics. It must not import, call, patch, copy, modify, or inspect the
consumed VR9P or VR11P executors, certificates, markers, outputs, or results.

Qualification must replay all three registered generated run-index spellings
in canonical and reversed order with two exact replays. All 12 success paths
must call VR12A exactly once, at least 50 direct refusal mutations must pass,
retained output must be zero, and all real/private/scientific counters must be
zero. No `.codex_work` or other real or ignored path may be statted, resolved,
hashed, opened, altered, deleted, or reused.

The exact Stage 1 implementation must be committed, pushed, and pass both
remote CI jobs. Its separate proof-only closeout must also be committed,
pushed, and pass both jobs before any private operation begins.

### Stage 2: one target-free structural confirmation

Only after the exact Stage 1 implementation and proof-only closeout are
remotely green may one command:

1. validate every exact green proof and the one-thread environment;
2. obtain three consecutive passing machine-readiness samples;
3. create one fresh mode-`0600` certificate at the fixed VR12P path;
4. require the fixed new output root to be absent and reject symlinks/aliases;
5. no-follow preflight only the registered target-free structural source;
6. write one mode-`0600` consumed marker immediately before content open;
7. read, hash, and strict-parse exactly 418,755 structural bytes once;
8. call the exact green VR12A adapter once without source mutation;
9. on R1 only, write one mode-`0600` source-exact private cohort manifest; and
10. write one aggregate-safe mode-`0644` R1-R6 report.

The private manifest may contain only the source-exact selected structural
rows and frozen provenance required by the packet. The public report may
retain only aggregate route, counts, hashes, bytes, exact proof identity,
bounded measurements, warnings, unavailable fields, zero forbidden-operation
counters, and claim boundaries. It may not retain a member name, source path,
subject or participant ID, session, run, companion, offset, CRC, reason,
exception, label, target, prediction, or score.

## Fixed Route Contract

| Aggregate route | Maximum engineering meaning |
|---|---|
| `MARC2VR12P-R1` | repaired selector passed and one source-exact structural cohort was frozen |
| `MARC2VR12P-R2` | readiness, fixed-path, or output precondition failed |
| `MARC2VR12P-R3` | source identity or strict structural envelope failed |
| `MARC2VR12P-R4` | repaired identity, task, or companion validation refused |
| `MARC2VR12P-R5` | selection, split, rank, or reservation validation refused |
| `MARC2VR12P-R6` | privacy, deterministic-output, or resource validation refused |

Every route consumes the one invocation. No route permits retry, rerun,
resume, repair, fallback, substitution, private reinspection, or post-result
amendment.

## Frozen R1 Cohort Envelope

R1 requires a maximal contiguous prefix of 12-19 ranked subjects, exactly
three complete runs from each of `ses-01` and `ses-02` per selected subject,
72-114 selected run bundles, 288-456 source-exact core members, equal fit and
held-out bundle counts, and reservation at or below 8 GiB using exact UTF-8
source names. No later-subject inspection, skipping, substitution, backfill,
or cap raise is permitted.

R1 makes a separate prospective FW2 preregistration eligible. It does not
authorize FW2 execution, open any archive member or neural payload, or make
CIL1 eligible.

## Fixed Paths And Resources

```text
readiness certificate: .codex_work/marc2_machine_readiness/vr12p/readiness.v0.json
new output root:        .codex_work/marc2_p15_private_confirmation/v0
private source:         .codex_work/marc1_central_directory/live_audit_v0/member_inventory.private.v0.json
private source bytes:   418,755 exactly once
private source SHA-256: 2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031
CPU / workers / jobs:   1 / 1 / 1
generated runtime:      <= 45 seconds
private runtime:        <= 650 seconds
peak RSS:               < 256 MiB
minimum free disk:      >= 15 GiB
combined output:        <= 2 MiB
network / new payload:  0 / 0 bytes
archive/signal/target:  0 / 0 / 0 bytes
retry/rerun/resume:     0 / 0 / 0
```

A pre-marker refusal opens zero private content and still consumes the one
registered invocation. Every post-marker route also consumes the lane.
Cleanup is limited to invocation-created temporary files.

## Explicitly Not Authorized

This decision does not authorize implementation before this decision is
remotely green; private operation before the exact wrapper and proof closeout
are remotely green; operation on a consumed VR9P/VR11P path; another file,
source, output root, project, dataset, participant, session, or run; network or
download; archive local-header or member-payload access; EEG, MEG, signal,
event, onset, channel, geometry, sentence, key, label, target, response, trial,
quality, or payload access; derivative, cache, feature, or NeuroToken creation;
training, parameter update, checkpoint access, inference, prediction, freeze,
target delivery, scoring, tuning, or language-model use; `MARC2-FW2` or
`MARC2-CIL1`; provider, RW3, stream, device, or hardware operation; release or
publication; or any scientific, decoding, neural, real-time, portable,
home-use, assistive, or clinical claim upgrade.

## Next Gate

After this decision is remotely green, implement and generated-qualify the
fixed-path wrapper. After that exact implementation and its proof closeout are
remotely green, perform the one registered structural confirmation. Only R1
may unlock design of a separate prospective FW2 packet. CIL1 remains later and
conditional on a successful controlled offline neural result.

Engineering capability authorized after green decision: one newly proven,
machine-stable wrapper may confirm the P15 repair and freeze a bounded,
source-exact target-free structural cohort without opening neural payloads.

Scientific claim not established: this decision is not neural data or a result
and establishes no neural effect, decoding performance, brain-specific origin,
language decoding, live decoding, or thought-to-text capability.
