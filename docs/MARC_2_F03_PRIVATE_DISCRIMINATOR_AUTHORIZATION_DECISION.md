# MARC2-VR11P F03 Private Discriminator Authorization Decision

Date: 2026-08-17

Lane: `MARC2-VR11P`

Status: **Packet-bound short-form authorization recorded; ineffective until
this decision is committed, pushed, and both required CI jobs are green; no
implementation, readiness, private path, structural source, archive, neural,
target, model, prediction, score, or claim operation occurred while recording
this decision**

Machine decision:
`registries/marc2_f03_private_discriminator_authorization_decision.v0.json`

## Maintainer Decision

After Codex identified `MARC2-VR11P` as the sole active Tier C packet and named
its green request and proof-closeout commits, both CI runs and jobs, two-stage
order, exact target-free structural scope, privacy firewall, and fresh-decision
boundary, the maintainer's next message was exactly:

```text
okay lets continue
```

The machine record preserves those actual 18 UTF-8 bytes and their SHA-256. It
does not fabricate the packet's long scope as a maintainer utterance. Under the
approved Research Autonomy Charter, this fresh short form binds only the
immutable, remotely green `MARC2-VR11P` packet by reference, without expansion.

## Green Packet Bound By This Decision

```text
request commit:       6e72c8f797201359777454a750b1dea9704665c0
request CI:           32009557248
request Base job:     95326004060
request Optional job: 95326004145
proof commit:         136f7b999d3514bd8d62f8dc9e7d7c01b89662f7
proof CI:             32011020786
proof Base job:       95330380822
proof Optional job:   95330380918
```

Both required jobs passed for both exact commits. The machine record preserves
the request-time packet, registry, and test identities separately from the
additive proof-closeout snapshots. The unrelated untracked tracker inspection
sidecar was not opened, modified, staged, or deleted.

## Delayed Effect

This decision is not effective merely because these files exist. Before any
wrapper implementation, readiness invocation, output-root operation, or
private-source path check, this exact decision must:

1. pass focused local verification;
2. be committed as one coherent decision-only milestone;
3. be pushed on the current branch; and
4. pass both required remote CI jobs.

Until all four conditions hold, implementation and private access remain
closed.

## Authorized Sequence After Green Decision

### Stage 1: additive generated/mock wrapper

After this exact decision is remotely green, one additive standard-library
wrapper may be implemented and qualified using only invocation-created
generated source fixtures, injected machine samples, mocked no-follow
filesystem facts, and temporary generated output roots.

The wrapper must expose no generic source, output-root, path, URL, threshold,
retry, resume, fallback, cleanup, substitution, or arbitrary execute override.
It must preserve the fixed VR11P paths, fresh-readiness state machine,
marker-before-open transition, one strict JSON read, one exact VR6 consistency
call, one exact VR10B discriminator call, deterministic replay, resource
monitors, and zero-retry semantics. It must not import, call, patch, copy, or
modify the consumed VR9P executor.

Qualification must exercise generated clean control plus all five exact VR10B
witnesses in canonical and reversed order with two exact replays. Each path
must call VR6 once and VR10B once, all six routes must appear exactly four
times, at least 70 direct refusal mutations must pass, and retained output must
be zero. No real or ignored path may be statted, resolved, hashed, opened,
altered, deleted, or reused.

The exact Stage 1 implementation and proof record must then be committed,
pushed, and pass both remote CI jobs before any private operation begins.

### Stage 2: one target-free structural discriminator

Only after the exact Stage 1 implementation is remotely green may one command:

1. validate its exact green implementation proof;
2. obtain three consecutive passing machine-readiness samples;
3. create one fresh mode-`0600` certificate at the fixed VR11P path;
4. require the fixed new output root to be absent;
5. no-follow preflight the exact registered structural source;
6. write one mode-`0600` consumed marker immediately before content open;
7. read, hash, and strict-parse exactly 418,755 structural bytes once;
8. call VR6 once and require outer F02 plus nested F03;
9. call VR10B once; and
10. write at most one aggregate-safe R1-R5 report.

The report may retain only one coarse route, exact proof identity, bounded
measurements, zero forbidden-operation counters, warnings, unavailable fields,
and claim boundaries. It may not retain a reason, exception, predicate, failed
value, row, member, path, offset, CRC, private hash, person identity, session,
run, companion identity, candidate, selection, or cohort.

## Fixed Route Contract

| Aggregate route | Maximum engineering meaning |
|---|---|
| `MARC2VR11P-R1` | the structural manifest reaches frozen class P03 |
| `MARC2VR11P-R2` | the structural manifest reaches frozen class P15 |
| `MARC2VR11P-R3` | the structural manifest reaches frozen class P16 |
| `MARC2VR11P-R4` | the structural manifest reaches frozen class P18 |
| `MARC2VR11P-R5` | the structural manifest reaches frozen class P19 |

VR6 success, another broad or nested route, VR10B G1, an unknown route, source
mutation, or leakage consumes and parks the lane. No route authorizes an F03
repair, candidate retention, cohort freeze, archive open, or neural work.

## Fixed Paths And Resources

```text
readiness certificate: .codex_work/marc2_machine_readiness/vr11p/readiness.v0.json
new output root:        .codex_work/marc2_f03_private_discriminator/v0
private source:         .codex_work/marc1_central_directory/live_audit_v0/member_inventory.private.v0.json
private source bytes:   418,755 exactly once
private source SHA-256: 2a2e48b88ee59332a199d926554bb6921222fff92046a0fc9b07cf73fd6c3031
CPU / workers / jobs:   1 / 1 / 1
generated runtime:      <= 45 seconds
private runtime:        <= 650 seconds
peak RSS:               < 256 MiB
minimum free disk:      >= 15 GiB
combined output:        <= 1 MiB
network bytes:          0
archive/signal/target:  0 / 0 / 0 bytes
retry/rerun/resume:     0 / 0 / 0
```

A pre-marker refusal opens zero private content and still consumes the one
registered invocation. Every post-marker route also consumes the diagnostic.
Neither case permits retry, rerun, resume, repair, fallback, substitution, or
private reinspection. Cleanup is limited to invocation-created temporary files.

## Explicitly Not Authorized

This decision does not authorize implementation before this decision is
remotely green; private operation before the exact wrapper is remotely green;
operation on any consumed VR9P path; another file, source, output root, project,
dataset, participant, session, or run; network or download; archive local-header
or member-payload access; EEG, MEG, signal, event, onset, channel, geometry,
sentence, key, label, target, response, trial, quality, or payload access;
cohort freeze, derivative, cache, split, feature, or NeuroToken creation;
training, parameter update, checkpoint access, inference, prediction, freeze,
target delivery, scoring, tuning, or language-model use; `MARC2-FW2` or
`MARC2-CIL1`; provider, RW3, stream, device, or hardware operation; release or
publication; or any scientific, decoding, neural, real-time, portable,
home-use, assistive, or clinical claim upgrade.

## Next Gate

After this decision is remotely green, implement and generated-qualify the
fixed-path wrapper. After that exact implementation is remotely green, perform
the one registered structural discriminator. An R1-R5 result may select the
subject of a later prospective repair, but it does not authorize that repair.
FW2 and CIL1 remain closed until a separately designed, authorized, and
successful real cohort freeze exists.

Engineering capability authorized after green decision: one newly proven,
machine-stable wrapper may distinguish five frozen structural refusal classes
while retaining no private reason, row, path, identity, value, selection, or
cohort.

Scientific claim not established: this decision is not neural data or a result
and establishes no neural effect, decoding performance, brain-specific origin,
language decoding, live decoding, or thought-to-text capability.
