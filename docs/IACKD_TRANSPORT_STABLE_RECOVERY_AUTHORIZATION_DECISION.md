# IACKD-2R Transport-Stable Recovery Authorization Decision

Date: 2026-08-11

Status: **Authorized only after this decision is tested, committed, pushed,
and remotely green; no executor implementation, public request, local IACKD
operation, model run, target delivery, or score has occurred**

Machine decision:
`registries/iackd_transport_stable_recovery_authorization_decision.v0.json`

Frozen request commit:
`525e97e64d04c64aa3243f94790dd70db5fd30e7`

Frozen packet:
`docs/IACKD_TRANSPORT_STABLE_RECOVERY_AUTHORIZATION_PACKET.md`

## Actual Maintainer Decision

After Codex identified IACKD-2R as the sole active Tier C packet, named request
commit `525e97e64d04c64aa3243f94790dd70db5fd30e7`, green CI
`31475356506`, both required green jobs, the additive executor, storage-safe
stream, 660-fit/900-prediction freeze and score sequence, machine-safety gate,
and need for a fresh decision, the maintainer said:

> continue

This record preserves that word exactly. It does not claim that the maintainer
typed the packet's long scope. The short instruction incorporates the
immutable, remotely green packet by reference without widening it.

Every object, byte, participant, arm, model, target-firewall, resource,
machine-safety, no-retry, no-rerun, and claim boundary remains unchanged.

## Why The Short Form Is Valid

All fail-closed conditions were satisfied before the instruction:

1. IACKD-2R was the sole active Tier C packet.
2. The packet and all-false request were committed and pushed at `525e97e`.
3. Base Python job `93727674791` and Optional Neuro Readers job `93727674875`
   were green in CI `31475356506`.
4. Codex named the packet, commit, CI proof, exact scope, and decision gate.
5. The maintainer then unambiguously said `continue`.
6. This separate record quotes the actual word and binds every immutable scope
   artifact.
7. No release, hardware, destructive action, scientific upgrade, or other
   scope is inferred.

The decision is ineffective until its own commit passes both remote CI jobs.

## Bound Evidence

```text
authorization parent: 525e97e64d04c64aa3243f94790dd70db5fd30e7
request push CI:       31475356506
Base Python job:       93727674791
Optional Neuro job:   93727674875
contract SHA-256:     d2208ea8a7aca1a3c2788ec00e9a892c1cc4b630e25b2a911f822530a80624e4
request SHA-256:      ac27f19bba1cec104a44f04a9349f25531690776765803a0461504fc9307ae4e
packet SHA-256:       fdd4062ddcf7eacdf8675c7de0bd4d5e7538b5d90e48cee593d47654d87cb778
user-message SHA-256: e256ee8e7aff6957a781d8328f0f68e26996564c81fa458da59fbca2305138ad
```

The transport registration and implementation, frozen parent science,
inventory, consumed failure, packet, request, and tests remain immutable
snapshots. Their pending fields are not rewritten after approval. This
additive record supplies the packet-bound permission.

## Ordered Authorization

### Gate 1: this decision must become remotely green

This exact decision must be tested, committed, pushed, and pass Base Python
and Optional Neuro Readers CI. Until then, implementation and access remain
closed.

### Gate 2: generated qualification of a new additive executor

After Gate 1, Codex may implement and qualify the exact new module named by
the packet. Qualification uses generated fixtures and mocked transport only.
The module must integrate the green metadata validator, preserve the frozen
science and target firewall, refuse old roots, implement the pre-consumption
machine-safety check, and fail closed before any public operation unless every
proof and resource gate passes.

The existing exact optional environment may be reused without dependency
installation, resolution, substitution, or tooling network. No public URL or
local IACKD path operation is allowed during implementation. The exact
executor commit must pass both remote CI jobs before Gate 3.

### Gate 3: one fresh stream and derivative build

After Gate 2 is green, first require at least 10 GiB free, all numerical thread
settings equal one, and one-minute load no greater than one runnable process
per logical CPU. Any unavailable or failed safety measure refuses before the
new consumed marker.

One no-retry invocation may then reverify the four registered metadata bodies
using fixed-length, chunked, or clean close-delimited framing plus exact
observed bytes and SHA-256. It may stream exactly 1,340 allowlisted objects
totaling 7,249,113,684 bytes into a new isolated Git-ignored root.

Only one of 128 ten-object raw run groups may exist at a time. Each group must
be verified, parsed once, promoted into bounded private derivatives, and then
only that invocation-created raw group may be removed. The consumed IACKD-2
executor, old invocation root, old retained bundle, and every preexisting path
remain forbidden.

### Gate 4: one target-blind fit, prediction, and freeze

After complete derivatives pass, one no-network execution may complete exactly
660 registered parameter-update fits and 900 target-blind prediction sets. It
must emit one aggregate hash-only freeze containing no individual prediction,
probability, target, trajectory, coefficient, or participant outcome, then
stop with both final target views sealed.

The freeze must be committed, pushed, and green in both CI jobs before either
final target view opens.

### Gate 5: one combined score

After the freeze is remotely green, the isolated scorer may receive the
actual-action and exact-opposite cue-surrogate views together once, verify all
frozen identities and hashes, score both reversal arms, apply `IACKD2-R0`
through `IACKD2-R5`, emit aggregate output, and stop.

No refit, normalization update, threshold change, exclusion, second target
delivery, second score, retry, rerun, or post-target update is open.

## Exact Limits

```text
dataset / version:                 OpenNeuro ds006840 / 1.0.0
participants / hand units:         15 / 30
run groups / objects / bytes:      128 / 1,340 / 7,249,113,684
raw groups resident concurrently:  1
largest raw run group:             82,064,564 bytes
model family:                      fixed causal 0.5-4 Hz shrinkage LDA
fits / prediction sets:            exactly 660 / exactly 900
acquisition wall / RSS:            7,200 sec / 2,147,483,648 bytes
analysis wall through freeze:      10,800 sec
peak incremental disk:             1,073,741,824 bytes
minimum free disk:                 10,737,418,240 bytes
maximum load per logical CPU:      1.0
private derivatives / output:      536,870,912 / 536,870,912 bytes
public receipt / freeze / result:  at most 4,194,304 bytes each
threads / workers / jobs:          1 / 1 / 1
final deliveries / scores:         1 / 1
retries / reruns / updates:        0 / 0 / 0
```

## Decision-Only Measurements

```text
GitHub CI verification calls:                     1
OpenNeuro metadata / payload requests:            0 / 0
payload / network bytes:                          0 / 0
old-root / retained-bundle / local operations:    0 / 0 / 0
EEG / EOG / marker / trajectory / target reads:   0 / 0 / 0 / 0 / 0
dependency installs / executor operations:        0 / 0
derivatives / fits / predictions / freezes:       0 / 0 / 0 / 0
final target deliveries / scores:                 0 / 0
provider / stream / device / hardware operations: 0 / 0 / 0 / 0
generated experiment artifacts:                   0
end-to-end latency measured:                       false
```

## Claim Boundary

**Engineering capability authorized for testing:** one exact, storage-safe,
transport-stable, target-firewalled public IACKD dual reversal may proceed
through the ordered remote-green gates.

**Scientific claim not established:** this decision is not EEG data or a
result. Even future `IACKD2-R5` would not establish absolute brain-specific
origin, independent replication, unseen-person generalization, typing,
language or thought decoding, end-to-end real-time operation, portable
hardware, home use, assistive benefit, or clinical utility.
