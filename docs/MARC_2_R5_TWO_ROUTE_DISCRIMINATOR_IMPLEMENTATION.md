# MARC2-VR21A R5 Two-Route Discriminator Implementation

Date: 2026-08-22

Lane: `MARC2-VR21A`

Status: **Generated qualification passed; remote implementation proof pending**

Machine records:

- `registries/marc2_r5_two_route_discriminator_implementation.v0.json`
- `registries/marc2_r5_two_route_discriminator_result.v0.json`

## Exact Registration Proof

Registration `7ce0e16392fed2576031766bead32a5cab44031a` passed Base Python
job `96998477692`, Optional Neuro Readers job `96998477649`, and CI
`32559365362` before implementation. The implementation verifies the exact
8,304-byte contract and all 11 bound inputs before qualification.

## Implementation

The dependency-free module exposes only `plan` and `qualify`. It builds three
generated 1,227-row sources in canonical and reversed order across two
replays. Every path calls unchanged VR20A exactly once and retains only one
safe route:

- G1: generated success control;
- R1: upstream VR20A F06 taxonomy or eligibility arithmetic;
- R2: upstream VR20A F07 selection, split, rank, identity, or reservation
  arithmetic.

The unknown-participant witness reaches F06/R1. The semantic-run-zero witness
reaches F07/R2. The wrapper checks source bytes before and after every call,
rejects every other upstream route, and has no private executor or override
surface.

## Measured Qualification

`MARC2VR21A-G1` passed all 12 paths. G1, R1, and R2 each appeared four times;
VR20A was called 12 times; all six case/order source hashes replayed exactly;
and 48 direct refusals passed. Source mutations after calls and retained output
were zero.

The pass processed 5,301,432 generated bytes in 0.7890150410239585 seconds at
32,636,928-byte peak RSS. Aggregate output was 2,940 bytes under one CPU
thread, one worker, and one numerical job. Network, new payload, private,
archive, neural, target, model, prediction, score, FW2/CIL1, other-project,
and claim operations were zero.

## Boundary And Next Gate

This result proves that generated F06 and F07 cases can be distinguished
through unchanged VR20A. It does not determine whether the consumed private R5
source was F06 or F07. Commit, push, and green the exact implementation and
result, then add a proof-only closeout before preparing any later all-false
private discriminator packet.

Engineering capability added: deterministic generated discrimination of the
two structural arithmetic classes that remain compatible with VR20P R5.

Scientific claim not established: no real neural payload, target, model,
prediction, or score was accessed, so this establishes no neural effect,
decoding accuracy, language decoding, unseen-person generalization, live
decoding, or thought-to-text capability.
