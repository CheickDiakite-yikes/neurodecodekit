# MARC2-VR22P R5 Private Discriminator Proof Closeout

Date: 2026-08-22

Lane: `MARC2-VR22P`

Status: **Proof-only closeout pending its own remote-green barrier**

## Exact Implementation Proof

The generated-qualified implementation is exact commit
`bc8ecc38374c57f6be9269874cce8d4497f83551`. It passed:

```text
CI run:                 32594134914
Base Python job:        97082352527
Optional Neuro job:     97082352441
both required jobs:     green
qualification route:    MARC2VR22P-G1
scope changed:           false
```

The implementation registry before this closeout contained 7,292 bytes,
SHA-256
`834850e82a8833de96c2a734def4a2bc9a510c3c9a1377d9c8a2b062a03fa25d`,
and Git blob `75d2ed1b236991a6a95a954fea41c4aabed8cb66`. The canonical
implementation-artifact set has SHA-256
`f529f23077f13aeb96d2c0e7612501fed44ed0e7d03b1973dbbf44623f30146a`.
The proof record also binds the exact Git blobs for the wrapper, behavior
tests, implementation tests, and implementation document.

## No Reexecution

This closeout changes only proof state and tracked frontier documentation. It
does not change the wrapper, implementation artifacts, generated source,
12-path matrix, 121 refusals, measurements, route table, fixed paths, resource
caps, or claim boundary. It does not repeat the registered qualification.

Every readiness, repository `.codex_work`, source stat/hash/open/read/parse,
cohort, archive, neural, target, model, prediction, score, network, hardware,
other-project, FW2/CIL1, release, and claim operation remains zero.

## Delayed Effect

This closeout is ineffective until its own exact commit is pushed and both
Base Python and Optional Neuro Readers jobs are green. Before that proof, do
not run `execute`, collect readiness, inspect the registered private source,
or create the real output root.

After this exact closeout is remotely green, the existing packet and decision
permit one registered invocation only. That invocation may read and strict-
parse exactly 418,755 target-free structural bytes once, call VR20A once, and
call VR21A at most once if VR20A refuses at F06 or F07. It may either freeze
one R1 private structural cohort plus an aggregate report or consume at
aggregate R2-R6. It has no retry, rerun, resume, fallback, repair,
substitution, cleanup, amendment, or private reinspection.

R1 would make a separate FW2 preregistration eligible. It would not authorize
FW2 execution, CIL1, archive-member access, neural data, targets, models,
predictions, scores, release, or a scientific claim.

Engineering capability added: the exact generated-qualified F06/F07 wrapper
is bound to immutable remote-green implementation bytes before one target-free
structural discriminator may run.

Scientific claim not established: this proof-only closeout accesses no real
cohort, archive member, neural signal, target, model, prediction, or score and
establishes no neural effect, decoding performance, language decoding,
unseen-person generalization, live decoding, or thought-to-text capability.
