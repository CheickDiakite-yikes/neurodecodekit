# Communication EEG Triangulated Replication Refresh Proof Closeout

Date: 2026-08-27

Status: **source refresh remotely green; proof-only closeout pending its own
remote CI**

Source-refresh commit `85400fe13a87d82916a8338709239764b621c5c1`
passed Base Python job `98712772971`, Optional Neuro Readers job
`98712773150`, and CI run `33128665324`. The exact commit is present on both
the working branch and GitHub `main`.

## Bound Refresh

This closeout binds the exact human refresh, machine refresh, and refresh test:

- three tracked artifacts;
- 23,934 total bytes; and
- canonical artifact-set SHA-256
  `b286e505976d391391692652af55fc4e3d1c322ef0c9d657100f226557da38f6`.

The machine proof is
`registries/communication_eeg_triangulated_replication_refresh_proof.v0.json`.
Its tests recompute every byte count, SHA-256, Git blob, and canonical set
digest from the committed artifacts.

## Operation Boundary

This closeout read three tracked files, their three Git blob identities, and
public GitHub CI/ref metadata. It did not contact an EEG repository, read a
real or private path, request a payload, parse a neural file, read a signal,
event, target, or label, run a model, create a prediction, score, call a
provider, stream, operate hardware, publish a release, or upgrade a scientific
claim.

The 20 GiB total research-storage ceiling and 10 GiB selected-raw ceiling are
unchanged. No cleanup occurred, no other project was touched, and the unrelated
tracker inspection file remains untracked and untouched.

## Scientific Meaning

The remotely green refresh proves that NeuroDecodeKit's communication route
now distinguishes three evidence roles before any future target is exposed:

1. full-control discovery attribution in `ds003626`;
2. descriptive full-sensor mechanistic concordance in three-person
   `ds007591`; and
3. adequately sized but only partially controlled independent command
   transportability in TESSCCo.

It also proves why those roles cannot be silently merged. The three-person
bridge cannot attain the frozen participant-level significance threshold, and
the partial replication cannot establish information beyond peripherals it
did not record.

## Next Gate

After this exact closeout is committed, pushed, and both CI jobs pass, the
Tier A triangulated source refresh is fully closed. The next scientific action
remains a separate authorized source-identity path. `DREYER-C5R-1-HL` remains
the sole active Tier C packet and all of its authority flags remain false.

Engineering capability added: the three-role communication replication router
is now bound to exact files, hashes, Git objects, remote CI, and GitHub `main`.

Scientific claim not established: no neural payload was accessed, so this work
did not demonstrate communication decoding, EEG information beyond peripheral
signals, unseen-person generalization, independent replication, causal live
decoding, hardware performance, or clinical value.
