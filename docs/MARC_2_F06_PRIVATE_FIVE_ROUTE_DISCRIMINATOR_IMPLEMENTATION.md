# MARC2-VR24P Generated Wrapper Implementation

Date: 2026-08-22

Lane: `MARC2-VR24P`

Status: **Generated Stage 1 qualified; private Stage 2 remains proof-gated**

Machine record:
`registries/marc2_f06_private_five_route_discriminator_implementation.v0.json`

## Authority And Barrier

Packet-bound decision `53bc114db9a51a256cbf86d470f24397a50f12b2`
passed Base Python job `97096723985`, Optional Neuro Readers job
`97096724073`, and CI `32599996090` before implementation began. The decision
binds only the unchanged VR24P request and leaves private Stage 2 behind the
exact implementation and proof-closeout remote barriers.

No readiness, `.codex_work`, private source, consumed VR22P, archive, neural,
target, model, score, network, hardware, release, or scientific operation was
performed during Stage 1.

## Added Interface

The additive dependency-free module
`neurodecodekit.datasets.marc2_f06_private_five_route_discriminator` exposes
only fixed `plan`, `qualify`, `inspect`, and `execute` commands.

It has no generic path, URL, output, threshold, route, reason, cap, retry,
resume, fallback, or substitution argument. `execute` validates an exact
remote implementation proof before collecting readiness or accessing a fixed
private path. With the proof field still null, execution refuses at F01.

The wrapper preserves the earlier controls:

- strict duplicate-key and non-finite JSON refusal;
- no-follow source opens and fixed mode, byte, and SHA-256 identity;
- fresh fixed output roots and exclusive mode-controlled writes;
- a consumed marker immediately before source content open;
- three passing readiness samples under one-thread settings;
- aggregate-only public reports with private keys rejected recursively;
- exact runtime, memory, output, and storage checks; and
- no retry, rerun, repair, substitution, or private reinspection path.

Unlike VR22P, VR24P never emits a private cohort manifest. R1-R5 retain only
one class identity; R6-R9 park readiness, source, non-F06, or unknown states.

## Generated Qualification

One measured qualification used the exact six VR23A cases in canonical and
reversed order with two exact replays:

```text
route:                           MARC2VR24P-G1
paths:                           24 / 24
route counts:                    G1 and R1-R5 each 4
VR23A calls:                     24
nested unchanged VR20A calls:   24
generated source content opens: 24
direct refusals:                 130
deterministic replay:            true
generated input bytes:          10,603,766
generated output bytes written: 72,908
peak incremental output bytes:  3,038
retained generated output:      0
aggregate output bytes:         4,582
runtime:                         3.135488207975868 seconds
peak RSS:                        48,152,576 bytes
CPU threads / workers / jobs:   1 / 1 / 1
```

All route mappings matched the frozen contract:

| Generated case | Aggregate route |
|---|---|
| control success | `MARC2VR24P-G1` |
| entry-kind count drift | `MARC2VR24P-R1` |
| extra complete bundle | `MARC2VR24P-R2` |
| unknown participant taxonomy | `MARC2VR24P-R3` |
| 238/195/43 class arithmetic drift | `MARC2VR24P-R4` |
| eligible participant-session distribution drift | `MARC2VR24P-R5` |

All raw-data, real-cache, model, training, archive, signal, event, target,
prediction, score, FW2/CIL1, network, provider, stream, device, hardware,
other-project, and claim counters were zero.

## Next Barrier

Commit, push, and green this exact implementation. Then add a proof-only
closeout that binds the preproof registry and exact implementation Git blobs
without repeating qualification. Only after that closeout is itself committed,
pushed, and remotely green may the one registered target-free 418,755-byte
structural execution collect readiness and access the fixed source once.

Engineering capability added: an exact fixed-path wrapper can map generated
F06 structural failures into five aggregate public classes while withholding
private details and refusing private execution without remote proof.

Scientific claim not established: Stage 1 used generated structural fixtures
only and establishes no neural effect, decoding accuracy, language decoding,
unseen-person generalization, live decoding, or thought-to-text capability.
