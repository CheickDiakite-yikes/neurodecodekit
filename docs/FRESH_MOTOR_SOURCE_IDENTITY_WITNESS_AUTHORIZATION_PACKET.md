# Fresh Motor Source Identity Witness Authorization Packet

Date: 2026-08-31

Packet: `FMSR1-R1-W-v0`

Status: **all authorities false; request pending this exact packet commit and
remote proof; no network or protected operation**

Machine request:

- `registries/fresh_motor_source_identity_witness_authorization_request.v0.json`

## Decision Question

Can one bounded, target-free pass establish a complete replayable byte identity
for every frozen official index surface, so a later discovery can parse exactly
that universe rather than an unknown or drifting subset?

This is the last source-admission dependency before `D1`. It does not inspect
candidate science fields, rank datasets, select a source, or access neural
payloads. A complete witness advances directly to one witness-bound discovery
packet. An incomplete, drifting, malformed, or over-cap surface produces one
measured blocker and stops source hopping.

## Exact-Green Predecessor

The consumed generated admission qualification is closed at commit
`dbfaf682595c33d3c6de10e88503fb84a2a6254c`, GitHub `main` CI
`33352882661`, Base Python job `99369471021`, and Optional Neuro Readers job
`99369470875`. The proof-only closeout binds four result artifacts / 30,764
bytes. It ran no additional qualification and performed zero network, real
source, neural, target, model, prediction, score, release, or claim operation.

The packet also binds the exact FMSR1-v1 research contract, the 3D-inside-5D
evidence-map clarification, the discovery-admission correction, and the R1-G
generated contract. Their identities are listed in the machine request; none is
amended here.

## Conservative Admission Choice

All five real profiles use `OPAQUE_COMPLETE_SNAPSHOT_REPLAY`.
`SOURCE_GLOBAL_REVISION` is not selected because no source-issued immutable
value with documented scope over each complete registered traversal is frozen.
This avoids relabeling an `ETag`, date, API version, frontend build, or body hash
as a semantic source revision.

The ordered surfaces remain exactly:

1. OpenNeuro CRN GraphQL: four frozen POST request bodies;
2. NEMAR search: four frozen GET queries;
3. PhysioNet search: four frozen GET queries;
4. GigaDB search: four frozen GET queries; and
5. BNCI Horizon 2020: one complete motor-EEG catalogue root.

Their 17 root identities are the exact canonical plan produced by the green
discovery implementation: 7,439 bytes, SHA-256
`9eb3d4a37b7314423c0c5f1b2eae465ae2d3874c26436c75a1df25aaef72cff6`.
No query, category, method, body, path, host, or pagination grammar may be added
or substituted.

Every source request uses HTTP/1.1 over HTTPS port 443, the frozen user agent,
exact `Accept`, `Accept-Encoding: identity`, and `Connection: close` values in
the machine request. OpenNeuro POSTs additionally use canonical UTF-8 JSON with
`Content-Type: application/json`. The protocol-managed `Host` header is exact
for every request, and OpenNeuro also carries the exact decimal
`Content-Length`; both are inside the hashed request identity.
`Transfer-Encoding` and any other implicit or explicit header are forbidden.

## Requested Ordered Authority

This packet grants nothing. It requires two later authority decisions because
the live execution decision cannot honestly bind code that does not exist yet.
Only after this packet is exact-green and the maintainer gives fresh
packet-bound words may a first decision activate:

1. one additive standard-library witness implementation with no candidate
   parser, selector, payload reader, model, or score capability;
2. generated-fixture qualification of transport, pagination-only routing,
   hashing, marker, CI, caps, output, and refusal behavior;
3. commit, push, and both required GitHub CI jobs green for that exact
   implementation; and
4. stop before live network access.

The maintainer must then give fresh execution-bound words after the exact code,
artifact hashes, and implementation CI are known. A second authority-bearing
decision must bind those values, numeric repository/owner identities, the exact
workflow blob, and the complete three-request `CI-W0` REST profile. It does not
pretend to name its own future commit or future check-run IDs. After that
decision is remotely green, the executor treats the clean local `main` `HEAD`
that contains its exact tracked decision blob as the runtime authority commit.
One no-retry/no-rerun invocation may then create its durable consumed marker,
verify that exact commit live, and witness all five surfaces in the same
process.

The invocation is consumed on success, park, refusal, timeout, or process exit.
No receipt can resume it.

## Opaque Snapshot Ledger

The ledger is not one flat chain. It contains exactly 17 ordered root records,
one for every frozen initial request. Every root independently starts at page
ordinal zero, binds its initial identity, owns its continuation chain, and ends
in exactly one terminal page. Each page record keeps together:

- ordinal and root query/category identity;
- exact request identity and any POST-body SHA-256;
- ordered redirect transcript;
- pagination-control identity;
- raw identity-encoded response byte count and SHA-256;
- next-request identity or terminal state; and
- source host, peer, HTTP, content-type, and timing evidence required by the
  transport contract.

The executor necessarily reads candidate-containing entity bytes to count and
hash them. It may semantically decode only the minimum source-specific routing
fields needed to identify the next page or terminal state. The machine packet
freezes the exact JSON pointer types, three generic JSON control variants,
HTML container/next/terminal matches, continuation scalar types, canonical URL
algorithm, and OpenNeuro body patch. Any missing, ambiguous, mistyped, or
noncanonical control parks. Candidate records, titles, descriptions, sensors,
participants, licenses, payload links, labels, targets, and outcomes may not be
semantically decoded, extracted, retained, or iterated. The existing candidate
discovery parsers are unreachable from the witness CLI. Candidate
canonicalization, eligibility, ranking, and selection counters must all remain
zero.

The ledger is complete only if all 17 roots terminate exactly once with no gap,
duplicate, cycle, fork, truncation, or unconsumed next-page identity. Any single
incomplete surface parks the whole witness. Partial ledgers cannot authorize
`D1`.

The ledger is cryptographically nested, not summarized beside a flat page list.
Canonical page hashes feed ordered root hashes; the exact root memberships
`4/4/4/4/1` feed five ordered profile hashes; those profile hashes and the same
17 root hashes feed one global hash. Exact preimage fields, canonical JSON-plus-
LF encoding, ordinals, membership, aggregate-count reconciliation, and excluded
self-hash fields are frozen in the machine packet. A profile or global summary
that cannot be recomputed from its children is invalid.

## Live CI And Attempt Ordering

Before the first GitHub DNS or socket operation, the executor must create and
fsync a no-follow `0600` consumed marker inside a fresh `0700` invocation root,
then fsync the parent directory. It must reject any existing, linked, or
non-directory path.

`CI-W0` consists of exactly three direct TLS GETs to `api.github.com`, in order:
the exact `git/ref/heads/main` response, the exact commit-scoped
`check-runs?per_page=100&page=1` response for local `HEAD`, and the exact frozen
workflow Git-blob response. The second decision binds repository identities,
implementation artifacts, workflow path/blob, decision path/schema, packet,
API version, headers, request grammar, and byte caps. At runtime, remote `main`
must equal clean local `HEAD`; that commit must contain the exact decision blob,
have exactly one successful `Base Python` and one successful `Optional Neuro
Readers` GitHub Actions check and no additional check run, and return the exact
workflow blob. Searching for a run is forbidden. This commit-scoped proof
avoids a self-referential decision that would need to know its own future
commit and CI IDs. The live check and first source contact occur in one process
with no user input, pause, re-exec, or mutable handoff.

The executor rejects credentials, cookies, conditional requests, proxy or
custom-CA variables, proxy auto-discovery, redirects from GitHub, alternate
hosts, non-global peers, stale server dates, nonzero cache age, unsupported
content encoding, duplicate singleton headers, or mismatched identities.

## Network And Resource Ceiling

```text
CPU threads / workers / jobs:       1 / 1 / 1
runtime:                             <= 300 seconds
absolute peak RSS:                   <= 256 MiB
CI requests / decoded bytes:         3 / 3 MiB
official-index requests:             <= 125
total network requests:              <= 128
official-index wire / decoded bytes: <= 32 MiB / 32 MiB
retained witness artifacts:          <= 1 MiB
temporary generated disk:            <= 2 MiB
retry / rerun:                        0 / 0
payload or neural bytes:              0
candidate, target, model, score ops:  0
```

Every initial open, redirect hop, pagination request, error response, and failed
open increments the source request counter before contact. Each redirect hop is
a typed record binding its incoming request, raw and normalized `Location`,
DNS/TLS peer evidence, headers, entity bytes, timing, and outgoing request
identity. Source redirects are allowed only within the exact per-profile
scheme/host/port/path/method/query allowlist, without method rewriting; GitHub
redirects are forbidden. Only HTTP 200 terminal responses and identity or
absent content encoding are accepted. Every source uses its exact typed
JSON-control or HTML-pagination grammar; mixed page, cursor, and offset
mechanisms refuse. Cap-plus-one reads may detect and refuse a boundary breach
but may not retain the extra byte.

## Result Router

Exactly one aggregate route is allowed:

- `WITNESS_COMPLETE`: all five opaque snapshots are complete and replayable;
- `WITNESS_CAP_PARK`: a complete traversal exceeds a frozen cap;
- `WITNESS_TRANSPORT_PARK`: an official surface cannot satisfy the frozen
  transport profile; or
- one named fail-closed refusal before candidate access.

Only `WITNESS_COMPLETE`, followed by commit, push, and both remote jobs green,
permits preparation of an all-false `D1` packet. That packet must itself become
exact-green, receive fresh packet-bound maintainer words, and receive a separate
authority-bearing exact-green decision before one discovery invocation. `D1`
must replay every request and body hash before candidate parsing. Any drift
parks before selection. A measured park ends this five-index route and triggers
prospective synchronized EEG/EOG/EMG collection design rather than another
unregistered dataset search.

## Explicit Non-Authority

This packet performs no network request and authorizes no implementation,
execution, candidate parsing, source selection, publication read, payload URL,
download, archive, member, header, EEG, EOG, EMG, event, annotation, target,
label, model, checkpoint, training, inference, prediction, score, language
model, provider, stream, device, hardware, release, cleanup, deletion,
substitution, rerun, or scientific-claim upgrade.

Engineering capability requested: one consumed, same-process, CI-gated witness
can freeze a complete replayable byte identity for all five official source
surfaces without inspecting candidate science fields.

Scientific claim not established: a source-identity witness contains no neural
measurement, target, prediction, or score and therefore establishes no neural
advantage, movement-intention decoding, unseen-person generalization, language
decoding, or live operation.
