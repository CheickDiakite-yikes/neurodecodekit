# COMM-P0-G Generated Qualification Amendment 2

Date: 2026-08-28

Status: **prospective proof-order correction; no execution authorized**

Amendment: `COMM-P0-G-A2`

Machine record:
`registries/communication_eeg_prospective_generated_qualification_amendment_2.v0.json`

## Purpose

The frozen `COMM-P0-G-v0` registration correctly requires an implementation
and activation that are committed, pushed, and remotely green before the one
official generated invocation. It also says that replication predictions must
be remotely green before target delivery. Applied literally inside the same
zero-network, 180-second generated invocation, the latter requirement is
impossible: the invocation cannot publish a prediction artifact to GitHub or
wait for remote CI without violating its own resource contract.

This amendment resolves only that proof-order ambiguity. It does not weaken
the future real or human study rule.

## Corrected Generated Proof Order

For the official fictional qualification:

1. The exact implementation commit and its required Base Python and Optional
   Neuro Readers jobs must be remotely green before activation.
2. A separate activation must bind the exact implementation artifacts,
   implementation commit, CI run, both successful job identities, contract,
   and this amendment. The activation commit and both jobs must also be
   remotely green before invocation.
3. The zero-network invocation must create a non-replacing prediction file and
   a cryptographic freeze attestation before opening its target descriptor.
4. The score-only child must verify that attestation, the prediction file
   identity, its complete inventory, and the target delivery nonce before it
   may score once.
5. The generated result and proof closeout may be committed only after that
   consumed invocation. A failed, refused, crashed, or timed-out invocation
   remains consumed.

"Complete 42-person two-replay qualification executed" is therefore result
evidence, not an activation prerequisite. No network, Git operation, or remote
CI wait may occur inside the invocation.

For any future real or human study, the original stronger rule remains: the
aggregate prediction freeze must be separately committed, pushed, and remotely
green before target delivery and scoring. This amendment cannot activate or
authorize such a study.

## Required Shortcut Routes

The implementation must execute all seven registered mechanical fixtures:

- one residual-central-EEG positive fixture; and
- six negative fixtures where only EOG, oral EMG, microphone, cue, timing, or
  language context carries the command.

Each negative route must fail its corresponding neural-evidence acceptance
gate. These fixtures qualify control logic only and have no scientific value.

## Unchanged Boundaries

All other fields of `COMM-P0-G-v0` remain unchanged, including two isolated
replays, 42 complete fictional participants, all 70 refusal families, the
target firewall, resource limits, single-consumption rule, aggregate-only
publication, and claim boundary.

This amendment authorizes no generated invocation, real or private data,
person, recording, signal, target, model execution, score, network operation,
stream, device, release, or scientific claim. `DREYER-C5R-1-HL` remains the
sole active Tier C gate with every authority flag false.
