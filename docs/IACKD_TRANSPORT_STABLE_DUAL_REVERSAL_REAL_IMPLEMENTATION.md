# IACKD-2R Transport-Stable Dual-Reversal Executor Implementation

Date: 2026-08-11

Status: **Generated-fixture qualified; exact additive implementation must be
committed, pushed, and green in both CI jobs before any public request or real
IACKD operation**

Module:
`src/neurodecodekit/experiments/iackd_transport_stable_dual_reversal_real.py`

Machine record:
`registries/iackd_transport_stable_dual_reversal_real_implementation.v0.json`

## Immutable Authorization Boundary

The additive implementation began only after packet-bound decision
`feef8f721c5441f98829099a63a20dd264c98204` passed CI `31476158747`,
including Base Python job `93730242015` and Optional Neuro Readers job
`93730242090`.

It also binds:

- frozen scientific contract SHA-256
  `f3b38cb2c5bf0a55e0816072ef654cc87bd2e2f36bab50df19947d66d2abdb7f`;
- committed inventory SHA-256
  `aeaa4928192cca9086fcb0abf4711147c68a68ef5c5aacda2ebc67d162a1ef19`;
- transport validator implementation
  `93a067c4dcdb89ea5e5d17db6e5adaca454a64d1`, green in CI
  `31474412246`; and
- the consumed parent result at `IACKD2-F08`, which remains historical
  evidence with no retry and no scientific observation.

No public URL, local IACKD path, signal, event, trajectory, target, model,
prediction, or score was accessed while implementing or qualifying this
module.

## Additive Isolation

The new module is self-contained. It does not import, call, modify, or expose
an argument for the consumed real executor. It names a new private root:

```text
.codex_work/iackd_transport_stable_dual_reversal/real_execution_v0
```

It also names new public receipt, freeze, and result paths. No CLI mode accepts
an old root, retained bundle, arbitrary dataset path, URL, object substitution,
or output overwrite.

This duplication is deliberate. The consumed executor is immutable evidence;
sharing its runtime entry points would make accidental reuse possible.

## Ordered Proof Gates

The real modes fail closed unless their preceding proof is present:

1. `--execute-stream` validates the exact implementation commit and both
   supplied green CI job identifiers.
2. Before any consumed marker, it measures all five numerical thread settings,
   free disk, logical CPU count, and one-minute load.
3. It refuses unless free disk is at least 10,737,418,240 bytes and one-minute
   load divided by logical CPUs is at most `1.0`. An unavailable load metric
   also refuses before consumption.
4. Only then may it create the new exclusive root and consumed marker.
5. `--execute-analysis` is unavailable until the exact public acquisition
   receipt is committed and the complete private derivatives validate.
6. `--score` is unavailable until the aggregate prediction freeze is committed,
   pushed, and green in both CI jobs.

No retry, rerun, resume, restart, substitution, second target delivery, second
score, or post-target update exists.

## Transport Repair

Only the four small metadata responses changed relative to the frozen parent:

- accepted framing: fixed length, valid chunked transfer, or clean close;
- status `200`, unchanged final URL, no redirect, and identity content encoding;
- one bounded read of registered bytes plus one;
- exact observed byte count and registered SHA-256 before semantic parsing;
- valid `Content-Length`, when present, recorded as exact or different advisory
  evidence; and
- malformed or ambiguous framing, compression, underflow, overflow, read
  error, URL drift, or hash drift refused.

The additive adapter first checks the real requested URL and status, then
presents an inert generated alias to the already green transport validator.
The original stream supplies the headers and sole body read. This preserves
the generated-only validator boundary without weakening real final-URL
identity.

The 1,340 payload objects do not use the relaxed metadata policy. They still
require exact `Content-Length`, exact registered ETag, exact observed bytes,
identity encoding, no transfer coding, and one full-stream SHA-256 pass.

## Frozen Scientific Pipeline

After a successful stream, the inherited scientific design remains unchanged:

- 15 participants and 30 participant-hand units;
- symmetric `C2I` and `I2C` cue reversals;
- 26 source-declared EEG predictors, excluding M1/M2, HEOG, VEOG, and Trigger;
- causal `[-1.0, 0.0]` second windows at 1024 Hz;
- fixed 0.5-4 Hz role-aware shrinkage LDA;
- recorded-EOG, timing, corruption, displacement, central-support, and
  action-versus-cue controls;
- exactly 660 parameter-update fits and 900 target-blind prediction sets; and
- one aggregate hash-only freeze before one combined delivery of both sealed
  target views.

At most one raw run group may exist. Invocation-created raw files are removed
only after derivative promotion. Final target values and signed trajectories
remain structurally absent from predictive code.

## Generated Qualification

One permitted generated qualification completed with the exact optional stack:

```text
NumPy:          2.5.2
SciPy:          1.18.0
MNE:            1.12.1
scikit-learn:   1.9.0
CPU threads:    1
workers:        1
numerical jobs: 1
```

Measured outcome:

```text
acceptance gates:               18 / 18
generated input bytes:          3,257,217
generated BrainVision parses:   2
generated retained trials:      8
metadata read/hash/parse calls: 4 / 4 / 4
metadata framing profiles:      fixed, fixed-different, chunked, close
primary fits/prediction sets:   660 / 900
replay fits/prediction sets:    660 / 900
deterministic replay:           true
private prediction bytes:       192,358
mutation attempts:              13
runtime seconds:                4.939357291907072
peak RSS bytes:                 261,488,640
peak temporary bytes:           328,606
report bytes:                   5,825
network/public/real operations: 0
```

The temporary report was removed after measurement. The planted generated
route was `IACKD2-R5`; it has zero scientific value.

The refusal suite covers metadata framing ambiguity and hash drift, payload
status/URL/size/ETag/encoding drift, low disk, excessive or unavailable load,
target leakage, and freeze drift. Focused tests also prove that a failed
machine gate cannot call the streaming builder.

Final local verification passed:

```text
focused tests:                 28 tests (base: 20 passed, 8 skipped)
complete base suite:          2,029 tests, 204 skipped
pre-change base comparison:   2,001 tests, 196 skipped
complete optional suite:      2,085 tests, 34 skipped
pre-change optional comparison: 2,057 tests, 34 skipped
Ruff:                         0.15.20 passed
compileall:                   passed
registry JSON files:          149 valid
CLI help and dry-run plan:    passed
```

## CLI

Dry-run plan, with no path stat or network access:

```bash
python -m neurodecodekit.experiments.iackd_transport_stable_dual_reversal_real
```

Generated qualification and bounded inspection:

```bash
python -m neurodecodekit.experiments.iackd_transport_stable_dual_reversal_real \
  --qualify NEW_REPORT.json
python -m neurodecodekit.experiments.iackd_transport_stable_dual_reversal_real \
  --inspect REPORT.json
```

The real stream, analysis, and scorer flags exist for the registered ordered
sequence, but remain ineligible until their exact preceding commits and both CI
jobs are green.

## Next Gate

Commit and push this exact implementation, then require Base Python and
Optional Neuro Readers CI to pass. Until both are green, public metadata
requests, payload requests, local IACKD operations, derivative creation, fits,
predictions, freezes, target delivery, and scoring remain zero.

Engineering capability added: NeuroDecodeKit now has a generated-qualified,
transport-stable, storage- and load-gated additive executor for the frozen
IACKD-2R sequence without calling or modifying the consumed executor.

Scientific claim not established: no real or public IACKD observation was
accessed, so this implementation establishes no neural effect, action decoding,
brain-specific origin, unseen-person generalization, language or thought
decoding, real-time operation, portable hardware, home use, assistive benefit,
or clinical utility.
