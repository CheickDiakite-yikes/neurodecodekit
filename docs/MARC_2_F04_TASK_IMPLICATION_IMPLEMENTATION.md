# MARC2-VR19A F04 Task-Implication Implementation

Date: 2026-08-21

Lane: `MARC2-VR19A`

Status: **Generated qualification passed; exact implementation proof pending**

## Implemented Surface

The standard-library module
`neurodecodekit.datasets.marc2_f04_task_implication` exposes only `plan` and
`qualify`. It has no `execute`, path override, private reader, output root,
model, or scoring surface.

The implementation composes the unchanged VR16A adapter and adds:

- exact contract and remote-registration proof binding;
- byte and SHA-256 verification of eight tracked inputs;
- AST inventory of both F04 producer references inside
  `_validate_variable_entry`;
- eight generated cases under two orders and two replays;
- one-call source immutability checks around every VR16A invocation;
- exact route-count and replay validation;
- 40 direct fail-closed mutations;
- public-output and resource firewalls; and
- aggregate-only JSON output with explicit unavailable fields.

## Static Proof

The audit finds exactly two F04 producer references. The translated reference
selects F04 only when `match.group("task") != "freewill"` and emits
`core identity differs`. The later direct reference uses the same inequality
and emits `Freewill task differs`.

The consumed VR18P R4 map accepts only the first route-and-reason pair. Under
the exact bound source, that pair therefore implies the non-lowercase-
`freewill` task class. It does not reveal the actual token.

## Generated Qualification

All 32 paths passed:

- success control: 4;
- non-`freewill` task class: 16; and
- task-preserving identity counterexample class: 12.

Both replays produced SHA-256
`6a8af6b89ef2de7577bd968eebe778f7342b26f36327d811b659e8f21c62f20f`.
Every source remained byte-identical after its one VR16A call, and all 40
direct refusal checks passed.

The pass processed 13,748,732 generated bytes in 1.759938333008904 seconds at
51,429,376-byte peak RSS. It emitted 2,326 aggregate bytes and retained zero.

## Boundary

Every private, consumed-state, archive, neural, target, model, prediction,
score, network, hardware, other-project, FW2/CIL1, retry, release, and claim
counter is zero. The generated task witnesses are controls, not guesses about
the private source.

Engineering capability added: deterministic code-and-fixture proof of the
condition that produces the consumed aggregate route under exact VR16A
semantics.

Scientific claim not established: no neural payload, target, model,
prediction, or score was accessed, so this establishes no neural effect,
decoding performance, language decoding, live decoding, or thought-to-text
capability.
